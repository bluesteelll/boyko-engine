# Открытые вопросы к владельцу

Трудности, спорные решения и то, чего я не понял — записывается по мере появления, чтобы владелец
мог прочитать позже и высказаться, а не находил это погребённым в отчёте, когда решение уже принято.

> Русская версия документа [`../OPEN-QUESTIONS.md`](../OPEN-QUESTIONS.md), который остаётся
> **источником истины**; правка любой стороны обновляет вторую в том же коммите.
> См. [README.md](README.md).

**Соглашение.** Свежее сверху. В каждом пункте — ситуация, варианты и что он блокирует. Закрытый
пункт помечается `РЕШЕНО` с датой и решением владельца, а не удаляется: запись о том, *почему* так
решили, переживает само решение. Перф- и архитектурные развилки я решаю сам, с числами; сюда
попадают ЦЕННОСТНЫЕ и ОБЪЁМНЫЕ вопросы и всё непонятое.

---

## 2026-08-10 — РЕШЕНО: списан весь харнесс S1.5 вместе с драйвером фаз. Механический гейт ступени 7 ЗАКРЫТ.

Ответ владельца на оба вопроса объёма один: списать. Отгружено. `rg
'TimestampCollector|VbTimedPass|Sv0TimedPass' crates/` возвращает **ноль совпадений в коде** — гейт
ступени, открытый с начала кампании, закрыт.

Две вещи о цене удаления; решать ничего не нужно, но знать стоит:

**A/B ступени S1.5 исчез как ЭКСПЕРИМЕНТ, а не только как принтер.** `sv0_bench_lighting_flags`
гасил `SHADOWS|AO` на двух кадрах из четырёх; теперь каждый кадр пушит то, что всегда пушил любой
не-бенчевый кадр. Переписанные числа остаются в `sv0_deferred_term_bench.rs` с его
устройство-независимыми арифметическими гейтами — и к ним применимо собственное правило репозитория:
результат, полученный на списанном приборе, ничего не ограничивает на текущем. Любой ступени, которой
нужна АКТУАЛЬНАЯ цифра Deferred-марчера, придётся мерить заново на зонном артефакте.

**Ступень 7 заканчивается без A/B-гейта на любом GPU-семействе.** `gbuffer_zone_port_gate.rs` ушёл
вместе со своей ногой A, как до него `vb_zone_ab_witness_gate.rs`. Это правильно — сравнивать больше
не с чем — и означает, что таблицы стадий под `zone_begin_stage` пинятся `const`-блоками, а не
меряются против независимой копии. Названо у самой функции, а не оставлено на обнаружение.

---

## 2026-08-10 — ступень 7 шаг 6c ПОПРОБОВАН и ОТКАЧЕН: SV0-бенч не принтер, он УПРАВЛЯЕТ тем A/B, о котором отчитывается.

На предыдущий пункт вы ответили «списать харнесс» — эта часть решена, ногу замера в
`window_present_gbuffer.rs` убираем. Удаление всё равно не легло, и препятствие новое: оно вылезло
только при попытке. **Дерево возвращено на последний зелёный коммит; ничего недоделанного в нём нет.**

Шаг 2 ступени 7 удалил VB-*печатный измерительный канал* как чистый вывод. Корпус переносит ту же
формулировку на SV0-половину — и там она неверна. Харнесс S1.5 в `runner.rs` вычисляет
`sv0_bench_lighting_flags` из ABBA-счётчика фаз и продевает его в сцену каждый кадр: он не просто
ОТЧИТЫВАЕТСЯ об переслоённом A/B, он им УПРАВЛЯЕТ, меняя то, что затеняет марчер. Значит удаление
канала замера удаляет **вход рендер-пути** — а это не то же самое, что удалить принтер.

Отсюда второй вопрос, и он ваш по той же причине, что и первый:

* **Списать весь харнесс S1.5** — драйвер фаз вместе с принтером. Переписанные числа остаются в
  плане; в дереве их после этого никто не воспроизводит.
* **Оставить драйвер фаз, удалить только замер** — A/B продолжает идти и продолжает менять кадр, но
  его никто не мерит. Это вход сцены без потребителя, и у кампании для такого есть имя: величина,
  которую ничто не может заставить сдвинуться.

Рекомендую первое. Второе оставляет механизм, единственным смыслом которого было быть измеренным.

**Это блокирует два последних коллектора и механический гейт ступени.** Всё выше по течению отгружено
и зелено.

---

## 2026-08-10 — ступень 7, шаг 6 упёрлась в вопрос ОБЪЁМА: `engine_grand_showcase_512_gpu_pass_cost` портируем или списываем?

Два последних GPU-коллектора (`TimestampCollector`, `Sv0TimestampCollector`) нельзя удалить, пока
кто-то читает их длительности, — и один такой есть: `#[ignore]`-принтер
`engine_grand_showcase_512_gpu_pass_cost` в `crates/boyko_rhi_vulkan/tests/window_present_gbuffer.rs`.
Его сосед `software_ray_baseline_cost.rs` мигрировал за десять минут — выяснилось, что он держал
коллектор как обычный массив query-пулов. Этот действительно читает потайминги.

**Почему это не просто порт.** `run_showcase_body_ddgi` строит ОДИН литерал `GBufferScene` (~230
строк) и держит его весь цикл замера. Зонной ноге нужно каждый кадр: `open_frame` (`&mut`) → общий
заём, положенный в `scene.gpu_zone` → present → `retire` (`&mut`). Время жизни этого заёма сидит в
типе `GBufferScene<'a>`, поэтому присвоение `None` между кадрами его не отпускает, и `&mut`/`&` не
чередуются. Раннер `boyko_app` с этим не сталкивается, потому что пересобирает сцену каждый кадр;
здесь пришлось бы занести 230-строчный литерал внутрь цикла.

**Три выхода.**

1. **Пересобирать литерал каждый кадр.** Механически, локально — и 230-строчная конструкция начнёт
   выполняться 200+ раз вместо одного. Её никто не мерил, так что цена неизвестна, а не мала.
2. **Дать `GpuZoneRecorder` внутреннюю изменяемость**, чтобы `open_frame`/`retire` брали `&self`. У
   `FrameSlot` уже есть два атомика и `UnsafeCell`, так что это та же правка, что ступень 5c сделала
   для `CommandWitness`, уровнем ниже. Самый переиспользуемый ответ — и тот, что расширяет
   поверхность ядра: слот кольца сможет занять любой, у кого есть `&GpuZoneRecorder`.
3. **Списать харнесс.** Это `#[ignore]`-принтер; те же четыре скобки несёт зонный артефакт; его числа
   уже переписаны в план HW-RT. На этот вариант указывает собственный прецедент корпуса —
   `vb_bench_totality_gate.rs` и `vb_zone_ab_witness_gate.rs` оба были удалены, а не мигрированы,
   когда их предмет уехал.

**Это вопрос ОБЪЁМА, поэтому он ваш.** Рекомендую **3**: вариант 2 покупает возможность ядра ради
одного вызывающего, которому пересборка сцены за кадр даёт то же самое бесплатно, — но харнесс это
опубликованный измерительный канал HW-RT R0, а удалять канал не мне. **Пока ответа нет, ступень 7
шаг 6 стоит**, и механический гейт остаётся невыполнимым.

---

## 2026-08-10 — механический гейт ступени 7 удовлетворяется УДАЛЕНИЕМ записи о том, что он гейтил. Исправлено в корпусе; выношу сюда, потому что это правка СПЕЦИФИКАЦИИ.

Корпус гейтит ступень 7 на `rg 'TimestampCollector|VbTimedPass|Sv0TimedPass' crates/` → ноль
совпадений. После VB-половины удаления в дереве **ноль ссылок в КОДЕ и около дюжины в прозе** —
`gpu_zone.rs` объясняет, остатком чего являются его десять `ZONE_VB_*`; `command_witness.rs`
восстанавливает дефект стадий 7c по коллектору, который его нёс; `vg_occ_split_timing.rs` называет
канал, который читала его таблица.

`rg` не отличает выжившего ПОТРЕБИТЕЛЯ от комментария, который ЗАПИСЫВАЕТ, что было удалено и
почему. Удовлетворить гейт буквально — значит стереть ровно ту измеренную историю, ради которой
кампания и существует. **Я сузил его до КОДА** и записал обоснование прямо у гейта — так же, как уже
сужен второй механический гейт (`crates/*/src`). Пишу сюда, а не решаю молча, потому что это сужение
заданного гейта, а расширить его обратно — решение владельца.

## 2026-08-10 — удаление старого VB-коллектора удалило единственный сторож таблицы стадий. Констатировано, не починено.

`zone_begin_stage` говорит, на какой стадии конвейера открывается каждая VB-зона. У неё был
настоящий сторож, пока существовала независимо написанная вторая копия `VbTimedPass::begin_stage`:
клауза стадий в `G10` сравнивала их штамп за штампом — 26 кадров, 520 таймстампов, всё совпало.
Именно эта клауза поймала молча изменённые стадии 7c после пяти зелёных коммитов.

Ступень 7, шаг 5 удаляет ногу A — у сравнения не осталось второй стороны. Замена: `const`-блок,
пинящий каждый из десяти id к той стадии, на которой обе таблицы сходились. **Он ловит правку строки
рукой и НЕ ловит скобку, перенесённую туда, где верна другая стадия** — это измерение, и после этой
ступени оно принадлежит ступени 8. Действий не требуется; потеря названа прямо у функции, чтобы
никто не переводил таблицу заново, считая её проверенной.

---

## 2026-08-09 — Ступень 3d отгрузила две зоны там, где корпус предписывал колонку `RoundRecord` на 90.8 КиБ. Обратимо; владельцу стоит знать цену.

**ОБЪЁМНОЕ решение, раскрытое, а не спрошенное**: сама развилка была перф-архитектурной, а такие я
решаю сам, с числами. Сюда попадает единственное, что числа не решают: предписанной публичной
поверхности (`Profiler::rounds(back) -> &[RoundRecord]`) не существует, и отменить это — решение
владельца, если потерянная величина важна.

**Что просил корпус.** `RoundRecord { frame, round, dispatched, begin, end }`, 24 Б × 121 кадр ×
`MAX_ROUNDS_PER_FRAME = 32` = **90.8 КиБ** резервации, хранящие *«только форму диспетчеризации:
раунды на кадр, ширина волны, длительность раунда»*.

**Что отгружено.** Два зонных сайта на диспетчере — `__round` (Span) и `__round_width` (Counter), —
из которых раунды на кадр это `count` у `__round`, длительность раунда — его `total`/`min`/`max`, а
ширина волны — у `__round_width`. Все три названные величины, покадрово, распределениями, а не одной
строкой на каждую.

**Почему, в порядке реального веса причин.**

1. **Путь записи.** Это решающая причина, и она не про оптимизацию. Диспетчер **не держит**
   `&mut EcsMaster`, пока раунд в полёте: отчеканенный им `UnsafeEcsCell` разделён с воркерами.
   Запись в колонку оттуда требует либо второго опубликованного указателя в резервацию, пишущего с
   потока, которого `&mut` свёртки не покрывает, либо скретч-буфера на расписание, сбрасываемого
   после прогона, — то есть профилировочного состояния во владении планировщика. У пуша в полосу нет
   ни того, ни другого, и это тот самый механизм, который ступень 3c уже благословила для
   `SystemSpan`.
2. **Никакого усечения.** `MAX_ROUNDS_PER_FRAME = 32` посчитал бы 33-й раунд расписания с глубокой
   цепочкой зависимостей *отброшенным*, а не измеренным, да ещё со своим классом потерь. Две зоны не
   усекают ничего.
3. **90.8 КиБ и один класс потерь не потрачены.**

**Что потеряно, и это настоящая потеря.** **Корреляция** между шириной одного раунда и длительностью
того же раунда — «был ли самый широкий раунд и самым длинным?». Покадровые агрегаты на это не
отвечают. Ни один вопрос в корпусе профилирования сегодня этого не спрашивает — поэтому решение
такое.

**Как отменить:** вернуть `RoundRecord` со скретч-сбросом в `ExecutorScratch`, принять усечение на
32 раунда со счётчиком, а две зоны оставить или убрать. Скажите слово.

*(Попутно едут два меньших отступления, решения не требующие: `Interval.sys` стал `Interval.zone`,
потому что `sys_of` больше нет и зона → система разрешается во время отчёта; и у `G8` нет ветки
SKIP, потому что `ThreadPoolBuilder::num_threads(2)` не спрашивает машину, так что меньше двух
воркеров было бы дефектом пула, а не средой, которую надо извинять. Оба разобраны в
`docs/diagnostics/profiling/05-LADDER-GATES.md`.)*

---

## 2026-08-09 — У dev-бюджета резидентности запас 1.3 МиБ, а не 9 МиБ, как следует из таблицы корпуса. Противоречие по `Z` из `J1`, теперь с числом.

**Записано, а не спрошено** — это решать `J1`, и уже логировано на ступени 3a. Ново здесь измерение,
и оно меняет ощущение срочности.

`profiling_residency` теперь печатает свою конфигурацию. На этой машине, вооружённым, анализ ВКЛ:
**итого 14 667 776 Б (резервация 14 614 528, статики 53 248)** против dev-бюджета в 16 МиБ.

Собственные dev-строки корпуса — **6.67 МиБ** (анализ выкл) и **7.05 МиБ** (вкл), примерно вдвое
меньше. Разрыв — *не* новое кольцо интервалов, на которое приходится 262 144 Б. Это `D8` с
`Z = 1024` против отгруженного `ENGINE_ZONE_SLOTS = 4096`: пять колонок дают
21 Б × 4096 × 121 = 10 407 936 Б там, где таблица закладывает 21 Б × 1024 × 121 = 2 601 984 Б.

То есть таблица размеров и отгруженная константа расходятся вчетверо со ступени 2, и следствие в
том, что dev-бюджет утилизирован на **87 %**, а не на ~44 %, как читается по таблице. Починка за
`J1`: либо `ENGINE_ZONE_SLOTS` опускается до 1024, либо каждая строка размеров в
`profiling/01-EMISSION-STORAGE.md` пересчитывается на 4096 и вместе с ней переводится розничный
бюджет. Сегодня ничто не заблокировано; просто запас куда тоньше, чем читается.

---

## 2026-08-08 — Весь блок кодов `92xx` описывал не те восемнадцать условий. Починено; записано из-за ТОГО, КАК оно спряталось.

**Записано, а не спрошено.** Направление починки не было спорным, и она уже закоммичена. Сюда
относится механизм — потому что это дыра в собственной конструкции гейтов этого проекта, и она
повторится.

**Что было не так.** L2 зарезервировала восемнадцать строк `92xx` под профилировщик — правильно и по
хорошей причине — а затем написала восемнадцать правдоподобных сводок, сочинённых из *номеров*
кодов. Шестнадцать из них называют условия, которых в корпусе профилирования нет. `W9207` — самый
острый случай: корпус закрепляет его как **отсутствие инвариантного TSC** в пяти документах, а
логгерский `W0101` **вычеркнут в его пользу**, так что выдуманная сводка («пул GPU-запросов вернул
меньше результатов, чем было выдано») оставила единственный в движке код про инвариантный TSC
называющим другое, а условие, ради которого вычеркнули `W0101`, — вовсе без кода. `9213` в корпусе —
`E9213` (шесть упоминаний, четыре файла), а засеян был как `W9213`.

**Почему это никто не поймал — и вот эта часть стоит того, чтобы её сохранить.** У реестра семь
проверок, и все семь были зелёными. Строка `Pending` не должна **ни doc-страницы** (проверка 2 —
только для `Live`), **ни эмиттера** (проверка 3a — только для `Live`); оба сужения верны сами по
себе, и L2 их письменно обосновала: *«иначе L2 задолжала бы восемнадцать страниц для кодов без
эмиттеров, а это doc-rot, изготовленный гейтом»*. Это рассуждение по-прежнему правильное. Но два
сужения вместе означают, что сводка строки `Pending` сравнивается **ни с чем**, по построению — а
сообщение самой проверки 4 этот класс дефекта уже называет: *«сочинять сводку здесь — это то, как
три строки этого реестра разошлись с сообщениями, которые движок печатает»*. Реестр задокументировал
свой отказ, а потом отгрузил его в шестикратном масштабе — ровно в том статусе, куда ни одна
проверка не смотрит.

**Обобщение, которое НЕ починено и ради которого это записано.** Строка `Pending` — это **обещание
без гейта на содержимое**. Сегодня единственное, что когда-либо проверит сводку `92xx`, — ступень,
переводящая её в `Live`, то есть от одной до четырнадцати ступеней отсюда. Ступень 2 переводит семь
из них и перечитывает остальные одиннадцать; эти одиннадцать всё ещё не сверены, и если более
поздняя ступень переведёт одну из них, не перечитав корпус, выдуманная фраза уедет в отгрузку.

**Два способа закрыть, ни один здесь не взят** (оба крупнее ступени 2, и один — вопрос ЦЕННОСТЕЙ):

* **(a) Проверка, пришпиливающая сводку каждой строки к корпусу.** Механически: для каждой строки
  `92xx` требовать, чтобы текст её условия встречался в списке §Integration файла
  `docs/diagnostics/profiling/05-LADDER-GATES.md`. Дёшево, и поймала бы все шестнадцать. Цена: это
  привязывает формулировку реестра к формулировке документа — второе утверждение одного факта,
  ровно та форма, которую этот корпус в других местах удаляет.
* **(b) Не засевать сводки вовсе** — строка `Pending` несёт ступень и пустую сводку, а сводка
  пишется тогда же, когда эмиттер. Структурно правильно, и именно так уже устроен `FORWARD_DECLARED`
  для семнадцати кодов логирования. Цена: `explain()` не вернёт ничего полезного для кода `Pending`,
  а это реальная потеря для того, кто читает документ корпуса сегодня.

**Ничего не блокирует.** Ступень 2 приехала с починенными сводками и семью строками, переведёнными в
`Live` вместе со страницами. Остальные одиннадцать верны на момент этого коммита и не загейчены
после него.

---

## 2026-08-08 — L5 отгружена, и она ослабила заданную границу задержки. Решение не требуется, если только вы не захотите вернуть кадр.

**Записано, а не спрошено.** Это архитектурная развилка, которую я решил сам, имея дерево перед
глазами; она здесь потому, что делает *заданное* число хуже, а число, которое тихо ухудшилось, —
ровно то, ради чего этот файл существует.

**Ситуация.** Корпус логирования говорит, что `log_drain_system` идёт «в `Last`», и задаёт границу
внутрикадровой задержки как **один кадр** под `Scheduled` / «парковка стока + один кадр» под
`Thread`. В этом движке `Last` НЕТ. `CoreSchedule` — **закрытое множество из двух** (`Main`,
`Fixed`), и его собственная документация даёт нужный ответ: *«более мелкая структура ВНУТРИ
расписания — это то, ради чего существуют множества фазы 15»*.

**Что отгружено.** Дренаж идёт в `Main`, `in_set(LogSet)`, а `LogPlugin::build` интернирует
множество, чтобы хостовое `.before(LogSet)` разрешалось независимо от порядка добавления плагинов.

**Чего это стоит.** Без рёбер порядка планировщик волен поставить дренаж в любое место кадра, так
что запись, испущенная *после* него, попадёт в кольцо следующего кадра. **Каждая заданная граница
получает +1 кадр** для хоста, который ребро не добавил. У дренажа нет конфликтов по данным ни с
чем, так что сам по себе он поздно не встанет.

**Три способа вернуть кадр**, ни один не выбран — все крупнее, чем L5:

* **(а) Ничего не делать** — задокументировать ребро и дать каждому хосту добавить
  `.before(LogSet)`. Это и отгружено. Стоит одной строки на хост; стоит кадра, если хост забыл, —
  молча.
* **(б) Добавить третий вариант `CoreSchedule`.** Честно, дословно совпадает с корпусом — и трогает
  драйвер кадра, методы маршрутизации и каждый вызов `add_systems_in`. Правка движка ради одной
  подсистемы.
* **(в) Дать движку постоянное `EngineSet::Last`**, перед которым плагины приложения выстраивают
  всё остальное. Дешевле (б) и полезно шире логирования, но это соглашение о планировании, которого
  у движка пока нет, — вводить его из шва логирования значит дать хвосту вилять собакой.

**Ничего не блокирует.** Граница реально измеряется на `G15` (L16), и мерить её надо против того из
трёх вариантов, который будет истинным тогда.

---

## РЕШЕНО 2026-08-08 — владелец выбрал (б): поднять бюджет. Q1 остаётся, правок кода нет.

**Решение:** принять след и поднять отгружаемый бюджет гейта с **1024 КиБ до 1280 КиБ**.
`LANE_COUNT` остаётся 80 во всех профилях; `REGION_CAPACITY` остаётся 128; D15 по-прежнему
коммитит плиту сэмплов при первом `arm`. **Ни одной правки исходников.**

**Почему это верный ход — в формулировке самого владельца.** Тревожное число мерило
*резервацию*, а не то, что держит машина. Строка теперь печатается в трёх колонках вместо одной,
которая их смешивала:

| Колонка | Профайлер, shipping | Что это |
|---|---|---|
| объявлено / зарезервировано | **1 208.2 КиБ** | адресное пространство; на 64 битах бесплатно в любом практическом смысле |
| закоммичено при `arm` | **≈ 1 142 КиБ** | резервация, берётся когда диагностику **включили** |
| резидентно при выключенном флаге | **≈ 0** | ничего не вооружено, полоса не захвачена, страница `.bss` не тронута |

Владелец спросил, стоит ли это рантайма или оперативы. В отгружаемом умолчании — ни того, ни
другого: при выключенных флагах сайт платит **одну загрузку байта из `.bss` и одно предсказанное
ветвление**, а выше компиляционного потолка не платит вовсе — сайт и его аргументы удалены. Этот
пол — единственное, что рантайм-флаг снять не может, и выбор бюджета его не трогает.

**Следствия применены:** `G23a` и `G23b` **разблокированы** — проверяют против 1280 КиБ и снова
имеют достижимое зелёное состояние. Варианты (г) `REGION_CAPACITY = 64` и (а) ленивый коммит по
полосе **остаются ниже как рычаги**, а не как работа: тянуть один из них только если замер потом
скажет, что велика *закоммиченная* цифра.

*(Ниже — исходная запись без правок: запись о том, почему так решили, переживает само решение.)*

---

## 2026-08-08 — ⚠️ Q1 поднял отгружаемый след диагностики на 1.07 МиБ, и заголовок профайлера «≤ 1 МиБ retail» теперь ЛОЖЕН

**Это отменяет цифру ≈ 2.08 МиБ из записи третьего раунда ниже.** Верная совместная цифра —
**≈ 3.15 МиБ**.

**Что произошло.** На ступени D0 я закрыл архитектурный блокер **Q1**, удалив профильную ось у
`LANE_COUNT`: он был 32 в отгружаемых профилях, тогда как индексируемая им величина
`boyko_threadpool::MAX_WORKERS = 64` безусловна, — значит 32 несостоятельны и ниже собственного пола
топологии в 66. Решение (80 во всех профилях, `455c074`) верное, и я за ним стою. **Чего я тогда не
сделал — не протянул его стоимость**, а этой константой были размерены четыре ячейки в двух планах:

| Ячейка | Было (32 полосы) | Стало (80 полос) | Род |
|---|---|---|---|
| профилирование `LANES` (`.bss`) | 8 КиБ | 20 КиБ | зарезервировано |
| профилирование **плита сэмплов** | 192 КиБ | **480 КиБ** | **коммитится при первом `arm`** |
| логирование `LOG_LANES` (`.bss`) | 512 КиБ | 1.25 МиБ | зарезервировано |
| логирование `SAMPLE_CTR` (`.bss`) | 16 КиБ | 40 КиБ | зарезервировано |

Половина профайлера **908.2 → 1 208.2 КиБ**; половина логгера **1 220.26 → 2 012.26 КиБ**;
совместно **2.08 → 3.15 МиБ**. Цифры `dev` не сдвинулись вовсе, потому что `dev` уже был на 80, —
**именно поэтому это никто и не поймал: всякая проверка, смотревшая на одну строку, смотрела на ту,
которая осталась верной.**

**Из этого следуют две вещи, и это не «число выросло».**

1. **Профайлер теперь превышает СОБСТВЕННЫЙ бюджет**, а не только совместный: 1.18 МиБ против
   заявленного ≤ 1 МиБ. Гейты **G23a и G23b проверяют эту границу**, значит оба падают уже на базовой
   линии — у них нет достижимого зелёного состояния, пока вопрос не решён.
2. **Из +1.07 МиБ закоммиченной памяти только 288 КиБ.** Остальное — резервированный `.bss`, чья
   резидентная стоимость идёт по *тронутым* полосам, и это ровно то свойство, которое и делало
   «80 везде» приемлемым. Закоммиченная часть — целиком плита сэмплов профайлера, которую **D15**
   коммитит сразу на все `LANE_COUNT` полос при первом `arm`.

### Решение

> **РЕКОМЕНДАЦИЯ, окончательная: (б) — поднять бюджет и переписать строку честно.** Владелец задал
> верный вопрос: *3.15 МиБ — это вообще много?* Нет. И, что важнее, **эта цифра меряет не то.**
> Это **резервация**. Машина реально держит:
>
> | Конфигурация | Резидентно |
> |---|---|
> | отгружаемая игра, флаг диагностики ВЫКЛ (умолчание) | **~0** — все таблицы это demand-zero `.bss`, которого никто не касается; ничего не вооружено, ничего не закоммичено |
> | отгружаемая игра, диагностика ВКЛ | плита сэмплов профайлера (480 КиБ, коммит при `arm`) плюс **тронутые** полосы логгера и стейджинг — порядка **1 МиБ**, а не 3.15 |
>
> Адресное пространство на 64 битах бесплатно в любом практическом смысле, а 1 МиБ резидентно
> против одной текстуры 2048² RGBA8 в 16 МиБ — не та сделка, которую стоит покупать кодом. Значит
> самая дешёвая правка та, что не меняет ни кода, ни константы: **поднять бюджет гейта**
> (1024 → 1280 КиБ покрывает 1208.2 с запасом) и печатать строку в трёх колонках —
> зарезервировано, закоммичено при arm, резидентно при выключенном флаге — вместо одного числа,
> которое их смешивает.
>
> Это ещё и разблокирует G23a/G23b сразу, тогда как (г) и (а) — только после правки кода.
> **(г) и (а) ниже остаются рычагами на случай, если замер потом скажет, что закоммиченная цифра
> велика**, а не задачами на сейчас.

- **(г) — остаётся дешёвым рычагом, но больше не рекомендация.** Поставить `REGION_CAPACITY = 64` в отгружаемых
  профилях вместо 128. Плита считается как `LANE_COUNT × 2 региона × REGION_CAPACITY × 24 Б`,
  значит `80 × 2 × 64 × 24` = **240 КиБ** вместо 480, и строка встаёт на
  `66 + 240 + 636 + 6.8 + 11.4 + 8` = **968.2 КиБ — под бюджетом в 1024 КиБ**, при нетронутом Q1.

  **Цена:** регион держит 64 сэмпла вместо 128 до того, как свёртка обязана его слить, — под
  всплеском отгружаемая сборка начнёт ронять сэмплы раньше. В shipping ярус только `Always`, так
  что поток сэмплов там и без того на порядок реже, чем в `dev`.

  **Чего это НЕ стоит:** ни строки кода, ни одной ветки на горячем пути, ни одного
  переспецифицированного гейта. `REGION_CAPACITY` **уже** профильная константа — в отличие от
  `LANE_COUNT`, чью ось Q1 удалил как несостоятельную, — то есть это ручка, делающая ровно свою
  работу.

  *Я предложил (а) первой, потому что смотрел, где лежат байты, а не что дешевле всего их убрать.
  Порядок обратный: сперва константа, которая для этого и заведена, потом код.*

- **(а) — теперь запасной вариант, если замер покажет, что 64 сэмпла на регион мелко.** Коммитить
  регионы сэмплов **по полосе при первом использовании**, а не
  все 80 при `arm`.

  **Проблема (а) не в производительности, а в гейте.** Горячий путь и так грузит
  `buf: AtomicPtr<Sample>` на каждом сэмпле, так что проверка на null — один `test`+`jz`,
  предсказываемый не-взятым после первого сэмпла на полосе, то есть ноль. Сам коммит — один
  системный вызов на полосу на `#[cold]` пути, десяток за процесс. Но есть две настоящие цены:
  вызов ложится **внутрь кадра** (первая зона рабочего потока, в первых кадрах, где времена и так
  шумные — лечится коммитом при `arm()` для уже существующих полос, пул строится раньше `arm`), и
  он добавляет unsafe-поверхность на самый горячий путь профайлера. Главная же:
  **G23a/G23b перестают уметь утверждать один итог при arm** — цифра становится зависящей от
  прогрева, и чёткий гейт превращается в «после N кадров». Отгружаемая игра на 8-ядерной машине занимает примерно
  `рабочие + диспетчер + хост ≈ 10` полос, то есть ≈ 60 КиБ плиты вместо 480 — профайлер снова под
  1 МиБ **при нетронутом Q1 и без единой изменённой константы**. Это правка **D15** («коммитится
  один раз при первом arm, никогда не освобождается»), а это решение об отгружаемом поведении, —
  потому оно ваше, а не моё.
- **(б)** Принять 3.15 МиБ резерва / ~1.2 МиБ коммита и переформулировать заголовок.
- **(в)** Вместо этого срезать таблицу: `LOG_LANES` логгера (1.25 МиБ), `SINK_OUT` (256 КиБ) или
  арены динамических зон профайлера (96 КиБ по плану профилирования против 40 КиБ по `SEAM.md` —
  это расхождение всё ещё открыто и закрывать его плану профилирования).

**Блокирует:** ступени 2 и 10 профилирования (G23a/G23b). **Не** блокирует L0 логирования и ничего
на лестнице подложки.

**Урок, записанный потому, что эта форма встречается в кампании четвёртый раз:** итог, идеально
сходящийся со своими напечатанными операндами, не доказывает ничего о том, актуальны ли *операнды*.
2.08 МиБ были верной арифметикой над двумя половинами, которые подложка уже отменила. Ловит это не
проверка «сходится ли сумма», а вопрос «не было ли принято решение о том, от чего эта сумма
зависит, уже после того как её записали».

---

## 2026-08-06 — ⚠️ ИЗМЕРЕНО: sync-validation не жива, поэтому нога `-ValidationOn` ничего не доказывает про барьеры

**Настоящий пропущенный барьер не сдвинул ни одного пикселя и не выдал ни одного сообщения.**
Исполнено при разрешении первого шага части 2, который ровно для этого и существовал.

Зонд: удалить ЕДИНСТВЕННОЕ объявленное чтение ресурса, у которого ровно один читатель — чтение мипа
`d-1` пирамидой HZB, — оставив диспатч, который его читает. Пас 0 пишет мип 5, пас 1 читает мип 5,
зависимость не выводится.

| | сообщений | `SYNC-HAZARD-*` | golden |
|---|---|---|---|
| база (×2, тот же билд) | 19 | — | побайтово тот же |
| **настоящий пропущенный барьер** | **19** | **нет** | **побайтово тот же** |

Бит фичи в `boyko_rhi_vulkan/src/device.rs` ЕСТЬ, но цепочка инстанса **молча** деградирует при
отсутствии `VK_EXT_validation_features`, а вся база из 19 сообщений — это `vkCreate*`-время: ни одно
из них не порождено записанным кадром.

**Почему это здесь, а не просто записано.** Это не факт части 2. Это значит, что валидационная нога
движка — инструмент, на который кампания опирается с момента починки `-ValidationOn` на P1-2, —
покрывает легальность объектов, дескрипторов и форматов и **ничего про синхронизацию**. Каждое
«validation clean» в сообщениях коммитов истинно и уже, чем читается.

**Варианты.** (a) Оставить, а корректность барьеров гейтить структурно (пинить выведенный поток
барьеров по ПОЛЯМ — что G4 части 2 теперь и делает). (b) Выяснить, действительно ли
`VK_EXT_validation_features` отсутствует на устройстве или просто не доходит до слоя, и починить,
если второе, — это расследование примерно на час, и оно вернуло бы универсальный инструмент.
(c) Оба.

**Моя рекомендация — (c), сначала (a)**: (a) уже описан и ничего не блокирует, а (b) стоит сделать
до части 3 — именно там появляется первый ЧИТАТЕЛЬ пирамиды, а read-after-write через два паса это
ровно тот класс гонки, который слой ловит, а golden не видит.

⚠️ **Методическая заметка, стоящая не меньше самой находки.** ПЕРВЫЙ зонд был неразрешающим по
построению: он удалил одного из ШЕСТИ объявленных читателей того же образа, соседи по-прежнему несли
и переход, и зависимость, и не проверялось ничего. Его отрицательный результат был бы записан как
«расширение отсутствует на этом устройстве» — верное утверждение, полученное негодным путём. Прежде
чем зондировать пропущенную зависимость, сосчитайте ОСТАЛЬНЫЕ объявленные доступы к этому ресурсу.

---

## 2026-08-05 — релизная нога CI красная, и два класса из неё — вопрос СТРОГОСТИ

Найдено при подготовке базовой линии P1-5a, которой нужна проходящая нога. Прогон собственной
команды CI — `cargo test --workspace --all-targets --release --exclude boyko_demo --exclude
bench-bevy-vs-boyko` ([ci.yml:62](../../.github/workflows/ci.yml), `:103`) — **падает на шести
целях**. Ещё две всплывают на втором прогоне, и это само по себе диагноз: они флейковые, а не
релизные.

Шесть были механическими и **починены**: пять `#[should_panic]` над `debug_assert!` без
`#[cfg(debug_assertions)]` (boyko_math, boyko_ecs, boyko_render ×3 плюс две в boyko_rhi_vulkan) и
одна пропущенная запись в `VB_PINS` — моя, `vb_mesh_hzb` из VG R3 P1-2.

Осталось два класса, и оба — решения о том, насколько строгим должен быть гейт, а не архитектурные
развилки, поэтому они здесь, а не приняты мной.

### 1. `boyko_shaderdsl --test eval_byte_identity` — три падения, ровно на ЗНАКОВОМ БИТЕ NaN

`NaN (0xffc00000)` против `NaN (0x7fc00000)`. Оба тихие NaN; значения тождественны (NaN не равен
даже себе); различается только знак.

Это то же семейство, что гейт G3 намерил на пирамиде глубины в тот же день: **знак нуля и знак NaN —
ровно те два бита, которых не видит ни одно `<` в программе**, поэтому железо и оптимизаторы вправе
их двигать. G3 поймал драйвер, свернувший compare-and-select в аппаратный min с другим разрывом
ничьей на ±0. Ожидать стабильности любого из этих битов между `-O0` и `-O3` необоснованно.

**Варианты.** (a) Сравнивать NaN как «оба NaN», а не по битам. (b) Канонизировать знак перед
сравнением. (c) Оставить как есть, признав, что релизная нога eDSL — не гейт.

**Моя рекомендация — (a).** Контракт, ради которого eDSL существует, — про ЗНАЧЕНИЯ, а знак NaN
значением не является. На конечной области — то есть на всей области, которая важна, — это не стоит
ничего, и это снимает вечную красноту с настоящего гейта. Вечно красный гейт хуже слегка суженного:
его перестают читать.

### 2. Два теста над глобальным состоянием, флейкующие при параллельном исполнении

`boyko-scene bundles_s6::interner_is_off_the_per_frame_path` читает процессно-глобальный
`identity::interner_len()`. `boyko-ui zero_alloc::unchanged_frame_layout_pair_allocates_zero_over_baseline`
читает глобальный счётчик аллокаций — и сообщил дельту **минус один**, то есть улучшение, которое его
`assert_eq!` выразить не может, хотя собственное сообщение теста говорит «не больше».

Оба проходят в одиночку, проходят при `--test-threads=1` и проходят в debug. Падают только в релизе
при параллелизме по умолчанию.

**Варианты.** (a) Последовательный guard в каждом из этих файлов. (b) `--test-threads=1` для этих
бинарей в CI. (c) Сделать утверждение UI `<=` по его же сообщению, а сериализовать только scene.

**Моя рекомендация — (a) плюс починка на `<=`**: флаг гарнизона замедлит все тесты этих крейтов ради
двух, а равенство, падающее на улучшении, упадёт снова, как только кто-нибудь улучшит ещё раз.

---

## 2026-08-05 — ОБЪЁМ: пирамиде нужна правка ядра фреймграфа (per-subresource состояние)

**Решено и уже делается, ничего не блокирует — записано потому, что это расширяет часть 1 за
пределы её плана.** Шаг P1-5 (объявить пасы сборки HZB) невозможно написать против нынешнего
фреймграфа, и фреймграф говорит об этом сам.

**Стена.** `framegraph/graph.rs:360-445` содержит `INVARIANT HZB-SUBRESOURCE-UNIFORM`: все доступы к
одному `ResId` обязаны объявлять один и тот же `(base_mip, mip_count, base_layer, layer_count)`,
потому что `FrameGraph::state` — это `Vec<ResSync>`, индексируемый только по `ResId`, а `transition`
пролёта вообще не получает. Сборке HZB нужно на ОДНОМ образе в ОДНОМ пасе читать мип `6p-1` и писать
мипы `[6p, 6p+n)`.

Тот комментарий написан заранее, называет именно этот пас и предписывает ответ:

> «PER-SUBRESOURCE TRACKING — ПРАВИЛЬНЫЙ ДОЛГОСРОЧНЫЙ ОТВЕТ, и этот assert — его ТРИГГЕР… сборка HZB
> пишет мип k, читая мип k-1. Когда этот пас будет написан, он уронит этот assert. Это и есть
> ЗАДУМАННЫЙ способ обнаружить работу… Ответ — построить per-subresource трекинг, а не ослаблять
> условие, пока оно не замолчит.»

**Это не просто debug-assert.** В release он вырезан, и вывод барьеров становится по-настоящему
неверным; прослежено на 512×512: пас 0 впервые касается мипов [0,6), только они выходят из
`UNDEFINED`; пас 1 затем пишет мипы [6,10), а состояние считает GENERAL для всего ResId, поэтому у
выведенного барьера `old_layout == new_layout` и мипы 6..9 **не переводятся никогда**, хотя диспатч
пишет в них через storage-дескрипторы, объявленные как `GENERAL`. Достижимо на любом экстенте с
`prev_pow2(max(W,H)) >= 64`.

**Обходной путь, который я отверг.** Три ResId, ссылающиеся на один `VkImage` по непересекающимся
диапазонам мипов, единообразны по построению и не требуют правки фреймграфа. Отказался по двум
причинам. Это буквально то, что запрещает текст самого инварианта («не подгоняя объявления руками»),
и это тупик уже через одну часть: cull части 3 выбирает УРОВЕНЬ пирамиды per-instance, а ResId
на-пас динамическим уровнем не назовёшь. Взять его — значит принять временный дизайн с откладыванием
правильного, что в этом проекте запрещено.

**Чего это стоит.** Новый шаг P1-5a перед P1-5, трогающий ядро машины состояний, через которое
компилируется каждый путь рендера. Аргумент за побайтовую идентичность сильный —
`SubRange::color_mips` сегодня не вызывается ниоткуда, и все существующие сайты `image_access`
передают `base_mip: 0, mip_count: 1`, так что per-mip машина обязана свернуться в сегодняшнее
поведение барьер-в-барьер, — но именно из-за слова «обязана» этому нужен свой гейт, а не golden-пин:
пин не видит ни лишнего барьера, ни пропавшего.

**Ответ ничего не блокирует.** Архитектурные развилки я решаю сам; это здесь потому, что вырос
ОБЪЁМ, и владелец может предпочесть остановить часть 1 на «выделено и скомпилировано», отдав работу
по фреймграфу отдельной кампании. Скажите — разделю.

---

## 2026-08-04 — ⚠️ `golden.ps1 -ValidationOn` НИКОГДА не включал слой валидации ни на одном пине `boyko-app`

**Найдено при гейтинге VG R3 P1-2, и это снова форма «вакуумно-зелёного».** Этот переключатель —
инструмент валидационного аудита движка; в кампании записана веха «Validation-ON audit — COMPLETE»,
проходившая через него. На 22 пинах из 25, которые грузятся через `boyko_app`, он не мог упасть.

**Механизм.** Бэкенд включает слой по конъюнкции —
`config.enable_validation && BOYKO_DISABLE_VALIDATION не задан` (`boyko_rhi_vulkan/src/device.rs:2350`),
— а рантайм `boyko_app` зашивал `enable_validation: false`. `-ValidationOn` только *снимал
переменную окружения*, то есть удовлетворял второй конъюнкт, пока первый оставался ложным. Слой не
запрашивался, мессенджера не существовало, и сканирование строк `[vk-validation]` поэтому
докладывало **«clean (0 messages)»** безусловно.

Док самого рантайма это и говорил — но читался как проектная заметка, а не как дефект гейта:
«The shipped runner does NOT request the validation layer… a debug validation knob arrives with a
later rung».

**Измерено, а не выведено.** Я собрал образ `512×512` с `mip_levels: 12` (законный максимум — 10).
`vkCreateImage` вернул успех, аудит доложил «чисто». После починки та же порча даёт ровно то
сообщение: `vkCreateImage(): pCreateInfo->mipLevels (12) must be less than or equal to 10`.

**Починено здесь**, потому что от этого зависит несущий гейт P1-2: `BOYKO_ENABLE_VALIDATION`
включает рантайм явно (без переменной загрузка байт-идентична прежней), а `-ValidationOn` теперь
задаёт её вместе со снятием.

**⚠️ ЧТО ЭТО ВСКРЫЛО И В ЧЁМ ВОПРОС.** С реально живым слоем пин `vb_mesh` выдаёт **19 сообщений
валидации** — базовый уровень, которого никто не видел:

| штук | сообщение |
|---|---|
| 9 | `vkCreateComputePipelines()`: compute-шейдер использует дескриптор `[Set 1, …]` |
| 6 | `vkCreateGraphicsPipelines()`: вершинный атрибут на location 1/2 не потребляется вершинным шейдером |
| 1 | **`vkDestroyDevice(): VkDevice has 13 leaked objects that have not been destroyed`** |
| 1 | `vkCreateShaderModule()`: возможность SPIR-V `Geometry` объявлена без включённой фичи |
| 1 | `vkCreateShaderModule()`: возможность SPIR-V `DemoteToHelperInvocation` объявлена без включённой фичи |
| 1 | предупреждение о лимите дубликатов |

Пирамида ни при чём: логи вооружённого и невооружённого прогонов **побайтово совпадают** после
нормализации хендлов, то есть P1-2 не добавляет ничего. Но два сообщения о возможностях шейдеров и
13 утёкших объектов — настоящие, они на флагманском VB-пине, и одно из них — утечка ресурсов.

**Вопрос — про ОБЪЁМ, не про метод.** Варианты: (а) я разбираю и чиню эти 19 сейчас, до продолжения
пирамиды — это утечка и два бага объявления фич на основном пути; (б) я доделываю первую часть, а
валидационный базовый уровень беру отдельной кампанией после; (в) чиню сейчас только утечку,
остальное откладываю. Я склоняюсь к (б): эти 19 появились задолго до нынешней работы, пирамида
доказано чиста относительно них, а вплетение неограниченного аудита в декомпозицию, созданную
*именно* ради локальности объёма, эту декомпозицию и отменит. Но решение ваше — это объём, а
утечка относится к тому роду вещей, которые в ожидании только усугубляются.

---

## РЕШЕНО 2026-08-03 — проект фичи HZB не сходится целиком: разбит

**Ситуация, измеренная по трём кругам ревью, а не почувствованная.**

| круг | закрыто прежних | новых блокеров | новых major |
|---|---|---|---|
| 1 | — | 8 | — |
| 2 | 10 YES / 6 PARTIAL / 0 NO | 3 | ~12 |
| 3 | 31 YES / 12 PARTIAL / 2 NO | 6 | 11 |

Каждый круг честно закрывает почти всё, что поднял предыдущий, и поднимает примерно столько же
заново. После третьего круга блокер есть у **каждого содержательного шага** (S4, S6a, S6b, S7, S9);
четыре чистых шага — это гейты и записи, зависящие от заблокированных. То есть, в отличие от
фундамента, независимого подмножества для сдачи нет.

Изменился и ХАРАКТЕР новых блокеров, и это полезный признак. Это уже не «алгоритм неверен», а
столкновения с отгруженными инвариантами: четвёртый маршрут, которым проект разоружает ступень
R2d-6 (удвоение списка выживших ломает тот самый const-assert, добавленный в R2d-4 против чтения за
границей); вершина `+INFINITY` из фикстуры, доходящая до второго, неогороженного потребителя на
отгруженном пути VB; способность, которая есть покадровый факт ECS, гейтящая объекты, чеканящиеся на
буте, без названного шва между ними.

**Что я из этого читаю.** Фича просто больше, чем удерживает один проходной проект. Фундамент
сошёлся за один круг на каждую часть не потому, что процесс там был лучше, а потому, что S1/S2/S3
были малы, независимы и самодостаточны.

**Предложение.** Разбить фичу так же, как уже разбит фундамент, и дать каждой части собственный круг
проектирования и ревью:

1. **Только пирамида** — выделить, построить, загейтить против хостового оракула S3. Никакой
   интеграции с cull. Она самодостаточна, оракул уже есть, её блокеры локальны.
2. **Только способность и раскол растеризации**, инертно — вторая область не рисует ничего,
   байт-идентичность на пинах доказана.
3. **Интеграция cull** — когда 1 и 2 сданы, и столкновения выше конкретны, а не предсказаны.
4. **Вооружение** с гейтом по множеству отрисованных.

**Цена, названная прямо.** Четыре круга проектирования вместо одного, и фича приезжает позже.
**Цена альтернативы**, тоже названная: четвёртый круг по всей фиче, который по этой статистике
закроет ~30 пунктов и поднимет ~6 новых.

**РЕШЕНИЕ ВЛАДЕЛЬЦА: разбивать.** Каждая из четырёх частей выше получает собственный круг
проектирования и ревью и приземляется независимо, начиная с одной только пирамиды. Записано, чтобы
будущий читатель видел: четыре круга — намеренная структура, а не проект, который всё падал.

**Блокирует.** Ничего.

---

## РЕШЕНО 2026-08-02 — фикстурой глубинной сложности будет Khronos Sponza (решение делегировано мне)

**Ситуация.** Решение (б) выше обязывает завести отдельную фикстуру с настоящей окклюзией. Что в ней
должно быть — не было решено; вопрос по ассетам я вынес, а не закрыл самовольно, после чего владелец делегировал его мне.

Корпус VG — семь скачанных образцов Khronos glTF на сетке 5×3×3, подобранных под плотность
треугольников, и окклюзии в нём почти нет по построению. Фикстуре глубинной сложности нужно
обратное: крупные окклюдеры и существенная геометрия за ними. Классический выбор — Sponza (интерьер
с колоннадой, которая закрывает сильно) или Bistro; ни того, ни другого в манифесте сегодня нет.

**РЕШЕНИЕ ВЛАДЕЛЬЦА: делегировано мне.** Выбрана **Sponza из того же семейства Khronos
glTF-Sample-Assets**, откуда уже берётся корпус плотности; тянется в `assets/vg_occlusion/` под
собственным манифестом, в gitignore и с пином по содержимому — ровно как `assets/vg_corpus/`.

**Почему Sponza, а не Bistro и не Intel Sponza.**

- *Тот же источник, что у корпуса*: форма скрипта загрузки, лицензионная позиция и правило gitignore
  переносятся целиком. Ни новой инфраструктуры, ни нового лицензионного вопроса — а именно эти три
  вещи превратили бы задачу в многосессионный крюк.
- *Тот же путь загрузчика*: glTF/`.glb` через `GlbMeshLoader`, уже отработанный каждым ассетом
  корпуса.
- *У неё правильная СТРУКТУРА окклюзии*, в чём и весь смысл: колоннада плюс верхняя галерея, так что
  камера на уровне пола вдоль нефа имеет дальнюю половину закрытой колоннами. Ровно то, чего у
  корпуса плотности нет по построению.
- *Размер*. В этой сессии мы уже упёрлись в ноль свободного места при `target/` в 73 ГБ. Bistro —
  ~2.4 ГБ, и её glTF-конверсии разного происхождения, что здесь важнее обычного: репозиторий пинит
  по хешу содержимого, а пин на артефакт, который никто не может перевывести, — не пин.
- *Сопоставимость*: Sponza — общепринятый эталон окклюзии и GI, так что снятое на ней число значимо
  и за пределами этого репозитория.

**Риск, который я беру, — называю сразу, а не после.** ОДНА сцена есть та же выборочная уязвимость,
о которой предупреждают заметки корпуса: одну рамку можно подобрать под желаемое. Смягчение —
корпусное же: несколько закоммиченных путей камеры, охватывающих степени закрытости (вдоль нефа —
сильная; с галереи — умеренная; снаружи внутрь — слабая), и связывает **слабейшая**, ровно как
`orbit_mid` связывает корпус плотности. Выигрыш, заявленный с одной только сильной рамки, был бы
дефектом, а не результатом.

**Что останется сделать при постройке.** Пины `source_url` / `archive_sha256` / пофайловый
`glb_sha256` заполняются с первой проверенной загрузки, как заполнялись у `CORPUS.toml`, — а не
пишутся по догадке.

**Блокирует.** Любую перф-заявку по окклюзии. Реализацию не блокирует и на критическом пути HZB не
стоит.

---

## РЕШЕНО 2026-08-02 — перф-заявка окклюзии: вариант (б), отдельная фикстура с глубинной сложностью

**Ситуация.** Корпус VG — инструмент измерения плотности треугольников, намеренно перекомпонованный
на ступени R0b′ ради плотности, а не окклюзии. Измеренный на нём потолок: **1 инстанс из 44
рисуемых** на `orbit_mid` (связывающая рамка) и 11 из 31 на `approach_close`. Min-редуцированный HZB
способен отвергнуть только те инстансы, что не выигрывают ни одного пикселя, так что это жёсткие
верхние границы — и они ограничивают не только окклюзию: инстанс не выигрывает пикселей и когда он
субпиксельный.

Значит HZB и двухпроходную окклюзию можно построить правильно и загейтить на корректность, но
**показать ускорение нельзя ни на одном материале в этом дереве**.

**Варианты.** (а) Сдать с гейтами на корректность и без перф-заявки — как ступень R2d сдана
структурной; честно, и заявка остаётся несделанной, пока не появится материал. (б) Завести сцену с
настоящей глубинной сложностью (интерьер, улица) **отдельной** перф-фикстурой, вне корпуса
плотности, чтобы инструменты не загрязняли друг друга. (в) Признать, что заявку сделает тот проект,
который будет писаться на движке, а не этот репозиторий.

**РЕШЕНИЕ ВЛАДЕЛЬЦА: (б).** Построить сцену с настоящей глубинной сложностью — интерьер или улицу —
**отдельной** перф-фикстурой, намеренно вне корпуса плотности, чтобы два инструмента не загрязняли
друг друга. Пока её нет, ступень HZB сдаётся с гейтами на корректность и без заявки на скорость.

**Продолжение требует решения по ассету** — вынесено отдельным пунктом выше, а не предположено.

---

## РЕШЕНО 2026-08-02 — K2: вариант (в), остаётся отложенным

**Ситуация.** Критерий отсева K2 требует эталонной таблицы Nanite. Она никогда не была получена: UE
не установлен, а установить его я не могу — там принятие EULA и создание аккаунта, что мне запрещено.
Текст самого K2 говорит, что невоспроизводимый эталон *вынуждает переформулировать цель*: абсолютный
целевой показатель вместо относительного.

**Варианты.** (а) Владелец ставит UE и снимает таблицу. (б) Переформулировать цель на абсолютный
ориентир (время кадра при заданном числе треугольников и границе ошибки) и записать, что K2 взят
собственным аварийным выходом. (в) Оставить отложенным и держать цель формально нефальсифицируемой.

**РЕШЕНИЕ ВЛАДЕЛЬЦА: (в).** Остаётся отложенным; цель сознательно остаётся формально
нефальсифицируемой. Ни одна ступень этим не блокируется. Записано, а не тихо снято, — чтобы будущий
читатель не принял молчание кампании о K2 за то, что K2 выполнен.

---

## РЕШЕНО 2026-08-02 — межкадровая корректность окклюзии

**Опасение было моим, и оно было поставлено неверно.** Пирамида предыдущего кадра действительно не
консервативна — но только для **однопроходного** отсечения. В двухпроходном она никогда не последнее
слово: корректность целиком живёт в позднем проходе, который тестирует против пирамиды, построенной
из глубины **этого** кадра. Ранний проход — *непроверяемая эвристика*, чья единственная задача набить
буфер глубины хорошим набором окклюдеров; его ошибки стоят работы в позднем проходе и никогда не
стоят геометрии. Теорема квантифицирована по **всем** возможным выходам раннего прохода, поэтому о
нём вообще ничего доказывать не требуется.

Подтверждено практикой, а не предположено: UE5 Nanite, Assassin's Creed Unity (SIGGRAPH 2015),
Granite, Bevy 0.16 и Unity 6 GPU Resident Drawer устроены одинаково.

Полная формулировка и доказательство: [`../VG-R3-HZB-PLAN.md`](../VG-R3-HZB-PLAN.md) §1. **Решения
владельца не требуется.**

---

## РЕШЕНО 2026-08-02 — HZB: вариант (а), затем ПЕРЕСМОТРЕН НА (б) — сперва фундамент

**Ситуация.** С корректностью разобрались, после чего проект реализации прошёл ревью и вернулся
REJECTED от обоих рецензентов. Блокеры содержательные, не стилистические. Среди них: проект
воскрешает фрустумно отсечённые инстансы и тем самым стирает вооружение ступени R2d-6; неизвестные
границы меша дают **постоянное** ложное отвержение для любого стримящегося меша, и дефект переживает
оба прохода; единственный гейт, способный увидеть ложное отвержение, нельзя построить как написано,
потому что у `vb_depth` нет `TRANSFER_SRC`, а путь считывания помечен как непроверенный и при этом
несущий; и сборка пирамиды, будучи compute, обязана разрезать единственную область dynamic-rendering
растеризатора VB надвое — чего план не рассматривает, а наивная вторая область стёрла бы ранний
проход через `LOAD_OP_CLEAR`.

Полный список: [`../VG-R3-HZB-PLAN.md`](../VG-R3-HZB-PLAN.md) §5.

**Варианты.** (а) Ещё один круг ревизии проекта против восьми блокеров, затем реализация — тот же
цикл, что провёл ступень R2d от восьми блокеров до сданной. (б) Строить сейчас бесспорный фундамент
(S1 — `TextureView` в RHI, S2 — страж пер-субресурсной синхронизации во фреймграфе, S3 — хостовый
оракул), пока проект отсечения переделывается: эти три самостоятельно полезны и ни один не зависит
от спорных частей. (в) Заморозить ступень.

**РЕШЕНИЕ ВЛАДЕЛЬЦА: (а).** Один полный круг ревизии против всех восьми блокеров, затем реализация
по порядку шагов. Моей рекомендацией была (б) — сперва бесспорный фундамент, — и она не принята.
Вариант (а) есть тот же цикл, что провёл ступень R2d от восьми блокеров до сданной, и он сохраняет
порядок шагов вместо того, чтобы вплетать работы фундамента в проект, который ещё в движении.

**ПЕРЕСМОТРЕНО НА (б) в тот же день, владельцем, после возврата круга ревизии.** Круг закрыл все
прежние блокеры (10 YES / 6 PARTIAL / **0 NO**) и принёс **три новых** плюс десяток MAJOR — и все они
до единого сидят в **фиче**: маршрутизация кандидатов, предикат способности без конъюнкта
`mesh_leg`, очистка на буте, требующая `TRANSFER_DST`, которого у образа нет, некольцованный
покадровый UBO, ненаблюдаемый `prev_view_proj`, невыполнимый анти-вакуумный пункт.

**Ни один не попадает в фундамент** — ни в `TextureView` для RHI, ни в стража субресурсной
синхронизации, ни в хостовый оракул. Три круга проектирования, ноль блокеров там. Это улика, а не
предпочтение, и она переворачивает основание моей же первой рекомендации: тогда это была догадка,
теперь — измерение.

Часть блокеров существует ИМЕННО ПОТОМУ, что фундамента нет: один прямо говорит, что приёмка шага
не может быть выполнена на этом шаге, поскольку инструмента ещё не существует. Постройка фундамента
убирает класс замечаний, а не откладывает его.

**Итак: реализуем сейчас S1 (`TextureView` в RHI), S2 (страж субресурсной синхронизации) и S3
(хостовый оракул).** Каждый самостоятельно верен, нужен независимо от того, будет ли окклюзия
когда-нибудь вооружена, и ни один не зависит от спорных частей. Проект фичи продолжает устаканиваться
против оставшихся блокеров — уже на существующем фундаменте.

**Блокирует.** Только фичу окклюзии. Фундамент идёт.

---

## 2026-08-07 — ДВЕ ВЕЩИ, КОТОРЫЕ ЧАСТЬ 3 НЕ МОЖЕТ РЕШИТЬ САМА

VG R3 часть 3 ЗАВЕРШЕНА и запушена (`b6337dd`..`6a9a7f9`). Два пункта упираются во владельца, и ни
один из них не дефект.

### 1. Четыре новых пина не благословлены, и благословение не моё — **ЗАКРЫТО: благословлены @e160434**

**Владелец посмотрел BMP и подписал; все четыре ноги записали `85b7d378…4d2913d9` и
перепроверяются зелёными.** Приёмка подняла один настоящий вопрос — угловые сферы выглядят
вытянутыми, — и он был исследован ДО благословения, а не отмахнут: силуэты — эллипсы с
РАДИАЛЬНЫМИ большими осями при `1/cos θ ≈ 1.18` (FOV_Y = 52°), а попиксельная реплика двух
угловых сфер в Bevy 0.14 воспроизвела те же эллипсы с точностью до 0.2 px. Прямолинейная
перспектива, не дефект. Исходная запись ниже.

`goldens/PINS.toml` получил `vb_occ_mixed_off`, `vb_occ_mixed_keep`, `vb_occ_mixed` и
`vb_occ_mixed_late`, каждый `sha256_*` засеян литералом `PENDING`. Это путь, предписанный шапкой
самого файла для добавления ноги руками; `golden.ps1` репортит «NO PIN recorded» и выходит с кодом 2
на всех четырёх, а не пропускает. **Проверено на всех четырёх.**

Все четыре рисуют ОДНУ картинку:

    actual = 85b7d3788130a8bb65f0b5b92ba86c71499bd7a4babe7d6900a711944d2913d9

Это совпадение по четырём режимам — разоружён, FORCE_KEEP, вооружён (отсекает 4), FORCE_LATE
(отсекает 6, возвращает 2) — и есть центральное утверждение части: кулл отсекает геометрию, а кадр
не шевелится.

**Что нужно:** визуальная приёмка свежих BMP. Затем благословить сначала `vb_occ_mixed_off` (обе
ноги) и проверить, что остальные три воспроизводят тот же литерал;
`the_pins_declared_byte_identical_actually_agree` дальше не даст им разойтись. До тех пор эти четыре
гейта не утверждают ничего — `PENDING == PENDING` вакуумно, и доккомментарий охранника теперь это
прямо говорит.

### 2. У части 4 нет плана, а её объём — решение по ЦЕННОСТЯМ — **ЗАКРЫТО: план @799db99, отгружено P4-1…P4-7**

**Обе половины закрыты.** `docs/VG-R3-P4-CONFIG-AND-INSTRUMENT-PLAN.md` прошёл четыре прохода
архитектора × четыре прохода критика до APPROVED (@799db99) и лёг семью ступенями, каждая
коммитится отдельно и зелёной: `49e5630` · `28c3772` · `85b3313` · `c7465bf` · `58687d3` ·
`cf2d367` · эта.

- **Config-поле есть.** `boyko_render::OcclusionConfig { mode: OcclusionMode }` — два варианта,
  `Off` (по умолчанию) и `TwoPhase`, — Resource на поверхности `HzbConfig`, читается живьём
  покадрово. `BOYKO_VG_OCC_FORCE` и его boot-паника ушли из боевого кода; переопределения вердикта
  теперь `boyko_app::OcclusionForce`, тестовый инструмент. **`FORCE_KEEP` больше НЕ маршрут
  разоружения; им стал `OcclusionMode::Off`** — и, в отличие от `FORCE_KEEP`, он подавляет предикат
  сплита, поздние проходы и привязку дополнительных дескрипторных сетов.
- **Число есть, и оно говорит NOT RESOLVED.** Десять таймстамп-бракетов в боевом рекордере,
  протокол части 3 перезапущен на этом канале: `NetRun +10 240 нс` при полосе `49 152 нс`. Все
  контрасты `NOT RESOLVED` — и это РЕЗУЛЬТАТ, а не провал: инструмент разрешает поп-проходные
  стоимости, а эти фикстуры не разделяют плечи. **Умолчание остаётся `Off` как утверждение об
  ОБЪЁМЕ**, а не как неубедительный замер: чтобы включить по умолчанию, нужно `NetRun < −band` на
  ≥3 присестах и ≥2 фикстурах разной окклюзионной плотности **и** второй потребитель пирамиды,
  чтобы `HzbBuild` не вешался на одну эту фичу. P4-6 — одна кампания, две фикстуры, одна машина,
  второго потребителя нет.
- **Владельцу остаётся одно решение по ЦЕННОСТЯМ** — оно записано следующим пунктом: переключать ли
  умолчание.

Исходная запись ниже.

Файла `VG-R3-P4-*.md` не существует. Текст самой части 3 назначает части 4 **owner-facing
config-поле** — окклюзионный кулл как настройка, а не переменная окружения; до тех пор поддержанное
разоружение — это `BOYKO_VG_OCC_FORCE=keep`. Это решение о поверхности продукта, а не развилка по
перфу или архитектуре, поэтому оно не моё.

Вторая унаследованная работа части 4 — число. Замер трёх чисел вернул **NOT RESOLVED по всем
контрастам**, и причина структурная, а не статистическая: ни `vb_batch_cull`, ни `vb_cull_late`, ни
поздний растр не обрамлены таймстампами, а `swapchain.rs` ставит `VK_PRESENT_MODE_FIFO_KHR`
безусловно, поэтому настенные часы снизу ограничены развёрткой (замерено 6.893 мс/кадр = 145.1 Гц).
Нулевой контроль дал 0.47 % при полосе разрешения 287.91 %. Добавление бракета трогает боевой
рекордер, что граница части 3 исключает, — значит это первая работа части 4, если ей нужно число.

---

## 2026-08-07 — VG R3 ЧАСТЬ 4 ЗАВЕРШЕНА: два решения по ЦЕННОСТЯМ и диспозиции, закрывающие часть

Ступени P4-1…P4-7 все легли. **Ничто здесь ничего не блокирует** — часть отгружена с тем
умолчанием, под которое она и проектировалась. Два пункта — за владельцем, остальное записано,
чтобы ни одна диспозиция не осталась подразумеваемой.

### ЦЕННОСТИ 1 — делать ли `TwoPhase` умолчанием `OcclusionMode`?

Это один атрибут — и настоящее изменение поведения: с хостовой дизъюнкцией части 4 любой мир, несущий
маркер `OcclusionCulling`, начнёт по умолчанию строить пирамиду глубины.

**Моя позиция: нет, и это не «неубедительный замер».** Режим отказа этого решения — УДАЛЁННАЯ
ГЕОМЕТРИЯ, а выигрыш сверху ограничен долей раннего растра в кадре: та самая асимметрия, из-за
которой сам маркер — opt-in. На этом корпусе выгода доказуемо нулевая (на сошедшейся статической
сцене поздний скоуп корректно рисует ничего), а стоимость — нет. Планка для переключения записана и
не взята: `NetRun < −band` на ≥3 присестах и ≥2 фикстурах разной окклюзионной плотности **и** второй
потребитель пирамиды, чтобы `HzbBuild` не вешался на одну окклюзию. P4-6 — одна кампания, две
фикстуры, одна машина, второго потребителя нет.

### ЦЕННОСТИ 2 — режим представления это поверхность продукта, и ею никто не владеет

`present/swapchain.rs` создаёт swapchain с `VK_PRESENT_MODE_FIFO_KHR` **безусловно**, поэтому любой
замер по настенным часам в этом репозитории снизу ограничен развёрткой, и попросить что-то другое
owner-facing способом нельзя.

Часть 4 сознательно это НЕ чинила (диспозиция (c1)). Причина не в цене: канал, который правка
улучшила бы, — хостовые настенные часы — вытеснен таймстамп-бракетами и теперь помечен
`KNOWN-BLIND`, решая ровно одно (*не сломало ли вооружение инструмента кадр?*). Режим представления
— это vsync, разрывы и энергопотребление: owner-facing `PresentConfig` и продуктовое решение, а не
дело окклюзионной части. **Записано сюда как пункт по ЦЕННОСТЯМ, а не молча унесено перф-TODO.**

### Диспозиции, чтобы ни одна не была подразумеваемой

| пункт | диспозиция |
|---|---|
| **(c1)** безусловный FIFO | **OUT** — вытесненный канал + поверхность продукта. ЦЕННОСТИ 2 выше |
| **(c2)** D8: происхождение `vb_indirect_late` не покрыто ничем | **OUT, ЗАПИСАНО на ядро фреймграфа.** Часть 4 не декларировала ни одного нового доступа, поэтому не улучшила и не ухудшила это; P4-5 дополнительно утверждает, что боевая поздняя цепочка поле-в-поле одинакова с зондом и без него. Починка — правка P2-7 (`is_write \|\| res_written \|\| res_seeded` плюс аудит 14 сайтов), единственный гейт которой — реплика, ИЗМЕРЕННО слепая к тому классу, который она ловила бы; значит той ступени сперва нужен инструмент, а не код |
| **(c3)** строки барьерного потока при PROBE-ON | **СДЕЛАНО в P4-5**, выведенной дельтой. Две находки: предсказание плана о пере-источнике опровергнуто деревом, а возмущение зонда больше, чем говорил любой документ, — девять деклараций доступа по двум проходам на семи буферах, восемь выведенных барьеров, пять пинованных барьеров съезжают |
| **(c4)** внутрипроходное ребро `TRANSFER → COMPUTE` на `VbCullUniform` | **СДЕЛАНО в P4-3, только половина про ПОРЯДОК ЗАПИСИ** — и она краснеет на КОМПИЛЯЦИИ в обоих профилях, тогда как форма из плана осталась бы зелёной ровно на том дефекте, ради которого существует. Половина про ДЕКЛАРАЦИЮ остаётся открытой (OQ 9): `FrameGraph::pass_access_count` приватен, попроходного аксессора нет |
| **(c5)** протухшая шапка в будущем времени в `vb_occ_split_gate.rs` | **СДЕЛАНО в P4-7.** Она сидела в ЧЕТЫРЁХ местах, а не в двух, названных планом |
| **(c6)** UTF-8 BOM в `goldens/PINS.toml`, который строгий TOML отвергает | **ОСТАВЛЕН ОСОЗНАННО, и причина измерена.** `golden.ps1 -Bless` пишет файл обратно через `Set-Content -Encoding UTF8`, а единственный PowerShell на этой машине — 5.1, у которого `-Encoding UTF8` пишет BOM. Проверено: файл без BOM, прогнанный ровно через этот вызов, вернулся с `EF BB BF` в начале. Снятие одного лишь BOM молча откатится следующим благословением; чинить надо ПИСАТЕЛЯ, и это ложится вместе с прогоном благословения. Влияния сегодня нет: `golden.ps1` парсит построчными регекспами, а каждая строгая TOML-проверка в этой кампании снимает BOM явно |

### Два зазора, которые часть 4 открыла и закрыть не смогла, — записаны, а не поглощены

- **Гейт сплита внутри пинованного бинаря идёт при PROBE-ON, а пины — при PROBE-OFF.** Зазор
  маленький и назван: `vb_probe_dump` — хостовый счётчик, он не пишет команд и не входит в предикат
  сплита. Но это всё равно зазор.
- **Инвариант двойного чтения живёт только в dev-профиле.** Релизный прогон бенча его не исполняет.
  Если когда-нибудь заподозрят расхождение двух читателей пула только в релизе, проверку придётся
  перезапустить в dev-профиле на той же сцене; в релизе её не увидит ничто в лестнице.

---

## 2026-08-07 — ЗАКРЫТО — вердикт кулла делил, а деление не может совпасть с хостовым оракулом

**ОТВЕЧЕНО и реализовано в той же сессии. Оставлено целиком, потому что переносимая часть — это ход
рассуждения, и потому что ПЕРВОЕ прочтение ниже оказалось неверным способом, который стоит
сохранить: оно назвало направление по выборке из одной пробы.**

**Решение.** Вердикт больше не делит. `для всех i: cz_i < occ * cw_i` заменило
`max_i(cz_i/cw_i) < occ` и в шейдере, и в оракуле `boyko_render::hzb`; `depth_near` уехал под
`#ifdef VB_CULL_DEBUG_PROBE`, так что боевой модуль больше не вычисляет величину, которая раньше
решала; граничный корпус переписан так, чтобы сажать плант под новый предикат. Измерено после:

    DepthNearCensus { compared: 72, identical: 72, gpu_below: 0, gpu_above: 0, max_ulps: 0 }
    verdict disagreements: 0 of 72
    24 EXACT-TIE KEEP probes, 24 strict KEEP probes, 24 strict REJECT probes

Арм точного касания — то, что доказывает строгость `<`, — теперь достижим по построению, а не по
везению: плант берёт `z = near·2^k` и `occ = 2^-k`, оба двоичные, поэтому равенство точно с обеих
сторон.

**Что фикс стоил, и это самая ценная для переноса часть.** Перепин переписи артефакта показал, что
`op_ford_less_than` УПАЛ на два ровно на том шаге, который ДОБАВИЛ покорнерное сравнение — потому что
`!(cz < bound)` понижается до `OpFUnordGreaterThanEqual`, а поля под него в переписи не было.
Перепись, считающая только упорядоченное сравнение, прочла бы *удаление* вердикта как небольшое
уменьшение и запинила бы его без единого слова. Поле добавлено
(`op_funord_greater_than_equal: 4`, два из них — сам вердикт, по одному на заинлайненную копию).

---

### Находка в исходной записи

**Не блокер. Записано, потому что это ИЗМЕРЕННАЯ находка по корректности и потому что открывшаяся
развилка была моей, а владелец должен иметь возможность отменить решение до того, как оно уедет в
код.**

VG R3 часть 3, шаг P3-4 (лист окклюзии) лежит в рабочем дереве, не закоммичен. Его новый гейт
`crates/boyko_app/tests/hzb_verdict_oracle_gate.rs` гоняет четыре корпуса. Три зелены, включая
случайный на 131 072 пары и сентинельный. Четвёртый — точное касание — падает на первой же пробе:

    [64x48 boundary probe 0 (equal)] batch 0: the record's instanceCount is 0 but the oracle
    keeps 1 of 1 instances early. (deferred: gpu 1 / oracle 0)

GPU **отсекает** там, где оракул **сохраняет**. Сравнение в шейдере
(`vb_batch_cull.comp.hlsl:872`) — `return depth_near < occ;`, строгое и верное: равенство обязано
сохранять. Значит оператор правильный, а расходится ЗНАЧЕНИЕ — `depth_near` шейдера ниже хостового.
Комментарий самого шейдера на `:766-767` назвал это заранее *стороной, удаляющей геометрию*, а
комментарий фикстуры на `:1309-1313` предсказал точную сигнатуру: расхождение в 1 ULP «проявится на
арме "ровно равно" и больше нигде».

**Оба предсказания написаны ДО прогона, и оба сбылись.** Гейт работает. Это восьмой случай паттерна
за кампанию — и первый, где инструмент поймал дефект, а не оказался вакуумным над ним.

### Замер и что он опроверг

Вариант `-D VB_CULL_DEBUG_PROBE=1` экспортирует собственный `depth_near` шейдера, уровень и тапы, так
что расхождение теперь НАБЛЮДАЕТСЯ, а не выводится. Боевой модуль не тронут: исходник с
неопределённым макросом препроцессируется посимвольно так же, а `vb_batch_cull_spv_byte_identical`
остаётся зелёным — значит числа описывают именно тот модуль, который едет. По 72 пробам:

    DepthNearCensus { compared: 72, identical: 66, gpu_below: 3, gpu_above: 3, max_ulps: 1, incomparable: 0 }
    verdict disagreements: 2 of 72   (одно host=Early gpu=Late, другое host=Late gpu=Early)

**Это опровергает первое прочтение выше.** Расхождение идёт НЕ в сторону удаления геометрии:
`gpu_below` и `gpu_above` — 3 и 3, а два несогласия по вердикту смотрят в разные стороны. Это
симметричное округление в 1 ULP, а не смещение. Первое прочтение было сделано по одной пробе — ровно
тот объём выборки, при котором утверждение о направлении не стоит ничего.

`level` и все четыре `taps` ИДЕНТИЧНЫ на всех 72 пробах, то есть оконный прямоугольник и выбор
уровня уже совпадают точно, и расходится только глубина.

### Причина и почему она закрывает вариант (A)

Под матрицей корпуса row2 = `[0,0,0,near]`, row3 = `[0,0,1,0]`, поэтому `cz = near` и `cw = z` точны
и побитово равны с обеих сторон — что и подтверждается совпадением тапов. Единственный неточный шаг —
обратная величина. Приложение о точности Vulkan специфицирует `OpFAdd`/`OpFSub`/`OpFMul` как
корректно округляемые, но допускает **`OpFDiv` 2.5 ULP** на 32 битах; деление в Rust — это IEEE
0.5 ULP. `precise` эмитит `NoContraction`, который ограничивает контракцию и переассоциацию и НИЧЕГО
не говорит про допуск деления.

Значит **(A) в исходной постановке мёртв** — не дорог, а *невозможен*: никакое ужесточение
существующей свёртки не даёт побитового равенства, потому что зазор задан спецификацией, а не формой
кода. Комментарий шейдера на `:909-910`, утверждающий, что `precise` «запрещает подстановку
приближённой обратной величины», — ложное заявление и правится.

**(B) теперь тоже неверен**, когда направление известно: округление ВВЕРХ променяло бы одно
произвольное направление на другое, а граница в 1 ULP, на которую оно опиралось бы, измерена на
72 пробах на ОДНОМ устройстве, тогда как спецификация разрешает 2.5.

### Что делается вместо этого: убрать деление из РЕШЕНИЯ

При `cw_i > 0` — уже под охраной раннего выхода «за глазом» —

    max_i (cz_i / cw_i) < occ    <=>    для всех i:  cz_i  <  occ * cw_i

Правая часть — одно корректно округляемое умножение под `NoContraction`, поэтому шейдер и оракул
совпадают **по построению**, а не с допуском, и это *дешевле* деления. Это точная переформулировка,
не смягчение и не смещение — почему она и предпочтена всем изначально перечисленным вариантам.
Оконный прямоугольник сохраняет своё деление: измерено, что оно совпадает точно.

**(C) — объявить касание непроверяемым и ослабить арм — остаётся отклонённым** и теперь не нужен.

---

## 2026-08-07 — Профилирование + логирование, третий раунд рецензии: оба ОТКЛОНЕНЫ, а СТЫК несовместим

Оба плана дошли до ревизии 3 и были прорецензированы в третий раз — по отдельности и **впервые друг
против друга**. Вердикты: профилирование `REJECTED (6 блокеров)`, логирование
`REJECTED (10 блокеров)`, стык `INCOMPATIBLE AS WRITTEN (6 блокеров)`. Ревизия 4 в работе; эти три
пункта рецензентам не принадлежат.

### Стык никто не проектировал, и это главная находка раунда

Два предыдущих раунда читали по одному плану. Первый читатель стыка обнаружил, что документы
утверждают **взаимоисключающие факты**: профилирование обосновывает переезд своего ABI в
`boyko_utils` тем, что у того нулевые зависимости; логирование прямо пишет, что `boyko_utils`
зависит от `boyko_log`. Обе посылки одновременно ложны. Ниже этого каждый план независимо изобрёл
одни и те же четыре примитива — индекс полосы на потоке, калибровку `rdtsc`, никогда не
освобождающий аллокатор полос и учёт потерь — с несовместимой семантикой: один и тот же воркер был
бы полосой 5 для профайлера и полосой 37 для логгера, а про suspend/resume знали бы только одни из
двух часов. Это ровно тот отказ, который называет принцип 0: возможность, нужная двум подсистемам,
построена дважды как адаптер в каждом крейте вместо одной черты ядра.

**Решено мной, не владельцем** (архитектура, по действующей договорённости): новый крейт
`boyko_diag` с нулевыми зависимостями владеет часами, реестром полос, словарём потерь и политикой
никогда не освобождаемого хранилища; он *диагностически нем* (не эмитит ни одного кода
`boyko-####` и ничего не печатает — именно это держит граф ацикличным); `profiling_abi` размещён в
нём, а не в `boyko_utils`, который сохраняет пустой `[dependencies]`. Полный проект:
`docs/DIAGNOSTICS-SUBSTRATE-PLAN.md`.

### ЦЕННОСТИ 1 — сколько ОТГРУЖЕННАЯ игра платит за диагностику?

Совместное число не считал никто. Измерено по собственным таблицам обоих планов:

| | профилирование одно | логирование одно | **вместе** |
|---|---|---|---|
| dev, `.bss` + резерв | 6.65 МиБ | 3.46 МиБ | **9.33 МиБ** (наивно 10.11; общий крейт экономит 0.78) |
| **shipping** | 0.85 МиБ | 1.16 МиБ | **1.95 МиБ** — **НЕВЕРНО; исправлено сразу ниже** |
| линий кэша на горячем пути | 3-4 | ≤ 4 | **7-8** |

> **ИСПРАВЛЕНО 2026-08-08 — цифра shipping выше была неверна уже в тот день, когда её впервые
> положили вам на стол, и здесь она исправлена открыто, а не пересчитана молча.**
>
> **1.95 МиБ никогда не равнялось сумме собственных слагаемых — ни в одной ревизии.**
>
> - В том виде, в каком вопрос был задан выше (рев. 3): `0.85 + 1.16 = **2.01**`, напечатано
>   **1.95**.
> - В первой нарезанной ревизии корпуса слагаемые сдвинулись, а итог — нет:
>   `0.89 | 1.15 | наивно 1.95`, и `0.89 + 1.15 = **2.04**`.
> - **Затем под первой ошибкой обнаружилась вторая, независимая.** Логгер пересчитал собственный
>   столбец `shipping` по слагаемым (`docs/diagnostics/logging/01-EMISSION-RING.md:130`:
>   512 + 32 + 16 + 16 + 4.25 + 0.008 + 256 + 64 + 320 = **1 220.26 КиБ ≈ 1.19 МиБ**) и показал,
>   что **ни одно подмножество его строк не даёт 1 180 КиБ**, которые цитировал стык. То есть
>   1.15 — не другая конфигурация, а тоже ошибка.
>
> Третьей величины, которой могло бы быть 1.95, попросту нет. Обе ревизии утверждают, что общий
> субстрат экономит в shipping **НОЛЬ байт** — 0.78 МиБ существуют только в dev, — а при нулевой
> экономии совместная цифра **и есть** наивная сумма. **Исправленная цифра shipping — ≈ 2.08 МиБ**
> (908 + 1 220.26 = 2 128.26 КиБ) против ≈ 2.01 МиБ на тех числах, которые вам передали. Ошибка
> каждый раз была не в вашу пользу: запрос занижен на 0.06 МиБ тогда и на 0.13 МиБ сейчас.
>
> 🔑 **Урок, который переживёт само число, — потому что он победил проход починки, созданный
> ловить ровно это.** После первого исправления таблица стыка стала *внутренне непротиворечивой*:
> `0.89 + 1.15` действительно равно `2.04`, — и именно поэтому устаревшее **слагаемое** выжило.
> **Итог, сходящийся со своими напечатанными слагаемыми, не доказывает про эти слагаемые ничего.**
> Правило на будущее: при нулевой экономии в shipping совместная цифра равна сумме двух столбцов,
> и любая правка этой строки обязана перечитать строки-источники, которые она цитирует, а не
> заново складывать числа, уже напечатанные рядом.
>
> **Смысл числа тоже изменился, и это сужает вопрос.** S13 — *бесплатно, пока не включено*,
> добавленное уже после этой записи — увело все системные вызовы, потоки, хуки и первую запись с
> пути загрузки на путь включения, поэтому отгруженный процесс, который диагностику ни разу не
> включил, **этих таблиц не касается вообще**. Нетронутую полностью нулевую таблицу в `.bss`
> линкер эмитит с виртуальным размером и без сырых данных, поэтому ≈ 2.08 МиБ — это **объявленное
> адресное пространство, а не резидентная память** (`docs/diagnostics/SEAM.md` §S13, строка
> MEMORY). Два ограничения на это, и оба проговорены самим корпусом, а не заглажены: свойство
> держится, **только если загрузка ничего не трогает** — одна запись в один буфер полосы коммитит
> эту страницу, и для неё свойство потеряно; и корпус **прямо отказывается утверждать**, что
> загрузчик оставляет нетронутую страницу незакоммиченной (`substrate/section-report` доказывает
> лишь отсутствие байт в *образе*, и не более того; `docs/diagnostics/substrate/05-LADDER-GATES.md`,
> гейт DG12).
>
> **Поэтому вопрос уже, чем следует из заголовка раздела.** Не «сколько машина игрока тратит на
> диагностику», а: **приемлемая ли это цена для отгруженной игры — ≈ 2.08 МиБ объявленного
> адресного пространства, резидентного только в тех сессиях, где диагностика действительно
> включена?** По-прежнему решение по ЦЕННОСТЯМ, и по-прежнему не моё.

То есть заголовочное **«≤ 1 МиБ в retail» ложно в той конфигурации, которая реально поедет**, а
общий крейт в shipping не экономит **ничего** — его 0.78 МиБ существуют только в dev. Он куплен за
корректность (один номер полосы, одна эпоха часов, отчёт о потерях, который сам не может быть
потерян), а не за объём, и ни один план не вправе утверждать иначе.

Урезать **≈ 2.08 МиБ** — значит урезать одно из: 32 × 16 КиБ полос логгера (512 КиБ), `SINK_OUT`
(256 КиБ) или арены динамических зон профайлера (96 КиБ — *текущая ревизия называет этого третьего
кандидата **40 КиБ** в `shipping`, `docs/diagnostics/SEAM.md` §Open — needs the OWNER, пункт 1;
расхождение здесь зафиксировано, а не разрешено, потому что разрешать его — дело плана
профилирования*). **Это решение по ЦЕННОСТЯМ о том, что машина игрока тратит на диагностику, и оно
не моё.**

### ОБЪЁМ 1 — что на самом деле значит `shipping-min`?

`shipping-min` у логирования существует для игры, которой **не нужен резидентный поток
диагностики**. Но уровень `Always` у профилирования всё равно пишет окно телеметрии синхронно на
диспетчере, то есть такая игра всё равно платит периодический `write_all`. Либо `shipping-min`
выключает и телеметрию, либо профиль значит не то, что написано в его имени. **Решение по ОБЪЁМУ.**

### ОБЪЁМ 2 — планы растут быстрее, чем сходятся

Три раунда рецензии, и счёт блокеров не падает: **35 находок → 17 → 22**. Два документа — уже 3370
строк на две подсистемы, которых не существует ни одной строкой кода, и больше половины новых
блокеров третьего раунда порождено тем, что добавил второй раунд — игровой половиной, — а стык
стал виден только потому, что оба документа доросли до самостоятельных архитектур.

Это сигнал о том, **сколько проектируется за раз**, а не о рецензентах. Альтернатива — сузить
первую очередь до `boyko_diag` + CPU-зоны + уровни логов, построить и измерить, а телеметрию,
ретенцию и игровой API вернуть потом, на работающем основании. Здесь это записано как вариант;
**объём определяет владелец, не я.** Работа продолжается по полной ревизии 4, пока не сказано иное.

### ОБЪЁМ 3 — нога `--cfg loom` во всём воркспейсе не собирается, и об этом ничто не сообщало

Найдено на ступени D1, 2026-08-08, при проверке того, что новое ребро `boyko_threadpool ->
boyko_diag` не потревожило loom-сборку. Не потревожило. Loom-сборка была **уже сломана**:

```
RUSTFLAGS=--cfg loom cargo check -p boyko-threadpool --lib
error[E0599]: no method named `get_mut` found for struct `loom::sync::atomic::AtomicPtr<T>`
  --> crates/boyko_threadpool/src/scope.rs:185:39
```

У loom 0.7.2 есть `with_mut`, а не `get_mut`. `-p boyko-ecs` падает на **той же** ошибке, потому
что доходит до той же библиотеки, — значит мертвы **оба** крейта с блоком
`[target.'cfg(loom)'.dependencies]`, а не один. `rg loom .github/workflows` не возвращает ничего:
ни одна нога CI не передаёт `--cfg loom`, поэтому это и было невидимо. Проверено, что это не
регрессия D1: `git stash` диффа D1 воспроизводит ту же ошибку на `93dbcf8`.

Почему это требует решения, а не тихой починки:

1. План подложки ссылается на эти два крейта как на **работающий прецедент**, который скопирует
   loom-модель `claim_lane` в `boyko_diag`. Форма **манифеста** прецедентом остаётся; утверждение,
   что рядом *работает* модель, — нет. Текст плана теперь оговорён, ссылка не оставлена как есть.
2. Починка в `scope.rs:185` — одна строка внутри `unsafe` `Drop` на пути разбора scope, а зелёный
   `cargo check` под `--cfg loom` — **не** прогнанная модель. Чтобы нога что-то значила, модели
   надо запускать, а на этой машине loom-бинарники в **release** падают на старте (записано ранее,
   к делу не относится).
3. Значит настоящий вопрос — объём: (а) починить `scope.rs:185`, прогнать существующие модели в
   debug и добавить ногу CI, которая держит их собираемыми; (б) починить без ноги CI, приняв то же
   молчаливое гниение позже; (в) оставить как есть и сдать доказательства параллелизма `boyko_diag`
   только ногами proptest + Miri, записав в плане, что loom-модели за `claim_lane` нет.

**На D1 не чинится** — это вне предмета D1, а выбор выше не мой. Ноги Miri и свойства для пути
захвата полосы не затронуты и остаются в плане.

---

## ИЗВЕСТНЫЕ ТРЕНИЯ — решения не требуют, записаны чтобы не переоткрывать

- **`target/` растёт неограниченно и молча ломает сборку.** В этой сессии дорос до 73 ГБ и упёрся в
  ноль посреди сборки. Отказ выглядит как **ошибка линкера mingw**; настоящая причина — в последней
  строке (`no space on device`). `cargo clean` вернул 73.6 ГиБ.
- **Тест trybuild падает при параллельном полном прогоне** (`compile_fail_frame_write_token`, 3 из 3
  фикстур) и проходит в одиночку. trybuild запускает собственный cargo в тот же `target`. Это не
  флейк, а воспроизводимая конкуренция.
- **`cargo test` ОСТАНАВЛИВАЕТСЯ на первом упавшем бинаре, и счётчик сюит молча падает.** Прогон,
  задевший флейк выше, отчитывается о ~51 сюите вместо ~445 — то есть «прогнал полную сюиту» может
  означать «прогнал девятую часть», и в выводе об этом ничего не сказано. Всегда передавайте
  `--no-fail-fast`, когда утверждение касается покрытия, и читайте ЧИСЛО сюит, а не только число
  провалов.
- **graphify всю сессию мимо render/VB-пути**: каждый запрос возвращал внутренности `boyko_demo`.
  Рабочий путь там — Grep/Read.
- **Бинаря `graphify` в этом окружении нет вообще** (не в `PATH`), при том что хуки `PreToolUse`
  требуют `graphify query` перед каждым Read и Grep. `graphify-out/` существует, но самый свежий
  подкаталог в нём — июльский, то есть граф ещё и устарел. Хуки только напоминают и никогда не
  блокируют — но срабатывают на каждом обращении к файлу. Чинить либо установкой, либо хуком,
  который проверяет наличие бинаря, прежде чем его требовать.
- **Глобальный реестр типов запросов ECS может исчерпаться на полном прогоне lib-сюиты.**
  `MAX_QUERY_TYPES = 1024` — потолок на процесс, заполняется лениво, а `boyko-ecs --lib` гоняет 864
  теста параллельно. Когда планировщик успевает отчеканить 1025-ю различную форму запроса,
  терминальной паникой с именем потолка падает тот тест, кому не повезло. Наблюдалось один раз,
  затем 3 чистых прогона того же бинаря подряд. Это зависимость от порядка, а не сигнал регресса —
  перепроверьте повтором, прежде чем что-то бисектить.
- **`boyko-engine --test internal_docs_anchors` КРАСНЫЙ на содержимом `master`, и давно.**
  13 протухших якорей строк (10 в `docs/MESHLET-VIRTUAL-GEOMETRY-PLAN.md`, 3 в `docs/SYSTEMS.md`)
  плюс 8 лишних waiver'ов против потолка 6 в meshlet-плане. **Доказано предсуществующим на L5**:
  застэшил всю ступень и перезапустил — побайтово тот же провал. Из-за него
  `cargo test --workspace` красный для всех, так что следующий, кто его запустит, потратит те же
  пятнадцать минут на доказательство «это не моё» — это и есть цена оставленного красного.
  Перевывод якорей — самодостаточная работа и заслуживает отдельного коммита.
- **Строка, вставленная в широко цитируемый исходник, молча ломает все якоря ниже неё.** L5 добавил
  `VmColumn::as_mut_slice` около строки 355 и сдвинул `vm_column.rs:437-449` в `462-474` в двух
  файлах корпуса. Тест якорей не покрывает `docs/diagnostics/**`, так что об этом не сказал бы
  никто. Грепайте `<file>.rs:[0-9]` по `docs/` после любой вставки в примитив ядра.
- **Гейт может быть КРАСНЫМ пять коммитов подряд просто потому, что его никто не запускает, и ни
  один гейт этого не закрывает.** Файловая перепись `G2a` (`tests/gpu_blocking_reader_census.rs`)
  покраснела ровно в тот момент, когда ступень 5a внесла `present/gpu_zone.rs`, чья док-шапка
  называет `vkGetQueryPoolResults`, объясняя, чего стоила БЛОКИРОВКА. Красным она оставалась через
  `ee9196b6`, `cb54752d`, `8ca4e05b`, `cf8ffd20`, `7ae9162a` — три из них отчитались «воркспейс
  зелёный». Найдено на 5c только потому, что наконец случился полный прогон `--workspace`. Механизм
  сработал безупречно; провалом был сам ВОПРОС. Практическое правило: **ступень, добавляющая файл,
  обязана прогнать гейты-переписи, а не только свои собственные.**
- **Диск заполняется до нуля, а выглядит это как баг линкера mingw.** Второй случай (см. заметку
  аудита 2026-07-23). `target/` дорос до 72 ГБ при 8 КБ свободных на `D:`, и симптомом была стена
  `linking with x86_64-w64-mingw32-gcc failed` по десятку несвязанных целей. Лечение:
  `rm -rf target/debug/incremental` (здесь — 12 ГБ). Смотрите `df -h /d` ПРЕЖДЕ чем читать ошибку
  линкера.
- **A/B гейта G10 работает двумя процессами, а корпус говорит «в одном»; причина записана в
  документах, но это отклонение, и аудитор должен его видеть.** `read_vb_bench_ns` ноги A ждёт с
  `VK_QUERY_RESULT_WAIT_BIT`, поэтому один процесс, чередующий ноги, дошёл бы до неё на кадре,
  записанном ногой B, — тот самый класс зависаний, который убрала P4-1. Если будущая ступень
  захочет буквальную форму корпуса, ей сначала придётся сделать читатель ноги A неблокирующим, а
  это и есть удаление ступени 7.
- **РЕШЕНО (владелец: «что производительнее») — форк `G10` ступени 6 ушёл в хостовую ручку
  вооружения.** `BOYKO_GBUF_BENCH` стоит одного бут-`Option`, который `None` в каждом отгружаемом
  прогоне и исчезает вместе со ступенью 7; альтернатива заставила бы `GpuZoneRecorder::open_frame` и
  `retire` брать `&self`, навсегда снеся клаузу (c) обоснования `Sync` у `FrameSlot` и толкнув
  `set_mark` к залоченному RMW в горячем рекордере. **Нога вооружена и никогда не читается** —
  клаузе-свидетелю тайминги не нужны, а `read_query_pool_ns` завис бы на кадре, пропустившем проход.
  ⚠️ **Читателю ступени 8 обязательно сверяться с масками свидетеля, прежде чем чего-то ждать**: три
  из четырёх software-ray проходов обёрнуты внутри собственных `if let`-ветвей, и ни у одного
  старого gbuffer-коллектора нет эпилога тотальности.
- **РЕШЕНО (владелец: «реши сам, что оптимальнее») — формат артефакта ступени 7 выбран, писатель,
  читатель и гейт отгружены.** Каждое решение с его ценой — в док-шапке `profiling/artifact.rs`.
  ⚠️ **Одно из шести оказалось верным по СУТИ и ошибочным по ОБОСНОВАНИЮ, и это нашло требование
  КРАСНОГО**: страх, что более широкий файл схлопывает НОД в `vg_occ_split_timing.rs:916`, ложен —
  собственный `(v * 10.0).round()` потребителя поглощает лишние цифры (измерено на семи значениях).
  Одна десятая всё равно правильна, но по меньшим причинам (прямая сопоставимость с печатными
  строками, без которой невозможен A/B следующего шага). Шесть значений, как решены: Строка самой
  ступени была неверна о её форме и исправлена в `05-LADDER-GATES.md` (писатель переезжает со
  ступени 8 на 7; печатных сайтов ОДИННАДЦАТЬ, а не четыре; `vb_bench_totality_gate.rs` удаляется, а
  не мигрирует). Остаётся не развилка, которую я решу числами, а шесть ЦЕННОСТНЫХ решений:
  1. **Числовая точность в артефакте.** `vg_occ_split_timing.rs:916` восстанавливает решётку GPU-тиков
     НОД'ом по **десятым**, потому что именно эту точность печатает сводка. Полная точность `f64`
     схлопывает НОД и подрезает снизу каждую полосу; док того же файла измеряет ошибку в **32×** и
     говорит, что такой выбор «удовлетворит здесь каждое утверждение, занизив разрешение прибора на
     весь множитель решётки». **Это молчаливая ложная победа, а не красный тест.**
  2. **Путь файла, уникальность на прогон, усечение.** `append_artifact(p, path)` берёт путь от
     вызывающего и больше ничего — ни умолчания, ни ротации, ни env-ручки.
     `vg_decidability_floor.rs` порождает 42 последовательных ребёнка; фиксированный путь — генератор
     чтений устаревшего.
  3. **Один файл — это одна посадка, один процесс или много дописанных прогонов.** Обратное КРАСНОЕ
     `G24` ОПРЕДЕЛЕНО на устарелости и не может быть написано, пока это не выбрано.
  4. **Кто сводит 21 посессионный артефакт в `docs/PROFILING-FLOOR.md`** — ступень 7b от этого
     зависит, и ни одна строка этого не назначает.
  5. **Является ли `WorkloadTag` полем артефакта.** `resolve` его проверяет, 7b публикует его в
     markdown, но что посессионный файл его несёт — не сказано нигде.
  6. **Что артефакт пишет, когда устройство отказывает в таймстампах** — сегодня это `eprintln!`, на
     который три потребителя вешают свой третий исход.
  ⚠️ **Обратное КРАСНОЕ `G24` назвало два поля, которые не справляются, и одного из них НЕ
  СУЩЕСТВУЕТ.** У `crates/boyko_diag/` **нет `build.rs`**, а `BUILD_HASH` не встречается в воркспейсе
  нигде — запланированный на ступени 0 артефакт, который так и не появился. `SessionId` есть, но
  чеканится ВНУТРИ ребёнка, и родитель его не предскажет. Дискриминатор — **токен прогона, выданный
  родителем**, единственное поле, способное поймать устарелость внутри одного прогона.
  Оставшиеся половины ступени 7 по порядку: редьюсер, наполняющий артефакт; проводка производителя
  (проверяется A/B против ещё печатающего канала, пока живы ОБА); перемер пола 7b; затем удаления —
  **713** строк из `gpu_timing.rs`, **1381** из `runner.rs` (31 % файла), **465** из `gpu_scene`,
  плюс пять миграций потребителей.
- **РЕШЕНО (владелец выбрал СТРОГИЙ вариант из трёх) — тег нагрузки состоит из двух половин, и
  необъявленная не годится в пол.** Измерено при открытии миграций потребителей ступени 7. Тег — это
  `format!("{path:?}_{legs:?}")` по `ResolvedRenderPath`: `deferred_both`, `visibilitybuffer_mesh` и
  так далее. `vg_decidability_floor.rs` прогоняет свой НУЛЕВОЙ эксперимент дважды за повторение — с
  `BOYKO_VB_FROXEL_FORCE_OFF` и без, — и **ни `path`, ни `legs` между ними не меняются**:
  `froxel_light_cull` — отдельное поле той же структуры, и тег его не читает. `BOYKO_VB_BENCH_LIGHTS`
  (`N_ps`) движку не виден вообще.
  Сама миграция этим не блокируется — тест пола пишет каждую ногу в свой путь и никогда не разделяет
  их по тегу. Дыра стоит **ниже по течению**: `resolve` отказывает `Floor`, у которого `workload` не
  совпал, и этот отказ — ЕДИНСТВЕННЫЙ механизм, не дающий полу, измеренному на одной конфигурации,
  ограничивать дельту, измеренную на другой. С таким тегом пол плоской ноги молча ограничивает
  утверждение о фроксельной — что собственный раздел «Что это НЕ решает» у
  `vg_decidability_floor.rs` запрещает словами (*«это одна КОНФИГУРАЦИЯ»*, *«ступень, меряющая на
  другом масштабе, обязана перемерить свой пол, а не цитировать этот»*).
  ⚠️ **И поправка к первой же фразе этой записи про механизм.** Она говорила, что `resolve` отказывает
  несовпавшему `Floor`, как будто это работающий код. ИЗМЕРЕНО: `Floor`, `resolve`,
  `FloorWorkloadMismatch` и `NotResolved` есть **только в документах корпуса** — `rg` по `crates/`
  не находит ничего. Это содержание ступени 8, ненаписанное. Значит молча ничего не ломалось: тег —
  ВХОД для компаратора, которого ещё нет, и именно поэтому решить, что он называет, надо было до
  того, как 7b опубликует пол, на который сошлются поздние ступени.
  **Отгружено, как решено:**
  * **Выводимая** — [`config_tag`] по ВСЕЙ структуре `ResolvedRenderPath` (читаемый префикс
    `path_legs` + 8 hex FNV-1a по каждому полю), а не по выбранному подмножеству: ошибка была не в
    том, что выбрали не то поле, а в том, что поля вообще выбирали. Добавление поля в эту структуру
    намеренно обесценивает прежние полы — они на этой машине дрейфуют быстрее.
  * **Объявляемая** — `content_tag` из `BOYKO_PROFILE_WORKLOAD`, выставляется измеряющим тестом в его
    собственном коде-запускателе, где значение и так живёт, а не в шелле оператора.
  * **Отказ обеспечен СЕЙЧАС, а не обещан ступени 8**: `Artifact::floor_source` возвращает
    `UndeclaredContent` на пустом или пробельном теге, потому что клауза, предмета которой ещё нет,
    это обещание, а не гейт. ОТСУТСТВУЮЩИЙ ключ — испорченная шапка, и это отличается от
    присутствующего пустого: всё содержание отказа — в этом различии.
  КРАСНОЕ прогнано: вернуть вывод к `path × legs` ⇒ *«плоская и фроксельная ноги дали ОДИН И ТОТ ЖЕ
  тег ... flat: deferred_both, froxel: deferred_both»*. На живом прогоне:
  `workload_tag = "deferred_both#99f4482e"`.
  ⚠️ **Сужено 2026-08-10, и очевидная причина ИСКЛЮЧЕНА.** Модуль уже сериализует себя: `armed()`
  берёт `test_serial()` и возвращает `MutexGuard` вызывающему, так что каждый тест, вооружающий
  хранилище, держит единственный замок модуля, а второй явный сайт покрывает тест плагина. Значит
  контенция — **не** профилировочный тест против профилировочного. Остаётся состояние, глобальное для
  ПРОЦЕССА, а не для модуля: реестр зон `REGISTRY`/`NEXT_SLOT` в `boyko_diag::profiling_abi` (слоты
  чеканятся лениво через `declare_zone!` и не возвращаются) и `bind_world` хранилища — их может
  сдвинуть любой из ~880 остальных тестов того же бинарника. Оба флейка дают 915/915 в изоляции и в
  повторных полных прогонах lib; падают только на полном прогоне воркспейса. **Следующий шаг —
  найти, какой НЕпрофилировочный тест трогает реестр**, а не расширять замок модуля: он уже шириной
  во весь модуль.
- **Профилировочные тесты `boyko-ecs --lib` — ФЛЕЙКИ, ЗАВИСЯЩИЕ ОТ ПОРЯДКА, и их теперь ДВА.**
  `every_dispatching_round_records_one_span_and_one_width` присоединился к
  `one_zone_taking_a_hundred_thousand_samples_keeps_count_exact` 2026-08-10: упал один раз на полном
  `--workspace --all-targets`, затем прошёл в изоляции и в двух полных прогонах lib подряд
  (915/915). **Второе имя в том же классе поднимает приоритет**: это уже не один невезучий тест, а
  свойство модуля, и настоящая регрессия здесь была бы неотличима от флейка. Исходная запись ниже. Упал один раз на полном `--workspace --all-targets`, затем прошёл в
  изоляции и в двух полных прогонах lib подряд (915/915). Тот же класс, что заметка про
  `MAX_QUERY_TYPES` выше: глобальное на процесс состояние профайлера против 915 тестов параллельно.
  Перезапустите, прежде чем бисектить.
- **⚠️ ИЗВЕСТНАЯ КРАСНАЯ ЦЕЛЬ — ЭТО ТЕНЬ; воркспейсный гейт обязан идти с `--no-fail-fast`.**
  ИЗМЕРЕНО 2026-08-10. `cargo test --workspace --all-targets` **останавливается на первой упавшей
  цели**, поэтому `internal_docs_anchors` — давно известный и красный у всех — прятал каждую цель,
  идущую после него. Всякое «воркспейс зелёный кроме предсуществующего отказа якорей», сказанное за
  кампанию диагностики, было утверждением о том, **докуда дошёл cargo**, а не о воркспейсе. С флагом
  красных целей оказалось **три**, и две из них старше той сессии, что их нашла:
  * `boyko_rhi_vulkan --test compile_fail_frame_write_token` — `26b385eb` (2026-08-06) добавил ОДНУ
    строку в `token_use_after_submit_rejected.rs` и не переблагословил `.stderr`; вся разница —
    `41:5` против `42:5`. **Красный 87 коммитов.** ПОЧИНЕНО правкой двух номеров вручную, а не
    `TRYBUILD=overwrite`: собственный комментарий той фикстуры предупреждает, что благословение
    способно превратить «красное по верной причине» в «зелёное по неверной», поэтому сперва
    проверено, что вид ошибки, перемещённое значение и место перемещения не изменились.
  * `boyko_rhi_vulkan --test cluster_bound_arraylength` — `vb_batch_cull` из VG R3 (и его `-D DEBUG`
    близнец) получил `OpArrayLength`, а `BOUND_BY_ARRAYLENGTH` в том же коммите не обновили — ровно
    то, что предписывает сообщение самого гейта. ПОЧИНЕНО, и пин **расширен, чтобы нести ПРЕДМЕТ**
    каждой записи: у множества больше нет одного предмета — эти двое ограничивают зарезервированный
    хвостовой слот `VbLateCount`, а не фроксельный обход света, и старый текст отказа давал бы
    читателю совет про `use_clusters`, которого у этих шейдеров нет.
  * `boyko-ui --test p3_watch_zero_alloc` — `watch_nochange_path_is_tiny_and_far_below_reload`
    отчитался `nochange 1, reload 1`: «согласующий» тик докладывал стоимость пути «без изменений»,
    потому что ЭТО И БЫЛ путь «без изменений». Сигнатура наблюдателя — `(mtime, size)`, а фикстура
    писала `Px(77)` поверх `Px(40)` — **та же длина в байтах**, поэтому детект целиком висел на
    файловых часах, а разрешение mtime на этой машине проглатывает фикстурные 3 мс. `4956420c` уже
    диагностировал этот класс однажды («flake хот-релоада — это ФАЙЛОВЫЕ ЧАСЫ, а не общее
    состояние») и ответил удлинением пауз, что покупает запас против неизмеренной гранулярности.
    ПОЧИНЕНО так, что переписка меняет РАЗМЕР, — это убирает зависимость, а не расширяет её:
    после — `nochange=1 reload=53`.
  ⚠️ **И мой собственный отчёт об этой находке был в первый раз ОБРЕЗАН.** Список отказов первого
  прогона прошёл через `head -10`, и я прочитал обрезку как итог — «три красные цели» там, где
  честный счёт был шесть. Та же ошибка на уровень выше той, о которой отчёт. Полная картина,
  измеренная:
  * **По-настоящему красные, теперь ПОЧИНЕНЫ**: `compile_fail_frame_write_token`,
    `cluster_bound_arraylength`, `p3_watch_zero_alloc`.
  * **По-настоящему красная, НЕ починена**: `internal_docs_anchors` — 25 устаревших якорей в трёх
    внутренних документах плюс превышение потолка вейверов. Предсуществующая, к здешним кампаниям
    отношения не имеет и достаточно велика, чтобы быть отдельной единицей работы.
  * **Не красные вовсе, но ПАДАЮТ НА ПОЛНОМ ПАРАЛЛЕЛЬНОМ ПРОГОНЕ**: `boyko-log --lib` (84/85 на
    прогоне, **85/85** в изоляции), `boyko_rhi_vulkan --test sdf_gbuffer_hybrid` (**43/43** в
    изоляции, 54 с) и два профилировочных теста `boyko-ecs --lib` выше. Четыре цели в одном классе.
    **Значит сюита воркспейса НЕДЕТЕРМИНИРОВАНА**, и настоящая регрессия в любой из них была бы
    неотличима от шума. Каждая трогает глобальное на процесс или на устройство состояние — полосы и
    слоты зон профайлера, стоки логгера, GPU-устройство, — что лечится ограничением `--test-threads`
    или пометкой последовательного запуска для этих целей.
  Блок команд сборки в `CLAUDE.md` теперь несёт этот флаг и причину.
- **`.claude/settings.local.json` грязный** с прошлых сессий и намеренно никогда не стейджится.

## Ступень 9 — проверка сессии в `resolve` отказывает КАЖДОЙ реальной паре ног и называет это `EpochBreak`

**Найдено при подключении корреляции ступени 9, которая впервые даёт `clock_epoch` в этом дереве
реальный смысл. Ступенью 9 не внесено — ею вскрыто.**

В `crates/boyko_app/src/profiling/contrast.rs` у `LegSummary` было поле с именем `clock_epoch`,
хранившее `(header.session_lo, header.session_hi)` — то есть `SessionId`, а не
`boyko_diag::clock::clock_epoch()`. Ступень 9 переименовала его в `session`: артефакт теперь несёт
настоящий `cpu_gpu_epoch`, и две разные вещи иначе делили бы одно имя в одном модуле. Переименование
сделано. **Две вещи о проверке, которую это поле питает, — НЕ сделаны, и обе за владельцем:**

1. **Отказ докладывается как `NotResolvedReason::EpochBreak` при различии СЕССИЙ.** По духу это
   защитимо — `clock_epoch()` счётчик на процесс, поэтому «обе на эпохе 0» из двух процессов
   сравнивает два числа, ничего друг о друге не значащих, — но слово причины называет то, чего
   проверка не проверяет. Побочный пункт `G13` пинит слово `EpochBreak`, так что переименование —
   поверхность корпуса, а не локальная правка.

2. ⚠️ **На реальных входах проверка отказывает безусловно.** ИЗМЕРЕНО: у `resolve` и
   `LegSummary::from_artifact` **нет ни одного вызывающего вне собственных тестов `contrast.rs`** —
   ступень 8 отгрузила компаратор, чтобы *лицензировать* будущие вердикты, а не обслуживать
   сегодняшний, — и любая пара ног, которую собрал бы настоящий харнесс, приходит из двух
   порождённых дочерних процессов (протокол `vg_decidability_floor.rs` — семь процессов на условие).
   У двух процессов два разных `session_id` по построению, поэтому первый же боевой потребитель
   `resolve` обнаружит, что тот возвращает `NotResolved { EpochBreak }` на любую пару. Существующие
   тесты этого не ловят: в них обе ноги выставлены руками в одно значение.

**Что должна решить починка** (здесь не решено): сравнимы ли межпроцессные ноги вообще. Если да — а
весь протокол пола это предполагает, поскольку пулит семь сессий, — то проверка написана неверно, и
настоящая защита в другом (тот же `workload_tag`, та же машина, тот же `clock_epoch` ВНУТРИ каждого
артефакта). Если нет — то протокол пола и эта проверка противоречат друг другу и одно из них ошибка.

## Ступень 9 — покадровое кольцо стало ДВУМЯ отсрочками, указывающими на ОДИН механизм

Корреляция публикуется один раз на окно, с измеренным дрейфом 173 ppm поперёк него
(`Correlated::deviation_at_ns` интерполирует). Счётчики `vkCmd*` по зонам со ступени 8 доходят до
печатной переписи, но не до артефакта — по структурной причине той же формы (витнесс сбрасывается
покадрово, а `retire` отдаёт кадр четырёхкадровой давности). Обеим нужен покадровый канал, не
сводящийся к медианам, — то же, что нужно исходной просьбе владельца («разложить кадр по системам и
проходам, ловить покадровые всплески»). Записано, чтобы построить это один раз, а не дважды.

## Нога фичи `hwrt` НЕ СОБИРАЕТСЯ — предсуществующее, найдено clippy-свипом ступени 9

**ИЗМЕРЕНО и доказано, что не от этой кампании:** `cargo clippy -p boyko-app --lib --features
boyko_rhi_vulkan/hwrt` падает с

```
error[E0063]: missing fields `atrous_layout_denoise_hwrt`, `motion_cam_ubo_ring`, `mv_bind_group`
and 21 other fields in initializer of `boyko_rhi_vulkan::present::GBufferScene<'_>`
    --> crates\boyko_app\src\gpu_scene\mod.rs:6298:25
```

Двадцать четыре недостающих поля. Предсуществование подтверждено: все правки ступени 9 убраны
`git stash`, прогон повторён — **та же самая ошибка на нетронутом дереве на `71085737`**.

**Почему это никто не поймал.** `hwrt` — `default = false`, а каждый гейт в дереве
(`cargo check --workspace --all-targets`, гейт clippy, тестовый свип) идёт по ДЕФОЛТНОМУ набору
фич. Нога не собирается никогда, поэтому может гнить, ничего не окрашивая в красное. Это та же
форма, что и две находки выше (цель, стоящая за уже красной; виртуальный манифест, проверяющий
подмножество): **конфигурация, которую ничто не собирает, — это конфигурация, которую ничто не
гейтит.** Ступень 8 записала этот урок для `profiling-alloc` и починила его, сделав собираемыми обе
конфигурации; `hwrt` — тот же случай крупнее, и не собирается достаточно давно, чтобы расхождение
достигло двадцати четырёх полей.

**Здесь не чинится.** К ступени 9 отношения не имеет, требует разбора всех двадцати четырёх
`hwrt`-полей `GBufferScene` по одному, и угадывание было бы хуже нынешнего честного слома. **Решение
владельца:** починить ногу и добавить её в набор гейтов — либо заявить, что `hwrt` спящая, и
перестать подразумевать обратное.

## Ступень 10 — два гейта корпуса не могут идти как написаны, по двум разным причинам

Оба записаны с арифметикой в `docs/diagnostics/profiling/05-LADDER-GATES.md`, в блоке ступени 10.
Здесь — потому что каждое **решение владелец может захотеть принять иначе**, а не просто заметка.

**1. Абсолютные наносекундные пороги `G17` заменены на отношение A/B.** Строка просит пять
наносекундных бюджетов в одном сеансе, самая тесная пара — «статика armed ≤ 12 нс» против «dyn armed
≤ 14 нс», то есть **два наносекунды запаса**. Эта кампания измерила собственный пол канала артефакта
в **6.5 %** при разбросе повторений 4.7–14.3 % — на GPU-проходах ценой в микросекунды. Бюджет в 2 нс
внутри этого шума на порядок. Вместо этого отгружено: реализованы **обе** ветки — отгруженный гейт и
тот, что с разыменованием `REGISTRY[id]`, который строка называет своим КРАСНЫМ, — и они чередуются в
одном процессе, а утверждается, что отгруженная не медленнее. ИЗМЕРЕНО (debug): 10.89 против 22.01
нс/итерацию, 2.02×. **Если владелец хочет абсолютные пороги, нужны релизный бенч-харнесс и
записанный пол на эту машину для наносекундной шкалы** — ни того, ни другого нет, а выдумывать порог
без них — ровно тот способ, каким гейт начинает краснеть от фонового процесса.

**2. Клауза 2 `G22b` называет символ, которого нет, и отказ, который здесь невозможно написать.**
Клауза: *«`#[test]`, объявляющий `.bss`-массив, размер которого взят из значения `ProfilerConfig`,
обязан провалить `assert_bss_eligible` НА КОМПИЛЯЦИИ; убрать const-assert ⇒ компилируется ⇒
красное».* ИЗМЕРЕНО: у `assert_bss_eligible` **ноль вхождений** в `crates/` (символ называется
`assert_zero_init_eligible`), а описанный отказ здесь невозможен — `SyncCells<T, N>` берёт свой
экстент **константным генериком**, поэтому рантайм-значение `ProfilerConfig` не может его задать, что
бы ни утверждал любой assert. Убирать нечего, потому что ловить нечего. Вместо этого отгружен
`trybuild`-кейс на свойство, которое ступень 10 ДЕЙСТВИТЕЛЬНО внесла: удаление обёртки `MaybeUninit`
у `DYN_DESCS` обязано быть `E0277`. **Решение владельца: переписать клаузу под реальный символ либо
удалить её как удовлетворённую системой типов.**

**И клауза 1 `G22b` остаётся ЗАБЛОКИРОВАННОЙ тем же отсутствующим инструментом, что и `G22a`** — нет
`llvm-readobj`/`objdump`/`nm` под активным `stable-x86_64-pc-windows-gnu`. Ступень 10 добавила ещё
два символа (`DYN_DESCS`, `DYN_NAMES`) в множество, которое этот зонд обязан покрыть, когда появится
`rustup component add llvm-tools`, — то есть пункт D0 разблокирует теперь четыре имени, а не два.

## Ступень 10 — буквальное КРАСНОЕ `G23b` непроизводимо, и никакое значение константы его не даёт

КРАСНОЕ строки: поднять `MAX_USER_BUDGET` в **shipping**-профиле с 512 до 3072 ⇒ +20 КиБ `REGISTRY`
и +120 КиБ `DYN_DESCS` ⇒ 1 348.2 КиБ пересекают бюджет 1 280 КиБ.

Shipping-профиля нет: ось `BOYKO_PROFILE` — ступень 14, и ИЗМЕРЕНО, что **`build.rs` не существует
нигде в воркспейсе**. Значит гейт резидентности идёт по строке dev против бюджета в **16 МиБ**. А у
константы есть жёсткий потолок, и он не вопрос политики: id зон — `u16`, поэтому
`ENGINE_ZONE_SLOTS + MAX_USER_BUDGET` обязано остаться под `u16::MAX`, что ограничивает
`MAX_USER_BUDGET` величиной ~61 439. При 8 Б/id в `REGISTRY` плюс 24 Б/id в `DYN_DESCS` это **~1.9
МиБ прироста против 16 МиБ бюджета** — не пересекает, и ничто из того, что можно выставить, не
заставит пересечь.

Верхняя граница, которую нечем перешагнуть, — это гейт, который не может упасть. Отгружено
**тождество композиции**: домен 3 обязан равняться сумме четырёх `.bss`-членов, посчитанной
независимо, — и вот ЕГО красное производимо: выкидывание `dyn_descs_bytes()` даёт
`left: 143360, right: 217088`. Это ровно то утверждение, которое делает заголовок самой строки
(*«эта строка — то, что кладёт их байты внутрь суммы бюджета»*), и оно загейчено. **Сама клауза
бюджета остаётся честной, но беззубой, пока ступень 14 не даст ей shipping-строку, против которой
она была бы тесной.**
