# Открытые вопросы к владельцу

Трудности, спорные решения и то, чего я не понял — записывается по мере появления, чтобы владелец
мог прочитать позже и высказаться, а не находил это погребённым в отчёте, когда решение уже принято.

> Русская версия документа [`../OPEN-QUESTIONS.md`](../OPEN-QUESTIONS.md), который остаётся
> **источником истины**; правка любой стороны обновляет вторую в том же коммите.
> См. [README.md](README.md).

**Соглашение.** Свежее сверху. В каждом пункте — ситуация, варианты и что он блокирует. Закрытый
пункт помечается `РЕШЕНО` с датой и решением владельца, а не удаляется: запись о том, *почему* так
решили, переживает само решение. Перф- и архитектурные развилки я решаю сам, с числами; сюда
попадают ЦЕННОСТНЫЕ и ОБЪЁМНЫЕ вопросы и всё непонятое.

---

## 2026-08-06 — ⚠️ ИЗМЕРЕНО: sync-validation не жива, поэтому нога `-ValidationOn` ничего не доказывает про барьеры

**Настоящий пропущенный барьер не сдвинул ни одного пикселя и не выдал ни одного сообщения.**
Исполнено при разрешении первого шага части 2, который ровно для этого и существовал.

Зонд: удалить ЕДИНСТВЕННОЕ объявленное чтение ресурса, у которого ровно один читатель — чтение мипа
`d-1` пирамидой HZB, — оставив диспатч, который его читает. Пас 0 пишет мип 5, пас 1 читает мип 5,
зависимость не выводится.

| | сообщений | `SYNC-HAZARD-*` | golden |
|---|---|---|---|
| база (×2, тот же билд) | 19 | — | побайтово тот же |
| **настоящий пропущенный барьер** | **19** | **нет** | **побайтово тот же** |

Бит фичи в `boyko_rhi_vulkan/src/device.rs` ЕСТЬ, но цепочка инстанса **молча** деградирует при
отсутствии `VK_EXT_validation_features`, а вся база из 19 сообщений — это `vkCreate*`-время: ни одно
из них не порождено записанным кадром.

**Почему это здесь, а не просто записано.** Это не факт части 2. Это значит, что валидационная нога
движка — инструмент, на который кампания опирается с момента починки `-ValidationOn` на P1-2, —
покрывает легальность объектов, дескрипторов и форматов и **ничего про синхронизацию**. Каждое
«validation clean» в сообщениях коммитов истинно и уже, чем читается.

**Варианты.** (a) Оставить, а корректность барьеров гейтить структурно (пинить выведенный поток
барьеров по ПОЛЯМ — что G4 части 2 теперь и делает). (b) Выяснить, действительно ли
`VK_EXT_validation_features` отсутствует на устройстве или просто не доходит до слоя, и починить,
если второе, — это расследование примерно на час, и оно вернуло бы универсальный инструмент.
(c) Оба.

**Моя рекомендация — (c), сначала (a)**: (a) уже описан и ничего не блокирует, а (b) стоит сделать
до части 3 — именно там появляется первый ЧИТАТЕЛЬ пирамиды, а read-after-write через два паса это
ровно тот класс гонки, который слой ловит, а golden не видит.

⚠️ **Методическая заметка, стоящая не меньше самой находки.** ПЕРВЫЙ зонд был неразрешающим по
построению: он удалил одного из ШЕСТИ объявленных читателей того же образа, соседи по-прежнему несли
и переход, и зависимость, и не проверялось ничего. Его отрицательный результат был бы записан как
«расширение отсутствует на этом устройстве» — верное утверждение, полученное негодным путём. Прежде
чем зондировать пропущенную зависимость, сосчитайте ОСТАЛЬНЫЕ объявленные доступы к этому ресурсу.

---

## 2026-08-05 — релизная нога CI красная, и два класса из неё — вопрос СТРОГОСТИ

Найдено при подготовке базовой линии P1-5a, которой нужна проходящая нога. Прогон собственной
команды CI — `cargo test --workspace --all-targets --release --exclude boyko_demo --exclude
bench-bevy-vs-boyko` ([ci.yml:62](../../.github/workflows/ci.yml), `:103`) — **падает на шести
целях**. Ещё две всплывают на втором прогоне, и это само по себе диагноз: они флейковые, а не
релизные.

Шесть были механическими и **починены**: пять `#[should_panic]` над `debug_assert!` без
`#[cfg(debug_assertions)]` (boyko_math, boyko_ecs, boyko_render ×3 плюс две в boyko_rhi_vulkan) и
одна пропущенная запись в `VB_PINS` — моя, `vb_mesh_hzb` из VG R3 P1-2.

Осталось два класса, и оба — решения о том, насколько строгим должен быть гейт, а не архитектурные
развилки, поэтому они здесь, а не приняты мной.

### 1. `boyko_shaderdsl --test eval_byte_identity` — три падения, ровно на ЗНАКОВОМ БИТЕ NaN

`NaN (0xffc00000)` против `NaN (0x7fc00000)`. Оба тихие NaN; значения тождественны (NaN не равен
даже себе); различается только знак.

Это то же семейство, что гейт G3 намерил на пирамиде глубины в тот же день: **знак нуля и знак NaN —
ровно те два бита, которых не видит ни одно `<` в программе**, поэтому железо и оптимизаторы вправе
их двигать. G3 поймал драйвер, свернувший compare-and-select в аппаратный min с другим разрывом
ничьей на ±0. Ожидать стабильности любого из этих битов между `-O0` и `-O3` необоснованно.

**Варианты.** (a) Сравнивать NaN как «оба NaN», а не по битам. (b) Канонизировать знак перед
сравнением. (c) Оставить как есть, признав, что релизная нога eDSL — не гейт.

**Моя рекомендация — (a).** Контракт, ради которого eDSL существует, — про ЗНАЧЕНИЯ, а знак NaN
значением не является. На конечной области — то есть на всей области, которая важна, — это не стоит
ничего, и это снимает вечную красноту с настоящего гейта. Вечно красный гейт хуже слегка суженного:
его перестают читать.

### 2. Два теста над глобальным состоянием, флейкующие при параллельном исполнении

`boyko-scene bundles_s6::interner_is_off_the_per_frame_path` читает процессно-глобальный
`identity::interner_len()`. `boyko-ui zero_alloc::unchanged_frame_layout_pair_allocates_zero_over_baseline`
читает глобальный счётчик аллокаций — и сообщил дельту **минус один**, то есть улучшение, которое его
`assert_eq!` выразить не может, хотя собственное сообщение теста говорит «не больше».

Оба проходят в одиночку, проходят при `--test-threads=1` и проходят в debug. Падают только в релизе
при параллелизме по умолчанию.

**Варианты.** (a) Последовательный guard в каждом из этих файлов. (b) `--test-threads=1` для этих
бинарей в CI. (c) Сделать утверждение UI `<=` по его же сообщению, а сериализовать только scene.

**Моя рекомендация — (a) плюс починка на `<=`**: флаг гарнизона замедлит все тесты этих крейтов ради
двух, а равенство, падающее на улучшении, упадёт снова, как только кто-нибудь улучшит ещё раз.

---

## 2026-08-05 — ОБЪЁМ: пирамиде нужна правка ядра фреймграфа (per-subresource состояние)

**Решено и уже делается, ничего не блокирует — записано потому, что это расширяет часть 1 за
пределы её плана.** Шаг P1-5 (объявить пасы сборки HZB) невозможно написать против нынешнего
фреймграфа, и фреймграф говорит об этом сам.

**Стена.** `framegraph/graph.rs:360-445` содержит `INVARIANT HZB-SUBRESOURCE-UNIFORM`: все доступы к
одному `ResId` обязаны объявлять один и тот же `(base_mip, mip_count, base_layer, layer_count)`,
потому что `FrameGraph::state` — это `Vec<ResSync>`, индексируемый только по `ResId`, а `transition`
пролёта вообще не получает. Сборке HZB нужно на ОДНОМ образе в ОДНОМ пасе читать мип `6p-1` и писать
мипы `[6p, 6p+n)`.

Тот комментарий написан заранее, называет именно этот пас и предписывает ответ:

> «PER-SUBRESOURCE TRACKING — ПРАВИЛЬНЫЙ ДОЛГОСРОЧНЫЙ ОТВЕТ, и этот assert — его ТРИГГЕР… сборка HZB
> пишет мип k, читая мип k-1. Когда этот пас будет написан, он уронит этот assert. Это и есть
> ЗАДУМАННЫЙ способ обнаружить работу… Ответ — построить per-subresource трекинг, а не ослаблять
> условие, пока оно не замолчит.»

**Это не просто debug-assert.** В release он вырезан, и вывод барьеров становится по-настоящему
неверным; прослежено на 512×512: пас 0 впервые касается мипов [0,6), только они выходят из
`UNDEFINED`; пас 1 затем пишет мипы [6,10), а состояние считает GENERAL для всего ResId, поэтому у
выведенного барьера `old_layout == new_layout` и мипы 6..9 **не переводятся никогда**, хотя диспатч
пишет в них через storage-дескрипторы, объявленные как `GENERAL`. Достижимо на любом экстенте с
`prev_pow2(max(W,H)) >= 64`.

**Обходной путь, который я отверг.** Три ResId, ссылающиеся на один `VkImage` по непересекающимся
диапазонам мипов, единообразны по построению и не требуют правки фреймграфа. Отказался по двум
причинам. Это буквально то, что запрещает текст самого инварианта («не подгоняя объявления руками»),
и это тупик уже через одну часть: cull части 3 выбирает УРОВЕНЬ пирамиды per-instance, а ResId
на-пас динамическим уровнем не назовёшь. Взять его — значит принять временный дизайн с откладыванием
правильного, что в этом проекте запрещено.

**Чего это стоит.** Новый шаг P1-5a перед P1-5, трогающий ядро машины состояний, через которое
компилируется каждый путь рендера. Аргумент за побайтовую идентичность сильный —
`SubRange::color_mips` сегодня не вызывается ниоткуда, и все существующие сайты `image_access`
передают `base_mip: 0, mip_count: 1`, так что per-mip машина обязана свернуться в сегодняшнее
поведение барьер-в-барьер, — но именно из-за слова «обязана» этому нужен свой гейт, а не golden-пин:
пин не видит ни лишнего барьера, ни пропавшего.

**Ответ ничего не блокирует.** Архитектурные развилки я решаю сам; это здесь потому, что вырос
ОБЪЁМ, и владелец может предпочесть остановить часть 1 на «выделено и скомпилировано», отдав работу
по фреймграфу отдельной кампании. Скажите — разделю.

---

## 2026-08-04 — ⚠️ `golden.ps1 -ValidationOn` НИКОГДА не включал слой валидации ни на одном пине `boyko-app`

**Найдено при гейтинге VG R3 P1-2, и это снова форма «вакуумно-зелёного».** Этот переключатель —
инструмент валидационного аудита движка; в кампании записана веха «Validation-ON audit — COMPLETE»,
проходившая через него. На 22 пинах из 25, которые грузятся через `boyko_app`, он не мог упасть.

**Механизм.** Бэкенд включает слой по конъюнкции —
`config.enable_validation && BOYKO_DISABLE_VALIDATION не задан` (`boyko_rhi_vulkan/src/device.rs:2350`),
— а рантайм `boyko_app` зашивал `enable_validation: false`. `-ValidationOn` только *снимал
переменную окружения*, то есть удовлетворял второй конъюнкт, пока первый оставался ложным. Слой не
запрашивался, мессенджера не существовало, и сканирование строк `[vk-validation]` поэтому
докладывало **«clean (0 messages)»** безусловно.

Док самого рантайма это и говорил — но читался как проектная заметка, а не как дефект гейта:
«The shipped runner does NOT request the validation layer… a debug validation knob arrives with a
later rung».

**Измерено, а не выведено.** Я собрал образ `512×512` с `mip_levels: 12` (законный максимум — 10).
`vkCreateImage` вернул успех, аудит доложил «чисто». После починки та же порча даёт ровно то
сообщение: `vkCreateImage(): pCreateInfo->mipLevels (12) must be less than or equal to 10`.

**Починено здесь**, потому что от этого зависит несущий гейт P1-2: `BOYKO_ENABLE_VALIDATION`
включает рантайм явно (без переменной загрузка байт-идентична прежней), а `-ValidationOn` теперь
задаёт её вместе со снятием.

**⚠️ ЧТО ЭТО ВСКРЫЛО И В ЧЁМ ВОПРОС.** С реально живым слоем пин `vb_mesh` выдаёт **19 сообщений
валидации** — базовый уровень, которого никто не видел:

| штук | сообщение |
|---|---|
| 9 | `vkCreateComputePipelines()`: compute-шейдер использует дескриптор `[Set 1, …]` |
| 6 | `vkCreateGraphicsPipelines()`: вершинный атрибут на location 1/2 не потребляется вершинным шейдером |
| 1 | **`vkDestroyDevice(): VkDevice has 13 leaked objects that have not been destroyed`** |
| 1 | `vkCreateShaderModule()`: возможность SPIR-V `Geometry` объявлена без включённой фичи |
| 1 | `vkCreateShaderModule()`: возможность SPIR-V `DemoteToHelperInvocation` объявлена без включённой фичи |
| 1 | предупреждение о лимите дубликатов |

Пирамида ни при чём: логи вооружённого и невооружённого прогонов **побайтово совпадают** после
нормализации хендлов, то есть P1-2 не добавляет ничего. Но два сообщения о возможностях шейдеров и
13 утёкших объектов — настоящие, они на флагманском VB-пине, и одно из них — утечка ресурсов.

**Вопрос — про ОБЪЁМ, не про метод.** Варианты: (а) я разбираю и чиню эти 19 сейчас, до продолжения
пирамиды — это утечка и два бага объявления фич на основном пути; (б) я доделываю первую часть, а
валидационный базовый уровень беру отдельной кампанией после; (в) чиню сейчас только утечку,
остальное откладываю. Я склоняюсь к (б): эти 19 появились задолго до нынешней работы, пирамида
доказано чиста относительно них, а вплетение неограниченного аудита в декомпозицию, созданную
*именно* ради локальности объёма, эту декомпозицию и отменит. Но решение ваше — это объём, а
утечка относится к тому роду вещей, которые в ожидании только усугубляются.

---

## РЕШЕНО 2026-08-03 — проект фичи HZB не сходится целиком: разбит

**Ситуация, измеренная по трём кругам ревью, а не почувствованная.**

| круг | закрыто прежних | новых блокеров | новых major |
|---|---|---|---|
| 1 | — | 8 | — |
| 2 | 10 YES / 6 PARTIAL / 0 NO | 3 | ~12 |
| 3 | 31 YES / 12 PARTIAL / 2 NO | 6 | 11 |

Каждый круг честно закрывает почти всё, что поднял предыдущий, и поднимает примерно столько же
заново. После третьего круга блокер есть у **каждого содержательного шага** (S4, S6a, S6b, S7, S9);
четыре чистых шага — это гейты и записи, зависящие от заблокированных. То есть, в отличие от
фундамента, независимого подмножества для сдачи нет.

Изменился и ХАРАКТЕР новых блокеров, и это полезный признак. Это уже не «алгоритм неверен», а
столкновения с отгруженными инвариантами: четвёртый маршрут, которым проект разоружает ступень
R2d-6 (удвоение списка выживших ломает тот самый const-assert, добавленный в R2d-4 против чтения за
границей); вершина `+INFINITY` из фикстуры, доходящая до второго, неогороженного потребителя на
отгруженном пути VB; способность, которая есть покадровый факт ECS, гейтящая объекты, чеканящиеся на
буте, без названного шва между ними.

**Что я из этого читаю.** Фича просто больше, чем удерживает один проходной проект. Фундамент
сошёлся за один круг на каждую часть не потому, что процесс там был лучше, а потому, что S1/S2/S3
были малы, независимы и самодостаточны.

**Предложение.** Разбить фичу так же, как уже разбит фундамент, и дать каждой части собственный круг
проектирования и ревью:

1. **Только пирамида** — выделить, построить, загейтить против хостового оракула S3. Никакой
   интеграции с cull. Она самодостаточна, оракул уже есть, её блокеры локальны.
2. **Только способность и раскол растеризации**, инертно — вторая область не рисует ничего,
   байт-идентичность на пинах доказана.
3. **Интеграция cull** — когда 1 и 2 сданы, и столкновения выше конкретны, а не предсказаны.
4. **Вооружение** с гейтом по множеству отрисованных.

**Цена, названная прямо.** Четыре круга проектирования вместо одного, и фича приезжает позже.
**Цена альтернативы**, тоже названная: четвёртый круг по всей фиче, который по этой статистике
закроет ~30 пунктов и поднимет ~6 новых.

**РЕШЕНИЕ ВЛАДЕЛЬЦА: разбивать.** Каждая из четырёх частей выше получает собственный круг
проектирования и ревью и приземляется независимо, начиная с одной только пирамиды. Записано, чтобы
будущий читатель видел: четыре круга — намеренная структура, а не проект, который всё падал.

**Блокирует.** Ничего.

---

## РЕШЕНО 2026-08-02 — фикстурой глубинной сложности будет Khronos Sponza (решение делегировано мне)

**Ситуация.** Решение (б) выше обязывает завести отдельную фикстуру с настоящей окклюзией. Что в ней
должно быть — не было решено; вопрос по ассетам я вынес, а не закрыл самовольно, после чего владелец делегировал его мне.

Корпус VG — семь скачанных образцов Khronos glTF на сетке 5×3×3, подобранных под плотность
треугольников, и окклюзии в нём почти нет по построению. Фикстуре глубинной сложности нужно
обратное: крупные окклюдеры и существенная геометрия за ними. Классический выбор — Sponza (интерьер
с колоннадой, которая закрывает сильно) или Bistro; ни того, ни другого в манифесте сегодня нет.

**РЕШЕНИЕ ВЛАДЕЛЬЦА: делегировано мне.** Выбрана **Sponza из того же семейства Khronos
glTF-Sample-Assets**, откуда уже берётся корпус плотности; тянется в `assets/vg_occlusion/` под
собственным манифестом, в gitignore и с пином по содержимому — ровно как `assets/vg_corpus/`.

**Почему Sponza, а не Bistro и не Intel Sponza.**

- *Тот же источник, что у корпуса*: форма скрипта загрузки, лицензионная позиция и правило gitignore
  переносятся целиком. Ни новой инфраструктуры, ни нового лицензионного вопроса — а именно эти три
  вещи превратили бы задачу в многосессионный крюк.
- *Тот же путь загрузчика*: glTF/`.glb` через `GlbMeshLoader`, уже отработанный каждым ассетом
  корпуса.
- *У неё правильная СТРУКТУРА окклюзии*, в чём и весь смысл: колоннада плюс верхняя галерея, так что
  камера на уровне пола вдоль нефа имеет дальнюю половину закрытой колоннами. Ровно то, чего у
  корпуса плотности нет по построению.
- *Размер*. В этой сессии мы уже упёрлись в ноль свободного места при `target/` в 73 ГБ. Bistro —
  ~2.4 ГБ, и её glTF-конверсии разного происхождения, что здесь важнее обычного: репозиторий пинит
  по хешу содержимого, а пин на артефакт, который никто не может перевывести, — не пин.
- *Сопоставимость*: Sponza — общепринятый эталон окклюзии и GI, так что снятое на ней число значимо
  и за пределами этого репозитория.

**Риск, который я беру, — называю сразу, а не после.** ОДНА сцена есть та же выборочная уязвимость,
о которой предупреждают заметки корпуса: одну рамку можно подобрать под желаемое. Смягчение —
корпусное же: несколько закоммиченных путей камеры, охватывающих степени закрытости (вдоль нефа —
сильная; с галереи — умеренная; снаружи внутрь — слабая), и связывает **слабейшая**, ровно как
`orbit_mid` связывает корпус плотности. Выигрыш, заявленный с одной только сильной рамки, был бы
дефектом, а не результатом.

**Что останется сделать при постройке.** Пины `source_url` / `archive_sha256` / пофайловый
`glb_sha256` заполняются с первой проверенной загрузки, как заполнялись у `CORPUS.toml`, — а не
пишутся по догадке.

**Блокирует.** Любую перф-заявку по окклюзии. Реализацию не блокирует и на критическом пути HZB не
стоит.

---

## РЕШЕНО 2026-08-02 — перф-заявка окклюзии: вариант (б), отдельная фикстура с глубинной сложностью

**Ситуация.** Корпус VG — инструмент измерения плотности треугольников, намеренно перекомпонованный
на ступени R0b′ ради плотности, а не окклюзии. Измеренный на нём потолок: **1 инстанс из 44
рисуемых** на `orbit_mid` (связывающая рамка) и 11 из 31 на `approach_close`. Min-редуцированный HZB
способен отвергнуть только те инстансы, что не выигрывают ни одного пикселя, так что это жёсткие
верхние границы — и они ограничивают не только окклюзию: инстанс не выигрывает пикселей и когда он
субпиксельный.

Значит HZB и двухпроходную окклюзию можно построить правильно и загейтить на корректность, но
**показать ускорение нельзя ни на одном материале в этом дереве**.

**Варианты.** (а) Сдать с гейтами на корректность и без перф-заявки — как ступень R2d сдана
структурной; честно, и заявка остаётся несделанной, пока не появится материал. (б) Завести сцену с
настоящей глубинной сложностью (интерьер, улица) **отдельной** перф-фикстурой, вне корпуса
плотности, чтобы инструменты не загрязняли друг друга. (в) Признать, что заявку сделает тот проект,
который будет писаться на движке, а не этот репозиторий.

**РЕШЕНИЕ ВЛАДЕЛЬЦА: (б).** Построить сцену с настоящей глубинной сложностью — интерьер или улицу —
**отдельной** перф-фикстурой, намеренно вне корпуса плотности, чтобы два инструмента не загрязняли
друг друга. Пока её нет, ступень HZB сдаётся с гейтами на корректность и без заявки на скорость.

**Продолжение требует решения по ассету** — вынесено отдельным пунктом выше, а не предположено.

---

## РЕШЕНО 2026-08-02 — K2: вариант (в), остаётся отложенным

**Ситуация.** Критерий отсева K2 требует эталонной таблицы Nanite. Она никогда не была получена: UE
не установлен, а установить его я не могу — там принятие EULA и создание аккаунта, что мне запрещено.
Текст самого K2 говорит, что невоспроизводимый эталон *вынуждает переформулировать цель*: абсолютный
целевой показатель вместо относительного.

**Варианты.** (а) Владелец ставит UE и снимает таблицу. (б) Переформулировать цель на абсолютный
ориентир (время кадра при заданном числе треугольников и границе ошибки) и записать, что K2 взят
собственным аварийным выходом. (в) Оставить отложенным и держать цель формально нефальсифицируемой.

**РЕШЕНИЕ ВЛАДЕЛЬЦА: (в).** Остаётся отложенным; цель сознательно остаётся формально
нефальсифицируемой. Ни одна ступень этим не блокируется. Записано, а не тихо снято, — чтобы будущий
читатель не принял молчание кампании о K2 за то, что K2 выполнен.

---

## РЕШЕНО 2026-08-02 — межкадровая корректность окклюзии

**Опасение было моим, и оно было поставлено неверно.** Пирамида предыдущего кадра действительно не
консервативна — но только для **однопроходного** отсечения. В двухпроходном она никогда не последнее
слово: корректность целиком живёт в позднем проходе, который тестирует против пирамиды, построенной
из глубины **этого** кадра. Ранний проход — *непроверяемая эвристика*, чья единственная задача набить
буфер глубины хорошим набором окклюдеров; его ошибки стоят работы в позднем проходе и никогда не
стоят геометрии. Теорема квантифицирована по **всем** возможным выходам раннего прохода, поэтому о
нём вообще ничего доказывать не требуется.

Подтверждено практикой, а не предположено: UE5 Nanite, Assassin's Creed Unity (SIGGRAPH 2015),
Granite, Bevy 0.16 и Unity 6 GPU Resident Drawer устроены одинаково.

Полная формулировка и доказательство: [`../VG-R3-HZB-PLAN.md`](../VG-R3-HZB-PLAN.md) §1. **Решения
владельца не требуется.**

---

## РЕШЕНО 2026-08-02 — HZB: вариант (а), затем ПЕРЕСМОТРЕН НА (б) — сперва фундамент

**Ситуация.** С корректностью разобрались, после чего проект реализации прошёл ревью и вернулся
REJECTED от обоих рецензентов. Блокеры содержательные, не стилистические. Среди них: проект
воскрешает фрустумно отсечённые инстансы и тем самым стирает вооружение ступени R2d-6; неизвестные
границы меша дают **постоянное** ложное отвержение для любого стримящегося меша, и дефект переживает
оба прохода; единственный гейт, способный увидеть ложное отвержение, нельзя построить как написано,
потому что у `vb_depth` нет `TRANSFER_SRC`, а путь считывания помечен как непроверенный и при этом
несущий; и сборка пирамиды, будучи compute, обязана разрезать единственную область dynamic-rendering
растеризатора VB надвое — чего план не рассматривает, а наивная вторая область стёрла бы ранний
проход через `LOAD_OP_CLEAR`.

Полный список: [`../VG-R3-HZB-PLAN.md`](../VG-R3-HZB-PLAN.md) §5.

**Варианты.** (а) Ещё один круг ревизии проекта против восьми блокеров, затем реализация — тот же
цикл, что провёл ступень R2d от восьми блокеров до сданной. (б) Строить сейчас бесспорный фундамент
(S1 — `TextureView` в RHI, S2 — страж пер-субресурсной синхронизации во фреймграфе, S3 — хостовый
оракул), пока проект отсечения переделывается: эти три самостоятельно полезны и ни один не зависит
от спорных частей. (в) Заморозить ступень.

**РЕШЕНИЕ ВЛАДЕЛЬЦА: (а).** Один полный круг ревизии против всех восьми блокеров, затем реализация
по порядку шагов. Моей рекомендацией была (б) — сперва бесспорный фундамент, — и она не принята.
Вариант (а) есть тот же цикл, что провёл ступень R2d от восьми блокеров до сданной, и он сохраняет
порядок шагов вместо того, чтобы вплетать работы фундамента в проект, который ещё в движении.

**ПЕРЕСМОТРЕНО НА (б) в тот же день, владельцем, после возврата круга ревизии.** Круг закрыл все
прежние блокеры (10 YES / 6 PARTIAL / **0 NO**) и принёс **три новых** плюс десяток MAJOR — и все они
до единого сидят в **фиче**: маршрутизация кандидатов, предикат способности без конъюнкта
`mesh_leg`, очистка на буте, требующая `TRANSFER_DST`, которого у образа нет, некольцованный
покадровый UBO, ненаблюдаемый `prev_view_proj`, невыполнимый анти-вакуумный пункт.

**Ни один не попадает в фундамент** — ни в `TextureView` для RHI, ни в стража субресурсной
синхронизации, ни в хостовый оракул. Три круга проектирования, ноль блокеров там. Это улика, а не
предпочтение, и она переворачивает основание моей же первой рекомендации: тогда это была догадка,
теперь — измерение.

Часть блокеров существует ИМЕННО ПОТОМУ, что фундамента нет: один прямо говорит, что приёмка шага
не может быть выполнена на этом шаге, поскольку инструмента ещё не существует. Постройка фундамента
убирает класс замечаний, а не откладывает его.

**Итак: реализуем сейчас S1 (`TextureView` в RHI), S2 (страж субресурсной синхронизации) и S3
(хостовый оракул).** Каждый самостоятельно верен, нужен независимо от того, будет ли окклюзия
когда-нибудь вооружена, и ни один не зависит от спорных частей. Проект фичи продолжает устаканиваться
против оставшихся блокеров — уже на существующем фундаменте.

**Блокирует.** Только фичу окклюзии. Фундамент идёт.

---

## ИЗВЕСТНЫЕ ТРЕНИЯ — решения не требуют, записаны чтобы не переоткрывать

- **`target/` растёт неограниченно и молча ломает сборку.** В этой сессии дорос до 73 ГБ и упёрся в
  ноль посреди сборки. Отказ выглядит как **ошибка линкера mingw**; настоящая причина — в последней
  строке (`no space on device`). `cargo clean` вернул 73.6 ГиБ.
- **Тест trybuild падает при параллельном полном прогоне** (`compile_fail_frame_write_token`, 3 из 3
  фикстур) и проходит в одиночку. trybuild запускает собственный cargo в тот же `target`. Это не
  флейк, а воспроизводимая конкуренция.
- **`cargo test` ОСТАНАВЛИВАЕТСЯ на первом упавшем бинаре, и счётчик сюит молча падает.** Прогон,
  задевший флейк выше, отчитывается о ~51 сюите вместо ~445 — то есть «прогнал полную сюиту» может
  означать «прогнал девятую часть», и в выводе об этом ничего не сказано. Всегда передавайте
  `--no-fail-fast`, когда утверждение касается покрытия, и читайте ЧИСЛО сюит, а не только число
  провалов.
- **graphify всю сессию мимо render/VB-пути**: каждый запрос возвращал внутренности `boyko_demo`.
  Рабочий путь там — Grep/Read.
- **Глобальный реестр типов запросов ECS может исчерпаться на полном прогоне lib-сюиты.**
  `MAX_QUERY_TYPES = 1024` — потолок на процесс, заполняется лениво, а `boyko-ecs --lib` гоняет 864
  теста параллельно. Когда планировщик успевает отчеканить 1025-ю различную форму запроса,
  терминальной паникой с именем потолка падает тот тест, кому не повезло. Наблюдалось один раз,
  затем 3 чистых прогона того же бинаря подряд. Это зависимость от порядка, а не сигнал регресса —
  перепроверьте повтором, прежде чем что-то бисектить.
- **`.claude/settings.local.json` грязный** с прошлых сессий и намеренно никогда не стейджится.
