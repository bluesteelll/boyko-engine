# Runtime Data Ledger rev 3 - group pool-utils-log

Part of the [runtime data ledger, rev 3](../RUNTIME-DATA-LEDGER.md) (the index: vocabularies, totals, entity model, kernel features, decisions). **Tree:** `D:/wt/joltab`, `merge/ke16-into-ecsnative` @ `d11962a9`. **Rows:** 131, of which **131 active**; 712 non_rows (kept in the group JSON ledger).

Active forms: kernel-internal 34, out-of-scope:test-only 28, out-of-scope:compile-time 28, resource-column 15, diagnostics 12, out-of-scope:os-owned 10, scope-arena 4. Active classes: K 3, R 3, S 5, B 27, D 14, T 23, C 56. Crates: boyko_diag 28, boyko_log 61, boyko_threadpool 36, boyko_utils 6. Rungs: OOS 66, R2 6, R4 15, R5 32, R6 12.

## Contents

- [Findings](#findings)
- [Kernel features](#kernel-features)
- [Primitives refuted](#primitives-refuted)
- [Plan conflicts](#plan-conflicts)
- [Rows](#rows)

## Findings

- block.rs is wired (scope.rs:41, :1050), so the brief's "wired to NOTHING" is stale. task.rs:80-82 no longer exists, because task/ is now a directory. Every install/scope entry allocates a `Box<ScopeShared>` (thread_pool.rs:278/:328); only the ScopeBlock chunk is lazy.
- crossbeam-epoch allocates a heap `Local` per thread on the first pin, through `Stealer::steal`. Its destructor entry goes into std's TLS destructor list, which is allocated with `alloc::System`, so a `#[global_allocator]` gate never sees it. Only replacing crossbeam-deque (KF-34) removes it.
- Dead storage: `injector_local` (thread_pool.rs:116 "**Nothing feeds it and nothing drains it.**"). It should be deleted, not migrated.
- PROCESS NOTE: the pool-utils-log recount ran `rustc --version` once, at about 2026-09-11T04:25+03:00, inside the protected timing window. It built nothing. Whoever owns the wall-clock measurement should decide whether that window is dirty.

## Kernel features

The features this group proposed, with the index id they merged into.

| group name | index id | rev-3 status |
|---|---|---|
| KF-memory-below-the-pool | KF-32 | active |
| KF-scope-arena-vm | KF-33 | active |
| KF-inhouse-lanes | KF-34 | active |
| KF-pool-single-reservation | KF-31 | active |
| KF-vmcolumn-ensure-len-zeroed | KF-35 | active |
| KF-generational-table-reachable-from-rhi | KF-36 | decided |
| KF-owning-scratch-column (render group's name; CONDITIONAL here) | KF-02 | active |
| KF45-engine-thread-context | KF-45 | active |

## Primitives refuted

- **boyko_mem - the memory library lifted below the kernel (verified new_primitives[0])** - *NOT a shape, KEPT as a layering kernel feature (KF-memory-below-the-pool), user list NARROWED to boyko_threadpool*
  - replaced by: kernel-internal (the pool's storage moves onto the SAME memory library the ECS uses)
  - evidence: [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:105 "Reason: `boyko_threadpool` and `boyko_utils` need reservations and cannot depend on `boyko_ecs`"; SparseMap users all in boyko_ecs (crates/boyko_ecs/src/ecs/core/archetype/archetype_registry.rs:1 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/clone/prefab.rs:457 "let mut node_of: boyko_utils::sparse_map::sparse_map::SparseMap&lt;u32> ="); log rows go to crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {"
- **ChunkPool (power-of-two chunks from one VmReservation, recycled through a free list)** - *REFUTED*
  - replaced by: scope-arena: ScopeBlock as a mark/rewind window on one per-thread VmReservation (KF-scope-arena-vm)
  - evidence: Scopes nest LIFO on their opening thread (crates/boyko_threadpool/src/scope.rs:1130 "`Scope: !Sync`, so `&Scope: !Send` — no other thread can hold this"; closures and bodies must be Send: crates/boyko_threadpool/src/thread_pool.rs:240 "F: FnOnce(&Scope&lt;'scope>) -> R + Send,"; crates/boyko_threadpool/src/scope.rs:1122 "F: FnOnce() + Send + 'scope,"), so rewind at join recycles every byte and a free list / chunk table has nothing to do; the plan already routes chunks this way ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:326 "chunks = sub-ranges of the same `ScopeArena`") and its critique fixes the arena identity ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:461 "\| C2 \| **`ScopeArena` slots keyed by `wid` collide.**").
- **ScopeSharedStack (per-thread LIFO of ScopeShared cells on a VmReservation)** - *REFUTED*
  - replaced by: scope-arena, at its zero-storage limit: a local of the install/scope frame (proposed), or the plan's arena mark (owning plan)
  - evidence: The only stated reason for the heap is crates/boyko_threadpool/src/scope.rs:494 "/// Allocated on the heap so that every spawned task's payload cell can hold a" crates/boyko_threadpool/src/scope.rs:495 "raw pointer to it that remains stable across `Scope` moves"; a separate frame local does not move with the Scope and outlives it on both exit paths. Owning plan: [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:325 "emplaced in the owner thread's `ScopeArena` at `mark`, rewound at join". Either removes the stack-of-cells primitive.
- **BootArray&lt;T, const N: usize>** - *REFUTED*
  - replaced by: kernel-internal: plain [MaybeUninit&lt;T>; MAX_WORKERS] arrays inline in the VM-backed PoolInner (a language feature, no allocator)
  - evidence: crates/boyko_threadpool/src/thread_pool.rs:52 "pub const MAX_WORKERS: usize = 64;"; crates/boyko_threadpool/src/thread_pool.rs:158 "pub(crate) worker_count: u32,"; the render group refuted InlineVec&lt;T, N> the same way (plain fixed arrays).
- **VmVec&lt;T> (growable, drop-aware, non-Copy column) and VmSparseMap&lt;U>** - *REFUTED*
  - replaced by: kernel-internal: SparseMap moved into boyko_ecs on three VmColumns (sparse as index+1, dense Copy, indices u32)
  - evidence: s2e precedent crates/boyko_ecs/src/ecs/core/component/dense/dense_store.rs:118 "s2e: VmColumn&lt;EntityId>,"; the only non-Copy U is crates/boyko_ecs/src/ecs/core/archetype/archetype_registry.rs:11 "block_groups: SparseMap&lt;Vec&lt;(ArchetypeId, ArchetypeSignature)>>,", an ecs-storage row that flattens to Copy ranges; the fallback for any surviving !Copy element is a ComponentPool with drop glue (crates/boyko_ecs/src/ecs/memory/component_pool.rs:237 "drop_fn: Option&lt;DropFn>,"). The plan's HeapVec for the same rows ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:344 "\| `boyko_utils::SparseMap`/`SparseSlotMap` (3 `Vec`s each) \| `HeapVec` fields, `new_in(&mut Heap)` \| — \|") carries a blocking Tree-Borrows finding ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:460 "\| C1 \| **Heap back-pointer aliasing is UB under Tree Borrows as designed.**").
- **VmSparseSlotMap&lt;U>** - *REFUTED*
  - replaced by: resource-column: the RHI registry as kernel generational tables (the Assets&lt;T> shape) owned by the RhiContext NonSend resource; SparseSlotMap deleted
  - evidence: crates/boyko_ecs/src/ecs/core/asset/assets.rs:200 "pub struct Assets&lt;T: AssetBacking> {"; crates/boyko_ecs/src/ecs/core/asset/assets.rs:201 "col: ComponentPool,"; crates/boyko_ecs/src/ecs/core/asset/assets.rs:202 "slot_word: VmColumn&lt;u32>,"; crates/boyko_render/src/gpu_column.rs:529 "registry: ResourceRegistry&lt;Vulkan>,"; sole user crates/boyko_rhi/src/handle.rs:76 "map: SparseSlotMap&lt;U>,"
- **InlineStr&lt;const N: usize>** - *REFUTED*
  - replaced by: kernel-internal: the existing DspBuf&lt;N> of the same crate for the 10 log rows; Option&lt;&'static str> for the pool's thread-name prefix
  - evidence: crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {"; the per-worker name String is std-forced (os-thread tag, [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:46 "\| `os-thread` \| `std::thread::spawn` per worker"); no caller passes a dynamic prefix (recount).
- **DetachedCellSlab - or deletion of the detached task kind** - *REFUTED as a primitive; destination = delete the detached kind*
  - replaced by: none (deleted); fallback kernel-internal cells in the pool's lane storage
  - evidence: crates/boyko_threadpool/src/thread_pool.rs:465 "pub fn spawn&lt;F>(&self, f: F)"; its only caller is a test: crates/boyko_physics/tests/ke16_app1_soft_colored_from_a_worker.rs:183 "pool.spawn(move \|\| {"
- **InjectorQueue (in-house MPMC; + the recount addendum on crossbeam-epoch's Local)** - *NOT REFUTED as a mechanism; reclassified - it is kernel-internal pool storage, not a new memory primitive beside ComponentPool (KF-inhouse-lanes)*
  - replaced by: kernel-internal
  - evidence: [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:328 "**in-house bounded Chase-Lev** per lane on a `Table<Task>` ring in the pool's `TableSet`". Every ECS form above kernel-internal fails for the same reason the pool rows do: the queue is the substrate that executes systems (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"), below the World (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }"). It is also the only route that removes crossbeam-epoch's per-thread Local and the System-allocated std TLS destructor entry (row worker.rs:347).

## Plan conflicts

- Exemptions revoked. docs/ARCH-AUDIT-ECS-DATA-REMEDIATION.md:9 "boyko_utils (SparseMap/BitSet — the array-by-id primitives the principle mandates); boyko_threadpool (Chase-Lev deques + cold-only Mutexes)" files SparseMap and the pool's Chase-Lev deques as LEGIT exceptions to Principle 0; the owner's 2026-09-10 order ('move ALL runtime data structures onto our system') and [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:53 "Principle 0's "lock-free threadpool internals" exception covers the *shape* (ring buffers, atomics), not the allocator they come from" take them back. This ledger follows the order: K rows are not exempt, and the pool rows are in scope.
- Same for docs/MEMORY-SYSTEM-AUDIT.md:95 "\| SparseMap&lt;U> (primitive: sparse layer) \|" and docs/MEMORY-SYSTEM-AUDIT.md:97 "external handles (GPU buffers/pipelines/shaders/fences — realistically 10^2-10^3, never entities)": both rows are graded 'OK-by-design' there; under the order they are migrated (SparseMap -> kernel-internal VmColumns, SparseSlotMap -> kernel generational table).
- SparseMap / SparseSlotMap destination. [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:344 "\| `boyko_utils::SparseMap`/`SparseSlotMap` (3 `Vec`s each) \| `HeapVec` fields, `new_in(&mut Heap)` \| — \|" routes them to HeapVec, a new heap primitive beside ComponentPool, whose design carries a blocking finding ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:460 "\| C1 \| **Heap back-pointer aliasing is UB under Tree Borrows as designed.**"). This ledger routes SparseMap to kernel-internal VmColumns inside boyko_ecs (no new primitive, no layering edge) and SparseSlotMap to the kernel's existing generational-table shape (Assets&lt;T>). Both recorded; the owner's rule against a new primitive beside ComponentPool decides for this ledger unless rev 2 refutes it with evidence.
- ScopeShared placement (rung 1a). [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:325 "emplaced in the owner thread's `ScopeArena` at `mark`, rewound at join" vs this ledger's frame-local proposal. Both remove the per-entry Box; the frame local needs no arena identity (so C2 does not arise for it) and keeps a deallocation Miri judges, while the arena mark is what the plan owns. Recorded, not overridden: rows thread_pool.rs:278/:328 and scope.rs:1096 carry the plan as plan_ref.
- Arena identity, inside the plan itself: [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:113 "indexed by `wid` from the existing single TLS read" (Part I) vs [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:461 "\| C2 \| **`ScopeArena` slots keyed by `wid` collide.**" (Part II, CONFIRMED, FIX IN REV 2). This ledger adopts the critique's fix ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:461 "the arena must be owned by a THREAD identity: either a TLS-owned per-thread arena (the `LANE_DEPOSIT` single-read shape) with its lifetime relative to the pool stated, or a claim/release pool of dispatcher arenas").
- Pool tables. [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them" introduces a TableSet primitive for them; this ledger finds plain inline MAX_WORKERS arrays sufficient (crates/boyko_threadpool/src/thread_pool.rs:52 "pub const MAX_WORKERS: usize = 64;") and records BootArray / TableSet-for-the-pool as unnecessary.
- RHI handles. docs/RENDER-PHYSICS-GPU-PLAN.md:124 "over `boyko_utils::SparseSlotMap`/`Slot` (ABA-safe, `Copy`, zero-heap)" calls the handle registry zero-heap; it is three SparseSlotMap Vecs per kind plus crates/boyko_rhi/src/handle.rs:78 "free: Vec&lt;usize>,". (The rhi group flags the same.)
- RHI layering. crates/boyko_rhi/src/lib.rs:28 "The `boyko_ecs` dependency and core's" / crates/boyko_rhi/src/lib.rs:29 "`DeviceColumnHandle(u64)` newtype land in Phase 4; for now the registry uses" planned a boyko_rhi -> boyko_ecs edge; docs/ARCHITECTURE.md:174 "itself does NOT depend on `boyko_ecs` (so no cycle)." states the opposite as architecture. KF-generational-table-reachable-from-rhi needs one of them to give way (or the table core moved to the bottom memory crate) - an owner/architecture call.
- Log strings. [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" routes boyko_log Strings to HeapString (and [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`InlineStr<N>` for titles"); this ledger routes the 10 rotation rows to the existing crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {" in the same crate - no new type.
- Closed exception list has gaps. [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed", but no tag covers: std::fs's Windows wide-path Vec&lt;u16> (12 T rows in sink/binary.rs and sink/file.rs), std::env::var's String (control.rs:163), the panic-hook Box&lt;dyn Fn> (lifecycle.rs:585, sink/crash.rs:160; the panic tag [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:47 "\| `panic` \| panic payload `Box<dyn Any + Send>` \| failure path \|" is for payloads), and crossbeam-epoch's System-allocated TLS destructor entry (worker.rs:347, removed only by rung 1d). The offline tag [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" also does not name logdec or the telemetry decoder (tools/prof_decode).
- Stale status. docs/threadpool/KE16-TASKREP-ALLOCATION.md:17 "\| 3b — the block wired to the scoped spawn path \| **IN THE WORKING TREE, UNCOMMITTED**" - the tree has 3b wired (crates/boyko_threadpool/src/scope.rs:1050 "block: ScopeBlock,"), so the register's status row and the brief's 'block.rs wired to NOTHING' are both stale; docs/threadpool/KE16-TASKREP-ALLOCATION.md:72 "**After 3b**, one allocation per chunk plus one free per chunk at the join." is the current truth.
- Already self-corrected, recorded for completeness: the plan's Part I says crossbeam's Injector allocates a 31-slot block; its own critique corrects it ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:496 "correct to 63 slots / one block per 64 pushes"), matching the recount's BLOCK_CAP = 63.

## Rows

Every row, no elision, sorted by file and line. Columns:

- **file:line + evidence:** the evidence is the verbatim source line in `D:/wt/joltab`.
- **owner**, **container&lt;elem&gt;**, **class**, **ecs_form**, **owning_entity**
- **memory destination:** the recount's. A `NEW:` name here was refuted in the index (Primitives refuted) unless it is KEPT there.
- **plan_ref**
- **note:** `[kind; growth; addr_cached; rung]`, then `ECS:` with the form rationale (one sentence per skipped earlier form, then any `[rev2 ...]` blocks), then `Class:` with the inventory/recount note, then `Readers:`.

| file:line + evidence | owner | container&lt;elem&gt; | class | ecs_form | owning_entity | memory destination | plan_ref | note |
|---|---|---|---|---|---|---|---|---|
| crates/boyko_diag/src/lane.rs:139 `static LANE: Cell<u16> = const { Cell::new(LANE_UNCLAIMED) };` | lane::LANE (thread_local!) | `other:thread_local! os-key Value<T> (std AlignedSystemBox, System.alloc)<Cell<u16> (this thread's diag lane)>` | T | kernel-internal | none | NEW:engine-owned per-thread context slot (KF-45 Engine thread-context column (replaces every thread_local!)) - removing the thread_local! is the only way to remove this allocation: std calls System.alloc directly, so neither a #[global_allocator] nor a per-type replacement reaches it | main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built." [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read.; joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call" | [static; growth once; addr_cached yes; rung R2] ECS: CHOSEN kernel-internal under ONE decision for every thread_local! in the engine (TLS-1): the value becomes a field of the engine thread-context record (KF-45 Engine thread-context column (replaces every thread_local!)), reached WITHOUT thread_local! - by an explicit context parameter wherever the call chain already carries one (worker loop -> task trampoline -> system run; the physics decisions already chose this for the gang nesting depth: main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built."), and otherwise through the thread-context slot. ROUTE FOR THIS STATIC: the lane IS the thread-context slot index: pool workers get wid+1, the apply/main thread 0, and a foreign thread claims one of the existing spare lanes at registration (joltab:crates/boyko_diag/src/lane.rs:134 "static SPARE_OWNER: [AtomicU32; SPARE_COUNT] = [const { AtomicU32::new(SPARE_FREE) }; SPARE_COUNT];"); lane() becomes a read of the passed context or of the slot. WHY THIS IS ALSO THE FASTER PATH: a passed context is a register; a slot read is one plain load of the platform thread-control-block pointer (TEB on Windows x64, TCB on Linux x86_64) plus a probe of a 64-entry table in one or two L1 lines - no locked RMW, no fence, no Win32 call, no allocation (reasoned, not measured) - against the measured windows-gnu cost of two contended RMWs + FlsSetValue per thread_local! read and one System allocation per thread per key. SKIPPED: component / dense-component / enable-state / relation - a per-THREAD value, and threads are not entities (the pool and the diag/log crates sit below every World); event - a state word, not a message; resource-column - per thread, not a world singleton, and boyko_threadpool/boyko_diag/boyko_log sit below boyko_ecs; system-scratch - outlives any one system run; scope-arena - outlives any one scope. OVERTURNED BY: an A/B of the ke16_nested_scope cell worker/body_1us_tasks_64W on x86_64-pc-windows-msvc (native TLS) in which the explicit-context build is slower beyond its band - msvc/Linux is the only place the old form could win. [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read. Class: [rev2 GAP 1] One System-allocated Value&lt;T> per THREAD that touches the key, freed at thread exit, on the owner's windows-gnu toolchain. std itself says rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/guard/windows.rs:116 "// `#[thread_local]` is unavailable on windows-gnu (`target_thread_local` is off),", so thread_local! resolves to the os-key backend (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/mod.rs:44 "mod os;"), whose try_initialize boxes the value (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:194 "let value = AlignedSystemBox::&lt;T, ALIGN>::new(Value {") through rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:110 "let ptr: *mut Value&lt;T> = (unsafe { System.alloc(layout) }).cast();" - System directly, bypassing any #[global_allocator]. A const initialiser does not avoid it (joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:91 "backend, whose key always has a dtor — `const {}` initialisation does **not** help — and (ii) `enable`'s"). Every read also pays guard::enable: joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call"; the file sizes it at joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:195 "978.7 − 110 = 868.7 µs for TWO reads, i.e. ≈ 434 µs per read per wave (1,024 tasks ⇒ ≈ 424 ns per". On native-TLS targets (x86_64-pc-windows-msvc, Linux) the same const-initialised cell allocates nothing and reads lock-free. CENSUS NOTE: a #[global_allocator] counting gate cannot see this cell - std calls System.alloc directly - so the allocation census must count thread_local! cells from this row (a static census of thread_local! statics), never from its counter; the counter reads 0 for them by construction. Readers: lane() / set_lane() (lane.rs:147, :160): the per-thread index both diagnostics subsystems (log ring, telemetry) key their per-thread state by |
| crates/boyko_diag/src/storage.rs:462 `fn run_tool(tool: &Path, args: &[&str]) -> Result<String, ProbeFailure> {` | gate::run_tool | `String<u8 (tool stdout)>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [return; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:463 `let Ok(image) = env::current_exe() else {` | gate::run_tool | `other:PathBuf<u16/u8 path>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern, not the brief's list. Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:466 `let Ok(out) = Command::new(tool).args(args).arg(&image).output() else {` | gate::run_tool | `other:std::process::Command + Output{stdout: Vec<u8>}<u8>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern, not the brief's list. Command::output collects stdout into a Vec&lt;u8> inside std. Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:472 `String::from_utf8(out.stdout).map_err(\|_\| ProbeFailure::ToolOutputNotUtf8)` | gate::run_tool | `String<u8>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:489 `fn resolve_tool(stem: &str) -> Option<PathBuf> {` | gate::resolve_tool | `other:PathBuf<path>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [return; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern, not the brief's list. Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:490 `let mut exe = String::from(stem);` | gate::resolve_tool | `String<u8>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:495 `if let Some(path) = env::var_os("PATH") {` | gate::resolve_tool | `other:OsString / ReadDir (std)<u16/u8>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern (env::var_os returns an owned OsString). Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:497 `let cand = dir.join(&exe);` | gate::resolve_tool | `other:PathBuf<path>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern, not the brief's list. Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:504 `let rustup_home = env::var_os("RUSTUP_HOME")` | gate::resolve_tool | `other:OsString / ReadDir (std)<u16/u8>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern (env::var_os returns an owned OsString). Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:505 `.map(PathBuf::from)` | gate::resolve_tool | `other:PathBuf<path>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern, not the brief's list. Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:507 `env::var_os("USERPROFILE")` | gate::resolve_tool | `other:OsString / ReadDir (std)<u16/u8>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern (env::var_os returns an owned OsString). Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:508 `.or_else(\|\| env::var_os("HOME"))` | gate::resolve_tool | `other:OsString / ReadDir (std)<u16/u8>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern (env::var_os returns an owned OsString). Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:509 `.map(\|h\| PathBuf::from(h).join(".rustup"))` | gate::resolve_tool | `other:PathBuf<path>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern, not the brief's list. Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:512 `let toolchains = rustup_home.join("toolchains");` | gate::resolve_tool | `other:PathBuf<path>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern, not the brief's list. Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:513 `let named: Vec<PathBuf> = match env::var_os("RUSTUP_TOOLCHAIN") {` | gate::resolve_tool | `Vec<PathBuf>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:515 `None => std::fs::read_dir(&toolchains)` | gate::resolve_tool | `other:OsString / ReadDir (std)<u16/u8>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern (env::var_os returns an owned OsString). Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:525 `let mut fallback: Option<PathBuf> = None;` | gate::resolve_tool | `other:PathBuf<path>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern, not the brief's list. Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:527 `let Ok(targets) = std::fs::read_dir(tc.join("lib").join("rustlib")) else {` | gate::resolve_tool | `other:PathBuf<path>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern, not the brief's list. Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:531 `let cand = target.path().join("bin").join(&exe);` | gate::resolve_tool | `other:PathBuf<path>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern, not the brief's list. Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/storage.rs:536 `let name = name.to_string_lossy();` | gate::resolve_tool | `Cow<str>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth once; addr_cached no; rung OOS] ECS: One line: inside crates/boyko_diag/src/storage.rs:278 "#[cfg(feature = "section-gate")]" (crates/boyko_diag/Cargo.toml:25 "section-gate = []"), a facility crates/boyko_diag/src/storage.rs:271 "switched on by each consumer's dev-dependency"; no Cargo.toml in the tree enables the feature, so no shipped binary contains it. Class: C: behind feature "section-gate" (storage.rs:278 "#[cfg(feature = \"section-gate\")]"); boyko_diag/Cargo.toml declares "default = []" and rg "section-gate" over every Cargo.toml finds only that declaration - no crate enables it, so no shipped binary compiles this code. Found by the extended allocation pattern, not the brief's list. Cow allocates only for non-UTF-8 names. Readers: section_report / symbol_report (test gates only) |
| crates/boyko_diag/src/telemetry.rs:567 `pub zone_rows: Vec<ZoneRow<'a>>,` | telemetry::Block::zone_rows | `Vec<ZoneRow<'a>>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; the telemetry decoder is not named in it) | [field; growth once; addr_cached no; rung OOS] ECS: One line: the offline decoder; its only non-test caller is the separate tool tools/prof_decode/src/main.rs:78 "let decoded = match telemetry::decode(&bytes) {" (plus boyko_app tests), so it never runs in a shipped engine binary. Class: C: telemetry::decode is offline decoder code - its doc says "one call per file, in a CLI or a gate, never in a frame" (telemetry.rs:706-707); rg "telemetry::decode" finds callers only in tools/prof_decode/src/main.rs:78 and crates/boyko_app/tests/profiling_telemetry_stream.rs; the shipped writer (boyko_app profiling/stream.rs:62) imports Encoder, not decode Readers: tools/prof_decode, boyko_app tests |
| crates/boyko_diag/src/telemetry.rs:569 `pub recs: Vec<WindowRec>,` | telemetry::Block::recs | `Vec<WindowRec>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; the telemetry decoder is not named in it) | [field; growth once; addr_cached no; rung OOS] ECS: One line: the offline decoder; its only non-test caller is the separate tool tools/prof_decode/src/main.rs:78 "let decoded = match telemetry::decode(&bytes) {" (plus boyko_app tests), so it never runs in a shipped engine binary. Class: C: telemetry::decode is offline decoder code - its doc says "one call per file, in a CLI or a gate, never in a frame" (telemetry.rs:706-707); rg "telemetry::decode" finds callers only in tools/prof_decode/src/main.rs:78 and crates/boyko_app/tests/profiling_telemetry_stream.rs; the shipped writer (boyko_app profiling/stream.rs:62) imports Encoder, not decode Readers: tools/prof_decode, boyko_app tests |
| crates/boyko_diag/src/telemetry.rs:578 `pub blocks: Vec<Block<'a>>,` | telemetry::Decoded::blocks | `Vec<Block<'a>>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; the telemetry decoder is not named in it) | [field; growth once; addr_cached no; rung OOS] ECS: One line: the offline decoder; its only non-test caller is the separate tool tools/prof_decode/src/main.rs:78 "let decoded = match telemetry::decode(&bytes) {" (plus boyko_app tests), so it never runs in a shipped engine binary. Class: C: telemetry::decode is offline decoder code - its doc says "one call per file, in a CLI or a gate, never in a frame" (telemetry.rs:706-707); rg "telemetry::decode" finds callers only in tools/prof_decode/src/main.rs:78 and crates/boyko_app/tests/profiling_telemetry_stream.rs; the shipped writer (boyko_app profiling/stream.rs:62) imports Encoder, not decode Readers: tools/prof_decode, boyko_app tests |
| crates/boyko_diag/src/telemetry.rs:647 `fn decode_payload(p: &[u8]) -> Option<(WindowHead, Vec<ZoneRow<'_>>, Vec<WindowRec>)> {` | telemetry::decode_payload | `Vec<ZoneRow<'_> and WindowRec (two Vecs in one tuple)>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; the telemetry decoder is not named in it) | [return; growth once; addr_cached no; rung OOS] ECS: One line: the offline decoder; its only non-test caller is the separate tool tools/prof_decode/src/main.rs:78 "let decoded = match telemetry::decode(&bytes) {" (plus boyko_app tests), so it never runs in a shipped engine binary. Class: C: telemetry::decode is offline decoder code - its doc says "one call per file, in a CLI or a gate, never in a frame" (telemetry.rs:706-707); rg "telemetry::decode" finds callers only in tools/prof_decode/src/main.rs:78 and crates/boyko_app/tests/profiling_telemetry_stream.rs; the shipped writer (boyko_app profiling/stream.rs:62) imports Encoder, not decode Readers: tools/prof_decode, boyko_app tests |
| crates/boyko_diag/src/telemetry.rs:662 `let mut rows = Vec::with_capacity(head.zone_rows as usize);` | telemetry::decode_payload | `Vec<ZoneRow<'_>>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; the telemetry decoder is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: the offline decoder; its only non-test caller is the separate tool tools/prof_decode/src/main.rs:78 "let decoded = match telemetry::decode(&bytes) {" (plus boyko_app tests), so it never runs in a shipped engine binary. Class: C: telemetry::decode is offline decoder code - its doc says "one call per file, in a CLI or a gate, never in a frame" (telemetry.rs:706-707); rg "telemetry::decode" finds callers only in tools/prof_decode/src/main.rs:78 and crates/boyko_app/tests/profiling_telemetry_stream.rs; the shipped writer (boyko_app profiling/stream.rs:62) imports Encoder, not decode Readers: tools/prof_decode, boyko_app tests |
| crates/boyko_diag/src/telemetry.rs:678 `let mut recs = Vec::with_capacity(head.recs as usize);` | telemetry::decode_payload | `Vec<WindowRec>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; the telemetry decoder is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: the offline decoder; its only non-test caller is the separate tool tools/prof_decode/src/main.rs:78 "let decoded = match telemetry::decode(&bytes) {" (plus boyko_app tests), so it never runs in a shipped engine binary. Class: C: telemetry::decode is offline decoder code - its doc says "one call per file, in a CLI or a gate, never in a frame" (telemetry.rs:706-707); rg "telemetry::decode" finds callers only in tools/prof_decode/src/main.rs:78 and crates/boyko_app/tests/profiling_telemetry_stream.rs; the shipped writer (boyko_app profiling/stream.rs:62) imports Encoder, not decode Readers: tools/prof_decode, boyko_app tests |
| crates/boyko_diag/src/telemetry.rs:711 `let mut blocks = Vec::new();` | telemetry::decode | `Vec<Block<'_>>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; the telemetry decoder is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: the offline decoder; its only non-test caller is the separate tool tools/prof_decode/src/main.rs:78 "let decoded = match telemetry::decode(&bytes) {" (plus boyko_app tests), so it never runs in a shipped engine binary. Class: C: telemetry::decode is offline decoder code - its doc says "one call per file, in a CLI or a gate, never in a frame" (telemetry.rs:706-707); rg "telemetry::decode" finds callers only in tools/prof_decode/src/main.rs:78 and crates/boyko_app/tests/profiling_telemetry_stream.rs; the shipped writer (boyko_app profiling/stream.rs:62) imports Encoder, not decode Readers: tools/prof_decode, boyko_app tests |
| crates/boyko_log/src/bin/logdec.rs:51 `let args: Vec<String> = std::env::args().skip(1).collect();` | logdec::main | `Vec<String (argv)>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:65 `let files: Vec<&String> = args.iter().filter(\|a\| !a.starts_with("--")).collect();` | logdec::main | `Vec<&String>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:73 `let mut loaded = Vec::with_capacity(files.len());` | logdec::main | `Vec<(String, Vec<u8>)>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:75 `match std::fs::read(path.as_str()) {` | logdec::main | `Vec<u8 (whole file)>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Found by the extended pattern. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:76 `Ok(b) => loaded.push(((*path).clone(), b)),` | logdec::main | `String<u8>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Found by the extended pattern. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:90 `match std::fs::read(path.as_str()) {` | logdec::main | `Vec<u8 (whole file)>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Found by the extended pattern. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:106 `text: String,` | logdec::Merged::text | `String<u8>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [field; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:122 `let mut out = std::io::BufWriter::new(out.lock());` | logdec::merge_all | `other:BufWriter (8 KiB Vec<u8> buffer)<u8>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Found by the extended pattern. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:125 `let mut mismatched = Vec::new();` | logdec::merge_all | `Vec<String (mismatched paths)>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:126 `let mut rows: Vec<Merged<'_>> = Vec::new();` | logdec::merge_all | `Vec<Merged<'_>>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:130 `let mut dict: Vec<Option<Site<'_>>> = Vec::new();` | logdec::merge_all | `Vec<Option<Site<'_>>>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:137 `Some(s) if s != (lo, hi) => mismatched.push(path.clone()),` | logdec::merge_all | `String<u8>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Found by the extended pattern. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:160 `text: line_buf.as_str().to_owned(),` | logdec::merge_all | `String<u8 (rendered record)>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:181 `mismatched.join(", ")` | logdec::merge_all | `String<u8>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Found by the extended pattern. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:194 `String::from_utf8_lossy(&hex),` | logdec::merge_all | `Cow<str>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Cow: session_hex is ASCII hex, so from_utf8_lossy borrows and does not allocate. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:207 `let mut out = std::io::BufWriter::new(out.lock());` | logdec::decode_one | `other:BufWriter (8 KiB Vec<u8> buffer)<u8>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Found by the extended pattern. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:212 `let mut dict: Vec<Option<Site<'_>>> = Vec::new();` | logdec::decode_one | `Vec<Option<Site<'_>>>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:234 `let _ = writeln!(out, "-- session {}", String::from_utf8_lossy(&hex));` | logdec::decode_one | `Cow<str>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Cow: session_hex is ASCII hex, so from_utf8_lossy borrows and does not allocate. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:308 `fn stamp(delta: u32, ticks_per_ns: f64) -> String {` | logdec::stamp | `String<u8>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [return; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:310 `return format!("t={delta}");` | logdec::stamp | `String<u8>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/bin/logdec.rs:312 `format!("+{:.3}ms", f64::from(delta) / ticks_per_ns / 1.0e6)` | logdec::stamp | `String<u8>` | C | out-of-scope:compile-time | none | out-of-scope:compile-time/tooling only (offline decoder binary) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \|" (closest tag; logdec is not named in it) | [local; growth once; addr_cached no; rung OOS] ECS: One line: a separate offline binary target (crates/boyko_log/src/bin/logdec.rs:1 "//! `logdec` — read a `.blog` back into text."), never linked into the engine. Class: C: src/bin/logdec.rs is the separate `logdec` binary target (header: "`logdec` - read a `.blog` back into text."); a bin target is never linked into a crate that depends on boyko_log, so none of this runs in the engine. Readers: the logdec CLI only |
| crates/boyko_log/src/control.rs:163 `ControlSource::Env => match std::env::var("BOYKO_LOG") {` | control::load_control | `String<u8 (control spec text)>` | B | out-of-scope:os-owned | none | out-of-scope:std::env::var returns an owned String allocated inside std; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own) | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth once; addr_cached no; rung OOS] ECS: One line: std::env::var allocates the String inside std, boot-only (crates/boyko_log/src/control.rs:150 "**Boot-time, and deliberately allowed to touch the filesystem**"); removable only by reading the variable through the OS API into our own bounded buffer. Class: Boot-only (control.rs:150 "**Boot-time, and deliberately allowed to touch the filesystem**"). Found by the extended pattern. Readers: host boot, once before enable() (control.rs:150-151) |
| crates/boyko_log/src/control.rs:167 `ControlSource::File(path) => match std::fs::read_to_string(path) {` | control::load_control | `String<u8 (control file text)>` | B | kernel-internal | none | VmReservation-raw | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" | [local; growth once; addr_cached no; rung R6] ECS: Logger boot configuration (the logger is the kernel's diagnostics substrate below the World; no entity, no system); one line: the file is read by std::fs::read_to_string into a String std allocates; reading through File::read into a bounded stack buffer (DspBuf-shaped) makes it ours. Class B, lowest priority. Class: std::fs::read_to_string allocates inside std; reading through File::read into a caller-owned VmReservation-backed buffer would make it ours. Boot-only. Found by the extended pattern. Readers: host boot, once before enable() |
| crates/boyko_log/src/control.rs:174 `let mut spec = String::with_capacity(text.len());` | control::load_control | `String<u8>` | B | kernel-internal | none | VmReservation-raw | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" | [local; growth once; addr_cached no; rung R6] ECS: Logger boot configuration (the logger is the kernel's diagnostics substrate below the World; no entity, no system); one line: the join buffer (crates/boyko_log/src/control.rs:183 "spec.push_str(line);" then crates/boyko_log/src/control.rs:185 "apply_control_spec(&spec)") disappears if each clause line is applied as it is read. Class B, lowest priority. Class: Join buffer for the comma-separated spec; applying the spec line by line would remove it outright. Boot-only. Readers: apply_control_spec (control.rs:185) |
| crates/boyko_log/src/drain_owner.rs:42 `static TOKEN_ANCHOR: u8 = const { 0 };` | drain_owner::TOKEN_ANCHOR (thread_local!) | `other:thread_local! os-key Value<T> (std AlignedSystemBox, System.alloc)<u8 (used only for its address)>` | T | kernel-internal | none | NEW:engine-owned per-thread context slot (KF-45 Engine thread-context column (replaces every thread_local!)) - removing the thread_local! is the only way to remove this allocation: std calls System.alloc directly, so neither a #[global_allocator] nor a per-type replacement reaches it | main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built." [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read.; joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call" | [static; growth once; addr_cached yes; rung R2] ECS: CHOSEN kernel-internal under ONE decision for every thread_local! in the engine (TLS-1): the value becomes a field of the engine thread-context record (KF-45 Engine thread-context column (replaces every thread_local!)), reached WITHOUT thread_local! - by an explicit context parameter wherever the call chain already carries one (worker loop -> task trampoline -> system run; the physics decisions already chose this for the gang nesting depth: main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built."), and otherwise through the thread-context slot. ROUTE FOR THIS STATIC: the opaque per-thread token becomes thread-context slot index + 1, unique among live threads and never zero by construction - the exact property the address-of-a-TLS-byte trick buys today; both boyko_log copies (this and sync_out.rs:75) collapse to the one slot. WHY THIS IS ALSO THE FASTER PATH: a passed context is a register; a slot read is one plain load of the platform thread-control-block pointer (TEB on Windows x64, TCB on Linux x86_64) plus a probe of a 64-entry table in one or two L1 lines - no locked RMW, no fence, no Win32 call, no allocation (reasoned, not measured) - against the measured windows-gnu cost of two contended RMWs + FlsSetValue per thread_local! read and one System allocation per thread per key. SKIPPED: component / dense-component / enable-state / relation - a per-THREAD value, and threads are not entities (the pool and the diag/log crates sit below every World); event - a state word, not a message; resource-column - per thread, not a world singleton, and boyko_threadpool/boyko_diag/boyko_log sit below boyko_ecs; system-scratch - outlives any one system run; scope-arena - outlives any one scope. OVERTURNED BY: an A/B of the ke16_nested_scope cell worker/body_1us_tasks_64W on x86_64-pc-windows-msvc (native TLS) in which the explicit-context build is slower beyond its band - msvc/Linux is the only place the old form could win. [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read. Class: [rev2 GAP 1] One System-allocated Value&lt;T> per THREAD that touches the key, freed at thread exit, on the owner's windows-gnu toolchain. std itself says rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/guard/windows.rs:116 "// `#[thread_local]` is unavailable on windows-gnu (`target_thread_local` is off),", so thread_local! resolves to the os-key backend (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/mod.rs:44 "mod os;"), whose try_initialize boxes the value (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:194 "let value = AlignedSystemBox::&lt;T, ALIGN>::new(Value {") through rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:110 "let ptr: *mut Value&lt;T> = (unsafe { System.alloc(layout) }).cast();" - System directly, bypassing any #[global_allocator]. A const initialiser does not avoid it (joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:91 "backend, whose key always has a dtor — `const {}` initialisation does **not** help — and (ii) `enable`'s"). Every read also pays guard::enable: joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call"; the file sizes it at joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:195 "978.7 − 110 = 868.7 µs for TWO reads, i.e. ≈ 434 µs per read per wave (1,024 tasks ⇒ ≈ 424 ns per". On native-TLS targets (x86_64-pc-windows-msvc, Linux) the same const-initialised cell allocates nothing and reads lock-free. CENSUS NOTE: a #[global_allocator] counting gate cannot see this cell - std calls System.alloc directly - so the allocation census must count thread_local! cells from this row (a static census of thread_local! statics), never from its counter; the counter reads 0 for them by construction. Readers: my_token (drain_owner.rs:46) -> the DRAIN_OWNER claim/release CAS |
| crates/boyko_log/src/lifecycle.rs:563 `.name("boyko-log-sink".into())` | lifecycle::enable | `String<u8>` | B | out-of-scope:os-owned | none | out-of-scope:std::thread::Builder::name takes an owned String; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:46 "\| `os-thread` \| `std::thread::spawn` per worker" | [local; growth once; addr_cached no; rung OOS] ECS: One line: the sink thread's std spawn internals and the owned name String std::thread::Builder requires; os-thread tag, once per enable(). Class: Not matched by either census pattern (it is a bare .into()); found by a growth-call sweep. Once per enable(). Readers: std::thread::Builder::name |
| crates/boyko_log/src/lifecycle.rs:564 `.spawn(sink_loop)` | lifecycle::enable | `other:std::thread internals<Arc<Packet>, Thread Arc<Inner>, boxed main>` | T | out-of-scope:os-owned | none | out-of-scope:std::thread internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own); the stack is X-os | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:46 "\| `os-thread` \| `std::thread::spawn` per worker" | [local; growth once; addr_cached no; rung OOS] ECS: One line: the sink thread's std spawn internals and the owned name String std::thread::Builder requires; os-thread tag, once per enable(). Class: One sink thread per enable() when SinkMode::Thread. Readers: the boyko-log-sink thread |
| crates/boyko_log/src/lifecycle.rs:585 `static PREV_HOOK: std::sync::OnceLock<PanicHook> = std::sync::OnceLock::new();` | lifecycle::PREV_HOOK | `Box<dyn><dyn Fn(&PanicHookInfo<'_>) + Sync + Send (the previous hook)>` | B | out-of-scope:os-owned | none | out-of-scope:std panic-hook API is Box&lt;dyn Fn>-typed (take_hook/set_hook); a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own) | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers a panic-hook Box (the panic tag is for payloads; see plan_conflicts) | [static; growth once; addr_cached no; rung OOS] ECS: One line: std's panic-hook API is Box&lt;dyn Fn>-typed (take_hook / set_hook), installed once at enable; only a #[global_allocator] reaches it. Class: Holds the Box returned by lifecycle.rs:621 "let prev = std::panic::take_hook();" - allocated by whoever installed it. Type alias PanicHook = Box&lt;dyn Fn..> is declared at :588 (alias; its use is this row). Readers: our panic hook (lifecycle.rs:632 "if let Some(prev) = PREV_HOOK.get() {") |
| crates/boyko_log/src/lifecycle.rs:625 `std::panic::set_hook(Box::new(\|info\| {` | lifecycle::install_panic_hook | `Box<dyn><closure (captures nothing -> zero-sized)>` | B | out-of-scope:os-owned | none | out-of-scope:std panic-hook API requires Box&lt;dyn Fn>; this Box performs no heap allocation | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers a panic-hook Box (the panic tag is for payloads; see plan_conflicts) | [local; growth once; addr_cached no; rung OOS] ECS: One line: std's panic-hook API is Box&lt;dyn Fn>-typed (take_hook / set_hook), installed once at enable; only a #[global_allocator] reaches it. The closure captures nothing, so this Box::new is of a ZST and performs no allocation. Class: The closure body reads only statics (HOOK_FIRED, state(), flush(), PREV_HOOK) and its parameter, so it is a ZST and Box::new of a ZST does not call the allocator. A row because it is a Box site; migration cost zero. Readers: std panic machinery on every panic |
| crates/boyko_log/src/probe.rs:79 `static WATCH: Cell<Option<(u8, u16)>> = const { Cell::new(None) };` | probe::WATCH (thread_local!) | `other:thread_local! os-key Value<T> (std AlignedSystemBox, System.alloc)<Cell<Option<(u8, u16)>>>` | C | out-of-scope:test-only | none | out-of-scope: test-probe feature, enabled only from [dev-dependencies] (the sibling LAST/FIRST rows probe.rs:87/:90 carry the same evidence) |  | [static; growth once; addr_cached yes; rung OOS] ECS: One line: compiled only under joltab:crates/boyko_log/src/lib.rs:98 "#[cfg(feature = "test-probe")]", a feature enabled solely from dev-dependencies (as rowed for probe.rs:87/:90). Class: [rev2 GAP 1, completeness] Rowed so that the thread_local! census is whole (31 statics, 31 rows). Same os-key System allocation on windows-gnu as every thread_local!, but never in a shipped build. Readers: probe::watch / watched (tests of the emitting crates) |
| crates/boyko_log/src/probe.rs:81 `static COUNT: Cell<u64> = const { Cell::new(0) };` | probe::COUNT (thread_local!) | `other:thread_local! os-key Value<T> (std AlignedSystemBox, System.alloc)<Cell<u64>>` | C | out-of-scope:test-only | none | out-of-scope: test-probe feature, enabled only from [dev-dependencies] (the sibling LAST/FIRST rows probe.rs:87/:90 carry the same evidence) |  | [static; growth once; addr_cached yes; rung OOS] ECS: One line: compiled only under joltab:crates/boyko_log/src/lib.rs:98 "#[cfg(feature = "test-probe")]", a feature enabled solely from dev-dependencies (as rowed for probe.rs:87/:90). Class: [rev2 GAP 1, completeness] Rowed so that the thread_local! census is whole (31 statics, 31 rows). Same os-key System allocation on windows-gnu as every thread_local!, but never in a shipped build. Readers: probe::watch / watched (tests of the emitting crates) |
| crates/boyko_log/src/probe.rs:87 `static LAST: Cell<String> = const { Cell::new(String::new()) };` | probe::LAST | `String<u8 (last rendered message, per thread)>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only (test-probe feature) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [static; growth perframe; addr_cached no; rung OOS] ECS: One line: compiled only under crates/boyko_log/src/lib.rs:98 "#[cfg(feature = "test-probe")]", a feature enabled solely from dev-dependencies (e.g. crates/boyko_app/Cargo.toml:65 "boyko-log = { path = "../boyko_log", features = ["test-probe"] }"). Class: C: module probe is behind feature "test-probe" (lib.rs:98 "#[cfg(feature = \"test-probe\")]"), and every crate that enables it does so under [dev-dependencies] (boyko_app/Cargo.toml:65, boyko_render:82, boyko_physics:71, boyko_rhi:21, boyko_rhi_vulkan:104, boyko_serialize:25, boyko_image:25 - section checked per file); the root package is edition 2024 (resolver 3), so dev-dependency features do not reach a shipping build Readers: probe::last_message (tests) |
| crates/boyko_log/src/probe.rs:90 `static FIRST: Cell<String> = const { Cell::new(String::new()) };` | probe::FIRST | `String<u8 (first rendered message, per thread)>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only (test-probe feature) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [static; growth perframe; addr_cached no; rung OOS] ECS: One line: compiled only under crates/boyko_log/src/lib.rs:98 "#[cfg(feature = "test-probe")]", a feature enabled solely from dev-dependencies (e.g. crates/boyko_app/Cargo.toml:65 "boyko-log = { path = "../boyko_log", features = ["test-probe"] }"). Class: C: module probe is behind feature "test-probe" (lib.rs:98 "#[cfg(feature = \"test-probe\")]"), and every crate that enables it does so under [dev-dependencies] (boyko_app/Cargo.toml:65, boyko_render:82, boyko_physics:71, boyko_rhi:21, boyko_rhi_vulkan:104, boyko_serialize:25, boyko_image:25 - section checked per file); the root package is edition 2024 (resolver 3), so dev-dependency features do not reach a shipping build Readers: probe::first_message (tests) |
| crates/boyko_log/src/probe.rs:133 `let mut line = String::new();` | probe::note_emission | `String<u8 (rendered line)>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only (test-probe feature) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth perframe; addr_cached no; rung OOS] ECS: One line: compiled only under crates/boyko_log/src/lib.rs:98 "#[cfg(feature = "test-probe")]", a feature enabled solely from dev-dependencies (e.g. crates/boyko_app/Cargo.toml:65 "boyko-log = { path = "../boyko_log", features = ["test-probe"] }"). Class: C: module probe is behind feature "test-probe" (lib.rs:98 "#[cfg(feature = \"test-probe\")]"), and every crate that enables it does so under [dev-dependencies] (boyko_app/Cargo.toml:65, boyko_render:82, boyko_physics:71, boyko_rhi:21, boyko_rhi_vulkan:104, boyko_serialize:25, boyko_image:25 - section checked per file); the root package is edition 2024 (resolver 3), so dev-dependency features do not reach a shipping build. Grows through LogFormatter on every matching emission in a test build. Readers: FIRST / LAST statics |
| crates/boyko_log/src/probe.rs:143 `fst.set(if held.is_empty() { line.clone() } else { held });` | probe::note_emission | `String<u8>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only (test-probe feature) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [local; growth perframe; addr_cached no; rung OOS] ECS: One line: compiled only under crates/boyko_log/src/lib.rs:98 "#[cfg(feature = "test-probe")]", a feature enabled solely from dev-dependencies (e.g. crates/boyko_app/Cargo.toml:65 "boyko-log = { path = "../boyko_log", features = ["test-probe"] }"). Class: C: module probe is behind feature "test-probe" (lib.rs:98 "#[cfg(feature = \"test-probe\")]"), and every crate that enables it does so under [dev-dependencies] (boyko_app/Cargo.toml:65, boyko_render:82, boyko_physics:71, boyko_rhi:21, boyko_rhi_vulkan:104, boyko_serialize:25, boyko_image:25 - section checked per file); the root package is edition 2024 (resolver 3), so dev-dependency features do not reach a shipping build. Found by the extended pattern. Readers: FIRST static |
| crates/boyko_log/src/probe.rs:189 `pub fn first_message() -> String {` | probe::first_message | `String<u8>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only (test-probe feature) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [return; growth unknown; addr_cached no; rung OOS] ECS: One line: compiled only under crates/boyko_log/src/lib.rs:98 "#[cfg(feature = "test-probe")]", a feature enabled solely from dev-dependencies (e.g. crates/boyko_app/Cargo.toml:65 "boyko-log = { path = "../boyko_log", features = ["test-probe"] }"). Class: C: module probe is behind feature "test-probe" (lib.rs:98 "#[cfg(feature = \"test-probe\")]"), and every crate that enables it does so under [dev-dependencies] (boyko_app/Cargo.toml:65, boyko_render:82, boyko_physics:71, boyko_rhi:21, boyko_rhi_vulkan:104, boyko_serialize:25, boyko_image:25 - section checked per file); the root package is edition 2024 (resolver 3), so dev-dependency features do not reach a shipping build. Allocation is the clone at probe.rs:193. Readers: boyko_app tests (log_host_enable_flag.rs:72) |
| crates/boyko_log/src/probe.rs:200 `pub fn last_message() -> String {` | probe::last_message | `String<u8>` | C | out-of-scope:test-only | none | out-of-scope:compile-time/tooling only (test-probe feature) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:49 "\| `harness` \| libtest / criterion \| outside the measured window \| not linked into shipped binaries \|" | [return; growth unknown; addr_cached no; rung OOS] ECS: One line: compiled only under crates/boyko_log/src/lib.rs:98 "#[cfg(feature = "test-probe")]", a feature enabled solely from dev-dependencies (e.g. crates/boyko_app/Cargo.toml:65 "boyko-log = { path = "../boyko_log", features = ["test-probe"] }"). Class: C: module probe is behind feature "test-probe" (lib.rs:98 "#[cfg(feature = \"test-probe\")]"), and every crate that enables it does so under [dev-dependencies] (boyko_app/Cargo.toml:65, boyko_render:82, boyko_physics:71, boyko_rhi:21, boyko_rhi_vulkan:104, boyko_serialize:25, boyko_image:25 - section checked per file); the root package is edition 2024 (resolver 3), so dev-dependency features do not reach a shipping build. Allocation is the clone at probe.rs:205. Readers: boyko_app tests (log_host_bad_preset.rs:44) |
| crates/boyko_log/src/sink/binary.rs:370 `Ok(p) => p.to_owned(),` | sink::binary::rotate_now | `String<u8 (sink path)>` | D | diagnostics | none | NEW:InlineStr&lt;N> (stack path buffer; DspBuf&lt;N> shape) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:51 "\| `diag` \|"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" - destination differs, see plan_conflicts | [local; growth unknown; addr_cached no; rung R6] ECS: Logger cold drain path (file rotation); one line: format each rotated name into the existing stack buffer type of the same crate, crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {", and hand std a &Path over it - no heap, and InlineStr&lt;N> is refuted as a new primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: D: rotation runs on the drain path only when the file reaches its size cap; per rotation it allocates 1 String + (keep + 1) format! Strings + PathBufs. The source path already lives in a fixed static cell of MAX_PATH_BYTES, so every name can be formatted into a stack buffer (boyko_log::record::DspBuf&lt;N> exists) and passed as &Path. Rotations per session: not counted. Readers: rotate_now itself (rename / remove / reopen) |
| crates/boyko_log/src/sink/binary.rs:381 `let oldest = std::path::PathBuf::from(format!("{path}.{keep}"));` | sink::binary::rotate_now | `String<u8 (rotated file name, String then PathBuf)>` | D | diagnostics | none | NEW:InlineStr&lt;N> (stack path buffer; DspBuf&lt;N> shape) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:51 "\| `diag` \|"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" - destination differs, see plan_conflicts | [local; growth unknown; addr_cached no; rung R6] ECS: Logger cold drain path (file rotation); one line: format each rotated name into the existing stack buffer type of the same crate, crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {", and hand std a &Path over it - no heap, and InlineStr&lt;N> is refuted as a new primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: D: rotation runs on the drain path only when the file reaches its size cap; per rotation it allocates 1 String + (keep + 1) format! Strings + PathBufs. The source path already lives in a fixed static cell of MAX_PATH_BYTES, so every name can be formatted into a stack buffer (boyko_log::record::DspBuf&lt;N> exists) and passed as &Path. Rotations per session: not counted. Readers: std::fs::metadata / remove_file |
| crates/boyko_log/src/sink/binary.rs:382 `let lost_oldest = std::fs::metadata(&oldest).map(\|m\| m.len()).unwrap_or(0);` | sink::binary::rotate_now | `other:std::fs internals (Vec<u16> wide path)<u16 (UTF-16 path, NUL-terminated)>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT. Removable only by calling the Win32 API with a wide path built in our own buffer. | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth unknown; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. Class: On Windows every std::fs call that takes a path converts it through maybe_verbatim -> Vec&lt;u16>: std sys/fs/mod.rs "pub fn remove_file(path: &Path) -> io::Result&lt;()> {" -> "with_native_path(path, &imp::unlink)", "pub fn metadata(path: &Path) -> io::Result&lt;FileAttr> {" -> "with_native_path(path, &imp::stat)", and sys/path/windows.rs "let path = maybe_verbatim(path)?;". Read in the rust-src of toolchain stable-x86_64-pc-windows-gnu (channel manifest date 2026-09-03), i.e. the std the tree's `channel = "stable"` pin resolves to on this box. The inventory rowed the same mechanism for OpenOptions::open/File::create (rows sink/binary.rs:400/:467, sink/file.rs:131/:217) but not for these calls in the same function. Runs once per rotation, on the drain thread holding the drain token. metadata of the oldest generation. Readers: rotate_now, from the binary sink write path (sink/binary.rs:501 "rotate_now();") |
| crates/boyko_log/src/sink/binary.rs:383 `let _ = std::fs::remove_file(&oldest);` | sink::binary::rotate_now | `other:std::fs internals (Vec<u16> wide path)<u16 (UTF-16 path, NUL-terminated)>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT. Removable only by calling the Win32 API with a wide path built in our own buffer. | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth unknown; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. Class: On Windows every std::fs call that takes a path converts it through maybe_verbatim -> Vec&lt;u16>: std sys/fs/mod.rs "pub fn remove_file(path: &Path) -> io::Result&lt;()> {" -> "with_native_path(path, &imp::unlink)", "pub fn metadata(path: &Path) -> io::Result&lt;FileAttr> {" -> "with_native_path(path, &imp::stat)", and sys/path/windows.rs "let path = maybe_verbatim(path)?;". Read in the rust-src of toolchain stable-x86_64-pc-windows-gnu (channel manifest date 2026-09-03), i.e. the std the tree's `channel = "stable"` pin resolves to on this box. The inventory rowed the same mechanism for OpenOptions::open/File::create (rows sink/binary.rs:400/:467, sink/file.rs:131/:217) but not for these calls in the same function. Runs once per rotation, on the drain thread holding the drain token. remove_file of the oldest generation. Readers: rotate_now (sink/binary.rs:501) |
| crates/boyko_log/src/sink/binary.rs:386 `std::path::PathBuf::from(&path)` | sink::binary::rotate_now | `other:PathBuf<path>` | D | diagnostics | none | NEW:InlineStr&lt;N> (stack path buffer; DspBuf&lt;N> shape) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:51 "\| `diag` \|"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" - destination differs, see plan_conflicts | [local; growth unknown; addr_cached no; rung R6] ECS: Logger cold drain path (file rotation); one line: format each rotated name into the existing stack buffer type of the same crate, crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {", and hand std a &Path over it - no heap, and InlineStr&lt;N> is refuted as a new primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: D: rotation runs on the drain path only when the file reaches its size cap; per rotation it allocates 1 String + (keep + 1) format! Strings + PathBufs. The source path already lives in a fixed static cell of MAX_PATH_BYTES, so every name can be formatted into a stack buffer (boyko_log::record::DspBuf&lt;N> exists) and passed as &Path. Rotations per session: not counted. Found by the extended pattern. Readers: std::fs::rename |
| crates/boyko_log/src/sink/binary.rs:388 `std::path::PathBuf::from(format!("{path}.{}", n - 1))` | sink::binary::rotate_now | `String<u8 (rotated file name)>` | D | diagnostics | none | NEW:InlineStr&lt;N> (stack path buffer; DspBuf&lt;N> shape) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:51 "\| `diag` \|"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" - destination differs, see plan_conflicts | [local; growth unknown; addr_cached no; rung R6] ECS: Logger cold drain path (file rotation); one line: format each rotated name into the existing stack buffer type of the same crate, crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {", and hand std a &Path over it - no heap, and InlineStr&lt;N> is refuted as a new primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: D: rotation runs on the drain path only when the file reaches its size cap; per rotation it allocates 1 String + (keep + 1) format! Strings + PathBufs. The source path already lives in a fixed static cell of MAX_PATH_BYTES, so every name can be formatted into a stack buffer (boyko_log::record::DspBuf&lt;N> exists) and passed as &Path. Rotations per session: not counted. Readers: std::fs::rename |
| crates/boyko_log/src/sink/binary.rs:390 `let _ = std::fs::rename(&from, std::path::PathBuf::from(format!("{path}.{n}")));` | sink::binary::rotate_now | `String<u8 (rotated file name)>` | D | diagnostics | none | NEW:InlineStr&lt;N> (stack path buffer; DspBuf&lt;N> shape) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:51 "\| `diag` \|"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" - destination differs, see plan_conflicts | [local; growth unknown; addr_cached no; rung R6] ECS: Logger cold drain path (file rotation); one line: format each rotated name into the existing stack buffer type of the same crate, crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {", and hand std a &Path over it - no heap, and InlineStr&lt;N> is refuted as a new primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: D: rotation runs on the drain path only when the file reaches its size cap; per rotation it allocates 1 String + (keep + 1) format! Strings + PathBufs. The source path already lives in a fixed static cell of MAX_PATH_BYTES, so every name can be formatted into a stack buffer (boyko_log::record::DspBuf&lt;N> exists) and passed as &Path. Rotations per session: not counted. Readers: std::fs::rename |
| crates/boyko_log/src/sink/binary.rs:393 `let n = std::fs::metadata(&path).map(\|m\| m.len()).unwrap_or(0);` | sink::binary::rotate_now | `other:std::fs internals (Vec<u16> wide path)<u16 (UTF-16 path, NUL-terminated)>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT. Removable only by calling the Win32 API with a wide path built in our own buffer. | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth unknown; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. Class: On Windows every std::fs call that takes a path converts it through maybe_verbatim -> Vec&lt;u16>: std sys/fs/mod.rs "pub fn remove_file(path: &Path) -> io::Result&lt;()> {" -> "with_native_path(path, &imp::unlink)", "pub fn metadata(path: &Path) -> io::Result&lt;FileAttr> {" -> "with_native_path(path, &imp::stat)", and sys/path/windows.rs "let path = maybe_verbatim(path)?;". Read in the rust-src of toolchain stable-x86_64-pc-windows-gnu (channel manifest date 2026-09-03), i.e. the std the tree's `channel = "stable"` pin resolves to on this box. The inventory rowed the same mechanism for OpenOptions::open/File::create (rows sink/binary.rs:400/:467, sink/file.rs:131/:217) but not for these calls in the same function. Runs once per rotation, on the drain thread holding the drain token. metadata of the live file. Readers: rotate_now (sink/binary.rs:501), keep == 0 branch |
| crates/boyko_log/src/sink/binary.rs:394 `let _ = std::fs::remove_file(&path);` | sink::binary::rotate_now | `other:std::fs internals (Vec<u16> wide path)<u16 (UTF-16 path, NUL-terminated)>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT. Removable only by calling the Win32 API with a wide path built in our own buffer. | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth unknown; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. Class: On Windows every std::fs call that takes a path converts it through maybe_verbatim -> Vec&lt;u16>: std sys/fs/mod.rs "pub fn remove_file(path: &Path) -> io::Result&lt;()> {" -> "with_native_path(path, &imp::unlink)", "pub fn metadata(path: &Path) -> io::Result&lt;FileAttr> {" -> "with_native_path(path, &imp::stat)", and sys/path/windows.rs "let path = maybe_verbatim(path)?;". Read in the rust-src of toolchain stable-x86_64-pc-windows-gnu (channel manifest date 2026-09-03), i.e. the std the tree's `channel = "stable"` pin resolves to on this box. The inventory rowed the same mechanism for OpenOptions::open/File::create (rows sink/binary.rs:400/:467, sink/file.rs:131/:217) but not for these calls in the same function. Runs once per rotation, on the drain thread holding the drain token. remove_file of the live file. Readers: rotate_now (sink/binary.rs:501), keep == 0 branch |
| crates/boyko_log/src/sink/binary.rs:400 `let fresh = std::fs::OpenOptions::new().create(true).write(true).truncate(true).open(&path).ok();` | sink::binary::rotate_now | `other:std::fs internals (Vec<u16> wide path)<u16>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own) | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth unknown; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. Class: T: std::fs on Windows converts the path to a NUL-terminated wide string on the heap before the syscall - verified in the rust-src of the installed 1.97.1-x86_64-pc-windows-gnu toolchain, library/std/src/sys/fs/windows.rs:339 "let path = maybe_verbatim(path)?;" with sys/path/windows.rs:81 "pub(crate) fn maybe_verbatim(path: &Path) -> io::Result&lt;Vec&lt;u16>> {". The tree pins channel = "stable" (rust-toolchain.toml:39), a floating pin, so the std that actually ships was NOT inspected. rename / remove_file / metadata go through other std paths not verified here. Per rotation. Readers: std::fs::OpenOptions::open |
| crates/boyko_log/src/sink/binary.rs:467 `let Ok(file) = std::fs::File::create(path) else { return false };` | sink::binary::open | `other:std::fs internals (Vec<u16> wide path)<u16>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own) | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth once; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. This open is not boot-only: a runtime log request re-opens the sink (joltab:crates/boyko_log/src/sink/request.rs:212 "let _ = crate::sink::file::open(cap_bytes);"), so it is covered too. Class: T: std::fs on Windows converts the path to a NUL-terminated wide string on the heap before the syscall - verified in the rust-src of the installed 1.97.1-x86_64-pc-windows-gnu toolchain, library/std/src/sys/fs/windows.rs:339 "let path = maybe_verbatim(path)?;" with sys/path/windows.rs:81 "pub(crate) fn maybe_verbatim(path: &Path) -> io::Result&lt;Vec&lt;u16>> {". The tree pins channel = "stable" (rust-toolchain.toml:39), a floating pin, so the std that actually ships was NOT inspected. rename / remove_file / metadata go through other std paths not verified here. Once per enable() (binary.rs:453 "pub fn open() -> bool {"). Readers: std::fs::File::create |
| crates/boyko_log/src/sink/crash.rs:160 `std::panic::set_hook(Box::new(move \|info\| {` | sink::crash::install_panic_hook | `Box<dyn><closure capturing previous: Box<dyn Fn> (16 B)>` | B | out-of-scope:os-owned | none | out-of-scope:std panic-hook API requires Box&lt;dyn Fn>; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own) | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers a panic-hook Box (the panic tag is for payloads; see plan_conflicts) | [local; growth once; addr_cached no; rung OOS] ECS: One line: std's panic-hook API is Box&lt;dyn Fn>-typed (take_hook / set_hook), installed once at enable; only a #[global_allocator] reaches it. Class: Captures the previous hook (a fat Box), so it does allocate - 16 bytes, once per install. Readers: std panic machinery on every panic |
| crates/boyko_log/src/sink/file.rs:131 `let Ok(file) = std::fs::File::create(path) else { return false };` | sink::file::open | `other:std::fs internals (Vec<u16> wide path)<u16>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own) | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth once; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. This open is not boot-only: a runtime log request re-opens the sink (joltab:crates/boyko_log/src/sink/request.rs:212 "let _ = crate::sink::file::open(cap_bytes);"), so it is covered too. Class: T: std::fs on Windows converts the path to a NUL-terminated wide string on the heap before the syscall - verified in the rust-src of the installed 1.97.1-x86_64-pc-windows-gnu toolchain, library/std/src/sys/fs/windows.rs:339 "let path = maybe_verbatim(path)?;" with sys/path/windows.rs:81 "pub(crate) fn maybe_verbatim(path: &Path) -> io::Result&lt;Vec&lt;u16>> {". The tree pins channel = "stable" (rust-toolchain.toml:39), a floating pin, so the std that actually ships was NOT inspected. rename / remove_file / metadata go through other std paths not verified here. Once per enable() (file.rs:116 "pub fn open(cap_bytes: u64) -> bool {"). Readers: std::fs::File::create |
| crates/boyko_log/src/sink/file.rs:187 `Ok(p) => p.to_owned(),` | sink::file::rotate_now | `String<u8 (sink path)>` | D | diagnostics | none | NEW:InlineStr&lt;N> (stack path buffer; DspBuf&lt;N> shape) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:51 "\| `diag` \|"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" - destination differs, see plan_conflicts | [local; growth unknown; addr_cached no; rung R6] ECS: Logger cold drain path (file rotation); one line: format each rotated name into the existing stack buffer type of the same crate, crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {", and hand std a &Path over it - no heap, and InlineStr&lt;N> is refuted as a new primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: D: rotation runs on the drain path only when the file reaches its size cap; per rotation it allocates 1 String + (keep + 1) format! Strings + PathBufs. The source path already lives in a fixed static cell of MAX_PATH_BYTES, so every name can be formatted into a stack buffer (boyko_log::record::DspBuf&lt;N> exists) and passed as &Path. Rotations per session: not counted. Readers: rotate_now itself (rename / remove / reopen) |
| crates/boyko_log/src/sink/file.rs:197 `let oldest = std::path::PathBuf::from(format!("{path}.{keep}"));` | sink::file::rotate_now | `String<u8 (rotated file name, String then PathBuf)>` | D | diagnostics | none | NEW:InlineStr&lt;N> (stack path buffer; DspBuf&lt;N> shape) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:51 "\| `diag` \|"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" - destination differs, see plan_conflicts | [local; growth unknown; addr_cached no; rung R6] ECS: Logger cold drain path (file rotation); one line: format each rotated name into the existing stack buffer type of the same crate, crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {", and hand std a &Path over it - no heap, and InlineStr&lt;N> is refuted as a new primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: D: rotation runs on the drain path only when the file reaches its size cap; per rotation it allocates 1 String + (keep + 1) format! Strings + PathBufs. The source path already lives in a fixed static cell of MAX_PATH_BYTES, so every name can be formatted into a stack buffer (boyko_log::record::DspBuf&lt;N> exists) and passed as &Path. Rotations per session: not counted. Readers: std::fs::metadata / remove_file |
| crates/boyko_log/src/sink/file.rs:198 `let lost = std::fs::metadata(&oldest).map(\|m\| m.len()).unwrap_or(0);` | sink::file::rotate_now | `other:std::fs internals (Vec<u16> wide path)<u16 (UTF-16 path, NUL-terminated)>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT. Removable only by calling the Win32 API with a wide path built in our own buffer. | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth unknown; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. Class: On Windows every std::fs call that takes a path converts it through maybe_verbatim -> Vec&lt;u16>: std sys/fs/mod.rs "pub fn remove_file(path: &Path) -> io::Result&lt;()> {" -> "with_native_path(path, &imp::unlink)", "pub fn metadata(path: &Path) -> io::Result&lt;FileAttr> {" -> "with_native_path(path, &imp::stat)", and sys/path/windows.rs "let path = maybe_verbatim(path)?;". Read in the rust-src of toolchain stable-x86_64-pc-windows-gnu (channel manifest date 2026-09-03), i.e. the std the tree's `channel = "stable"` pin resolves to on this box. The inventory rowed the same mechanism for OpenOptions::open/File::create (rows sink/binary.rs:400/:467, sink/file.rs:131/:217) but not for these calls in the same function. Runs once per rotation, on the drain thread holding the drain token. metadata of the oldest generation. Readers: rotate_now, from the text sink write path (sink/file.rs:264 "let lost = rotate_now(slot, written);") |
| crates/boyko_log/src/sink/file.rs:199 `let _ = std::fs::remove_file(&oldest);` | sink::file::rotate_now | `other:std::fs internals (Vec<u16> wide path)<u16 (UTF-16 path, NUL-terminated)>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT. Removable only by calling the Win32 API with a wide path built in our own buffer. | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth unknown; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. Class: On Windows every std::fs call that takes a path converts it through maybe_verbatim -> Vec&lt;u16>: std sys/fs/mod.rs "pub fn remove_file(path: &Path) -> io::Result&lt;()> {" -> "with_native_path(path, &imp::unlink)", "pub fn metadata(path: &Path) -> io::Result&lt;FileAttr> {" -> "with_native_path(path, &imp::stat)", and sys/path/windows.rs "let path = maybe_verbatim(path)?;". Read in the rust-src of toolchain stable-x86_64-pc-windows-gnu (channel manifest date 2026-09-03), i.e. the std the tree's `channel = "stable"` pin resolves to on this box. The inventory rowed the same mechanism for OpenOptions::open/File::create (rows sink/binary.rs:400/:467, sink/file.rs:131/:217) but not for these calls in the same function. Runs once per rotation, on the drain thread holding the drain token. remove_file of the oldest generation. Readers: rotate_now (sink/file.rs:264) |
| crates/boyko_log/src/sink/file.rs:202 `std::path::PathBuf::from(&path)` | sink::file::rotate_now | `other:PathBuf<path>` | D | diagnostics | none | NEW:InlineStr&lt;N> (stack path buffer; DspBuf&lt;N> shape) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:51 "\| `diag` \|"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" - destination differs, see plan_conflicts | [local; growth unknown; addr_cached no; rung R6] ECS: Logger cold drain path (file rotation); one line: format each rotated name into the existing stack buffer type of the same crate, crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {", and hand std a &Path over it - no heap, and InlineStr&lt;N> is refuted as a new primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: D: rotation runs on the drain path only when the file reaches its size cap; per rotation it allocates 1 String + (keep + 1) format! Strings + PathBufs. The source path already lives in a fixed static cell of MAX_PATH_BYTES, so every name can be formatted into a stack buffer (boyko_log::record::DspBuf&lt;N> exists) and passed as &Path. Rotations per session: not counted. Found by the extended pattern. Readers: std::fs::rename |
| crates/boyko_log/src/sink/file.rs:204 `std::path::PathBuf::from(format!("{path}.{}", n - 1))` | sink::file::rotate_now | `String<u8 (rotated file name)>` | D | diagnostics | none | NEW:InlineStr&lt;N> (stack path buffer; DspBuf&lt;N> shape) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:51 "\| `diag` \|"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" - destination differs, see plan_conflicts | [local; growth unknown; addr_cached no; rung R6] ECS: Logger cold drain path (file rotation); one line: format each rotated name into the existing stack buffer type of the same crate, crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {", and hand std a &Path over it - no heap, and InlineStr&lt;N> is refuted as a new primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: D: rotation runs on the drain path only when the file reaches its size cap; per rotation it allocates 1 String + (keep + 1) format! Strings + PathBufs. The source path already lives in a fixed static cell of MAX_PATH_BYTES, so every name can be formatted into a stack buffer (boyko_log::record::DspBuf&lt;N> exists) and passed as &Path. Rotations per session: not counted. Readers: std::fs::rename |
| crates/boyko_log/src/sink/file.rs:206 `let _ = std::fs::rename(&from, std::path::PathBuf::from(format!("{path}.{n}")));` | sink::file::rotate_now | `String<u8 (rotated file name)>` | D | diagnostics | none | NEW:InlineStr&lt;N> (stack path buffer; DspBuf&lt;N> shape) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:51 "\| `diag` \|"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:361 "`boyko_log` `String`s" - destination differs, see plan_conflicts | [local; growth unknown; addr_cached no; rung R6] ECS: Logger cold drain path (file rotation); one line: format each rotated name into the existing stack buffer type of the same crate, crates/boyko_log/src/record.rs:819 "pub struct DspBuf&lt;const N: usize> {", and hand std a &Path over it - no heap, and InlineStr&lt;N> is refuted as a new primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: D: rotation runs on the drain path only when the file reaches its size cap; per rotation it allocates 1 String + (keep + 1) format! Strings + PathBufs. The source path already lives in a fixed static cell of MAX_PATH_BYTES, so every name can be formatted into a stack buffer (boyko_log::record::DspBuf&lt;N> exists) and passed as &Path. Rotations per session: not counted. Readers: std::fs::rename |
| crates/boyko_log/src/sink/file.rs:210 `let n = std::fs::metadata(&path).map(\|m\| m.len()).unwrap_or(at);` | sink::file::rotate_now | `other:std::fs internals (Vec<u16> wide path)<u16 (UTF-16 path, NUL-terminated)>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT. Removable only by calling the Win32 API with a wide path built in our own buffer. | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth unknown; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. Class: On Windows every std::fs call that takes a path converts it through maybe_verbatim -> Vec&lt;u16>: std sys/fs/mod.rs "pub fn remove_file(path: &Path) -> io::Result&lt;()> {" -> "with_native_path(path, &imp::unlink)", "pub fn metadata(path: &Path) -> io::Result&lt;FileAttr> {" -> "with_native_path(path, &imp::stat)", and sys/path/windows.rs "let path = maybe_verbatim(path)?;". Read in the rust-src of toolchain stable-x86_64-pc-windows-gnu (channel manifest date 2026-09-03), i.e. the std the tree's `channel = "stable"` pin resolves to on this box. The inventory rowed the same mechanism for OpenOptions::open/File::create (rows sink/binary.rs:400/:467, sink/file.rs:131/:217) but not for these calls in the same function. Runs once per rotation, on the drain thread holding the drain token. metadata of the live file. Readers: rotate_now (sink/file.rs:264), keep == 0 branch |
| crates/boyko_log/src/sink/file.rs:211 `let _ = std::fs::remove_file(&path);` | sink::file::rotate_now | `other:std::fs internals (Vec<u16> wide path)<u16 (UTF-16 path, NUL-terminated)>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT. Removable only by calling the Win32 API with a wide path built in our own buffer. | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth unknown; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. Class: On Windows every std::fs call that takes a path converts it through maybe_verbatim -> Vec&lt;u16>: std sys/fs/mod.rs "pub fn remove_file(path: &Path) -> io::Result&lt;()> {" -> "with_native_path(path, &imp::unlink)", "pub fn metadata(path: &Path) -> io::Result&lt;FileAttr> {" -> "with_native_path(path, &imp::stat)", and sys/path/windows.rs "let path = maybe_verbatim(path)?;". Read in the rust-src of toolchain stable-x86_64-pc-windows-gnu (channel manifest date 2026-09-03), i.e. the std the tree's `channel = "stable"` pin resolves to on this box. The inventory rowed the same mechanism for OpenOptions::open/File::create (rows sink/binary.rs:400/:467, sink/file.rs:131/:217) but not for these calls in the same function. Runs once per rotation, on the drain thread holding the drain token. remove_file of the live file. Readers: rotate_now (sink/file.rs:264), keep == 0 branch |
| crates/boyko_log/src/sink/file.rs:217 `*slot = std::fs::OpenOptions::new().create(true).append(true).open(&path).ok();` | sink::file::rotate_now | `other:std::fs internals (Vec<u16> wide path)<u16>` | T | resource-column | none | out-of-scope:std internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own) | none - no closed tag of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:42 "the exception list is closed" covers it (see plan_conflicts) | [local; growth unknown; addr_cached no; rung R4] ECS: One line: std::fs on Windows builds a Vec&lt;u16> wide path inside std for every path-taking call; only a #[global_allocator] reaches it, unless the sink calls the Win32 file API directly with a wide path in a stack buffer (the tree already binds Win32 directly: crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). [rev3 item 6] ecs_form resource-column (was out-of-scope:os-owned). One rule for every std path conversion, decided by performance: a conversion reachable after steady state (hot-reload polls, log rotation and on-demand sink opens, asset load, save / load, end-of-run dumps) moves to in-house UTF-16 Win32 FFI on a path encoded once into the owning record (an inline [u16; N] plus length, loud error past N): zero allocations and zero UTF-8 to UTF-16 transcoding per call, and a deny-after-steady #[global_allocator] cannot abort inside std. Boot-only conversions stay out-of-scope:os-owned. The tree already binds Win32 directly (joltab:crates/boyko_ecs/src/ecs/memory/vm.rs:64 "pub fn VirtualAlloc("). Owning record: the sink's slot state, which encodes the live file name and its generation names once at open. Class: T: std::fs on Windows converts the path to a NUL-terminated wide string on the heap before the syscall - verified in the rust-src of the installed 1.97.1-x86_64-pc-windows-gnu toolchain, library/std/src/sys/fs/windows.rs:339 "let path = maybe_verbatim(path)?;" with sys/path/windows.rs:81 "pub(crate) fn maybe_verbatim(path: &Path) -> io::Result&lt;Vec&lt;u16>> {". The tree pins channel = "stable" (rust-toolchain.toml:39), a floating pin, so the std that actually ships was NOT inspected. rename / remove_file / metadata go through other std paths not verified here. Per rotation. Readers: std::fs::OpenOptions::open |
| crates/boyko_log/src/sync_out.rs:75 `static TOKEN_ANCHOR: u8 = const { 0 };` | sync_out::TOKEN_ANCHOR (thread_local!) | `other:thread_local! os-key Value<T> (std AlignedSystemBox, System.alloc)<u8 (used only for its address)>` | T | kernel-internal | none | NEW:engine-owned per-thread context slot (KF-45 Engine thread-context column (replaces every thread_local!)) - removing the thread_local! is the only way to remove this allocation: std calls System.alloc directly, so neither a #[global_allocator] nor a per-type replacement reaches it | main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built." [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read.; joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call" | [static; growth once; addr_cached yes; rung R2] ECS: CHOSEN kernel-internal under ONE decision for every thread_local! in the engine (TLS-1): the value becomes a field of the engine thread-context record (KF-45 Engine thread-context column (replaces every thread_local!)), reached WITHOUT thread_local! - by an explicit context parameter wherever the call chain already carries one (worker loop -> task trampoline -> system run; the physics decisions already chose this for the gang nesting depth: main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built."), and otherwise through the thread-context slot. ROUTE FOR THIS STATIC: the opaque per-thread token becomes thread-context slot index + 1 (see drain_owner.rs:42, the same datum): unique among live threads, never zero. WHY THIS IS ALSO THE FASTER PATH: a passed context is a register; a slot read is one plain load of the platform thread-control-block pointer (TEB on Windows x64, TCB on Linux x86_64) plus a probe of a 64-entry table in one or two L1 lines - no locked RMW, no fence, no Win32 call, no allocation (reasoned, not measured) - against the measured windows-gnu cost of two contended RMWs + FlsSetValue per thread_local! read and one System allocation per thread per key. SKIPPED: component / dense-component / enable-state / relation - a per-THREAD value, and threads are not entities (the pool and the diag/log crates sit below every World); event - a state word, not a message; resource-column - per thread, not a world singleton, and boyko_threadpool/boyko_diag/boyko_log sit below boyko_ecs; system-scratch - outlives any one system run; scope-arena - outlives any one scope. OVERTURNED BY: an A/B of the ke16_nested_scope cell worker/body_1us_tasks_64W on x86_64-pc-windows-msvc (native TLS) in which the explicit-context build is slower beyond its band - msvc/Linux is the only place the old form could win. [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read. Class: [rev2 GAP 1] One System-allocated Value&lt;T> per THREAD that touches the key, freed at thread exit, on the owner's windows-gnu toolchain. std itself says rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/guard/windows.rs:116 "// `#[thread_local]` is unavailable on windows-gnu (`target_thread_local` is off),", so thread_local! resolves to the os-key backend (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/mod.rs:44 "mod os;"), whose try_initialize boxes the value (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:194 "let value = AlignedSystemBox::&lt;T, ALIGN>::new(Value {") through rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:110 "let ptr: *mut Value&lt;T> = (unsafe { System.alloc(layout) }).cast();" - System directly, bypassing any #[global_allocator]. A const initialiser does not avoid it (joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:91 "backend, whose key always has a dtor — `const {}` initialisation does **not** help — and (ii) `enable`'s"). Every read also pays guard::enable: joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call"; the file sizes it at joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:195 "978.7 − 110 = 868.7 µs for TWO reads, i.e. ≈ 434 µs per read per wave (1,024 tasks ⇒ ≈ 424 ns per". On native-TLS targets (x86_64-pc-windows-msvc, Linux) the same const-initialised cell allocates nothing and reads lock-free. CENSUS NOTE: a #[global_allocator] counting gate cannot see this cell - std calls System.alloc directly - so the allocation census must count thread_local! cells from this row (a static census of thread_local! statics), never from its counter; the counter reads 0 for them by construction. Readers: my_token (sync_out.rs:80) -> the synchronous-console owner slot |
| crates/boyko_threadpool/src/block.rs:482 `let base = unsafe { alloc(layout) };` | ScopeBlock::grow | `other:std::alloc raw chunk<u8 chunk of CHUNK0 << e bytes (>= 4096, power of two), align 64>` | S | scope-arena | pool | NEW:ChunkPool (power-of-two chunks carved from one VmReservation, recycled through a free list, never returned to the OS) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:113 "\| **Scope** \| `ScopeArena` (one per thread slot, `W+1`), replaces `ScopeBlock`'s `std::alloc` chunks and `Box<ScopeShared>`"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:326 "chunks = sub-ranges of the same `ScopeArena`" - with the thread-identity fix of [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:461 "the arena must be owned by a THREAD identity: either a TLS-owned per-thread arena (the `LANE_DEPOSIT` single-read shape) with its lifetime relative to the pool stated, or a claim/release pool of dispatcher arenas" | [local; growth perframe; addr_cached yes; rung R5] ECS: component / dense-component / enable-state / relation skipped: a task cell or a scope's join state is not a datum about an entity and not an edge; it is a closure payload or a join counter that lives from spawn (or scope entry) to the join. event skipped: kernel event lanes are drained by systems at sync points, and systems are themselves dispatched as tasks of these scopes (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"), so the scope substrate sits below the event form rather than being an instance of it. resource-column skipped: not a durable table. system-scratch skipped: a system-owned ScratchColumn needs a ComponentId inside boyko_ecs, which the pool cannot name (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }"), and the outermost scope wraps every system of a run. scope-arena fits. block.rs IS the kernel scope arena (wired: crates/boyko_threadpool/src/scope.rs:1050 "block: ScopeBlock,"); only the source of its chunk bytes is wrong (crates/boyko_threadpool/src/block.rs:482 "let base = unsafe { alloc(layout) };"). Form destination: the ScopeBlock bump region becomes a mark/rewind window on ONE per-thread VmReservation. Scopes nest LIFO on the thread that opens them, and the compiler enforces it: crates/boyko_threadpool/src/scope.rs:1130 "`Scope: !Sync`, so `&Scope: !Send` — no other thread can hold this", while every scope closure and every task body must be Send (crates/boyko_threadpool/src/thread_pool.rs:240 "F: FnOnce(&Scope&lt;'scope>) -> R + Send,"; crates/boyko_threadpool/src/scope.rs:1122 "F: FnOnce() + Send + 'scope,"), so no inner scope's closure and no task can capture an outer &Scope and spawn into it while the inner one is open (crates/boyko_threadpool/src/block.rs:88 "`ScopeBlock` is `!Sync` by construction because `Cell` is, which is"). A scope opened while another is joining on the same thread (a joiner running a stolen task that opens a scope) closes before the outer frame resumes: the call stack IS the arena discipline, and rewind-at-join recycles every byte with no free list and no chunk table. Address stability (Task.payload points into the region until the task runs) is what a reservation gives for free. The arena must belong to a THREAD, not to a wid (the C2 finding): a const-initialised TLS pointer with no Drop (the shape of crates/boyko_threadpool/src/tls.rs:169 "pub(crate) static ACTIVE_POOL: Cell&lt;*const PoolInner> = const { Cell::new(ptr::null()) };", so std registers no TLS destructor), set from the pool's per-worker table in worker_main and, for an external installer, from a spare-slot claim at its outermost install released by InstallGuard (the crates/boyko_diag/src/lane.rs:187 "pub fn claim_lane() -> Option&lt;u16> {" pattern). Obligations carried: the D1/D2 Tree-Borrows rules of block.rs; the Miri block-free gate re-established RED-first at the rewind ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:467 "\| W1 \| The KE16 completion-protector Miri gate keys on a DEALLOCATION that rung 1a removes"). Needs KF-memory-below-the-pool and KF-scope-arena-vm. The proposed ChunkPool is refuted (primitives_refuted). Class: The arena itself; its chunks come from std::alloc (block.rs:101 "use std::alloc::{alloc, dealloc, handle_alloc_error};"). One alloc per chunk: every scope that spawns >= 1 task allocates >= 1 chunk and frees all of them at its drop, so the arena turns per-TASK allocs into per-SCOPE allocs, not into zero. Emplaced cells are addressed by Task.payload until run, so the replacement must be address-stable for the scope life (chunks never move today). CONTRADICTS THE BRIEF: block.rs is NOT wired to nothing in this tree - scope.rs:41 "use crate::block::ScopeBlock;", scope.rs:1050 "block: ScopeBlock,", and block.rs:529-531 itself records that the "wired to nothing" clause rotted when Stage 3b wired it. Per-frame chunk count on the 1240-body pile: not counted. Readers: ScopeBlock::emplace (via Task::new_scoped, task/mod.rs:279 "let cell = block.emplace(ScopedCell {"); freed by ScopeBlock::free_all (block.rs:341 "dealloc(") from Scope::drop (scope.rs:1333 "unsafe { self.block.free_all() };") |
| crates/boyko_threadpool/src/scope.rs:534 `pub(crate) panic_payload: AtomicPtr<Box<dyn Any + Send + 'static>>,` | ScopeShared::panic_payload | `Box<T><Box<dyn Any + Send + 'static> (thin box around the std panic payload)>` | D | diagnostics | pool | out-of-scope:panic path only; our outer thin Box can be removed without a primitive (fat pointer in two words behind a CAS-claimed flag inside ScopeShared); the inner payload Box is minted by std's unwinder (T, global_allocator only) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:47 "\| `panic` \| panic payload `Box<dyn Any + Send>` \| failure path \|" (covers std's payload Box, not this outer Box) | [field; growth unknown; addr_cached no; rung R5] ECS: Cold panic-path slot of the pool's own scope state; one line: the outer thin Box exists only because a fat pointer cannot sit in one AtomicPtr, so a two-word inline slot plus a CAS-once claim flag inside ScopeShared removes it with no storage and no primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: Written only when a task panics. Box&lt;Box&lt;dyn Any>> exists because a fat pointer cannot sit in one AtomicPtr (scope.rs:525-526). Readers: ScopeShared::capture_panic (task-body panic, cold) / take_panic in Scope::drop / ScopeShared::drop |
| crates/boyko_threadpool/src/scope.rs:626 `pub(crate) fn capture_panic(&self, payload: Box<dyn Any + Send + 'static>) {` | ScopeShared::capture_panic | `Box<dyn><dyn Any + Send + 'static>` | D | out-of-scope:os-owned | none | out-of-scope:std panic payload - allocated by std::panic machinery; a #[global_allocator] would cover it, a per-type replacement would not | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:47 "\| `panic` \| panic payload `Box<dyn Any + Send>` \| failure path \|" | [param; growth unknown; addr_cached no; rung OOS] ECS: std runtime-owned (closest closed tag: os-owned): the Box&lt;dyn Any + Send> is minted by std's unwinder and handed back to resume_unwind; only a #[global_allocator] reaches it. Class: The payload arrives from catch_unwind; we only move it. Readers: run_scoped catch_unwind Err arm (task/scoped.rs:98) |
| crates/boyko_threadpool/src/scope.rs:627 `let raw = Box::into_raw(Box::new(payload));` | ScopeShared::capture_panic | `Box<T><Box<dyn Any + Send + 'static>>` | D | diagnostics | pool | out-of-scope:cold panic path; removable by storing the fat pointer inline (see scope.rs:534 row) rather than migrated | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:47 "\| `panic` \| panic payload `Box<dyn Any + Send>` \| failure path \|" (covers std's payload Box, not this outer Box) | [local; growth unknown; addr_cached no; rung R5] ECS: Cold panic-path slot of the pool's own scope state; one line: the outer thin Box exists only because a fat pointer cannot sit in one AtomicPtr, so a two-word inline slot plus a CAS-once claim flag inside ScopeShared removes it with no storage and no primitive. [rev2 GAP 6] ecs_form diagnostics (new explicit form, decided by performance): cold diagnostic text and error payloads, built only on error / report paths, so the cost that matters is I-cache and the emitting thread's time, not allocation speed. Home, chosen per site: a &'static str or structured code (no bytes at all), a boyko_log ring record with formatting deferred to the drain (off the emitting thread), or a cold VmReservation-backed byte arena for a payload that must outlive the call; the recount's memory destination (DspBuf / InlineStr / DiagArena / KF-10 structured error) names which. Not kernel-internal: it is not the kernel's storage bookkeeping (most of these rows are in boyko_app and boyko_render), and moving it out lets the gate on kernel-internal close. Not out of scope: our code allocates it. Class: One 16-byte Box per panicking task; a racing second panic frees its own immediately (scope.rs:637). Readers: take_panic (scope.rs:646) / ScopeShared::drop (scope.rs:1001) |
| crates/boyko_threadpool/src/scope.rs:646 `pub(crate) fn take_panic(&self) -> Option<Box<dyn Any + Send + 'static>> {` | ScopeShared::take_panic | `Box<dyn><dyn Any + Send + 'static>` | D | out-of-scope:os-owned | none | out-of-scope:std panic payload handed back to resume_unwind; std owns the type | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:47 "\| `panic` \| panic payload `Box<dyn Any + Send>` \| failure path \|" | [return; growth unknown; addr_cached no; rung OOS] ECS: std runtime-owned (closest closed tag: os-owned): the Box&lt;dyn Any + Send> is minted by std's unwinder and handed back to resume_unwind; only a #[global_allocator] reaches it. Class: Unboxes the thin Box of row scope.rs:627 and returns std's payload Box. Readers: Scope::drop -> resume_unwind |
| crates/boyko_threadpool/src/scope.rs:1096 `pub(crate) fn new(inner: &'scope PoolInner, shared: Box<ScopeShared>) -> Self {` | Scope::new | `Box<T><ScopeShared>` | S | scope-arena | pool | NEW:ScopeSharedStack (per-thread LIFO of ScopeShared cells on a VmReservation) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:325 "emplaced in the owner thread's `ScopeArena` at `mark`, rewound at join" (owns rung 1a; see plan_conflicts for the frame-local alternative) | [param; growth perframe; addr_cached yes; rung R5] ECS: Carrier of the thread_pool.rs:278/:328 allocation, not a separate one; same form and destination. Under the frame-local destination the parameter becomes a NonNull&lt;ScopeShared> minted by &raw const in the opener's frame, and the single free site crates/boyko_threadpool/src/scope.rs:1406 "unsafe { drop(Box::from_raw(raw)) };" disappears (the frame pop frees it). Class: Carries the Box minted at thread_pool.rs:278/:328; not a separate allocation. Box::into_raw at scope.rs:1100. Readers: PoolInner::install / PoolInner::scope (thread_pool.rs:287 / :332 "let scope = Scope::new(self, shared);") |
| crates/boyko_threadpool/src/task/detached.rs:95 `let raw = unsafe { alloc(layout) };` | task::detached::alloc_cell (caller Task::new_detached, task/mod.rs:310) | `other:std::alloc raw cell<DetachedCell<F> { CellHead, F }>` | S | kernel-internal | pool | NEW:DetachedCellSlab (fixed-size-class cells on a VmReservation, freed on run) - or delete the detached kind | docs/threadpool/KE16-TASKREP-ALLOCATION.md:70 "remaining caller, on the detached path" (records the detached path as alloc_cell's only caller) | [local; growth unknown; addr_cached yes; rung R5] ECS: A detached task outlives the frame that spawns it, so it cannot live in a scope arena; if kept it would be pool-owned transient storage (kernel-internal), i.e. a per-lane cell ring. component / dense-component / enable-state / relation skipped: those are World storage, and the pool sits BELOW every World - boyko_ecs depends on it (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }") and one pool can be handed to several Apps (crates/boyko_ecs/src/ecs/core/app/app.rs:214 "pub fn with_pool(pool: Arc&lt;ThreadPool>) -> Self {") - while workers read this state from the steal/park loop holding no World borrow. event skipped: not a message between systems. resource-column skipped: a Resource belongs to one World and is resolved through the storage the pool's own dispatch runs beneath. system-scratch skipped: no system owns it; every system of a run is dispatched inside the pool (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"). scope-arena skipped: it lives for the pool, not for one scope. Form destination: DELETE the detached kind. Its public entry crates/boyko_threadpool/src/thread_pool.rs:465 "pub fn spawn&lt;F>(&self, f: F)" has one caller in the tree and it is a test (crates/boyko_physics/tests/ke16_app1_soft_colored_from_a_worker.rs:183 "pool.spawn(move \|\| {"); building a primitive nothing ships would add a parallel storage path for no reader. Fallback only if the owner keeps detached spawn: cells in the pool's VM-backed lane storage (KF-inhouse-lanes), freed on run. Class: The one per-task heap cell left in the pool: Stage 3b moved SCOPED cells into ScopeBlock, detached cells kept the per-spawn alloc/dealloc pair (detached.rs:4-8). No production caller of ThreadPool::spawn was found: rg for pool.spawn( / ThreadPool::spawn over crates/*/src and src outside boyko_threadpool hit only crates/boyko_threadpool/tests/*. So growth is unknown and deletion is the cheaper destination than a primitive nothing uses. Readers: ThreadPool::spawn -> PoolInner::spawn (thread_pool.rs:232 "crate::worker::push_task(self, Task::new_detached(f));"); freed by run_detached (detached.rs:132) or drop_unrun_detached (detached.rs:152) |
| crates/boyko_threadpool/src/thread_pool.rs:113 `pub(crate) injector_global: CachePadded<Injector<Task>>,` | PoolInner::injector_global | `other:crossbeam_deque::Injector<Task (16 B: payload ptr + fn ptr)>` | T | kernel-internal | pool | out-of-scope:crossbeam-deque internal; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own); the only per-type route is replacing Injector with an in-house MPMC queue on a VmReservation | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:328 "**in-house bounded Chase-Lev** per lane on a `Table<Task>` ring in the pool's `TableSet`" | [field; growth perframe; addr_cached no; rung R5] ECS: Pool dispatch substrate; one line: crossbeam's Injector allocates a Block per 63 pushes ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:496 "correct to 63 slots / one block per 64 pushes"); the per-type route is the in-house bounded MPMC injector ring on the pool's VM (KF-inhouse-lanes) - it is kernel-internal pool storage, not a new memory primitive. Class: crossbeam-deque 0.8.8 (Cargo.lock): Injector::new allocates its first Block (deque.rs:1362 "let block = Box::into_raw(Block::&lt;T>::new());"), push installs a new Block every BLOCK_CAP = 63 pushes (deque.rs:1438 "let next_block = Box::into_raw(next_block.unwrap());", Block::new = alloc_zeroed at deque.rs:1272) and a consumed Block is freed by the stealer. So dispatcher-side spawns allocate one Block per 63 injected tasks. Count per frame: not counted. Readers: worker::push_task from any non-worker thread (worker.rs:688 "inner.injector_global.push(task);"); drained by worker_main (worker.rs:250 "let popped = drain_one(\|\| inner.injector_global.steal_batch_and_pop(local));") and the external joiner |
| crates/boyko_threadpool/src/thread_pool.rs:128 `pub(crate) injector_local: Arc<[CachePadded<Injector<Task>>]>,` | PoolInner::injector_local | `Arc<[CachePadded<Injector<Task>>] (Arc<[T]>, len = worker_count)>` | B | kernel-internal | pool | out-of-scope:dead storage - delete, do not migrate (thread_pool.rs:116 "`worker_id`'s private inbox. **Nothing feeds it and nothing drains it.**") | none - no plan decides the dead field; the field's own doc defers it (crates/boyko_threadpool/src/thread_pool.rs:116 "**Nothing feeds it and nothing drains it.**") | [field; growth once; addr_cached no; rung R5] ECS: component / dense-component / enable-state / relation skipped: those are World storage, and the pool sits BELOW every World - boyko_ecs depends on it (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }") and one pool can be handed to several Apps (crates/boyko_ecs/src/ecs/core/app/app.rs:214 "pub fn with_pool(pool: Arc&lt;ThreadPool>) -> Self {") - while workers read this state from the steal/park loop holding no World borrow. event skipped: not a message between systems. resource-column skipped: a Resource belongs to one World and is resolved through the storage the pool's own dispatch runs beneath. system-scratch skipped: no system owns it; every system of a run is dispatched inside the pool (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"). scope-arena skipped: it lives for the pool, not for one scope. kernel-internal fits: the pool is the scheduler substrate of the SDK itself (the kernel's own dispatch bookkeeping), and its storage moves onto the memory library. Form destination: DELETE, not migrate: crates/boyko_threadpool/src/thread_pool.rs:116 "**Nothing feeds it and nothing drains it.**" Each element also allocates a crossbeam Block at construction, so deleting it removes worker_count boot allocations. Class: Dead storage (#[allow(dead_code)] at :127). Preferred action is deletion, not migration: each element also carries an Injector whose first Block is heap-allocated at construction (T, crossbeam). Allocated by the Vec -> Arc&lt;[T]> copy at thread_pool.rs:693. RECOUNT: destination was "VmReservation-raw"; the row's own evidence says the storage is dead, so a migration target is the wrong ledger entry. Readers: none - thread_pool.rs:116 "`worker_id`'s private inbox. **Nothing feeds it and nothing drains it.**" |
| crates/boyko_threadpool/src/thread_pool.rs:132 `pub(crate) stealers: Arc<[Stealer<Task>]>,` | PoolInner::stealers | `Arc<[Stealer<Task>] (Arc<[T]>, len = worker_count)>` | B | kernel-internal | pool | NEW:BootArray&lt;T, MAX_WORKERS> (inline in PoolInner, len written once) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:328 "**in-house bounded Chase-Lev** per lane on a `Table<Task>` ring in the pool's `TableSet`" | [field; growth once; addr_cached no; rung R5] ECS: component / dense-component / enable-state / relation skipped: those are World storage, and the pool sits BELOW every World - boyko_ecs depends on it (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }") and one pool can be handed to several Apps (crates/boyko_ecs/src/ecs/core/app/app.rs:214 "pub fn with_pool(pool: Arc&lt;ThreadPool>) -> Self {") - while workers read this state from the steal/park loop holding no World borrow. event skipped: not a message between systems. resource-column skipped: a Resource belongs to one World and is resolved through the storage the pool's own dispatch runs beneath. system-scratch skipped: no system owns it; every system of a run is dispatched inside the pool (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"). scope-arena skipped: it lives for the pool, not for one scope. kernel-internal fits: the pool is the scheduler substrate of the SDK itself (the kernel's own dispatch bookkeeping), and its storage moves onto the memory library. Read on every sibling steal, so this is per-frame-read data built once. Form destination: a plain inline array of MAX_WORKERS entries in PoolInner (crates/boyko_threadpool/src/thread_pool.rs:52 "pub const MAX_WORKERS: usize = 64;"; the length is already crates/boyko_threadpool/src/thread_pool.rs:158 "pub(crate) worker_count: u32,") - no allocator and no BootArray primitive. After KF-inhouse-lanes the entries are raw lane-ring handles into the same pool reservation instead of crossbeam Stealers (which are Arc clones of the deque's Inner). Class: Allocated by the Vec -> Arc&lt;[T]> copy at thread_pool.rs:686. worker_count &lt;= MAX_WORKERS = 64 (thread_pool.rs:52), so a fixed inline array inside PoolInner removes the allocation outright. Each Stealer is itself an Arc clone of the deque's crossbeam Inner (T). Readers: sibling steal sweep (worker.rs:347 "if let Some(t) = drain_one(\|\| stealer.steal_batch_and_pop(local)) {"), both joiners |
| crates/boyko_threadpool/src/thread_pool.rs:135 `pub(crate) workers: Arc<[CachePadded<WorkerHandle>]>,` | PoolInner::workers | `Arc<[CachePadded<WorkerHandle>] (Arc<[T]>, len = worker_count)>` | B | kernel-internal | pool | NEW:BootArray&lt;T, MAX_WORKERS> (inline in PoolInner, len written once) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [field; growth once; addr_cached yes; rung R5] ECS: component / dense-component / enable-state / relation skipped: those are World storage, and the pool sits BELOW every World - boyko_ecs depends on it (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }") and one pool can be handed to several Apps (crates/boyko_ecs/src/ecs/core/app/app.rs:214 "pub fn with_pool(pool: Arc&lt;ThreadPool>) -> Self {") - while workers read this state from the steal/park loop holding no World borrow. event skipped: not a message between systems. resource-column skipped: a Resource belongs to one World and is resolved through the storage the pool's own dispatch runs beneath. system-scratch skipped: no system owns it; every system of a run is dispatched inside the pool (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"). scope-arena skipped: it lives for the pool, not for one scope. kernel-internal fits: the pool is the scheduler substrate of the SDK itself (the kernel's own dispatch bookkeeping), and its storage moves onto the memory library. Read on every wake. ADDRESS RETAINED: crates/boyko_threadpool/src/thread_pool.rs:203 "&self.workers[lane.wid as usize].thread as *const crate::sync::WakeHandle" is stored in ScopeShared for the scope's life, so the table must be address-stable for the pool's life - an inline array inside a PoolInner that itself sits in one VmReservation is. Form destination: inline [CachePadded&lt;WorkerHandle>; MAX_WORKERS] with crates/boyko_threadpool/src/thread_pool.rs:158 "pub(crate) worker_count: u32,"; entries written once after spawn (MaybeUninit), before the publish. Class: ADDRESS RETAINED: joiner_wake_target returns thread_pool.rs:203 "&self.workers[lane.wid as usize].thread as *const crate::sync::WakeHandle" and ScopeShared.joiner_wake keeps it for the scope life, so the destination must be address-stable for the pool life - an inline array inside an address-stable PoolInner is. Allocated at thread_pool.rs:753-757 (collect then Vec -> Arc&lt;[T]> copy). Readers: unpark paths, shutdown_and_join (thread_pool.rs:535 "for w in self.inner.workers.iter() {"), joiner_wake_target |
| crates/boyko_threadpool/src/thread_pool.rs:278 `let shared = Box::new(ScopeShared::new(` | PoolInner::install | `Box<T><ScopeShared (repr(C), CachePadded<AtomicUsize> first)>` | S | scope-arena | pool | NEW:ScopeSharedStack (per-thread LIFO of ScopeShared cells on a VmReservation; install/scope frames nest LIFO on a thread) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:325 "emplaced in the owner thread's `ScopeArena` at `mark`, rewound at join" (owns rung 1a; see plan_conflicts for the frame-local alternative) | [local; growth perframe; addr_cached yes; rung R5] ECS: component / dense-component / enable-state / relation skipped: a task cell or a scope's join state is not a datum about an entity and not an edge; it is a closure payload or a join counter that lives from spawn (or scope entry) to the join. event skipped: kernel event lanes are drained by systems at sync points, and systems are themselves dispatched as tasks of these scopes (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"), so the scope substrate sits below the event form rather than being an instance of it. resource-column skipped: not a durable table. system-scratch skipped: a system-owned ScratchColumn needs a ComponentId inside boyko_ecs, which the pool cannot name (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }"), and the outermost scope wraps every system of a run. scope-arena fits. The ScopeShared is scope state, allocated on every entry, spawning or not (the block is lazy, crates/boyko_threadpool/src/scope.rs:1105 "// Lazy: no chunk is allocated until the first spawn, so a scope"; this Box is not). The one stated reason it is on the heap is crates/boyko_threadpool/src/scope.rs:494 "/// Allocated on the heap so that every spawned task's payload cell can hold a" crates/boyko_threadpool/src/scope.rs:495 "raw pointer to it that remains stable across `Scope` moves" - and that reason does not survive a separate frame local: a ScopeShared declared as a local of this install/scope frame BEFORE the Scope, published by &raw const (never through a reference, so no protector covers it), does not move when the Scope moves, and the frame strictly outlives the scope (the scope is dropped - joined - inside this frame on both the normal and the unwinding path). Form destination proposed: that frame local (the scope-arena form's zero-storage limit: the opener's own stack IS the LIFO arena), which needs no arena identity at all and so is untouched by C2. The owning plan's destination is different and is recorded, not overridden: [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:325 "emplaced in the owner thread's `ScopeArena` at `mark`, rewound at join". Both remove the allocation; the frame local additionally keeps a deallocation Miri judges (a local's StorageDead), so the completion-protector gate keeps a free to red on - at a moved site, and it must be re-established RED-first there either way ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:467 "\| W1 \| The KE16 completion-protector Miri gate keys on a DEALLOCATION that rung 1a removes"); that Miri claim is a design expectation, not a run. Refutes ScopeSharedStack. Class: One heap Box per install frame. Address retained: Scope holds NonNull&lt;ScopeShared> (scope.rs:1038 "pub(crate) shared: NonNull&lt;ScopeShared>,") and workers receive *const ScopeShared through each ScopedCell. It cannot go into the scope's own ScopeBlock: free_all runs BEFORE the ScopeShared free (task/scoped.rs:135-136 "`free_all` precedes the `ScopeShared` free by placement"), and it must stay outside the &mut Scope protector (scoped.rs:120-125). Freed at scope.rs:1406 "unsafe { drop(Box::from_raw(raw)) };". Allocations per frame: not counted. RECOUNT: this Box is made on EVERY install/scope entry, including scopes that spawn nothing - the ScopeBlock is lazy (scope.rs:1105 "// Lazy: no chunk is allocated until the first spawn, so a scope") but the ScopeShared Box is not. Readers: opened per frame by schedule.rs:455 "pool.install(\|scope\| {", par_iter.rs:341 "pool.scope(\|scope\| {", par_chunk.rs:139, boyko_physics resources.rs:1957/:2049, solver/colored.rs:2867, soft/colored.rs:1122; read by every worker that runs a task of the scope (complete_task) and by Scope::drop (join) |
| crates/boyko_threadpool/src/thread_pool.rs:328 `let shared = Box::new(ScopeShared::new(` | PoolInner::scope | `Box<T><ScopeShared>` | S | scope-arena | pool | NEW:ScopeSharedStack (per-thread LIFO of ScopeShared cells on a VmReservation) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:325 "emplaced in the owner thread's `ScopeArena` at `mark`, rewound at join" (owns rung 1a; see plan_conflicts for the frame-local alternative) | [local; growth perframe; addr_cached yes; rung R5] ECS: component / dense-component / enable-state / relation skipped: a task cell or a scope's join state is not a datum about an entity and not an edge; it is a closure payload or a join counter that lives from spawn (or scope entry) to the join. event skipped: kernel event lanes are drained by systems at sync points, and systems are themselves dispatched as tasks of these scopes (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"), so the scope substrate sits below the event form rather than being an instance of it. resource-column skipped: not a durable table. system-scratch skipped: a system-owned ScratchColumn needs a ComponentId inside boyko_ecs, which the pool cannot name (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }"), and the outermost scope wraps every system of a run. scope-arena fits. The ScopeShared is scope state, allocated on every entry, spawning or not (the block is lazy, crates/boyko_threadpool/src/scope.rs:1105 "// Lazy: no chunk is allocated until the first spawn, so a scope"; this Box is not). The one stated reason it is on the heap is crates/boyko_threadpool/src/scope.rs:494 "/// Allocated on the heap so that every spawned task's payload cell can hold a" crates/boyko_threadpool/src/scope.rs:495 "raw pointer to it that remains stable across `Scope` moves" - and that reason does not survive a separate frame local: a ScopeShared declared as a local of this install/scope frame BEFORE the Scope, published by &raw const (never through a reference, so no protector covers it), does not move when the Scope moves, and the frame strictly outlives the scope (the scope is dropped - joined - inside this frame on both the normal and the unwinding path). Form destination proposed: that frame local (the scope-arena form's zero-storage limit: the opener's own stack IS the LIFO arena), which needs no arena identity at all and so is untouched by C2. The owning plan's destination is different and is recorded, not overridden: [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:325 "emplaced in the owner thread's `ScopeArena` at `mark`, rewound at join". Both remove the allocation; the frame local additionally keeps a deallocation Miri judges (a local's StorageDead), so the completion-protector gate keeps a free to red on - at a moved site, and it must be re-established RED-first there either way ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:467 "\| W1 \| The KE16 completion-protector Miri gate keys on a DEALLOCATION that rung 1a removes"); that Miri claim is a design expectation, not a run. Refutes ScopeSharedStack. Class: Same allocation class as thread_pool.rs:278, on the nested-scope entry (par_iter, physics colored solve). One per scope. RECOUNT: this Box is made on EVERY install/scope entry, including scopes that spawn nothing - the ScopeBlock is lazy (scope.rs:1105 "// Lazy: no chunk is allocated until the first spawn, so a scope") but the ScopeShared Box is not. Readers: opened per frame by schedule.rs:455 "pool.install(\|scope\| {", par_iter.rs:341 "pool.scope(\|scope\| {", par_chunk.rs:139, boyko_physics resources.rs:1957/:2049, solver/colored.rs:2867, soft/colored.rs:1122; read by every worker that runs a task of the scope (complete_task) and by Scope::drop (join) |
| crates/boyko_threadpool/src/thread_pool.rs:401 `pub(crate) inner: Arc<PoolInner>,` | ThreadPool::inner | `Arc<PoolInner (repr(C))>` | B | kernel-internal | pool | VmReservation-raw | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [field; growth once; addr_cached yes; rung R5] ECS: component / dense-component / enable-state / relation skipped: those are World storage, and the pool sits BELOW every World - boyko_ecs depends on it (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }") and one pool can be handed to several Apps (crates/boyko_ecs/src/ecs/core/app/app.rs:214 "pub fn with_pool(pool: Arc&lt;ThreadPool>) -> Self {") - while workers read this state from the steal/park loop holding no World borrow. event skipped: not a message between systems. resource-column skipped: a Resource belongs to one World and is resolved through the storage the pool's own dispatch runs beneath. system-scratch skipped: no system owns it; every system of a run is dispatched inside the pool (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"). scope-arena skipped: it lives for the pool, not for one scope. kernel-internal fits: the pool is the scheduler substrate of the SDK itself (the kernel's own dispatch bookkeeping), and its storage moves onto the memory library. ADDRESS RETAINED (crates/boyko_threadpool/src/worker.rs:59 "tls::swap_active_pool(Arc::as_ptr(&inner));"). Form destination: PoolInner in ONE VmReservation (address-stable, released after ThreadPool::drop has joined every worker, which it already does), workers holding NonNull&lt;PoolInner>; the refcount is replaced by the join-before-release discipline. The Arc&lt;ThreadPool> return is public API (App::with_pool, crates/boyko_ecs/src/ecs/core/app/app.rs:202 "Self::with_pool(ThreadPoolBuilder::new().build())"), so this is the API change the plan lists, not only a storage change. Class: Allocated at thread_pool.rs:764 "let inner = Arc::new(PoolInner {". ADDRESS RETAINED: worker.rs:59 "tls::swap_active_pool(Arc::as_ptr(&inner));" and ThreadPool::current_pool hands out NonNull&lt;PoolInner>. The refcount is replaceable by the join-before-free discipline ThreadPool::drop already enforces (shutdown_and_join joins every worker before the handle drops). PoolInner embeds crossbeam Injector (T). Readers: every worker (worker_main owns a clone), every Scope (&PoolInner), TLS ACTIVE_POOL raw pointer |
| crates/boyko_threadpool/src/thread_pool.rs:412 `join_handles: std::sync::Mutex<Option<Vec<WorkerJoin>>>,` | ThreadPool::join_handles | `Vec<WorkerJoin { handle: JoinHandle<()> }>` | B | kernel-internal | pool | NEW:BootArray&lt;T, MAX_WORKERS> (inline, len written once) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [field; growth once; addr_cached no; rung R5] ECS: component / dense-component / enable-state / relation skipped: those are World storage, and the pool sits BELOW every World - boyko_ecs depends on it (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }") and one pool can be handed to several Apps (crates/boyko_ecs/src/ecs/core/app/app.rs:214 "pub fn with_pool(pool: Arc&lt;ThreadPool>) -> Self {") - while workers read this state from the steal/park loop holding no World borrow. event skipped: not a message between systems. resource-column skipped: a Resource belongs to one World and is resolved through the storage the pool's own dispatch runs beneath. system-scratch skipped: no system owns it; every system of a run is dispatched inside the pool (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"). scope-arena skipped: it lives for the pool, not for one scope. kernel-internal fits: the pool is the scheduler substrate of the SDK itself (the kernel's own dispatch bookkeeping), and its storage moves onto the memory library. Teardown-only. Form destination: an inline array of MAX_WORKERS JoinHandle slots in the pool object, taken at join; the Mutex&lt;Option&lt;..>> guard becomes a once-flag. JoinHandle's own Arc&lt;Packet> is std's (os-thread tag). Class: Vec collected at thread_pool.rs:759-762. JoinHandle holds std's Arc&lt;Packet> (T). Readers: ThreadPool::join / ThreadPool::drop only (teardown) |
| crates/boyko_threadpool/src/thread_pool.rs:529 `fn shutdown_and_join(&self, handles: Vec<WorkerJoin>) {` | ThreadPool::shutdown_and_join | `Vec<WorkerJoin>` | B | kernel-internal | pool | NEW:BootArray&lt;T, MAX_WORKERS> (inline, len written once) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [param; growth once; addr_cached no; rung R5] ECS: component / dense-component / enable-state / relation skipped: those are World storage, and the pool sits BELOW every World - boyko_ecs depends on it (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }") and one pool can be handed to several Apps (crates/boyko_ecs/src/ecs/core/app/app.rs:214 "pub fn with_pool(pool: Arc&lt;ThreadPool>) -> Self {") - while workers read this state from the steal/park loop holding no World borrow. event skipped: not a message between systems. resource-column skipped: a Resource belongs to one World and is resolved through the storage the pool's own dispatch runs beneath. system-scratch skipped: no system owns it; every system of a run is dispatched inside the pool (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"). scope-arena skipped: it lives for the pool, not for one scope. kernel-internal fits: the pool is the scheduler substrate of the SDK itself (the kernel's own dispatch bookkeeping), and its storage moves onto the memory library. Teardown-only. Form destination: an inline array of MAX_WORKERS JoinHandle slots in the pool object, taken at join; the Mutex&lt;Option&lt;..>> guard becomes a once-flag. JoinHandle's own Arc&lt;Packet> is std's (os-thread tag). Class: Carries the Vec of row thread_pool.rs:412; not a separate allocation. Readers: ThreadPool::join / Drop |
| crates/boyko_threadpool/src/thread_pool.rs:601 `thread_name_prefix: Option<String>,` | ThreadPoolBuilder::thread_name_prefix | `String<u8 (UTF-8 thread-name prefix)>` | B | kernel-internal | pool | NEW:InlineStr&lt;N> (fixed-capacity inline UTF-8; boyko_log::record::DspBuf&lt;N> already has this shape) | none - no plan decides this site | [field; growth once; addr_cached no; rung R5] ECS: Pool configuration, boot-only; one line: no caller passes a dynamic prefix, so the field becomes Option&lt;&'static str> and the conversion disappears (no InlineStr primitive needed). Class: Builder-only. The per-worker name still has to become a String because std::thread::Builder::name takes String (row :724). Readers: ThreadPoolBuilder::build (thread_pool.rs:716-718) |
| crates/boyko_threadpool/src/thread_pool.rs:649 `pub fn thread_name_prefix(mut self, s: impl Into<String>) -> Self {` | ThreadPoolBuilder::thread_name_prefix | `String<u8>` | B | kernel-internal | pool | NEW:InlineStr&lt;N> (fixed-capacity inline UTF-8) | none - no plan decides this site | [param; growth once; addr_cached no; rung R5] ECS: Pool configuration, boot-only; one line: no caller passes a dynamic prefix, so the field becomes Option&lt;&'static str> and the conversion disappears (no InlineStr primitive needed). Class: The conversion allocates at thread_pool.rs:650 "self.thread_name_prefix = Some(s.into());". Readers: no caller anywhere in the tree: rg "thread_name_prefix(" over crates/ finds only this definition |
| crates/boyko_threadpool/src/thread_pool.rs:664 `pub fn build(self) -> Arc<ThreadPool> {` | ThreadPoolBuilder::build | `Arc<ThreadPool (repr(C))>` | B | kernel-internal | pool | VmReservation-raw | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [return; growth once; addr_cached no; rung R5] ECS: component / dense-component / enable-state / relation skipped: those are World storage, and the pool sits BELOW every World - boyko_ecs depends on it (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }") and one pool can be handed to several Apps (crates/boyko_ecs/src/ecs/core/app/app.rs:214 "pub fn with_pool(pool: Arc&lt;ThreadPool>) -> Self {") - while workers read this state from the steal/park loop holding no World borrow. event skipped: not a message between systems. resource-column skipped: a Resource belongs to one World and is resolved through the storage the pool's own dispatch runs beneath. system-scratch skipped: no system owns it; every system of a run is dispatched inside the pool (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"). scope-arena skipped: it lives for the pool, not for one scope. kernel-internal fits: the pool is the scheduler substrate of the SDK itself (the kernel's own dispatch bookkeeping), and its storage moves onto the memory library. ADDRESS RETAINED (crates/boyko_threadpool/src/worker.rs:59 "tls::swap_active_pool(Arc::as_ptr(&inner));"). Form destination: PoolInner in ONE VmReservation (address-stable, released after ThreadPool::drop has joined every worker, which it already does), workers holding NonNull&lt;PoolInner>; the refcount is replaced by the join-before-release discipline. The Arc&lt;ThreadPool> return is public API (App::with_pool, crates/boyko_ecs/src/ecs/core/app/app.rs:202 "Self::with_pool(ThreadPoolBuilder::new().build())"), so this is the API change the plan lists, not only a storage change. Class: Allocated at thread_pool.rs:792 "Arc::new(ThreadPool {". Shared ownership is part of the public API (App::with_pool takes Arc&lt;ThreadPool>), so replacing it changes the API, not just the storage. Readers: boyko_ecs App::new builds the pool once (boyko_ecs app.rs:202 "Self::with_pool(ThreadPoolBuilder::new().build())"); the Arc&lt;ThreadPool> is held by App/Schedule owners |
| crates/boyko_threadpool/src/thread_pool.rs:672 `let mut deques: Vec<Worker<Task>> = Vec::with_capacity(worker_count);` | ThreadPoolBuilder::build | `Vec<Worker<Task>>` | B | kernel-internal | pool | NEW:BootArray&lt;T, MAX_WORKERS> (inline, stack-local) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [local; growth once; addr_cached no; rung R5] ECS: Boot-only build temporary; one line: with PoolInner reserved BEFORE the spawn loop, each value is written straight into its inline PoolInner slot (or a stack array of MAX_WORKERS) - no storage of its own and no BootArray primitive. Class: Temp; freed at the end of build. Readers: consumed by deques.into_iter() at thread_pool.rs:721 |
| crates/boyko_threadpool/src/thread_pool.rs:673 `let mut stealers: Vec<Stealer<Task>> = Vec::with_capacity(worker_count);` | ThreadPoolBuilder::build | `Vec<Stealer<Task>>` | B | kernel-internal | pool | NEW:BootArray&lt;T, MAX_WORKERS> (inline, stack-local) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [local; growth once; addr_cached no; rung R5] ECS: Boot-only build temporary; one line: with PoolInner reserved BEFORE the spawn loop, each value is written straight into its inline PoolInner slot (or a stack array of MAX_WORKERS) - no storage of its own and no BootArray primitive. Class: Temp; the copy at :686 is a second allocation (row thread_pool.rs:132). Readers: copied into Arc&lt;[Stealer&lt;Task>]> at thread_pool.rs:686 |
| crates/boyko_threadpool/src/thread_pool.rs:682 `let w: Worker<Task> = Worker::new_fifo();` | ThreadPoolBuilder::build -> worker_main's owned deque | `other:crossbeam_deque::Worker buffer<Task>` | T | kernel-internal | pool | out-of-scope:crossbeam-deque internal; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:328 "**in-house bounded Chase-Lev** per lane on a `Table<Task>` ring in the pool's `TableSet`" | [local; growth perframe; addr_cached yes; rung R5] ECS: Pool dispatch substrate; one line: crossbeam's Worker buffer doubles/halves and retires through crossbeam-epoch; replaced by a fixed-capacity per-lane Chase-Lev ring on the pool's VM (KF-inhouse-lanes). The retained deque address (TLS) stays valid because the ring lives in the address-stable pool reservation. Class: Buffer::alloc (deque.rs:60-68) is a Box&lt;[MaybeUninit&lt;Task>]> of MIN_CAP = 64; push doubles it when full (deque.rs:427 "self.resize(2 * buffer.cap);"), pop halves it when len &lt; cap/4 (deque.rs:497 / :551), and every resize retires the old buffer through crossbeam-epoch (deque.rs:333 "guard.defer_unchecked(...)"), whose deferred bag can itself allocate. A frame that queues > 64 tasks on one worker therefore allocates and frees buffers; how often that happens: not counted. The Stealer handles (row thread_pool.rs:132) share this deque's Arc&lt;CachePadded&lt;Inner>>, allocated here too. RECOUNT: addr_cached corrected no -> yes. The Worker HANDLE's address is retained for the worker's life: worker.rs:69 "let _deque_deposit = tls::WorkerDequeDeposit::new(Arc::as_ptr(&inner), &raw const deque);" stores it in TLS, tls.rs:134 "deque: *const Worker&lt;Task>,". The element buffer moves on resize (crossbeam handles that), but any in-house replacement must keep the handle itself address-stable. Readers: owner pushes (worker.rs:713 "lane.deque().push(task);"), sibling steals through Stealer (worker.rs:347) |
| crates/boyko_threadpool/src/thread_pool.rs:688 `let mut injector_local_vec: Vec<CachePadded<Injector<Task>>> =` | ThreadPoolBuilder::build | `Vec<CachePadded<Injector<Task>>>` | B | kernel-internal | pool | out-of-scope:dead storage - delete, do not migrate (thread_pool.rs:116 "`worker_id`'s private inbox. **Nothing feeds it and nothing drains it.**") | none - no plan decides the dead field; the field's own doc defers it (crates/boyko_threadpool/src/thread_pool.rs:116 "**Nothing feeds it and nothing drains it.**") | [local; growth once; addr_cached no; rung R5] ECS: component / dense-component / enable-state / relation skipped: those are World storage, and the pool sits BELOW every World - boyko_ecs depends on it (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }") and one pool can be handed to several Apps (crates/boyko_ecs/src/ecs/core/app/app.rs:214 "pub fn with_pool(pool: Arc&lt;ThreadPool>) -> Self {") - while workers read this state from the steal/park loop holding no World borrow. event skipped: not a message between systems. resource-column skipped: a Resource belongs to one World and is resolved through the storage the pool's own dispatch runs beneath. system-scratch skipped: no system owns it; every system of a run is dispatched inside the pool (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"). scope-arena skipped: it lives for the pool, not for one scope. kernel-internal fits: the pool is the scheduler substrate of the SDK itself (the kernel's own dispatch bookkeeping), and its storage moves onto the memory library. Form destination: DELETE, not migrate: crates/boyko_threadpool/src/thread_pool.rs:116 "**Nothing feeds it and nothing drains it.**" Each element also allocates a crossbeam Block at construction, so deleting it removes worker_count boot allocations. Class: Temp for the dead injector_local field (row thread_pool.rs:128); deleting that field deletes this too. Statement continues on :689 "Vec::with_capacity(worker_count);". RECOUNT: destination was "VmReservation-raw"; the row's own evidence says the storage is dead, so a migration target is the wrong ledger entry. Readers: copied into Arc&lt;[..]> at thread_pool.rs:693 |
| crates/boyko_threadpool/src/thread_pool.rs:709 `let bootstrap: Arc<std::sync::Mutex<Option<Arc<PoolInner>>>> =` | ThreadPoolBuilder::build (bootstrap handshake) | `Arc<Mutex<Option<Arc<PoolInner>>>>` | B | kernel-internal | pool | VmReservation-raw | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [local; growth once; addr_cached no; rung R5] ECS: Boot handshake; one line: it exists because crates/boyko_threadpool/src/thread_pool.rs:695 "// Workers are created lazily — we need their `Thread` handles to" ... crates/boyko_threadpool/src/thread_pool.rs:706 "// Alternative considered: pre-create `WorkerHandle`s with bogus". With PoolInner at a reserved address before spawning, each worker receives NonNull&lt;PoolInner> as a spawn argument and waits (park) until the parent publishes its slot through one atomic flag and unparks it via the JoinHandle's Thread - the Arc&lt;Mutex&lt;Option&lt;Arc&lt;..>>>> and Arc&lt;Condvar> are deleted. Class: Arc::new at thread_pool.rs:710. The inner Arc&lt;PoolInner> named on this line is the allocation of row thread_pool.rs:401, not a second one. Dropped once every worker has read it. Readers: every worker thread before worker_main (thread_pool.rs:735-743) |
| crates/boyko_threadpool/src/thread_pool.rs:711 `let bootstrap_cvar: Arc<std::sync::Condvar> = Arc::new(std::sync::Condvar::new());` | ThreadPoolBuilder::build (bootstrap handshake) | `Arc<std::sync::Condvar>` | B | kernel-internal | pool | VmReservation-raw | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [local; growth once; addr_cached no; rung R5] ECS: Boot handshake; one line: it exists because crates/boyko_threadpool/src/thread_pool.rs:695 "// Workers are created lazily — we need their `Thread` handles to" ... crates/boyko_threadpool/src/thread_pool.rs:706 "// Alternative considered: pre-create `WorkerHandle`s with bogus". With PoolInner at a reserved address before spawning, each worker receives NonNull&lt;PoolInner> as a spawn argument and waits (park) until the parent publishes its slot through one atomic flag and unparks it via the JoinHandle's Thread - the Arc&lt;Mutex&lt;Option&lt;Arc&lt;..>>>> and Arc&lt;Condvar> are deleted. Class: Boot handshake only. Readers: every worker thread before worker_main |
| crates/boyko_threadpool/src/thread_pool.rs:713 `let mut handles: Vec<JoinHandle<()>> = Vec::with_capacity(worker_count);` | ThreadPoolBuilder::build | `Vec<JoinHandle<()>>` | B | kernel-internal | pool | NEW:BootArray&lt;T, MAX_WORKERS> (inline, stack-local) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [local; growth once; addr_cached no; rung R5] ECS: Boot-only build temporary; one line: with PoolInner reserved BEFORE the spawn loop, each value is written straight into its inline PoolInner slot (or a stack array of MAX_WORKERS) - no storage of its own and no BootArray primitive. Class: Temp. Readers: mapped into WorkerJoin at thread_pool.rs:759-762 |
| crates/boyko_threadpool/src/thread_pool.rs:714 `let mut thread_handles: Vec<Thread> = Vec::with_capacity(worker_count);` | ThreadPoolBuilder::build | `Vec<std::thread::Thread>` | B | kernel-internal | pool | NEW:BootArray&lt;T, MAX_WORKERS> (inline, stack-local) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [local; growth once; addr_cached no; rung R5] ECS: Boot-only build temporary; one line: with PoolInner reserved BEFORE the spawn loop, each value is written straight into its inline PoolInner slot (or a stack array of MAX_WORKERS) - no storage of its own and no BootArray primitive. Class: Temp. join.thread().clone() at :749 is an Arc refcount bump, not an allocation. Readers: mapped into WorkerHandle at thread_pool.rs:753-757 |
| crates/boyko_threadpool/src/thread_pool.rs:718 `.unwrap_or_else(\|\| "boyko-worker".to_string());` | ThreadPoolBuilder::build | `String<u8>` | B | kernel-internal | pool | NEW:InlineStr&lt;N> (fixed-capacity inline UTF-8) | none - no plan decides this site | [local; growth once; addr_cached no; rung R5] ECS: Boot-only; one line: the default becomes a &'static str, so this String disappears; the name String std needs is row :724. Class: Only when no prefix was configured (the default App::new path, app.rs:202). Readers: format! at thread_pool.rs:724 |
| crates/boyko_threadpool/src/thread_pool.rs:724 `let name = format!("{name_prefix}-{worker_id}");` | ThreadPoolBuilder::build | `String<u8>` | B | out-of-scope:os-owned | none | out-of-scope:std::thread::Builder::name takes an owned String and std keeps it in the Thread; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:46 "\| `os-thread` \| `std::thread::spawn` per worker" | [local; growth once; addr_cached no; rung OOS] ECS: std::thread::Builder::name takes an owned String that std keeps in the Thread; one line: covered by the os-thread tag (boot, worker_count times). Class: worker_count allocations at boot. Could be skipped only by not naming threads. Readers: std::thread::Builder::name (thread_pool.rs:726) |
| crates/boyko_threadpool/src/thread_pool.rs:732 `.spawn(move \|\| {` | ThreadPoolBuilder::build | `other:std::thread internals<boxed thread main closure, Arc<Packet<()>>, Thread Arc<Inner>; thread stack is OS-owned>` | T | out-of-scope:os-owned | none | out-of-scope:std::thread internals; a #[global_allocator] WOULD cover it; a per-type replacement would NOT (the allocation is made inside code we do not own); the stack itself is X-os (OS owns it) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:46 "\| `os-thread` \| `std::thread::spawn` per worker" | [local; growth once; addr_cached no; rung OOS] ECS: std::thread::spawn internals (Packet, Thread, boxed main) at boot; the OS owns the stack. One line: accepted by the closed os-thread tag. Class: One spawn per worker (worker_count &lt;= MAX_WORKERS = 64, thread_pool.rs:52). Boot only. Readers: std::thread::Builder::spawn, once per worker at pool build |
| crates/boyko_threadpool/src/thread_pool.rs:756 `.collect::<Vec<_>>()` | ThreadPoolBuilder::build | `Vec<CachePadded<WorkerHandle>>` | B | kernel-internal | pool | NEW:BootArray&lt;T, MAX_WORKERS> (inline, stack-local) | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [local; growth once; addr_cached no; rung R5] ECS: Boot-only build temporary; one line: with PoolInner reserved BEFORE the spawn loop, each value is written straight into its inline PoolInner slot (or a stack array of MAX_WORKERS) - no storage of its own and no BootArray primitive. Class: Temp; the Arc&lt;[T]> copy is a second allocation. Readers: converted by .into() at thread_pool.rs:757 into Arc&lt;[..]> (row thread_pool.rs:135) |
| crates/boyko_threadpool/src/tls.rs:169 `pub(crate) static ACTIVE_POOL: Cell<*const PoolInner> = const { Cell::new(ptr::null()) };` | tls::ACTIVE_POOL (thread_local!) | `other:thread_local! os-key Value<T> (std AlignedSystemBox, System.alloc)<Cell<*const PoolInner>>` | T | kernel-internal | none | NEW:engine-owned per-thread context slot (KF-45 Engine thread-context column (replaces every thread_local!)) - removing the thread_local! is the only way to remove this allocation: std calls System.alloc directly, so neither a #[global_allocator] nor a per-type replacement reaches it | main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built." [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read.; joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call" | [static; growth once; addr_cached yes; rung R5] ECS: CHOSEN kernel-internal under ONE decision for every thread_local! in the engine (TLS-1): the value becomes a field of the engine thread-context record (KF-45 Engine thread-context column (replaces every thread_local!)), reached WITHOUT thread_local! - by an explicit context parameter wherever the call chain already carries one (worker loop -> task trampoline -> system run; the physics decisions already chose this for the gang nesting depth: main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built."), and otherwise through the thread-context slot. ROUTE FOR THIS STATIC: the pool pointer rides in the task/scope context the trampoline passes (RunCtx); ThreadPool::install writes it into the calling thread's context record for the frame and restores it on exit. WHY THIS IS ALSO THE FASTER PATH: a passed context is a register; a slot read is one plain load of the platform thread-control-block pointer (TEB on Windows x64, TCB on Linux x86_64) plus a probe of a 64-entry table in one or two L1 lines - no locked RMW, no fence, no Win32 call, no allocation (reasoned, not measured) - against the measured windows-gnu cost of two contended RMWs + FlsSetValue per thread_local! read and one System allocation per thread per key. SKIPPED: component / dense-component / enable-state / relation - a per-THREAD value, and threads are not entities (the pool and the diag/log crates sit below every World); event - a state word, not a message; resource-column - per thread, not a world singleton, and boyko_threadpool/boyko_diag/boyko_log sit below boyko_ecs; system-scratch - outlives any one system run; scope-arena - outlives any one scope. OVERTURNED BY: an A/B of the ke16_nested_scope cell worker/body_1us_tasks_64W on x86_64-pc-windows-msvc (native TLS) in which the explicit-context build is slower beyond its band - msvc/Linux is the only place the old form could win. [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read. Class: [rev2 GAP 1] One System-allocated Value&lt;T> per THREAD that touches the key, freed at thread exit, on the owner's windows-gnu toolchain. std itself says rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/guard/windows.rs:116 "// `#[thread_local]` is unavailable on windows-gnu (`target_thread_local` is off),", so thread_local! resolves to the os-key backend (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/mod.rs:44 "mod os;"), whose try_initialize boxes the value (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:194 "let value = AlignedSystemBox::&lt;T, ALIGN>::new(Value {") through rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:110 "let ptr: *mut Value&lt;T> = (unsafe { System.alloc(layout) }).cast();" - System directly, bypassing any #[global_allocator]. A const initialiser does not avoid it (joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:91 "backend, whose key always has a dtor — `const {}` initialisation does **not** help — and (ii) `enable`'s"). Every read also pays guard::enable: joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call"; the file sizes it at joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:195 "978.7 − 110 = 868.7 µs for TWO reads, i.e. ≈ 434 µs per read per wave (1,024 tasks ⇒ ≈ 424 ns per". On native-TLS targets (x86_64-pc-windows-msvc, Linux) the same const-initialised cell allocates nothing and reads lock-free. CENSUS NOTE: a #[global_allocator] counting gate cannot see this cell - std calls System.alloc directly - so the allocation census must count thread_local! cells from this row (a static census of thread_local! statics), never from its counter; the counter reads 0 for them by construction. Readers: swap_active_pool / active_pool_ptr / try_with_active_pool (tls.rs:483, :493, :505): ThreadPool::install entry/exit, worker_main entry, every ambient par_iter dispatch |
| crates/boyko_threadpool/src/tls.rs:196 `pub(crate) static LANE_DEPOSIT: Cell<LaneDeposit>` | tls::LANE_DEPOSIT (thread_local!) | `other:thread_local! os-key Value<T> (std AlignedSystemBox, System.alloc)<Cell<LaneDeposit> ({pool, deque, wid}, 2 pointers + 8 B)>` | T | kernel-internal | none | NEW:engine-owned per-thread context slot (KF-45 Engine thread-context column (replaces every thread_local!)) - removing the thread_local! is the only way to remove this allocation: std calls System.alloc directly, so neither a #[global_allocator] nor a per-type replacement reaches it | main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built." [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read.; joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call" | [static; growth once; addr_cached yes; rung R5] ECS: CHOSEN kernel-internal under ONE decision for every thread_local! in the engine (TLS-1): the value becomes a field of the engine thread-context record (KF-45 Engine thread-context column (replaces every thread_local!)), reached WITHOUT thread_local! - by an explicit context parameter wherever the call chain already carries one (worker loop -> task trampoline -> system run; the physics decisions already chose this for the gang nesting depth: main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built."), and otherwise through the thread-context slot. ROUTE FOR THIS STATIC: worker_main owns its context record {pool, deque, wid}; every task body receives it by parameter through the trampoline, so worker_lane_for reads the passed record instead of the FLS slot. WHY THIS IS ALSO THE FASTER PATH: a passed context is a register; a slot read is one plain load of the platform thread-control-block pointer (TEB on Windows x64, TCB on Linux x86_64) plus a probe of a 64-entry table in one or two L1 lines - no locked RMW, no fence, no Win32 call, no allocation (reasoned, not measured) - against the measured windows-gnu cost of two contended RMWs + FlsSetValue per thread_local! read and one System allocation per thread per key. SKIPPED: component / dense-component / enable-state / relation - a per-THREAD value, and threads are not entities (the pool and the diag/log crates sit below every World); event - a state word, not a message; resource-column - per thread, not a world singleton, and boyko_threadpool/boyko_diag/boyko_log sit below boyko_ecs; system-scratch - outlives any one system run; scope-arena - outlives any one scope. OVERTURNED BY: an A/B of the ke16_nested_scope cell worker/body_1us_tasks_64W on x86_64-pc-windows-msvc (native TLS) in which the explicit-context build is slower beyond its band - msvc/Linux is the only place the old form could win. [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read. Class: [rev2 GAP 1] One System-allocated Value&lt;T> per THREAD that touches the key, freed at thread exit, on the owner's windows-gnu toolchain. std itself says rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/guard/windows.rs:116 "// `#[thread_local]` is unavailable on windows-gnu (`target_thread_local` is off),", so thread_local! resolves to the os-key backend (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/mod.rs:44 "mod os;"), whose try_initialize boxes the value (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:194 "let value = AlignedSystemBox::&lt;T, ALIGN>::new(Value {") through rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:110 "let ptr: *mut Value&lt;T> = (unsafe { System.alloc(layout) }).cast();" - System directly, bypassing any #[global_allocator]. A const initialiser does not avoid it (joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:91 "backend, whose key always has a dtor — `const {}` initialisation does **not** help — and (ii) `enable`'s"). Every read also pays guard::enable: joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call"; the file sizes it at joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:195 "978.7 − 110 = 868.7 µs for TWO reads, i.e. ≈ 434 µs per read per wave (1,024 tasks ⇒ ≈ 424 ns per". On native-TLS targets (x86_64-pc-windows-msvc, Linux) the same const-initialised cell allocates nothing and reads lock-free. CENSUS NOTE: a #[global_allocator] counting gate cannot see this cell - std calls System.alloc directly - so the allocation census must count thread_local! cells from this row (a static census of thread_local! statics), never from its counter; the counter reads 0 for them by construction. Readers: lane_deposit (tls.rs:216) -> worker_lane_for, the ONE identity predicate every spawn asks; WorkerDequeDeposit::new/drop (tls.rs:360, :379); set_/clear_current_worker_id (tls.rs:439, :471) |
| crates/boyko_threadpool/src/tls.rs:205 `pub(crate) static IN_SYSTEM_RUN: Cell<u32> = const { Cell::new(0) };` | tls::IN_SYSTEM_RUN (thread_local!) | `other:thread_local! os-key Value<T> (std AlignedSystemBox, System.alloc)<Cell<u32> (nesting depth)>` | T | kernel-internal | none | NEW:engine-owned per-thread context slot (KF-45 Engine thread-context column (replaces every thread_local!)) - removing the thread_local! is the only way to remove this allocation: std calls System.alloc directly, so neither a #[global_allocator] nor a per-type replacement reaches it | main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built." [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read.; joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call" | [static; growth once; addr_cached yes; rung R5] ECS: CHOSEN kernel-internal under ONE decision for every thread_local! in the engine (TLS-1): the value becomes a field of the engine thread-context record (KF-45 Engine thread-context column (replaces every thread_local!)), reached WITHOUT thread_local! - by an explicit context parameter wherever the call chain already carries one (worker loop -> task trampoline -> system run; the physics decisions already chose this for the gang nesting depth: main:docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md:89 "\| ~~RunCtx trampoline vs TLS-depth fallback (gang nesting rule)~~ \| **Superseded by design rev 3** (P-§10.1): lanes are tickets on a per-pool `LaneBoard` that only `worker_main`'s top level probes. No `run_task` site carries a run context, so neither mechanism is built."), and otherwise through the thread-context slot. ROUTE FOR THIS STATIC: a u32 depth field of the thread-context record, bumped by the run-system guard through the context the executor passes to the system run. WHY THIS IS ALSO THE FASTER PATH: a passed context is a register; a slot read is one plain load of the platform thread-control-block pointer (TEB on Windows x64, TCB on Linux x86_64) plus a probe of a 64-entry table in one or two L1 lines - no locked RMW, no fence, no Win32 call, no allocation (reasoned, not measured) - against the measured windows-gnu cost of two contended RMWs + FlsSetValue per thread_local! read and one System allocation per thread per key. SKIPPED: component / dense-component / enable-state / relation - a per-THREAD value, and threads are not entities (the pool and the diag/log crates sit below every World); event - a state word, not a message; resource-column - per thread, not a world singleton, and boyko_threadpool/boyko_diag/boyko_log sit below boyko_ecs; system-scratch - outlives any one system run; scope-arena - outlives any one scope. OVERTURNED BY: an A/B of the ke16_nested_scope cell worker/body_1us_tasks_64W on x86_64-pc-windows-msvc (native TLS) in which the explicit-context build is slower beyond its band - msvc/Linux is the only place the old form could win. [rev2 writer correction] The decisions-file row cited above was superseded in place after gap 1 cited it: the gang uses a per-pool LaneBoard and no run_task site carries a run context. So the RunCtx half of the KF-45 route does not exist; a static here takes the slot route (thread-control-block pointer -> id->slot table) unless its own call chain already passes an explicit context. The performance comparison against thread_local! is unchanged: one plain load plus an L1 probe against two contended RMWs plus FlsSetValue per read. Class: [rev2 GAP 1] One System-allocated Value&lt;T> per THREAD that touches the key, freed at thread exit, on the owner's windows-gnu toolchain. std itself says rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/guard/windows.rs:116 "// `#[thread_local]` is unavailable on windows-gnu (`target_thread_local` is off),", so thread_local! resolves to the os-key backend (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/mod.rs:44 "mod os;"), whose try_initialize boxes the value (rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:194 "let value = AlignedSystemBox::&lt;T, ALIGN>::new(Value {") through rust-src(stable-x86_64-pc-windows-gnu):library/std/src/sys/thread_local/os.rs:110 "let ptr: *mut Value&lt;T> = (unsafe { System.alloc(layout) }).cast();" - System directly, bypassing any #[global_allocator]. A const initialiser does not avoid it (joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:91 "backend, whose key always has a dtor — `const {}` initialisation does **not** help — and (ii) `enable`'s"). Every read also pays guard::enable: joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:1 "# rustc 1.98 on `x86_64-pc-windows-gnu`: every `thread_local!` read costs two contended RMWs and a Win32 call"; the file sizes it at joltab:docs/threadpool/RUSTC-198-WINDOWS-GNU-TLS.md:195 "978.7 − 110 = 868.7 µs for TWO reads, i.e. ≈ 434 µs per read per wave (1,024 tasks ⇒ ≈ 424 ns per". On native-TLS targets (x86_64-pc-windows-msvc, Linux) the same const-initialised cell allocates nothing and reads lock-free. CENSUS NOTE: a #[global_allocator] counting gate cannot see this cell - std calls System.alloc directly - so the allocation census must count thread_local! cells from this row (a static census of thread_local! statics), never from its counter; the counter reads 0 for them by construction. Readers: is_in_system_run (tls.rs:425) behind the ECS context-restricted debug_asserts; the run-system RAII guard enter/drop (tls.rs:556, :568) |
| crates/boyko_threadpool/src/worker.rs:43 `pub(crate) fn worker_main(inner: Arc<PoolInner>, worker_id: u32, deque: Worker<Task>) {` | worker::worker_main | `Arc<PoolInner>` | B | kernel-internal | pool | VmReservation-raw | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:356 "one `TableSet` per pool ("PoolTables"): workers, stealers, lane rings, `ScopeArena`s, `PoolInner`; workers hold `NonNull<PoolInner>` valid until `Drop` joins them"; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:352 "`App` owns the pool by value" | [param; growth once; addr_cached yes; rung R5] ECS: component / dense-component / enable-state / relation skipped: those are World storage, and the pool sits BELOW every World - boyko_ecs depends on it (crates/boyko_ecs/Cargo.toml:8 "boyko-threadpool = { path = "../boyko_threadpool" }") and one pool can be handed to several Apps (crates/boyko_ecs/src/ecs/core/app/app.rs:214 "pub fn with_pool(pool: Arc&lt;ThreadPool>) -> Self {") - while workers read this state from the steal/park loop holding no World borrow. event skipped: not a message between systems. resource-column skipped: a Resource belongs to one World and is resolved through the storage the pool's own dispatch runs beneath. system-scratch skipped: no system owns it; every system of a run is dispatched inside the pool (crates/boyko_ecs/src/ecs/core/schedule/schedule.rs:455 "pool.install(\|scope\| {"). scope-arena skipped: it lives for the pool, not for one scope. kernel-internal fits: the pool is the scheduler substrate of the SDK itself (the kernel's own dispatch bookkeeping), and its storage moves onto the memory library. ADDRESS RETAINED (crates/boyko_threadpool/src/worker.rs:59 "tls::swap_active_pool(Arc::as_ptr(&inner));"). Form destination: PoolInner in ONE VmReservation (address-stable, released after ThreadPool::drop has joined every worker, which it already does), workers holding NonNull&lt;PoolInner>; the refcount is replaced by the join-before-release discipline. The Arc&lt;ThreadPool> return is public API (App::with_pool, crates/boyko_ecs/src/ecs/core/app/app.rs:202 "Self::with_pool(ThreadPoolBuilder::new().build())"), so this is the API change the plan lists, not only a storage change. Class: Carries a clone of the allocation of row thread_pool.rs:401 (refcount only). Its Arc::as_ptr goes into TLS (worker.rs:59). Readers: worker thread for its whole life |
| crates/boyko_threadpool/src/worker.rs:347 `if let Some(t) = drain_one(\|\| stealer.steal_batch_and_pop(local)) {` | worker::try_steal_random (sibling sweep); same mechanism at scope.rs:1747 in steal_one_random (joiner steal) and in Worker::resize | `other:crossbeam-epoch per-thread Local (+ std TLS destructor list)<crossbeam_epoch::internal::Local (one per thread that ever pins) and one (ptr, dtor) entry in std's per-thread DTORS Vec>` | T | kernel-internal | pool | out-of-scope:crossbeam-epoch internal. The Local is allocated with the global allocator, so a #[global_allocator] WOULD see it; a per-type replacement would NOT. The std TLS destructor entry is allocated with alloc::System explicitly, so NOT EVEN a #[global_allocator] sees that part - only replacing crossbeam-deque (the InjectorQueue candidate) removes both. | [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:328 "**in-house bounded Chase-Lev** per lane on a `Table<Task>` ring in the pool's `TableSet`" | [local; growth once; addr_cached yes; rung R5] ECS: Pool dispatch substrate; one line: the crossbeam-epoch per-thread Local (and std's TLS destructor entry, allocated with alloc::System and so invisible even to a #[global_allocator] deny gate) exists only because the steal path pins the epoch; the in-house lanes (KF-inhouse-lanes) remove the pin, and nothing else can. Class: Stealer::steal / steal_batch_and_pop pin the epoch (crossbeam-deque 0.8.8 deque.rs:669 and :1025 "let guard = &epoch::pin();"). The first pin on a thread initialises crossbeam-epoch's HANDLE thread_local (crossbeam-epoch 0.9.21 default.rs:34 "static HANDLE: LocalHandle = collector().register();"), whose register allocates the per-thread Local (internal.rs:338 "let local = Owned::new(Local {"), and the first pin in the process initialises the global Collector (default.rs:18 "COLLECTOR.get_or_init(Collector::new)"). LocalHandle needs Drop, so on std's native-TLS path its destructor is registered in DTORS, a Vec allocated with System (std sys/thread_local/destructors/list.rs:6 "static DTORS: RefCell&lt;Vec&lt;(*mut u8, unsafe extern "C" fn(*mut u8)), System>> ="). Whether windows-gnu takes the native or the OS-key TLS path was NOT determined (the target spec lives in the compiler; querying it needs rustc, which this workflow bars); on the OS-key path std instead boxes the value with the global allocator. The pool already avoids the pin for empty victims (worker.rs:341 "// The empty-victim gate: no epoch pin, hence no TLS, for a victim that"). Once per thread, so not a per-frame row; recorded because it is the one allocation in this group that a #[global_allocator] deny gate would be blind to. This row does not come from a regex hit; it was found by reading the steal path. Readers: every worker's sibling sweep (worker.rs:347), the external joiner (scope.rs:1747 "if let Some(t) = drain_one(\|\| inner.stealers[idx].steal()) {"), and any owner push/pop that resizes (crossbeam-deque deque.rs:323 "let guard = &epoch::pin();" inside resize) |
| crates/boyko_utils/src/sparse_map/sparse_map.rs:7 `sparse: Vec<Option<usize>>,` | SparseMap::sparse | `Vec<Option<usize> (16 B per external index)>` | K | kernel-internal | none | NEW:VmSparseMap&lt;U> (sparse half = reserve-once, zero-committed column of Copy entries) | docs/DENSE-COMPONENTS-PLAN.md:84 "4. SparseMap stays FLAT (architect call); page only if a future dense type uses sparse ids." (the flat layout is kept); docs/MEMORY-SYSTEM-AUDIT.md:95 "\| SparseMap&lt;U> (primitive: sparse layer) \|" - superseded by the owner's 2026-09-10 order; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:344 "\| `boyko_utils::SparseMap`/`SparseSlotMap` (3 `Vec`s each) \| `HeapVec` fields, `new_in(&mut Heap)` \| — \|" - CONFLICTS, see plan_conflicts | [field; growth highwater; addr_cached no; rung R2] ECS: component / dense-component / enable-state skipped: SparseMap is the container of kernel registries keyed by component id (docs/MEMORY-SYSTEM-AUDIT.md:102 "component types (bounded 512)") and by archetype id / block pattern (docs/MEMORY-SYSTEM-AUDIT.md:101 "archetypes (bounded 1024) + block patterns (bounded 256)") - not a datum an entity carries. The four entity-keyed users (docs/MEMORY-SYSTEM-AUDIT.md:118 "Only 4 sites are entity-keyed: DenseStore.e2s, EntityCloneMap, prefab node_of, observers by_entity") are ecs-storage-group rows whose own ECS forms (a component on the observed entity for observers/by_entity; system-scratch for the clone and prefab maps) take them off this type's user list - that decision is theirs, not this row's. relation skipped: id -> index lookups inside the kernel, not edges between entities. event skipped: durable. resource-column skipped: the remaining users (archetype registry, per-archetype pool bundle) are the registries that resolve where every Resource and component lives, so they cannot themselves be Resources. system-scratch / scope-arena skipped: durable, not per-run. kernel-internal fits: the ECS's own storage bookkeeping, which moves onto the memory library. Sparse half: VmColumn&lt;u32> holding dense index + 1, so a freshly committed (zero) page reads 'absent' and growth past the high water (crates/boyko_utils/src/sparse_map/sparse_map.rs:49 "self.sparse.resize(index + 1, None);") is a commit, not a fill - that is the ensure_len_zeroed op the plan already lists ([main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:406 "add `ensure_len_zeroed`, `pop`"), KF-vmcolumn-ensure-len-zeroed. Option&lt;usize> (16 B per key) becomes 4 B. The type moves from boyko_utils into boyko_ecs: every user is already there (crates/boyko_ecs/src/ecs/core/archetype/archetype_registry.rs:1 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/component/component_pool_bundle.rs:4 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/component/observers/entity_store.rs:40 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/clone/map.rs:13 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/clone/prefab.rs:457 "let mut node_of: boyko_utils::sparse_map::sparse_map::SparseMap&lt;u32> ="), so it needs no layering edge and no lifted crate. The kernel precedent for the migrated shape is DenseStore's slot->entity column, already a VmColumn (crates/boyko_ecs/src/ecs/core/component/dense/dense_store.rs:118 "s2e: VmColumn&lt;EntityId>,"). Refutes VmSparseMap / VmVec (primitives_refuted). Class: Grows to max external index + 1 (sparse_map.rs:49 "self.sparse.resize(index + 1, None);"), never shrinks. Class K because its dominant users are kernel bookkeeping; clone/map.rs and prefab.rs uses are per-operation (F-like) and archetype_registry is K. Option&lt;usize> is 16 B where a u32 sentinel would be 4 B. Readers: boyko_ecs archetype_registry.rs:11 "block_groups: SparseMap&lt;Vec&lt;(ArchetypeId, ArchetypeSignature)>>," and :24 "id_to_location: SparseMap&lt;(u8, usize)>,", component_pool_bundle.rs:14 "sparse_indexes: SparseMap&lt;InlandPoolId>,", observers/entity_store.rs:120 "by_entity: SparseMap&lt;u32>,", clone/map.rs:22 "sparse: SparseMap&lt;Entity>,", clone/prefab.rs:457 (function-local) |
| crates/boyko_utils/src/sparse_map/sparse_map.rs:10 `dense: Vec<U>,` | SparseMap::dense | `Vec<U (generic; NOT Copy at every user)>` | K | kernel-internal | none | NEW:VmSparseMap&lt;U> (dense half needs a growable, drop-aware, non-Copy column - VmColumn&lt;T: Copy> cannot hold it) | docs/DENSE-COMPONENTS-PLAN.md:84 "4. SparseMap stays FLAT (architect call); page only if a future dense type uses sparse ids." (the flat layout is kept); docs/MEMORY-SYSTEM-AUDIT.md:95 "\| SparseMap&lt;U> (primitive: sparse layer) \|" - superseded by the owner's 2026-09-10 order; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:344 "\| `boyko_utils::SparseMap`/`SparseSlotMap` (3 `Vec`s each) \| `HeapVec` fields, `new_in(&mut Heap)` \| — \|" - CONFLICTS, see plan_conflicts | [field; growth highwater; addr_cached no; rung R2] ECS: component / dense-component / enable-state skipped: SparseMap is the container of kernel registries keyed by component id (docs/MEMORY-SYSTEM-AUDIT.md:102 "component types (bounded 512)") and by archetype id / block pattern (docs/MEMORY-SYSTEM-AUDIT.md:101 "archetypes (bounded 1024) + block patterns (bounded 256)") - not a datum an entity carries. The four entity-keyed users (docs/MEMORY-SYSTEM-AUDIT.md:118 "Only 4 sites are entity-keyed: DenseStore.e2s, EntityCloneMap, prefab node_of, observers by_entity") are ecs-storage-group rows whose own ECS forms (a component on the observed entity for observers/by_entity; system-scratch for the clone and prefab maps) take them off this type's user list - that decision is theirs, not this row's. relation skipped: id -> index lookups inside the kernel, not edges between entities. event skipped: durable. resource-column skipped: the remaining users (archetype registry, per-archetype pool bundle) are the registries that resolve where every Resource and component lives, so they cannot themselves be Resources. system-scratch / scope-arena skipped: durable, not per-run. kernel-internal fits: the ECS's own storage bookkeeping, which moves onto the memory library. Dense half: VmColumn&lt;U> for the Copy users (InlandPoolId, (u8, usize), u32, Entity). The one non-Copy U is the inner Vec of crates/boyko_ecs/src/ecs/core/archetype/archetype_registry.rs:11 "block_groups: SparseMap&lt;Vec&lt;(ArchetypeId, ArchetypeSignature)>>," - itself an ecs-storage row, whose flattening into a Copy (start, len) range over one kernel column removes the need. If it stays non-Copy, the kernel form is a ComponentPool column with drop glue (crates/boyko_ecs/src/ecs/memory/component_pool.rs:237 "drop_fn: Option&lt;DropFn>,"; the render group's KF-owning-scratch-column), never a new VmVec. The type moves from boyko_utils into boyko_ecs: every user is already there (crates/boyko_ecs/src/ecs/core/archetype/archetype_registry.rs:1 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/component/component_pool_bundle.rs:4 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/component/observers/entity_store.rs:40 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/clone/map.rs:13 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/clone/prefab.rs:457 "let mut node_of: boyko_utils::sparse_map::sparse_map::SparseMap&lt;u32> ="), so it needs no layering edge and no lifted crate. The kernel precedent for the migrated shape is DenseStore's slot->entity column, already a VmColumn (crates/boyko_ecs/src/ecs/core/component/dense/dense_store.rs:118 "s2e: VmColumn&lt;EntityId>,"). Refutes VmSparseMap / VmVec (primitives_refuted). Class: archetype_registry stores a Vec per entry (a Vec inside this Vec), so the dense column must run destructors. swap_remove keeps it compact. Readers: boyko_ecs archetype_registry.rs:11 "block_groups: SparseMap&lt;Vec&lt;(ArchetypeId, ArchetypeSignature)>>," and :24 "id_to_location: SparseMap&lt;(u8, usize)>,", component_pool_bundle.rs:14 "sparse_indexes: SparseMap&lt;InlandPoolId>,", observers/entity_store.rs:120 "by_entity: SparseMap&lt;u32>,", clone/map.rs:22 "sparse: SparseMap&lt;Entity>,", clone/prefab.rs:457 (function-local); iter_dense (sparse_map.rs:170) hands out a borrowed slice iterator |
| crates/boyko_utils/src/sparse_map/sparse_map.rs:13 `indices: Vec<usize>,` | SparseMap::indices | `Vec<usize (reverse map dense -> external)>` | K | kernel-internal | none | NEW:VmSparseMap&lt;U> (indices half = Copy column, VmColumn-shaped) | docs/DENSE-COMPONENTS-PLAN.md:84 "4. SparseMap stays FLAT (architect call); page only if a future dense type uses sparse ids." (the flat layout is kept); docs/MEMORY-SYSTEM-AUDIT.md:95 "\| SparseMap&lt;U> (primitive: sparse layer) \|" - superseded by the owner's 2026-09-10 order; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:344 "\| `boyko_utils::SparseMap`/`SparseSlotMap` (3 `Vec`s each) \| `HeapVec` fields, `new_in(&mut Heap)` \| — \|" - CONFLICTS, see plan_conflicts | [field; growth highwater; addr_cached no; rung R2] ECS: component / dense-component / enable-state skipped: SparseMap is the container of kernel registries keyed by component id (docs/MEMORY-SYSTEM-AUDIT.md:102 "component types (bounded 512)") and by archetype id / block pattern (docs/MEMORY-SYSTEM-AUDIT.md:101 "archetypes (bounded 1024) + block patterns (bounded 256)") - not a datum an entity carries. The four entity-keyed users (docs/MEMORY-SYSTEM-AUDIT.md:118 "Only 4 sites are entity-keyed: DenseStore.e2s, EntityCloneMap, prefab node_of, observers by_entity") are ecs-storage-group rows whose own ECS forms (a component on the observed entity for observers/by_entity; system-scratch for the clone and prefab maps) take them off this type's user list - that decision is theirs, not this row's. relation skipped: id -> index lookups inside the kernel, not edges between entities. event skipped: durable. resource-column skipped: the remaining users (archetype registry, per-archetype pool bundle) are the registries that resolve where every Resource and component lives, so they cannot themselves be Resources. system-scratch / scope-arena skipped: durable, not per-run. kernel-internal fits: the ECS's own storage bookkeeping, which moves onto the memory library. Indices half: VmColumn&lt;u32>, parallel to dense under swap_remove; no pointer into it is retained across a grow, and a reservation is address-stable regardless. The type moves from boyko_utils into boyko_ecs: every user is already there (crates/boyko_ecs/src/ecs/core/archetype/archetype_registry.rs:1 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/component/component_pool_bundle.rs:4 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/component/observers/entity_store.rs:40 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/clone/map.rs:13 "use boyko_utils::sparse_map::sparse_map::SparseMap;"; crates/boyko_ecs/src/ecs/core/clone/prefab.rs:457 "let mut node_of: boyko_utils::sparse_map::sparse_map::SparseMap&lt;u32> ="), so it needs no layering edge and no lifted crate. The kernel precedent for the migrated shape is DenseStore's slot->entity column, already a VmColumn (crates/boyko_ecs/src/ecs/core/component/dense/dense_store.rs:118 "s2e: VmColumn&lt;EntityId>,"). Refutes VmSparseMap / VmVec (primitives_refuted). Class: Parallel to dense; same length. Readers: boyko_ecs archetype_registry.rs:11 "block_groups: SparseMap&lt;Vec&lt;(ArchetypeId, ArchetypeSignature)>>," and :24 "id_to_location: SparseMap&lt;(u8, usize)>,", component_pool_bundle.rs:14 "sparse_indexes: SparseMap&lt;InlandPoolId>,", observers/entity_store.rs:120 "by_entity: SparseMap&lt;u32>,", clone/map.rs:22 "sparse: SparseMap&lt;Entity>,", clone/prefab.rs:457 (function-local); active_indices (sparse_map.rs:161) returns &[usize] |
| crates/boyko_utils/src/sparse_map/sparse_slot_map.rs:41 `sparse: Vec<Option<Slot>>,` | SparseSlotMap::sparse | `Vec<Option<Slot> (index + generation, tombstones)>` | R | resource-column | none | NEW:VmSparseSlotMap&lt;U> (sparse half = reserve-once Copy column) | docs/RENDER-PHYSICS-GPU-PLAN.md:124 "over `boyko_utils::SparseSlotMap`/`Slot` (ABA-safe, `Copy`, zero-heap)" (decides generational handles; its 'zero-heap' is false - see plan_conflicts); docs/ARCH-AUDIT-ECS-DATA-REMEDIATION.md:19 "For non-entity bulk: a `Resource` that OWNS a `ComponentPool` host column."; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:344 "\| `boyko_utils::SparseMap`/`SparseSlotMap` (3 `Vec`s each) \| `HeapVec` fields, `new_in(&mut Heap)` \| — \|" - CONFLICTS, see plan_conflicts | [field; growth highwater; addr_cached no; rung R4] ECS: component / dense-component skipped: GPU buffers, pipelines, shaders and fences are docs/MEMORY-SYSTEM-AUDIT.md:97 "external handles (GPU buffers/pipelines/shaders/fences — realistically 10^2-10^3, never entities)"; the handle is an opaque u64 inside a kernel pool's device backing, so making it an entity would make a ComponentPool's backing depend on the entity table of the World that owns the pool. enable-state / relation / event skipped: a generational record table, not a flag, an edge or a message. resource-column fits: the registry is already owned by a NonSend resource (crates/boyko_render/src/gpu_column.rs:510 "impl NonSendResource for RhiContext {}"; crates/boyko_render/src/gpu_column.rs:529 "registry: ResourceRegistry&lt;Vulkan>,"), and the audit's rule for non-entity bulk is docs/ARCH-AUDIT-ECS-DATA-REMEDIATION.md:19 "For non-entity bulk: a `Resource` that OWNS a `ComponentPool` host column.". The kernel already ships this exact shape as Assets&lt;T> (crates/boyko_ecs/src/ecs/core/asset/assets.rs:200 "pub struct Assets&lt;T: AssetBacking> {"): a ComponentPool data column (crates/boyko_ecs/src/ecs/core/asset/assets.rs:201 "col: ComponentPool,") that takes non-Copy records by byte move, a packed generation+state word per row (crates/boyko_ecs/src/ecs/core/asset/assets.rs:202 "slot_word: VmColumn&lt;u32>,"), a LIFO free list and a live bitmap. That is SparseSlotMap plus Kind&lt;U>'s free stack (crates/boyko_rhi/src/handle.rs:78 "free: Vec&lt;usize>,") and watermark, in kernel storage. So the RHI registry becomes four kernel generational tables (one per kind), and SparseSlotMap - whose sole user is crates/boyko_rhi/src/handle.rs:76 "map: SparseSlotMap&lt;U>," - is deleted from boyko_utils. Blocker: docs/ARCHITECTURE.md:174 "itself does NOT depend on `boyko_ecs` (so no cycle).", although the RHI's own header planned the edge (crates/boyko_rhi/src/lib.rs:28 "The `boyko_ecs` dependency and core's" / crates/boyko_rhi/src/lib.rs:29 "`DeviceColumnHandle(u64)` newtype land in Phase 4; for now the registry uses"); resolved by KF-generational-table-reachable-from-rhi. Sparse half (Option&lt;Slot>, 24 B): becomes the kernel table's u32 generation+state word; the handle keeps its (index, generation) u64 packing. Refutes VmSparseSlotMap (primitives_refuted). Owning group for the registry itself: rhi (handle.rs rows); this row covers the storage type only. Class: Grows to max external index + 1 (sparse_slot_map.rs:113 "self.sparse.resize(idx + 1, None);"). R: resource registry of GPU handles, grows on resource creation (load path or runtime creation - frequency not counted). Readers: sole user boyko_rhi handle.rs:76 "map: SparseSlotMap&lt;U>," (per-resource-kind handle table, Kind&lt;U>) |
| crates/boyko_utils/src/sparse_map/sparse_slot_map.rs:42 `dense: Vec<U>,` | SparseSlotMap::dense | `Vec<U (generic RHI resource record)>` | R | resource-column | none | NEW:VmSparseSlotMap&lt;U> (dense half = growable drop-aware non-Copy column) | docs/RENDER-PHYSICS-GPU-PLAN.md:124 "over `boyko_utils::SparseSlotMap`/`Slot` (ABA-safe, `Copy`, zero-heap)" (decides generational handles; its 'zero-heap' is false - see plan_conflicts); docs/ARCH-AUDIT-ECS-DATA-REMEDIATION.md:19 "For non-entity bulk: a `Resource` that OWNS a `ComponentPool` host column."; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:344 "\| `boyko_utils::SparseMap`/`SparseSlotMap` (3 `Vec`s each) \| `HeapVec` fields, `new_in(&mut Heap)` \| — \|" - CONFLICTS, see plan_conflicts | [field; growth highwater; addr_cached no; rung R4] ECS: component / dense-component skipped: GPU buffers, pipelines, shaders and fences are docs/MEMORY-SYSTEM-AUDIT.md:97 "external handles (GPU buffers/pipelines/shaders/fences — realistically 10^2-10^3, never entities)"; the handle is an opaque u64 inside a kernel pool's device backing, so making it an entity would make a ComponentPool's backing depend on the entity table of the World that owns the pool. enable-state / relation / event skipped: a generational record table, not a flag, an edge or a message. resource-column fits: the registry is already owned by a NonSend resource (crates/boyko_render/src/gpu_column.rs:510 "impl NonSendResource for RhiContext {}"; crates/boyko_render/src/gpu_column.rs:529 "registry: ResourceRegistry&lt;Vulkan>,"), and the audit's rule for non-entity bulk is docs/ARCH-AUDIT-ECS-DATA-REMEDIATION.md:19 "For non-entity bulk: a `Resource` that OWNS a `ComponentPool` host column.". The kernel already ships this exact shape as Assets&lt;T> (crates/boyko_ecs/src/ecs/core/asset/assets.rs:200 "pub struct Assets&lt;T: AssetBacking> {"): a ComponentPool data column (crates/boyko_ecs/src/ecs/core/asset/assets.rs:201 "col: ComponentPool,") that takes non-Copy records by byte move, a packed generation+state word per row (crates/boyko_ecs/src/ecs/core/asset/assets.rs:202 "slot_word: VmColumn&lt;u32>,"), a LIFO free list and a live bitmap. That is SparseSlotMap plus Kind&lt;U>'s free stack (crates/boyko_rhi/src/handle.rs:78 "free: Vec&lt;usize>,") and watermark, in kernel storage. So the RHI registry becomes four kernel generational tables (one per kind), and SparseSlotMap - whose sole user is crates/boyko_rhi/src/handle.rs:76 "map: SparseSlotMap&lt;U>," - is deleted from boyko_utils. Blocker: docs/ARCHITECTURE.md:174 "itself does NOT depend on `boyko_ecs` (so no cycle).", although the RHI's own header planned the edge (crates/boyko_rhi/src/lib.rs:28 "The `boyko_ecs` dependency and core's" / crates/boyko_rhi/src/lib.rs:29 "`DeviceColumnHandle(u64)` newtype land in Phase 4; for now the registry uses"); resolved by KF-generational-table-reachable-from-rhi. Dense half: the backend records are not Copy (crates/boyko_rhi_vulkan/src/rhi_impl/mod.rs:303 "type Buffer = BoundBuffer;"; crates/boyko_rhi_vulkan/src/memory.rs:50 "pub struct BoundBuffer {" carries no derive), so they go in the table's ComponentPool column by byte move; no drop_fn is needed because the registry destroys them explicitly with the device (destroy_all). Refutes VmSparseSlotMap (primitives_refuted). Owning group for the registry itself: rhi (handle.rs rows); this row covers the storage type only. Class: push at sparse_slot_map.rs:285. Readers: sole user boyko_rhi handle.rs:76 "map: SparseSlotMap&lt;U>," (per-resource-kind handle table, Kind&lt;U>) |
| crates/boyko_utils/src/sparse_map/sparse_slot_map.rs:44 `indices: Vec<usize>,` | SparseSlotMap::indices | `Vec<usize>` | R | resource-column | none | NEW:VmSparseSlotMap&lt;U> (indices half = Copy column) | docs/RENDER-PHYSICS-GPU-PLAN.md:124 "over `boyko_utils::SparseSlotMap`/`Slot` (ABA-safe, `Copy`, zero-heap)" (decides generational handles; its 'zero-heap' is false - see plan_conflicts); docs/ARCH-AUDIT-ECS-DATA-REMEDIATION.md:19 "For non-entity bulk: a `Resource` that OWNS a `ComponentPool` host column."; [main checkout D:/claude/BoykoEngine, untracked rev-1 file] docs/memory/ALLOCATOR-DESIGN-SPACE.md:344 "\| `boyko_utils::SparseMap`/`SparseSlotMap` (3 `Vec`s each) \| `HeapVec` fields, `new_in(&mut Heap)` \| — \|" - CONFLICTS, see plan_conflicts | [field; growth highwater; addr_cached no; rung R4] ECS: component / dense-component skipped: GPU buffers, pipelines, shaders and fences are docs/MEMORY-SYSTEM-AUDIT.md:97 "external handles (GPU buffers/pipelines/shaders/fences — realistically 10^2-10^3, never entities)"; the handle is an opaque u64 inside a kernel pool's device backing, so making it an entity would make a ComponentPool's backing depend on the entity table of the World that owns the pool. enable-state / relation / event skipped: a generational record table, not a flag, an edge or a message. resource-column fits: the registry is already owned by a NonSend resource (crates/boyko_render/src/gpu_column.rs:510 "impl NonSendResource for RhiContext {}"; crates/boyko_render/src/gpu_column.rs:529 "registry: ResourceRegistry&lt;Vulkan>,"), and the audit's rule for non-entity bulk is docs/ARCH-AUDIT-ECS-DATA-REMEDIATION.md:19 "For non-entity bulk: a `Resource` that OWNS a `ComponentPool` host column.". The kernel already ships this exact shape as Assets&lt;T> (crates/boyko_ecs/src/ecs/core/asset/assets.rs:200 "pub struct Assets&lt;T: AssetBacking> {"): a ComponentPool data column (crates/boyko_ecs/src/ecs/core/asset/assets.rs:201 "col: ComponentPool,") that takes non-Copy records by byte move, a packed generation+state word per row (crates/boyko_ecs/src/ecs/core/asset/assets.rs:202 "slot_word: VmColumn&lt;u32>,"), a LIFO free list and a live bitmap. That is SparseSlotMap plus Kind&lt;U>'s free stack (crates/boyko_rhi/src/handle.rs:78 "free: Vec&lt;usize>,") and watermark, in kernel storage. So the RHI registry becomes four kernel generational tables (one per kind), and SparseSlotMap - whose sole user is crates/boyko_rhi/src/handle.rs:76 "map: SparseSlotMap&lt;U>," - is deleted from boyko_utils. Blocker: docs/ARCHITECTURE.md:174 "itself does NOT depend on `boyko_ecs` (so no cycle).", although the RHI's own header planned the edge (crates/boyko_rhi/src/lib.rs:28 "The `boyko_ecs` dependency and core's" / crates/boyko_rhi/src/lib.rs:29 "`DeviceColumnHandle(u64)` newtype land in Phase 4; for now the registry uses"); resolved by KF-generational-table-reachable-from-rhi. Indices half (dense -> external reverse map): not needed - a kernel generational table keeps rows in place and reuses them through its free list instead of swap_remove, so the column disappears. Refutes VmSparseSlotMap (primitives_refuted). Owning group for the registry itself: rhi (handle.rs rows); this row covers the storage type only. Class: push at sparse_slot_map.rs:286. Readers: sole user boyko_rhi handle.rs:76 "map: SparseSlotMap&lt;U>," (per-resource-kind handle table, Kind&lt;U>) |

---

*Row-count check: 131 rows in this file, 131 active, 0 superseded.*
