# Runtime Data Ledger rev 2 - group reflect-lane

Part of the [runtime data ledger, rev 2](../RUNTIME-DATA-LEDGER.md) (the index: vocabularies, totals, entity model, kernel features, decisions). **Tree:** `D:/wt/reflect`, `feat/reflection` @ `0e0b4c68`. **Rows:** 19, of which **19 active**; 161 non_rows (kept in the group JSON ledger).

Active forms: out-of-scope:compile-time 12, diagnostics 5, kernel-internal 2. Active classes: F 2, D 5, C 12. Crates: boyko_ecs 2, boyko_macros 12, boyko_reflect 5. Rungs: OOS 12, R2 2, R6 5.

## Contents

- [Findings](#findings)
- [Kernel features](#kernel-features)
- [Primitives refuted](#primitives-refuted)
- [Plan conflicts](#plan-conflicts)
- [Lane notes and decisions](#lane-notes-and-decisions)
- [Rows](#rows)

## Findings

- See the lane notes below.

## Kernel features

The features this group proposed, with the index id they merged into.

| group name | index id | rev-2 status |
|---|---|---|
| Static type descriptors (derive output allocation-free) | KF-43 | active |
| By-id structural seam (structural ops by ComponentId + bytes) | KF-47 | active |
| World scratch frames (re-entrant LIFO scratch for &mut paths) | KF-05 | decided |
| Byte column (bulk append + fmt::Write) / Serialize seam on kernel columns | KF-06, KF-08 | - |
| ScratchColumn by type (kernel-minted id, explicit stagger) | KF-01 | decided |
| Kernel name table / owned span on kernel columns | KF-26, KF-03 | - |

## Primitives refuted

None proposed.


## Plan conflicts

- *plan:* D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:1579 "1. **Kind/accessor coherence, asserted:** a `validate(info) -> Result<(), Vec<Problem>>` walked" *ledger:* rows type_info.rs:434/435/592/605/618 - eliminated by a visitor; the plan signature is amended
- *plan:* in-code exception text crates/boyko_reflect/src/type_info.rs:420 "/// `Vec` is the natural shape for the error path and is not a hot-path allocation:" *ledger:* same rows; the text predates the 2026-09-10 order and is rewritten with them
- *plan:* D:/wt/reflect/docs/REFLECTION-PLAN-ECS.md:1665 "`#[cold]` heap fallback" *ledger:* planned site - KF-05, not the std heap
- *plan:* D:/wt/reflect/docs/REFLECTION-PLAN-BOUNDARY.md:278 "`VecSink` / `SliceSource` over a caller-provided `&mut Vec<u8>`" *ledger:* planned site - ScratchColumn&lt;u8> + KF-06 (the ledger's KF-08 answer for SaveCursor)
- *plan:* D:/wt/reflect/docs/REFLECTION-PLAN-BOUNDARY.md:602 "dense `Vec<ResolvedType>`" *ledger:* planned site - system-scratch on ScratchColumn (KF-01), like boyko_serialize load.rs
- *plan:* D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:3800 "s.to_owned()" *ledger:* planned site - engine text carrier, not std String

## Lane notes and decisions

- **census:** {"script": "C:/Users/flint/AppData/Local/Temp/claude/D--claude-BoykoEngine/dad9a19e-5512-4bbd-9976-cb44400eeb64/scratchpad/ledger/rev2/reflect_census.py", "scope": "whole files the lane added + the ADDED hunk lines of files it modified, non-test (src/, src/bin/, benches/); tests/ dirs are test-only", "regex": "the ui-input recount regex UNION the completeness critic BRIEF+EXTRA+hidden.py patterns, plus thread_local!/static/OnceLock/LazyLock/Mutex, Box::leak/.leak(, push/extend/insert/append growth calls, panic!/println!, quote!/format_ident!/TokenStream::new, and the engine's own heap-holding types (SmallList4, SparseMap, SparseSlotMap, LiveBitmap, VisitedSet, UiParseReport)", "hits": 174, "by_category": {"code": 91, "test": 21, "doc": 62}, "unaccounted": 0, "tls_pass": "thread_local! in lane non-test code: 0 statics (the two non-test Box::leak / thread_local-adjacent hits in modified files, tags.rs:192 and component.rs:968, are pre-existing lines outside the lane hunks and are existing ledger rows). The lane reaches ONE existing TLS cell (hooks/scope.rs:31, ledger T row) from three new call sites, recorded as non_rows. Test-only TLS: crates/boyko_reflect/tests/c4_prim_zero_alloc.rs:113 (counting-allocator harness).", "box_leak_pass": "Box::leak in lane-added non-test code: 0. The derive emission uses per-type statics instead (KF-43 form). Test-only leaks: crates/boyko_reflect/tests/c6_nested.rs:593/622/627."}
- **diff_basis:** git diff merge/ke16-into-ecsnative...HEAD (merge base 5ec1699fbce05e18c313544adabf3ccf24be17b3); the lane is NOT based on joltab ca582e72 - merge-base --is-ancestor merge/ke16-into-ecsnative HEAD is false

**Notes**

- **lane_changed_non_test_source:** crates/boyko_diag/src/sample.rs (M, 5 lines in scope); crates/boyko_ecs/src/ecs/core/commands/migration_helpers.rs (M, 716 lines in scope); crates/boyko_ecs/src/ecs/core/component/component_registry/tags.rs (M, 28 lines in scope); crates/boyko_ecs/src/ecs/core/ecs_master/component_api.rs (M, 43 lines in scope); crates/boyko_ecs/src/ecs/core/ecs_master/mod.rs (M, 5 lines in scope); crates/boyko_ecs/src/ecs/core/ecs_master/seam_by_id.rs (A, 904 lines in scope); crates/boyko_macros/src/component.rs (M, 119 lines in scope); crates/boyko_macros/src/lib.rs (M, 31 lines in scope); crates/boyko_macros/src/reflect.rs (A, 871 lines in scope); crates/boyko_reflect/src/array.rs (A, 114 lines in scope); crates/boyko_reflect/src/cursor.rs (A, 178 lines in scope); crates/boyko_reflect/src/ecs.rs (A, 326 lines in scope); crates/boyko_reflect/src/lib.rs (A, 78 lines in scope); crates/boyko_reflect/src/prim.rs (A, 156 lines in scope); crates/boyko_reflect/src/reflect.rs (A, 87 lines in scope); crates/boyko_reflect/src/registry.rs (A, 355 lines in scope); crates/boyko_reflect/src/scalar.rs (A, 286 lines in scope); crates/boyko_reflect/src/type_info.rs (A, 668 lines in scope); crates/reflect_dogfood/src/address.rs (A, 107 lines in scope); crates/reflect_dogfood/src/lib.rs (A, 30 lines in scope); crates/reflect_fixture/benches/reflect_optin_cost.rs (A, 16 lines in scope); crates/reflect_fixture/src/bin/reflect_never.rs (A, 69 lines in scope); crates/reflect_fixture/src/bin/reflect_off_twin_plus.rs (A, 63 lines in scope); crates/reflect_fixture/src/bin/reflect_on.rs (A, 89 lines in scope)
- **superseded_check:** {'script': 'C:/Users/flint/AppData/Local/Temp/claude/D--claude-BoykoEngine/dad9a19e-5512-4bbd-9976-cb44400eeb64/scratchpad/ledger/rev2/reflect_superseded.py', 'method': 'every ledger row (11 group ecsform JSONs) in a file the lane MODIFIED: superseded iff its evidence exists at the merge base and the lane deletes that line or it is gone from the lane file', 'ledger_rows_checked': 48, 'superseded': 0, 'joltab_only_rows': [{'crate': 'boyko_macros', 'file': 'crates/boyko_macros/src/component.rs', 'owner': 'ComponentHookPaths::non_relationship_owned_assigns', 'kind': 'return', 'container': 'Vec', 'evidence': 'pub(crate) fn non_relationship_owned_assigns(&self) -> Vec&lt;TokenStream2> {'}, {'crate': 'boyko_macros', 'file': 'crates/boyko_macros/src/component.rs', 'owner': 'RequiresSpec::bitset_refusal_codegen', 'kind': 'local', 'container': 'Vec', 'evidence': 'let asserts: Vec&lt;TokenStream2> = self'}], 'lane_deletions_in_non_test_source': 'component_api.rs (the dense_insert_and_fire block body, no heap), boyko_macros component.rs (two meta.error string literals), boyko_macros lib.rs (the derive attributes list) - none is a ledger row', 'merge_note': 'migration_helpers.rs, component_api.rs, boyko_macros component.rs and lib.rs changed on BOTH sides since the fork point (joltab +194 / +98 / +181 / +93 lines); line numbers of the lane rows must be re-derived after the merge - the TSV key (crate, file, owner, kind, container) carries no line and survives it'}

**Decisions** (rev-2 status in the index, section Decisions)

- **** D-row form for this group's validate rows (ledger section (i) "D rows: diagnostics form") - **decided:** kernel-internal (diagnostics substrate), realised by eliminating the buffer: validate_with visitor + count-returning validate. **Performance:** one pass, zero allocation, cold path; the out-slice form needs two passes because the problem count is unbounded **Overturned by:** a consumer that must retain problems across frames - then resource-column (ScratchColumn&lt;Problem>) on its Resource
- **** CORE D16 - one table (reflect slots inside BIND_ACCESSORS) or two (REFLECT separate); "OWNER decision, the plan proceeds on Horn 2" - **decided:** Horn 2: two tables. **Performance:** BIND_ACCESSORS is a SHIPPED kernel static (crates/boyko_ecs/src/ecs/core/component/component_registry/serialize.rs:277 "static BIND_ACCESSORS: [OnceLock&lt;BindAccessor>; MAX_COMPONENTS] ="); merging would put reflect slots into every ship image, while REFLECT costs zero bytes and zero instructions in a ship build (crate absent). Each lookup is one acquire load either way. **Overturned by:** an editor profile showing the second table's lookup on a hot path (both are cold registry reads today)
- **** REFLECTION-ANALYSIS B.12 option (b) / owner sheet B.13 #1 - engine crates carry a non-default `reflect` feature + optional edge ("the plan proceeds on (b) pending the owner", crates/boyko_scene/Cargo.toml) - **decided:** option (b). **Performance:** feature off => boyko_reflect is not in the ship closure (zero cost, gated by G1/G2/G3); it is the only option under which engine components are reflectable in place, without a side table of wrappers (principle 0: no parallel data system) **Overturned by:** GATES G3's ship-absence census finding boyko_reflect symbols in a ship image
- **** ECS form of the reflection registry REFLECT itself (types as entities, flecs-style, vs a static table) - **decided:** kernel-internal static table (not a heap site, so not a row). **Performance:** one acquire load per lookup against a per-world entity lookup, and no per-EcsMaster duplication of a process-global datum; the macros-aether KF-43 analysis refutes type entities on the same grounds **Overturned by:** a per-world (not per-process) descriptor requirement, e.g. hot-reloaded types differing between two worlds
- **** EG6 add_default #[cold] heap fallback (planned) - **decided:** KF-05 world scratch frame; delete the fallback if the measured max layout fits the stack scratch. **Performance:** no allocator call on any path; address-stable aligned scratch **Overturned by:** EG6's max(size)/max(align) measurement
- **** CORE C11 String arm (planned) - **decided:** Str accessor over engine text carriers (inline bytes / KF-26 id / KF-03 span), no std String write path. **Performance:** zero allocation per write versus 1 alloc + 1 free (the plan's own gate count) **Overturned by:** a shipping component with a String field

## Rows

Every row, no elision, sorted by file and line. Columns:

- **file:line + evidence:** the evidence is the verbatim source line in `D:/wt/reflect`.
- **owner**, **container&lt;elem&gt;**, **class**, **ecs_form**, **owning_entity**
- **memory destination:** the recount's. A `NEW:` name here was refuted in the index (Primitives refuted) unless it is KEPT there.
- **plan_ref**
- **note:** `[kind; growth; addr_cached; rung]`, then `ECS:` with the form rationale (one sentence per skipped earlier form, then any `[rev2 ...]` blocks), then `Class:` with the inventory/recount note, then `Readers:`.

| file:line + evidence | owner | container&lt;elem&gt; | class | ecs_form | owning_entity | memory destination | plan_ref | note |
|---|---|---|---|---|---|---|---|---|
| crates/boyko_ecs/src/ecs/core/commands/migration_helpers.rs:2171 `let mut enable_scratch: SmallList4<(ComponentId, bool)> = SmallList4::new();` | migrate_entity_attach_ids_with_bytes (enable_scratch) | `other:SmallList4 (4 inline, Vec spill - component/enable/enable_store.rs:765-770)<(ComponentId, bool)>` | F | kernel-internal | none | SmallList4 spill into the per-EcsMaster migration scratch (VmColumn&lt;ComponentId>; type change owned by ecs-storage) - the ecs-services destination for the four sibling migrations ; ECS destination: SmallList4 spill into the per-EcsMaster migration scratch (type change owned by ecs-storage) | docs/REFLECTION-PLAN-ECS.md:936 "### EG2 — the by-id structural seam in `boyko_ecs` — **size M** — **B.13 #2 OWNER-APPROVED 2026-08-27**" (EG2 owns the function; no lane plan decides the enable scratch - the ledger sibling rows carry plan_ref null) | [local; growth perframe; addr_cached no; rung R2] ECS: FORM: kernel-internal (EnableTag bookkeeping of one migration). SKIPPED: component/dense-component - a list of an archetype's column ids, describing no entity; enable-state - not a per-entity boolean; relation - no edge between two entities; event - produced and consumed inside one function call; resource-column - not durable; system-scratch - not a per-system transient: it runs inside the kernel's own structural-change machinery on &mut EcsMaster (the direct-API by-id seam), not in a system body; scope-arena - no threadpool scope or task is involved. Heap only on the spill (crates/boyko_ecs/src/ecs/core/component/enable/enable_store.rs:769 "Spilled(Vec&lt;T>),"), i.e. more than 4 EnableTag ids in one migration. Destination: identical to the four sibling migrations the ledger already rows (ecs-services, migrate_entity_attach_ids enable_scratch / enable_newly_allocated): the spill goes into the per-EcsMaster reusable kernel migration scratch (VmColumn&lt;ComponentId>, lazy; taken out of EcsMaster and put back around the migration). The SmallList4 type is an ecs-storage row (enable_store.rs:765), which owns the change; migrating the type retires this row with its siblings (ledger section (a), 'double representation'). Class: NOT a token hit of the ledger recount regex; found by this group's own-type pass (SmallList4 is on the ledger's "double representation" list). Lane-added: the by-id, byte-carrying sibling of migrate_entity_attach_ids (EG2 D9), #[cold] #[inline(never)], reached from EcsMaster::add_component_by_id at crates/boyko_ecs/src/ecs/core/ecs_master/seam_by_id.rs:320 "migrate_entity_attach_ids_with_bytes(" and crates/boyko_ecs/src/ecs/core/ecs_master/seam_by_id.rs:455 "migrate_entity_attach_ids_with_bytes(". Same two locals, same lifetime, same spill condition as the sibling rows ledger ecs-services migration_helpers.rs (joltab lines 1604/1605). Readers: read_source_enable_bits (migration_helpers.rs:2196) / write_target_enable_bits (:2359) / fire_enable_column_alloc_bookkeeping (:2397) |
| crates/boyko_ecs/src/ecs/core/commands/migration_helpers.rs:2172 `let mut enable_newly_allocated: SmallList4<ComponentId> = SmallList4::new();` | migrate_entity_attach_ids_with_bytes (enable_newly_allocated) | `other:SmallList4 (4 inline, Vec spill - component/enable/enable_store.rs:765-770)<ComponentId>` | F | kernel-internal | none | SmallList4 spill into the per-EcsMaster migration scratch (VmColumn&lt;ComponentId>; type change owned by ecs-storage) - the ecs-services destination for the four sibling migrations ; ECS destination: SmallList4 spill into the per-EcsMaster migration scratch (type change owned by ecs-storage) | docs/REFLECTION-PLAN-ECS.md:936 "### EG2 — the by-id structural seam in `boyko_ecs` — **size M** — **B.13 #2 OWNER-APPROVED 2026-08-27**" (EG2 owns the function; no lane plan decides the enable scratch - the ledger sibling rows carry plan_ref null) | [local; growth perframe; addr_cached no; rung R2] ECS: FORM: kernel-internal (EnableTag bookkeeping of one migration). SKIPPED: component/dense-component - a list of an archetype's column ids, describing no entity; enable-state - not a per-entity boolean; relation - no edge between two entities; event - produced and consumed inside one function call; resource-column - not durable; system-scratch - not a per-system transient: it runs inside the kernel's own structural-change machinery on &mut EcsMaster (the direct-API by-id seam), not in a system body; scope-arena - no threadpool scope or task is involved. Heap only on the spill (crates/boyko_ecs/src/ecs/core/component/enable/enable_store.rs:769 "Spilled(Vec&lt;T>),"), i.e. more than 4 EnableTag ids in one migration. Destination: identical to the four sibling migrations the ledger already rows (ecs-services, migrate_entity_attach_ids enable_scratch / enable_newly_allocated): the spill goes into the per-EcsMaster reusable kernel migration scratch (VmColumn&lt;ComponentId>, lazy; taken out of EcsMaster and put back around the migration). The SmallList4 type is an ecs-storage row (enable_store.rs:765), which owns the change; migrating the type retires this row with its siblings (ledger section (a), 'double representation'). Class: NOT a token hit of the ledger recount regex; found by this group's own-type pass (SmallList4 is on the ledger's "double representation" list). Lane-added: the by-id, byte-carrying sibling of migrate_entity_attach_ids (EG2 D9), #[cold] #[inline(never)], reached from EcsMaster::add_component_by_id at crates/boyko_ecs/src/ecs/core/ecs_master/seam_by_id.rs:320 "migrate_entity_attach_ids_with_bytes(" and crates/boyko_ecs/src/ecs/core/ecs_master/seam_by_id.rs:455 "migrate_entity_attach_ids_with_bytes(". Same two locals, same lifetime, same spill condition as the sibling rows ledger ecs-services migration_helpers.rs (joltab lines 1604/1605). Readers: read_source_enable_bits (migration_helpers.rs:2196) / write_target_enable_bits (:2359) / fire_enable_column_alloc_bookkeeping (:2397) |
| crates/boyko_macros/src/reflect.rs:201 `fn push_opaque_field_refusal(refusals: &mut Vec<TokenStream2>, field: &syn::Field) {` | push_opaque_field_refusal | `Vec<TokenStream2>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [param; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: `&mut Vec<TokenStream2>` growth parameter of the refusal list rowed at reflect.rs:650 \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_macros/src/reflect.rs:218 `fn parse_skip_or_record(attrs: &[syn::Attribute], refusals: &mut Vec<TokenStream2>) -> bool {` | parse_skip_or_record | `Vec<TokenStream2>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [param; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: `&mut Vec<TokenStream2>` growth parameter of the refusal list rowed at reflect.rs:650 \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_macros/src/reflect.rs:329 `id.to_string().as_str(),` | has_integer_repr | `String<u8>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [local; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: temporary String of a repr ident, matched against the integer repr names \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_macros/src/reflect.rs:382 `Some(match seg.ident.to_string().as_str() {` | scalar_kind | `String<u8>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [local; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: temporary String of a field type ident, matched against the eleven scalar names \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_macros/src/reflect.rs:418 `format_ident!("get_{}", suffix)` | prim_accessors | `String<u8 (format! buffer inside quote::format_ident!)>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [local; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: format_ident! formats into a transient String before minting the Ident (the getter name) \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_macros/src/reflect.rs:418 `format_ident!("set_{}", suffix)` | prim_accessors | `String<u8 (format! buffer inside quote::format_ident!)>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [local; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: format_ident! formats into a transient String before minting the Ident (the setter name) \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_macros/src/reflect.rs:460 `Member::Named(id) => id.to_string(),` | field_info (name_str) | `String<u8>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [local; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: field name as a String, spliced into the emitted FieldInfo as a &'static str literal \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_macros/src/reflect.rs:461 `Member::Unnamed(idx) => idx.index.to_string(),` | field_info (name_str) | `String<u8>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [local; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: tuple-field index as a String, spliced into the emitted FieldInfo as a &'static str literal \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_macros/src/reflect.rs:498 `let kind_ident = format_ident!("{}", kind);` | field_info (kind_ident, Prim arm) | `String<u8 (format! buffer inside quote::format_ident!)>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [local; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: format_ident! formats the ScalarKind variant name into a transient String \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_macros/src/reflect.rs:522 `let kind_ident = format_ident!("{}", kind);` | field_info (kind_ident, Array arm) | `String<u8 (format! buffer inside quote::format_ident!)>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [local; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: format_ident! formats the ScalarKind variant name into a transient String \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_macros/src/reflect.rs:650 `let mut refusals: Vec<TokenStream2> = Vec::new();` | codegen (refusals) | `Vec<TokenStream2>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [local; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: the CORE C9 refusal list, collected so every refusal of one item is reported at once \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_macros/src/reflect.rs:668 `let (type_kind, fields): (Ident, Vec<TokenStream2>) = match &input.data {` | codegen (fields) | `Vec<TokenStream2>` | C | out-of-scope:compile-time | none | out-of-scope: compile-time proc-macro code, never linked into the shipped binary (crates/boyko_macros/Cargo.toml:7 `proc-macro = true`) | the allocator design space files this crate as offline: D:/claude/BoykoEngine/docs/memory/ALLOCATOR-DESIGN-SPACE.md:50 "\| `offline` \| `boyko_macros` (proc-macro), `aether_lang`, `boyko_shaderdsl` printer, `boyko_fontbake` \| compile/bake time \| not in the runtime binary or run once at bake \|" (main checkout). The emission shape is decided by D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:420 "### D22 — C7 lands in `boyko_reflect` too." | [local; growth once; addr_cached no; rung OOS] ECS: out-of-scope:compile-time. The line is in the body of a proc-macro crate (crates/boyko_macros/Cargo.toml:7 "proc-macro = true"), so it runs inside rustc and is never linked into a shipped binary. Only the EMITTED tokens reach runtime, and this group scanned every quote!/quote_spanned! body the lane added (reflect.rs:204-226, :470-591, :658-736, :746-867; component.rs:374-381): the emission is fn items, compile_error! refusals and two per-type statics (`static __REFLECT_FIELDS`, `static __REFLECT_TYPE_INFO`, reflect.rs:834/:838) plus one install call into the static REFLECT table - no heap token, i.e. KF-43's allocation-free descriptor form, already realised. No ECS form applies: the datum is rustc-process state, not engine data. owning_entity none. Class: one FieldInfo token fragment per field, spliced into `static __REFLECT_FIELDS`; built by .collect() at :685/:702, empty at :704/:727/:736 \| compile-time: runs inside rustc during expansion; zero bytes at runtime Readers: #[derive(Component)] + #[component(reflect)] expansion inside rustc (compile time) via crate::reflect::codegen &lt;- component::expand |
| crates/boyko_reflect/src/type_info.rs:434 `pub fn validate(info: &TypeInfo) -> Result<(), Vec<Problem>> {` | validate | `Vec<Problem (Copy: Option<usize>, &'static str, Violation)>` | D | diagnostics | none | eliminated: validate_with(info, &mut impl FnMut(Problem)) visitor + count-returning validate; zero allocation ; ECS destination: diagnostics substrate: structured Problem records streamed to a caller visitor (no buffer) | D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:1579 "1. **Kind/accessor coherence, asserted:** a `validate(info) -> Result<(), Vec<Problem>>` walked" [CONFLICT: the plan fixes a std Vec in the signature; superseded by the owner's 2026-09-10 order ('no allocator but ours')] + in-code exception text to rewrite with the row: crates/boyko_reflect/src/type_info.rs:420 "/// `Vec` is the natural shape for the error path and is not a hot-path allocation:" | [return; growth once; addr_cached no; rung R6] ECS: D row: a coherence-failure report, and an Ok descriptor never allocates (Vec::new() then no push). FORM kernel-internal - the diagnostics substrate the ledger agrees on for D rows (section (i) 'D rows: diagnostics form'), realised here with no buffer at all. SKIPPED: component / dense-component - a TypeInfo describes a Rust TYPE and types are not entities in this kernel (crates/boyko_ecs/src/ecs/identifiers/primitives.rs:15 "pub struct $name(pub usize);"; the macros-aether KF-43 refutation of type entities applies verbatim); enable-state - not a boolean; relation - no entity-to-entity edge; event - returned to the caller inside one call, never produced by one system for another; resource-column - not durable (dropped by the caller); system-scratch - validate runs in no system (tests, editor tool calls); if an editor system calls it, the problems land in THAT system's own ScratchColumn&lt;Problem> (Problem is Copy: crates/boyko_reflect/src/type_info.rs:392 "#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]"), which is the caller's row, not this one; scope-arena - no threadpool scope. DECIDED BY PERFORMANCE (owner 2026-09-11): replace the Vec with a visitor, `validate_with(info, &mut impl FnMut(Problem))`, plus `validate(info) -> Result<(), usize>` (the count) for the Ok/Err precondition NestedCursor::new names (crates/boyko_reflect/src/cursor.rs:136 "// SAFETY: the cursor invariant includes `validate(self.info) == Ok`"). One pass, zero allocation, and CORE C3 gate 1's contract survives because the visitor sees every violation (crates/boyko_reflect/src/type_info.rs:413 "/// Returns **every** violation found, not the first"). The out-slice alternative (components_of_into's complete-or-refuse contract, crates/boyko_reflect/src/ecs.rs:169 "pub fn components_of_into(") loses: the problem count is not bounded by the field count (NestedCycle pushes once per cycle-closing edge), so it needs a counting pass plus a filling pass - strictly more work for the same answer. OVERTURNED ONLY IF a consumer must retain the list across frames; then it is a resource-column (ScratchColumn&lt;Problem>) on that consumer's Resource - still no std heap. [rev2 writer, reconciliation] The reflect lane decided this D row as kernel-internal ("the diagnostics substrate") against the rev-1 vocabulary. Gap 6 added the form `diagnostics` and moved every D row there, so this row follows gap 6. The lane's decision on the MECHANISM stands unchanged: validate becomes a validate_with visitor plus a count-returning validate, one pass and zero allocation, and a caller that keeps the list uses a ScratchColumn&lt;Problem>. Class: RUNTIME code of the EDITOR build, not compile-time: boyko_reflect is an optional dependency behind every consumer's non-default `reflect` feature (crates/boyko_reflect/src/lib.rs:11 "Feature off ⇒ the crate is not in the consumer's resolved dependency closure at all"), so it is absent from ship closures (GATES G1/G2/G3) but runs in the editor. Heap only on the error path: the Vec's first push. No non-test caller exists in the tree today: `validate(` over crates/*/src finds the definition and one SAFETY comment only; the callers are boyko_reflect tests (c3_type_info, c6_nested) and reflect_fixture c7_derive_bake (CORE D25). Readers: caller of validate (tests; future editor tooling; NestedCursor::new precondition) |
| crates/boyko_reflect/src/type_info.rs:435 `let mut problems = Vec::new();` | validate (problems) | `Vec<Problem>` | D | diagnostics | none | eliminated with row type_info.rs:434 (the local becomes the visitor call) ; ECS destination: diagnostics substrate: structured Problem records streamed to a caller visitor (no buffer) | D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:1579 "1. **Kind/accessor coherence, asserted:** a `validate(info) -> Result<(), Vec<Problem>>` walked" [CONFLICT: the plan fixes a std Vec in the signature; superseded by the owner's 2026-09-10 order ('no allocator but ours')] + in-code exception text to rewrite with the row: crates/boyko_reflect/src/type_info.rs:420 "/// `Vec` is the natural shape for the error path and is not a hot-path allocation:" | [local; growth once; addr_cached no; rung R6] ECS: Same decision as row type_info.rs:434 (one datum, two sites, ledger convention: return + local both rowed). D row: a coherence-failure report, and an Ok descriptor never allocates (Vec::new() then no push). FORM kernel-internal - the diagnostics substrate the ledger agrees on for D rows (section (i) 'D rows: diagnostics form'), realised here with no buffer at all. SKIPPED: component / dense-component - a TypeInfo describes a Rust TYPE and types are not entities in this kernel (crates/boyko_ecs/src/ecs/identifiers/primitives.rs:15 "pub struct $name(pub usize);"; the macros-aether KF-43 refutation of type entities applies verbatim); enable-state - not a boolean; relation - no entity-to-entity edge; event - returned to the caller inside one call, never produced by one system for another; resource-column - not durable (dropped by the caller); system-scratch - validate runs in no system (tests, editor tool calls); if an editor system calls it, the problems land in THAT system's own ScratchColumn&lt;Problem> (Problem is Copy: crates/boyko_reflect/src/type_info.rs:392 "#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]"), which is the caller's row, not this one; scope-arena - no threadpool scope. DECIDED BY PERFORMANCE (owner 2026-09-11): replace the Vec with a visitor, `validate_with(info, &mut impl FnMut(Problem))`, plus `validate(info) -> Result<(), usize>` (the count) for the Ok/Err precondition NestedCursor::new names (crates/boyko_reflect/src/cursor.rs:136 "// SAFETY: the cursor invariant includes `validate(self.info) == Ok`"). One pass, zero allocation, and CORE C3 gate 1's contract survives because the visitor sees every violation (crates/boyko_reflect/src/type_info.rs:413 "/// Returns **every** violation found, not the first"). The out-slice alternative (components_of_into's complete-or-refuse contract, crates/boyko_reflect/src/ecs.rs:169 "pub fn components_of_into(") loses: the problem count is not bounded by the field count (NestedCycle pushes once per cycle-closing edge), so it needs a counting pass plus a filling pass - strictly more work for the same answer. OVERTURNED ONLY IF a consumer must retain the list across frames; then it is a resource-column (ScratchColumn&lt;Problem>) on that consumer's Resource - still no std heap. [rev2 writer, reconciliation] The reflect lane decided this D row as kernel-internal ("the diagnostics substrate") against the rev-1 vocabulary. Gap 6 added the form `diagnostics` and moved every D row there, so this row follows gap 6. The lane's decision on the MECHANISM stands unchanged: validate becomes a validate_with visitor plus a count-returning validate, one pass and zero allocation, and a caller that keeps the list uses a ScratchColumn&lt;Problem>. Class: The construction site of row type_info.rs:434's return value. RUNTIME code of the EDITOR build, not compile-time: boyko_reflect is an optional dependency behind every consumer's non-default `reflect` feature (crates/boyko_reflect/src/lib.rs:11 "Feature off ⇒ the crate is not in the consumer's resolved dependency closure at all"), so it is absent from ship closures (GATES G1/G2/G3) but runs in the editor. Heap only on the error path: the Vec's first push. No non-test caller exists in the tree today: `validate(` over crates/*/src finds the definition and one SAFETY comment only; the callers are boyko_reflect tests (c3_type_info, c6_nested) and reflect_fixture c7_derive_bake (CORE D25). Readers: validate's own arms (type_info.rs:443-512), check_nested_graph (:520), returned as Err at :526 |
| crates/boyko_reflect/src/type_info.rs:592 `fn exhaust(walk: &mut NestedWalk, problems: &mut Vec<Problem>, name: &'static str) {` | exhaust | `Vec<Problem>` | D | diagnostics | none | the param becomes the `&mut impl FnMut(Problem)` visitor (row type_info.rs:434) ; ECS destination: diagnostics substrate: structured Problem records streamed to a caller visitor (no buffer) | D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:1579 "1. **Kind/accessor coherence, asserted:** a `validate(info) -> Result<(), Vec<Problem>>` walked" [CONFLICT: the plan fixes a std Vec in the signature; superseded by the owner's 2026-09-10 order ('no allocator but ours')] + in-code exception text to rewrite with the row: crates/boyko_reflect/src/type_info.rs:420 "/// `Vec` is the natural shape for the error path and is not a hot-path allocation:" | [param; growth once; addr_cached no; rung R6] ECS: Same decision as row type_info.rs:434: the growth parameter of the eliminated Vec turns into the visitor. D row: a coherence-failure report, and an Ok descriptor never allocates (Vec::new() then no push). FORM kernel-internal - the diagnostics substrate the ledger agrees on for D rows (section (i) 'D rows: diagnostics form'), realised here with no buffer at all. SKIPPED: component / dense-component - a TypeInfo describes a Rust TYPE and types are not entities in this kernel (crates/boyko_ecs/src/ecs/identifiers/primitives.rs:15 "pub struct $name(pub usize);"; the macros-aether KF-43 refutation of type entities applies verbatim); enable-state - not a boolean; relation - no entity-to-entity edge; event - returned to the caller inside one call, never produced by one system for another; resource-column - not durable (dropped by the caller); system-scratch - validate runs in no system (tests, editor tool calls); if an editor system calls it, the problems land in THAT system's own ScratchColumn&lt;Problem> (Problem is Copy: crates/boyko_reflect/src/type_info.rs:392 "#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]"), which is the caller's row, not this one; scope-arena - no threadpool scope. DECIDED BY PERFORMANCE (owner 2026-09-11): replace the Vec with a visitor, `validate_with(info, &mut impl FnMut(Problem))`, plus `validate(info) -> Result<(), usize>` (the count) for the Ok/Err precondition NestedCursor::new names (crates/boyko_reflect/src/cursor.rs:136 "// SAFETY: the cursor invariant includes `validate(self.info) == Ok`"). One pass, zero allocation, and CORE C3 gate 1's contract survives because the visitor sees every violation (crates/boyko_reflect/src/type_info.rs:413 "/// Returns **every** violation found, not the first"). The out-slice alternative (components_of_into's complete-or-refuse contract, crates/boyko_reflect/src/ecs.rs:169 "pub fn components_of_into(") loses: the problem count is not bounded by the field count (NestedCycle pushes once per cycle-closing edge), so it needs a counting pass plus a filling pass - strictly more work for the same answer. OVERTURNED ONLY IF a consumer must retain the list across frames; then it is a resource-column (ScratchColumn&lt;Problem>) on that consumer's Resource - still no std heap. [rev2 writer, reconciliation] The reflect lane decided this D row as kernel-internal ("the diagnostics substrate") against the rev-1 vocabulary. Gap 6 added the form `diagnostics` and moved every D row there, so this row follows gap 6. The lane's decision on the MECHANISM stands unchanged: validate becomes a validate_with visitor plus a count-returning validate, one pass and zero allocation, and a caller that keeps the list uses a ScratchColumn&lt;Problem>. Class: `&mut Vec<Problem>` growth parameter of the Vec rowed at type_info.rs:435 (ledger convention: &mut Vec params are rows, e.g. boyko_macros/src/ui.rs:435). The walk's own state is two fixed on-stack arrays (type_info.rs:571-587) - no heap. RUNTIME code of the EDITOR build, not compile-time: boyko_reflect is an optional dependency behind every consumer's non-default `reflect` feature (crates/boyko_reflect/src/lib.rs:11 "Feature off ⇒ the crate is not in the consumer's resolved dependency closure at all"), so it is absent from ship closures (GATES G1/G2/G3) but runs in the editor. Heap only on the error path: the Vec's first push. No non-test caller exists in the tree today: `validate(` over crates/*/src finds the definition and one SAFETY comment only; the callers are boyko_reflect tests (c3_type_info, c6_nested) and reflect_fixture c7_derive_bake (CORE D25). Readers: the acyclicity walk (Check B, CORE D21); pushes at type_info.rs:595 / :644 |
| crates/boyko_reflect/src/type_info.rs:605 `fn check_nested_graph(root: &TypeInfo, problems: &mut Vec<Problem>) {` | check_nested_graph | `Vec<Problem>` | D | diagnostics | none | the param becomes the `&mut impl FnMut(Problem)` visitor (row type_info.rs:434) ; ECS destination: diagnostics substrate: structured Problem records streamed to a caller visitor (no buffer) | D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:1579 "1. **Kind/accessor coherence, asserted:** a `validate(info) -> Result<(), Vec<Problem>>` walked" [CONFLICT: the plan fixes a std Vec in the signature; superseded by the owner's 2026-09-10 order ('no allocator but ours')] + in-code exception text to rewrite with the row: crates/boyko_reflect/src/type_info.rs:420 "/// `Vec` is the natural shape for the error path and is not a hot-path allocation:" | [param; growth once; addr_cached no; rung R6] ECS: Same decision as row type_info.rs:434: the growth parameter of the eliminated Vec turns into the visitor. D row: a coherence-failure report, and an Ok descriptor never allocates (Vec::new() then no push). FORM kernel-internal - the diagnostics substrate the ledger agrees on for D rows (section (i) 'D rows: diagnostics form'), realised here with no buffer at all. SKIPPED: component / dense-component - a TypeInfo describes a Rust TYPE and types are not entities in this kernel (crates/boyko_ecs/src/ecs/identifiers/primitives.rs:15 "pub struct $name(pub usize);"; the macros-aether KF-43 refutation of type entities applies verbatim); enable-state - not a boolean; relation - no entity-to-entity edge; event - returned to the caller inside one call, never produced by one system for another; resource-column - not durable (dropped by the caller); system-scratch - validate runs in no system (tests, editor tool calls); if an editor system calls it, the problems land in THAT system's own ScratchColumn&lt;Problem> (Problem is Copy: crates/boyko_reflect/src/type_info.rs:392 "#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]"), which is the caller's row, not this one; scope-arena - no threadpool scope. DECIDED BY PERFORMANCE (owner 2026-09-11): replace the Vec with a visitor, `validate_with(info, &mut impl FnMut(Problem))`, plus `validate(info) -> Result<(), usize>` (the count) for the Ok/Err precondition NestedCursor::new names (crates/boyko_reflect/src/cursor.rs:136 "// SAFETY: the cursor invariant includes `validate(self.info) == Ok`"). One pass, zero allocation, and CORE C3 gate 1's contract survives because the visitor sees every violation (crates/boyko_reflect/src/type_info.rs:413 "/// Returns **every** violation found, not the first"). The out-slice alternative (components_of_into's complete-or-refuse contract, crates/boyko_reflect/src/ecs.rs:169 "pub fn components_of_into(") loses: the problem count is not bounded by the field count (NestedCycle pushes once per cycle-closing edge), so it needs a counting pass plus a filling pass - strictly more work for the same answer. OVERTURNED ONLY IF a consumer must retain the list across frames; then it is a resource-column (ScratchColumn&lt;Problem>) on that consumer's Resource - still no std heap. [rev2 writer, reconciliation] The reflect lane decided this D row as kernel-internal ("the diagnostics substrate") against the rev-1 vocabulary. Gap 6 added the form `diagnostics` and moved every D row there, so this row follows gap 6. The lane's decision on the MECHANISM stands unchanged: validate becomes a validate_with visitor plus a count-returning validate, one pass and zero allocation, and a caller that keeps the list uses a ScratchColumn&lt;Problem>. Class: `&mut Vec<Problem>` growth parameter of the Vec rowed at type_info.rs:435 (ledger convention: &mut Vec params are rows, e.g. boyko_macros/src/ui.rs:435). The walk's own state is two fixed on-stack arrays (type_info.rs:571-587) - no heap. RUNTIME code of the EDITOR build, not compile-time: boyko_reflect is an optional dependency behind every consumer's non-default `reflect` feature (crates/boyko_reflect/src/lib.rs:11 "Feature off ⇒ the crate is not in the consumer's resolved dependency closure at all"), so it is absent from ship closures (GATES G1/G2/G3) but runs in the editor. Heap only on the error path: the Vec's first push. No non-test caller exists in the tree today: `validate(` over crates/*/src finds the definition and one SAFETY comment only; the callers are boyko_reflect tests (c3_type_info, c6_nested) and reflect_fixture c7_derive_bake (CORE D25). Readers: the acyclicity walk (Check B, CORE D21); pushes at type_info.rs:595 / :644 |
| crates/boyko_reflect/src/type_info.rs:618 `fn walk_nested(node: &TypeInfo, walk: &mut NestedWalk, problems: &mut Vec<Problem>) {` | walk_nested | `Vec<Problem>` | D | diagnostics | none | the param becomes the `&mut impl FnMut(Problem)` visitor (row type_info.rs:434) ; ECS destination: diagnostics substrate: structured Problem records streamed to a caller visitor (no buffer) | D:/wt/reflect/docs/REFLECTION-PLAN-CORE.md:1579 "1. **Kind/accessor coherence, asserted:** a `validate(info) -> Result<(), Vec<Problem>>` walked" [CONFLICT: the plan fixes a std Vec in the signature; superseded by the owner's 2026-09-10 order ('no allocator but ours')] + in-code exception text to rewrite with the row: crates/boyko_reflect/src/type_info.rs:420 "/// `Vec` is the natural shape for the error path and is not a hot-path allocation:" | [param; growth once; addr_cached no; rung R6] ECS: Same decision as row type_info.rs:434: the growth parameter of the eliminated Vec turns into the visitor. D row: a coherence-failure report, and an Ok descriptor never allocates (Vec::new() then no push). FORM kernel-internal - the diagnostics substrate the ledger agrees on for D rows (section (i) 'D rows: diagnostics form'), realised here with no buffer at all. SKIPPED: component / dense-component - a TypeInfo describes a Rust TYPE and types are not entities in this kernel (crates/boyko_ecs/src/ecs/identifiers/primitives.rs:15 "pub struct $name(pub usize);"; the macros-aether KF-43 refutation of type entities applies verbatim); enable-state - not a boolean; relation - no entity-to-entity edge; event - returned to the caller inside one call, never produced by one system for another; resource-column - not durable (dropped by the caller); system-scratch - validate runs in no system (tests, editor tool calls); if an editor system calls it, the problems land in THAT system's own ScratchColumn&lt;Problem> (Problem is Copy: crates/boyko_reflect/src/type_info.rs:392 "#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]"), which is the caller's row, not this one; scope-arena - no threadpool scope. DECIDED BY PERFORMANCE (owner 2026-09-11): replace the Vec with a visitor, `validate_with(info, &mut impl FnMut(Problem))`, plus `validate(info) -> Result<(), usize>` (the count) for the Ok/Err precondition NestedCursor::new names (crates/boyko_reflect/src/cursor.rs:136 "// SAFETY: the cursor invariant includes `validate(self.info) == Ok`"). One pass, zero allocation, and CORE C3 gate 1's contract survives because the visitor sees every violation (crates/boyko_reflect/src/type_info.rs:413 "/// Returns **every** violation found, not the first"). The out-slice alternative (components_of_into's complete-or-refuse contract, crates/boyko_reflect/src/ecs.rs:169 "pub fn components_of_into(") loses: the problem count is not bounded by the field count (NestedCycle pushes once per cycle-closing edge), so it needs a counting pass plus a filling pass - strictly more work for the same answer. OVERTURNED ONLY IF a consumer must retain the list across frames; then it is a resource-column (ScratchColumn&lt;Problem>) on that consumer's Resource - still no std heap. [rev2 writer, reconciliation] The reflect lane decided this D row as kernel-internal ("the diagnostics substrate") against the rev-1 vocabulary. Gap 6 added the form `diagnostics` and moved every D row there, so this row follows gap 6. The lane's decision on the MECHANISM stands unchanged: validate becomes a validate_with visitor plus a count-returning validate, one pass and zero allocation, and a caller that keeps the list uses a ScratchColumn&lt;Problem>. Class: `&mut Vec<Problem>` growth parameter of the Vec rowed at type_info.rs:435 (ledger convention: &mut Vec params are rows, e.g. boyko_macros/src/ui.rs:435). The walk's own state is two fixed on-stack arrays (type_info.rs:571-587) - no heap. RUNTIME code of the EDITOR build, not compile-time: boyko_reflect is an optional dependency behind every consumer's non-default `reflect` feature (crates/boyko_reflect/src/lib.rs:11 "Feature off ⇒ the crate is not in the consumer's resolved dependency closure at all"), so it is absent from ship closures (GATES G1/G2/G3) but runs in the editor. Heap only on the error path: the Vec's first push. No non-test caller exists in the tree today: `validate(` over crates/*/src finds the definition and one SAFETY comment only; the callers are boyko_reflect tests (c3_type_info, c6_nested) and reflect_fixture c7_derive_bake (CORE D25). Readers: the acyclicity walk (Check B, CORE D21); pushes at type_info.rs:595 / :644 |

---

*Row-count check: 19 rows in this file, 19 active, 0 superseded.*
