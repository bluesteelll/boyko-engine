export const meta = {
  name: 'jolt-pyramid-per-stage-timing',
  description: 'Why boyko trails Jolt 2.5x at W=8 is narrowed to serial work but not proven: allocation cost is ruled out, and Amdahl fits a 0.43-0.53 serial fraction. Time every physics stage at W=1 / 8 / 16 over one fixed step window, and decide the next fix with numbers.',
  phases: [{ title: 'Instrument' }, { title: 'Measure' }, { title: 'Adjudicate' }],
}
const HDR = [
  'STANDING RULES (the owner\'s): work ONLY in D:/wt/stagetime (branch timing/per-stage-pyramid @ d11962a9, a fresh worktree of merge/ke16-into-ecsnative made for this measurement). NEVER touch D:/wt/joltab - another agent is building and editing there right now - nor any other tree.',
  'Every bash call: export PATH="$HOME/.cargo/bin:$PATH" RUSTUP_TOOLCHAIN=stable-x86_64-pc-windows-gnu CARGO_TARGET_DIR=D:/wt/_targets/stagetime ; NEVER RUSTFLAGS (it replaces the config rustflags carrying -C target-cpu=x86-64-v3). Git: never checkout/stash/restore/clean/reset/--force; restore a scratch edit by cp against a cmp-proved snapshot. Do not commit.',
  'THIS IS A TIMING, on the owner\'s workstation, which he has cleared for timings. Other agents are running on this machine (research agents that call rg/python in bursts, and one agent that runs cargo in D:/wt/joltab). Protocol: build every binary BEFORE the window; open the window only after three consecutive 20 s samples see zero cargo/rustc/link/cl/ld processes AND total CPU load below 20 %; take a load receipt (CPU %, build-process count, top 3 processes by CPU) immediately before AND after every timed region; a region whose receipt shows a build process OR CPU load above 25 % outside the bench itself is DIRTY and is re-taken (a build-process-only receipt was proven blind yesterday: it passed a region visibly disturbed by agent load). Interleave the arms; two passes minimum, three if the first two disagree beyond the band. Record rustc -vV\'s commit hash and available_parallelism. No wall-clock number without its receipt. If the window will not open within 30 minutes, report that instead of timing on a loaded box.',
  'English. Every unsafe carries // SAFETY:.',
].join('\n')
const WHY = [
  'THE QUESTION the owner just asked: why does boyko trail Jolt? What is KNOWN on this tree (docs/OPEN-QUESTIONS.md, the last two entries): on the 1240-body Jolt pyramid single-threaded the two engines are ~1.0x; boyko scales 1.00 / 1.33 / 1.65 / 1.71 / 1.54 at W = 1/2/4/8/16 while Jolt reaches 3.99 at W=8, so boyko is 2.47x slower at W=8. The curve SATURATES between W=4 and W=8; Amdahl at W=8 fits a serial fraction of 0.43-0.53 of the single-thread step. RULED OUT since: the allocator - swapping the system heap for mimalloc changes nothing at any W (0.998 / 1.023 / 0.992), and on this tree (thread-pool Stage 3b present) a step makes only ~332 same-thread allocations at W=8. KE16 already took the dispatch cost from 5.2x to 2.5x.',
  'THE SERIAL CANDIDATES from the source (verify each; line numbers may have moved): physics_narrowphase (crates/boyko_physics/src/systems.rs near :357) is a plain serial system building box-box manifolds for every pair; the broadphase is serial at 1240 bodies because MIN_PARALLEL_BODIES = 4096; colours under MIN_PARALLEL_SLOTS_PER_COLOR = 256 solve INLINE (solver/colored.rs) and an earlier entry records the pyramid as "many NARROW colours"; MIN_SLOTS_PER_CHUNK = 64 caps chunks at slots/64, so a ~500-slot colour yields 9 chunks - 8 lanes get a straggler round, 16 lanes leave 7 idle. And one parallel-machinery candidate: 12 solve passes per step each open pool.scope waves whose fixed cost does not shrink with W.',
  'The harness: crates/boyko_physics/benches/jolt_parity_pyramid.rs (read it in full). It sets parallel_solve = parallel_broadphase = (workers > 1), so W=1 is the NON-parallel path and T(1)/T(W) is serial-vs-parallel. A CONFOUNDER found yesterday: under criterion the pyramid world is never reset and warm-up is time-based, so arms that run different iteration counts sample different simulated-time windows (the pile settles, contact count drifts). For a per-stage comparison across W every arm MUST simulate the SAME step window.',
  'THE OWNER\'S DIRECTION for whatever fix follows: physics becomes one ECS system - stages as ECS systems on the engine scheduler, data in ECS forms (dense components etc.), intra-system parallelism as a KERNEL feature any system can use, not a physics-local pool.scope arrangement. A separate study is designing that; this measurement decides which stage the first rung attacks.',
].join('\n')
const INSTR = [
  'PHASE 1 - INSTRUMENT, minimally and reversibly. FIRST check what already exists: the tree has a diagnostics/profiling subsystem (boyko_diag, zones, docs/PROFILING-SYSTEM-PLAN.md) - if per-system wall time can be read from it without editing boyko_physics, use it. If not, add the SMALLEST scratch instrumentation that answers the question: one Instant per physics stage (or a per-system timer around each physics system in the schedule the harness runs), accumulating nanoseconds per stage per step, printed at the end. Every scratch line carries a SCRATCH comment; snapshot every touched file first (cp + cmp).',
  'DRIVER: do not use criterion for this. Write a scratch driver (an example or a bench with harness = false, or a test with --release) that for each W builds a FRESH pyramid world, runs a FIXED warm-up of K steps and then a FIXED measured window of N steps (choose K and N so the window is > 3 s at W=1; state them), identical for every arm, and prints per-stage totals, per-step means and the per-step total. Same scene, same K, same N for W=1, 8, 16.',
  'Stages to separate at minimum: broadphase / narrowphase / integrate / constraint-graph build and colouring / solve - split into colours DISPATCHED through pool.scope and colours solved INLINE, and inside dispatched colours the wall time of the scope call / everything else (name any other system the schedule runs). Also count per step: colours, colours that clear the 256-slot gate, slots per colour (min/median/max), chunks per dispatched colour, contact pairs, manifolds.',
  'PERTURBATION CHECK (a timing - follow the protocol): the per-step total with instrumentation vs the same driver with instrumentation compiled out, per W; the instrumented total must land inside the run-to-run band. If not, reduce the instrumentation and say so.',
].join('\n')
const MEAS = [
  'PHASE 2 - MEASURE. Arms W=1 and W=8 (the two the Amdahl argument turns on) and W=16 (SMT control), interleaved W1, W8, W16, W1, W8, W16 over at least two passes. Per arm: total step ms and per-stage ms and share. Then the numbers the question needs: (a) the fraction of T(1) spent in stages that do NOT scale (their W=8 time ~ their W=1 time) - name them; (b) each stage\'s own scaling T_stage(1)/T_stage(8); (c) the solve alone, split dispatched vs inline; (d) an Amdahl prediction from (a) of the best T(1)/T(8) reachable WITHOUT touching those stages, compared with the measured 1.7-1.8.',
  'Anti-vacuity: assert the scene did its work (bodies moved, contacts > 0, colours > 0) and that the per-stage sum equals the total within a stated tolerance - a stage missing from the sum is not measured. Then RESTORE every scratch-edited tracked file byte-identically (cmp) and report git status.',
].join('\n')
const SCHEMA = { type: 'object', properties: { instrumentation: { type: 'string' }, driver: { type: 'string' }, perturbation_check: { type: 'string' }, table: { type: 'string' }, non_scaling_fraction: { type: 'string' }, per_stage_scaling: { type: 'string' }, solve_split: { type: 'string' }, amdahl_prediction: { type: 'string' }, counts: { type: 'string' }, receipts: { type: 'string' }, restored: { type: 'string' }, open: { type: 'array', items: { type: 'string' } } }, required: ['instrumentation', 'driver', 'perturbation_check', 'table', 'non_scaling_fraction', 'per_stage_scaling', 'solve_split', 'amdahl_prediction', 'counts', 'receipts', 'restored', 'open'] }
phase('Instrument')
const i = await agent(HDR + '\n' + WHY + '\n' + INSTR + '\nYou are the DEVELOPER+TESTER. Phase 1 only: report what exists, what you added (file:line), the driver and its K/N, and the perturbation check.', { label: 'instrument', effort: 'high' })
phase('Measure')
const m = await agent(HDR + '\n' + WHY + '\nInstrumentation state: ' + i + '\n' + MEAS + '\nYou are the TESTER. Take the measurement.', { label: 'measure', effort: 'high', schema: SCHEMA })
phase('Adjudicate')
const a = await agent(HDR + '\n' + WHY + '\nThe tester reports: ' + JSON.stringify(m) + '\nYou are the ADJUDICATOR (independent; you may re-run one arm to check a claim, under the same protocol). Check: do the stages sum to the total; did the instrumentation perturb the total beyond the band; are the receipts clean INCLUDING the CPU-load column; did every arm simulate the same fixed step window; is the non-scaling fraction computed from stages that genuinely do not scale rather than from stages that scale badly (different diagnoses, different fixes). Then answer, with the numbers, in plain words the owner can act on: WHY boyko trails Jolt at W=8 - which stages, how much of the step each, and whether the rest is dispatch cost - and decide the next fix: (A) make the serial stages parallel (narrowphase, broadphase below 4096 bodies) and lower the per-colour gate / chunk quantisation, (B) the in-scope barrier for the dispatched colours, or (C) both in a stated order. For each, what it is predicted to buy at W=8, so the prediction can fail, and which KERNEL feature it needs in the owner\'s one-ECS-system direction. End with one line a reader can quote.', { label: 'adjudicate', effort: 'high' })
return { instrument: i, measure: m, adjudication: a }
