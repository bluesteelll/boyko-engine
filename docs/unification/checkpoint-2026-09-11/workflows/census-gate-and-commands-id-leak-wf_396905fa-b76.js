export const meta = {
  name: 'census-gate-and-commands-id-leak',
  description: 'The frame-allocation census on the KE16 tree is sound but its gate has two defects (upward headroom lets an extra fan-out per colour pass pass green; libtest\'s 60 s notice can allocate inside the window), and it found an unbounded kernel leak: Commands never recycles entity ids, so a flat population grows the free list and slot store forever. Fix and land the gate first, design the id recycling in parallel, then implement it red-first with review.',
  phases: [{ title: 'Gate' }, { title: 'Design' }, { title: 'Implement' }, { title: 'Review' }, { title: 'Test' }],
}
const TREE = 'D:/wt/joltab'
const HDR = [
  'STANDING RULES (the owner\'s): work ONLY in ' + TREE + ' (branch merge/ke16-into-ecsnative @ d11962a9: feat/ecs-native-storage with the KE16 thread pool). Never touch another tree. Other agents READ this tree concurrently (read-only research); that is fine.',
  'Every bash call: export PATH="$HOME/.cargo/bin:$PATH" RUSTUP_TOOLCHAIN=stable-x86_64-pc-windows-gnu CARGO_TARGET_DIR=D:/wt/_targets/allocgate ; NEVER set RUSTFLAGS; --config profile.dev.debug=0. NEVER put a target dir on C: or in the scratchpad (C: is full). NO BENCHMARKS and no wall-clock verdicts: the owner said the machine is not quiet. Allocation counts, tests, clippy, Miri are fine.',
  'Git: NEVER checkout/stash/restore/clean/reset/--force/--no-verify; restore a scratch mutation with plain cp and then touch the file (cp -p keeps the old mtime and cargo then keeps the MUTATED build - measured yesterday). DO NOT COMMIT: the orchestrator commits. Never stage anything.',
  'English. Every unsafe block carries // SAFETY: with the concrete invariants. expect("invariant: ...") instead of unwrap where a panic is by design.',
].join('\n')
const GATE = [
  'THE CENSUS (untracked in ' + TREE + '): crates/boyko_physics/tests/alloc_frame_census.rs (the census + gate, fn gate() in one #[test] frame_allocation_census) and crates/boyko_physics/tests/alloc_frame_attribution.rs. The adjudicator found it SOUND with two gate defects and three report corrections - fix exactly these:',
  '(1) GATE DEFECT: one extra fan-out per colour pass in the colored solver (+24 per step, 331.8 -> 355.8, MAX 363) stays GREEN, because S1c release pins scope 109..=133 and the upward one-colour headroom was never observed in 4,352 steady steps. Fix: pin S1c against what can actually occur - either pin scopes EXACTLY against the solver\'s own count of dispatched colours (only if that count is observable without editing boyko_physics src), or drop the upward headroom (scope <= 121, chunk <= 217, MAX <= 339) while keeping the downward range the long run used (scopes 109..121, chunks 193..217, the pile keeps settling: block means 331.5 -> 307.5). Same treatment for the S1c debug pins. Then re-run the adjudicator\'s mutation (one extra pool.scope fan-out per colour pass, restore with cp + touch) and show it goes RED; re-run the three mutations that were already red (a fresh Vec + push per step in physics_apply; the same per Schedule::run; a Local<Vec> pushed and never cleared) and show they stay RED.',
  '(2) LATENT FLAKE: libtest\'s "has been running for over 60 seconds" notice allocates on the main thread and is counted (2 OTHER in one frame in a long run). The census takes 17-24 s release / 34 s debug today, under the line, but a loaded machine can cross it. Fix it structurally - e.g. harness = false for these two [[test]] targets with a main() that runs the census and exits non-zero on failure - so the counter cannot see a harness allocation at any duration. If you pick another mechanism, show why it is equivalent.',
  '(3) REPORT CORRECTIONS in both file headers: quote S1c as 302..339 per step with 256-step block means ~307..332 (the "331.797 identical in 9 runs" figure is a fixed window, not the steady state); the "99.5 % physics_solve_colored" trace was a warm-up step (145 scopes, 386 allocations, 12 colours) - pair it with its own numbers; and state the coverage boundary - the counter sees the Rust heap only, VmReservation::commit (VirtualAlloc MEM_COMMIT) is not counted, so "0 from every physics buffer" means zero heap allocations, not zero memory growth.',
  'Then run both profiles (release and dev) green, cargo clippy -p boyko-physics --tests -- -D warnings, and cargo check --workspace --all-targets. Report the final pins, every mutation result with its log path under D:/tmp, and the sha256 of both files.',
].join('\n')
const LEAK = [
  'THE KERNEL DEFECT (verified against the source by two independent agents): Commands::spawn calls EntityCounter/EntityMaster::reserve_entity, which is a fetch_add on next_entity_id and never pops the free list (Phase 11 EM2 forbids a worker to pop it - crates/boyko_ecs/src/ecs/core/entity/entity_master.rs around :186 and the test reserve_entity_skips_free_list). DespawnCommand -> EcsMaster::delete_entity pushes the id onto free_entity_ids. So a flat population driven through Commands (spawn N + despawn N per frame) grows EntityMaster::free_entity_ids AND the entities_inland slot store by one entry per deferred despawn, FOREVER: +131,072 in 2048 frames at 64 churn a frame, measured. The census\'s S2 scene shows it as a realloc of the free list (pinned at exactly 2 in its 256-frame window) and as uncounted VirtualAlloc commits in the slot store. Proposed direction from the census: recycle on the deferred route, dispatcher-side in CommandQueue::apply, where EM2 allows touching the free list. Replacing the Vec with a column would NOT fix it - the growth is logical, not an allocator choice.',
  'Owner constraints that apply: the kernel is finished before anything else; no allocator but the engine\'s own (a new structure goes on the memory library - VmColumn / VmReservation - not a std Vec); lock-free on the hot path; entity identity invariants (generations, EM1-EM4, the handle a worker got from Commands::spawn stays valid) must hold.',
].join('\n')
const DESIGN_Q = 'Decide HOW ids get recycled without breaking EM2 or handles already returned to user code. Candidates to weigh with the code open (not an exhaustive list): (a) a lock-free recycled-id ring on VmColumn that the dispatcher refills from the free list at every apply window and workers pop with a CAS before falling back to fetch_add (the handle carries the recycled id with its bumped generation); (b) a per-frame pre-reserved batch of recycled ids handed to each worker\'s command buffer at dispatch; (c) remapping reserved-but-fresh ids at apply time (argue why it cannot work if a handle has escaped); (d) anything the code suggests. For the chosen one: the generation rule, what a stale handle to a recycled id sees, ordering with the apply window, determinism of id assignment (same op sequence -> same ids?), what happens under Miri/loom, and the test plan: a RED-FIRST test (flat Commands churn; assert free_entity_ids length and the slot store length stay bounded by the live population plus a stated constant), plus the existing EM1-EM4 tests, plus the census S2 realloc pin to re-derive.'
const REV_SCHEMA = { type: 'object', properties: { verdict: { type: 'string' }, blocking: { type: 'array', items: { type: 'string' } }, non_blocking: { type: 'array', items: { type: 'string' } } }, required: ['verdict', 'blocking', 'non_blocking'] }
const gateP = (async () => { phase('Gate'); return await agent(HDR + '\n' + GATE + '\nYou are the DEVELOPER+TESTER for the census gate. Do (1), (2), (3), then the runs.', { label: 'gate-fix', phase: 'Gate', effort: 'high' }) })()
const designP = (async () => {
  const d1 = await agent(HDR + '\n' + LEAK + '\n' + DESIGN_Q + '\nYou are the ARCHITECT. READ-ONLY (you cannot write files). Return the design as text with path:line citations.', { label: 'leak-design', phase: 'Design', agentType: 'architect', effort: 'high' })
  const c1 = await agent(HDR + '\n' + LEAK + '\nTHE DESIGN:\n' + d1 + '\nYou are the ARCHITECTURE CRITIC. READ-ONLY. Check it against the code: EM1-EM4, generations, the apply window, lock-freedom, what a worker holding a handle sees, determinism, Miri/loom soundness, and whether the red-first test can fail for the right reason. Blocking only if an implementer following it would write unsound or wrong code.', { label: 'leak-critique', phase: 'Design', agentType: 'architecture-critic', effort: 'high', schema: REV_SCHEMA })
  if (c1 && c1.blocking && c1.blocking.length) {
    const d2 = await agent(HDR + '\n' + LEAK + '\nTHE DESIGN:\n' + d1 + '\nTHE CRITIQUE: ' + JSON.stringify(c1) + '\nYou are the ARCHITECT, rev 2. Answer every blocking finding (fix or refute with path:line) and every non-blocking one; give the complete rev 2 as text.', { label: 'leak-design-2', phase: 'Design', agentType: 'architect', effort: 'high' })
    return { design: d2, critique: c1, rev: 2 }
  }
  return { design: d1, critique: c1, rev: 1 }
})()
const [gate, leak] = await Promise.all([gateP, designP])
phase('Implement')
const impl = await agent(HDR + '\n' + LEAK + '\nTHE APPROVED DESIGN (rev ' + leak.rev + '):\n' + leak.design + '\nCRITIQUE ON RECORD: ' + JSON.stringify(leak.critique) + '\nThe census gate work is finished in this tree: ' + (gate || '').slice(0, 6000) + '\nYou are the DEVELOPER. (1) Write the RED-FIRST test and show it FAILS on the current code for the stated reason (paste the failure). (2) Implement the design. (3) Show the test passes. (4) Re-derive the census S2 realloc pin in crates/boyko_physics/tests/alloc_frame_census.rs from the new behaviour (the pin rule written there: re-derive from the frame arithmetic, never widen it) and update its header note on the leak. Do not run the full workspace suite - the tester does.', { label: 'leak-implement', phase: 'Implement', agentType: 'developer', effort: 'high' })
phase('Review')
const rev = await agent(HDR + '\n' + LEAK + '\nTHE DESIGN:\n' + leak.design + '\nTHE DEVELOPER REPORTS:\n' + impl + '\nYou are the CODE REVIEWER. Review the diff (git diff in ' + TREE + ') for UB in unsafe blocks, atomics orderings, EM1-EM4, generation handling, a hidden allocation or a std container where the memory library is required, and whether the red-first test really failed for the right reason. Blocking only for defects.', { label: 'leak-review', phase: 'Review', agentType: 'code-reviewer', effort: 'high', schema: REV_SCHEMA })
let fix = null
if (rev && rev.blocking && rev.blocking.length) {
  fix = await agent(HDR + '\n' + LEAK + '\nTHE REVIEW: ' + JSON.stringify(rev) + '\nYou are the DEVELOPER. Fix every blocking finding and report the diff of your fixes.', { label: 'leak-fix', phase: 'Review', agentType: 'developer', effort: 'high' })
}
phase('Test')
const test = await agent(HDR + '\nWork done in this tree: the census gate fix (' + (gate || '').slice(0, 3000) + ') and the Commands id recycling (' + impl.slice(0, 5000) + (fix ? ' / review fixes: ' + fix.slice(0, 2000) : '') + ').\nYou are the TESTER. Run: cargo check --workspace --all-targets; cargo clippy --workspace --all-targets -- -D warnings; cargo test --workspace --all-targets --no-fail-fast (report per-target failures, and say whether each was red before this work - use git stash? NO: compare against the recorded state instead, or re-run a failing target\'s test on a scratch copy of HEAD only if needed); the census in release and dev; the red-first test; the boyko_ecs entity/commands tests; Miri on the new lock-free structure if it has unsafe or atomics (unset RUSTUP_TOOLCHAIN; cargo +nightly-x86_64-pc-windows-gnu miri test -p boyko-ecs --test <the new test or the unit tests>; MIRIFLAGS must spell all five flags: -Zmiri-tree-borrows -Zmiri-disable-isolation -Zmiri-permissive-provenance -Zmiri-ignore-leaks -Zmiri-preemption-rate=0). Report every command with its exit code and the count of passed/failed/ignored.', { label: 'test', phase: 'Test', agentType: 'tester', effort: 'high' })
return { gate, leak_design_rev: leak.rev, leak_critique: leak.critique, impl, review: rev, fix, test }
