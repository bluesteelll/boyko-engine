export const meta = {
  name: 'runtime-data-ledger-rev3',
  description: 'Ledger rev 2 is arithmetically exact (2244 active rows, 47 kernel features, 0 unclassified) but its delta recheck found seven consistency gaps and six minor items: the ui-lane census stopped at boyko_ui, engine Q1 not applied to asset rows, KF-24 rejected while a row needs it, the diagnostics form misfitted to numeric capture data, uncounted supplementary rows, the path-conversion decision applied two ways, and kernel rows placed after physics. Close exactly those, regenerate, recheck the delta.',
  phases: [{ title: 'Fix' }, { title: 'Regenerate' }, { title: 'Recheck' }],
}
const SP = 'C:/Users/flint/AppData/Local/Temp/claude/D--claude-BoykoEngine/dad9a19e-5512-4bbd-9976-cb44400eeb64/scratchpad/ledger'
const HDR = [
  'HARD RULES. READ-ONLY on every code tree (D:/wt/joltab, D:/wt/ui, D:/wt/reflect). No cargo, no rustc, no tests. English. Cite tree:path:line with the quoted line. Work files go under ' + SP + '/rev3/ (create it; copy from rev2/, never edit rev2/). The only files you may write outside the scratchpad are the final ledger documents in D:/claude/BoykoEngine/docs/memory/ (RUNTIME-DATA-LEDGER.md, ledger/*.md, runtime-data-ledger.tsv) - and only in the Regenerate phase.',
  'INPUTS: the rev-2 ledger (D:/claude/BoykoEngine/docs/memory/RUNTIME-DATA-LEDGER.md = index, ledger/<group>.md = rows, runtime-data-ledger.tsv), its group JSONs and generator under ' + SP + '/rev2/ (synth2/ has the writer\'s scripts and writer_changes.json), and the rev-2 recheck report VERBATIM at ' + SP + '/rev2/recheck_rev2_report.md - read it in full; its seven work items and six minor items are the whole scope.',
  'DECISION AUTHORITY: the owner delegated every open question to performance ("decide whichever is best for performance"); the decisions files are D:/claude/BoykoEngine/docs/physics/PHYSICS-ECS-UNIFICATION-DECISIONS.md and D:/claude/BoykoEngine/docs/unification/ENGINE-RUNTIME-ECS-DECISIONS.md (Q1 assets ARE entities; Q2 UI opt-in; Q3 window and player are entities; Q4 delete dead paths). The owner\'s order "finish the kernel first" means EVERY boyko_ecs row whose destination is the kernel\'s own storage or a kernel feature lands in rung 2 - not after physics.',
].join('\n')
const FIX = [
  'THE WORK (from the recheck, with the orchestrator\'s decisions where the recheck left a choice):',
  '1. ui-lane census outside boyko_ui: census every non-test heap site the lane ADDS or CHANGES in any crate (the reflect-lane scope rule: every added file plus the added hunks - `git -C D:/wt/ui diff merge-base..HEAD` against joltab), including boyko_render/src/ui (gather.rs, upload.rs, pack.rs, bindless.rs), benches, and boyko_shaderdsl emit_ui / emit/mod.rs / emit/shaders.rs (compile-time C rows if they run only in generation). Add rows; supersede the stale joltab boyko_render/src/ui rows they replace.',
  '2. Apply engine Q1 (assets are entities) to every asset row: the 32 rows the recheck lists (render 19, codec-tools 8, ecs-services 4, asset_refs.rs:99). Asset values become components / K3 group columns on the asset entity; refcounting becomes the count-only relation (EK15b) - asset_refs.rs:99 moves from event to relation; decode payloads that exist only during load stay system-scratch with a note. Every changed row gets a rev-3 note naming Q1.',
  '3. KF-24 (same-frame event delivery): joltab boyko_scene propagation.rs:133 `detached: Vec<Entity>` needs same-frame delivery or a stale GlobalTransform shows for one frame. DECIDE by performance: the recheck\'s own evidence from the UI lane (executor applies a system\'s commands before its ordered successors run, schedule.rs apply-before-decrement) suggests the observer-to-system queue can be a command/trigger delivered at the ordering edge - verify that on joltab for this observer path; if it holds, the row\'s form is `event` delivered through that edge and KF-24 stays rejected with the evidence; if not, restore KF-24 for this row and say so.',
  '4. The diagnostics form: split it. Keep `diagnostics` for text and error payloads (cold). Re-form the 81 numeric capture rows (profiling reduce.rs, gpu_scene capture arrays, vg_census histograms, analysis bit matrices) to what their notes prescribe (system-scratch on ScratchColumn lanes, or resource-column for retained captures), and the 10 per-frame-growing rows by their growth. Rewrite the 149 diagnostics notes that open "kernel-internal = ..." and the 16 ecs-services "CHOSEN kernel-internal" openings.',
  '5. The 38 supplementary std-internal rows become counted rows: replace out-of-scope:third-party for std internals by the rev-2 vocabulary\'s allowed forms; the 18 class-D ones follow item 4; the per-frame std::env::var at joltab gpu_scene/mod.rs:6415 gets a real row (read once at boot into a resource - that is the performance answer) so the gate can see it.',
  '6. Path conversions: apply ONE rule by performance - every path conversion reachable after steady state (hot reload polls, log rotation, asset load, save/load) goes to the in-house UTF-16 FFI with a pre-encoded path held in the owning resource (as ledger index line ~1775 already says); boot-only conversions stay out-of-scope:os-owned. Re-form the boyko_log file.rs/binary.rs, boyko_ecs asset server.rs:127, boyko_serialize save.rs:717/load.rs:312 rows and the two ui-lane rows consistently.',
  '7. Rung reach: extend rung-2 routing to every boyko_ecs row whose destination is kernel storage or a kernel feature - including the 11 class-K rows at R4 (schedule.rs:122/161/172, entity_store.rs:120/123/125, system_meta.rs:140, assets.rs:204-207) and the boyko_ecs rows in R4/R6 whose form is kernel-internal or kernel-feature; keep the others and correct the rung-4 text.',
  'MINOR: M1 non_row total without the boyko_ui double count (4425); M2 normalise the three raw owning_entity values to relation-endpoint; M3 the three index text errors; M4 the seven UiParseReport lines as non_rows, and reflect.rs:671/688 Ident::new rows consistent with format_ident!; M5 record the ScratchColumn-field exception for the gap-2 rows (they are kernel storage already, so the gate must not count them as migratable); M6 the copy-pasted gap-4 note on demo render/mod.rs:62 Arc<wgpu::Buffer>.',
  'Write every change as {item, tsv_key, field, old, new, evidence} to ' + SP + '/rev3/changes.json.',
].join('\n')
phase('Fix')
const fix = await agent(HDR + '\n' + FIX + '\nYOU ARE THE FIXER. Return a per-item summary with counts.', { label: 'fix', phase: 'Fix', agentType: 'general-purpose', effort: 'high' })
phase('Regenerate')
const regen = await agent(HDR + '\nThe fixer reports: ' + fix + '\nYOU ARE THE WRITER. Regenerate the ledger as rev 3 from rev3/ (reuse rev2/synth2/ scripts): the index RUNTIME-DATA-LEDGER.md (header now says rev 3, date 2026-09-11, with a rev-3 change log per item and counts), the per-group files ledger/<group>.md (add any new group file the ui-lane render census needs, or fold it into ui-lane.md), and runtime-data-ledger.tsv (12 columns, no line numbers, superseded rows dropped). Verify: index totals = TSV data lines = sum of per-group active rows; every rung and form matrix reproduces from the rows; report the numbers.', { label: 'regenerate', phase: 'Regenerate', agentType: 'general-purpose', effort: 'high' })
phase('Recheck')
const recheck = await agent(HDR + '\nThe writer reports: ' + regen + '\nYOU ARE THE COMPLETENESS CRITIC, rev-3 pass - SCOPE = THE SEVEN ITEMS AND THE SIX MINOR ITEMS ONLY: check each landed as specified by reading the changed rows against the code (sample at least 3 per item; all rows for items 3, 5 and 7), re-census the ui-lane changes outside boyko_ui with your own regex, and re-derive the index totals and matrices. Do not re-audit rows rev 3 did not touch. Verdict COMPLETE or GAPS with a finite list, and one headline paragraph with the rev-3 numbers.', { label: 'recheck', phase: 'Recheck', agentType: 'general-purpose', effort: 'high' })
return { fix, regen, recheck }
