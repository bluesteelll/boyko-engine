# Gaia — the data language: campaign plan

**Aether is for logic, Gaia is for data** (owner, 2026-08-28). Gaia is one language with three
profiles — **scene** (world/level content), **ui** (documents over the ECS-native `boyko_ui`), and
**data** (the DataAsset/DataTable analog: item defs, ability params, loot tables) — authored as
text, **baked at build time** (reflection allowed only there, behind a default-off cargo feature),
shipped as a binary the runtime loads with **zero reflection, zero interpreter, zero fallback**.

**Start here after 2026-08-29:**
[`../AETHER-GAIA-REVISION-2026-08-29.md`](../AETHER-GAIA-REVISION-2026-08-29.md) — the revision
record shared with the Aether v2 campaign: what the engine refuted, which gates were struck as
unfalsifiable, the per-file change summary, and the work order (which ballot blocks each rung).

This directory is the decision record and plan. File map: [`DECISIONS.md`](DECISIONS.md) (every
ruling with its rationale and rejected alternative — the AIR-17 carrier), [`LANGUAGE.md`](LANGUAGE.md)
(the research-stage language sketch), [`PENDING-SYNTAX-PLAN.md`](PENDING-SYNTAX-PLAN.md)
(**owner-gated, and COMMITTED**: added by `97c504c8` on 2026-08-29, so it is tracked — what it
lacks is the owner's APPROVAL. It carries the reworked syntax and the conflict-audit fix plan, and
holds the **syntax ruling series R1–R7** — spelled like the Aether campaign rungs and unrelated
to them — the **conformance-audit findings** (`M#`/`C#`/`I#`/`G-##`, one prefix per hunting lens and
**not a contiguous range**; their full bodies live outside the repo, so each is summarised at its use
site), the **K1–K16 adversarial-pass record**, and the fix **Tiers 0–4**. Every "PENDING &lt;id&gt;" cite
in this corpus resolves to a use site there, and until the owner approves the file, the rulings it
carries are proposed, not ratified — with the exception of the rows its own text now marks RESOLVED
under an owner ruling of 2026-08-30). The commissioning research: two multi-agent passes
(2026-08-28, ~25 systems surveyed with sources; engine inventory verified line-by-line).

## Relation to Aether — separate scopes, ONE body of work

Gaia and [Aether v2](../aether-v2/CAMPAIGN.md) are **one body of work split by subject matter, not
two projects**: *Aether for logic, Gaia for data*. The couplings are concrete and checkable today —
this file's [`DECISIONS.md`](DECISIONS.md) cites the Aether campaign's **`KE#` kernel-backlog ids**
directly; the AI-orientation requirement set that binds Gaia lives in the **Aether** directory
([`../aether-v2/AI-ORIENTATION.md`](../aether-v2/AI-ORIENTATION.md)), where **AIR-08** (stable node
ids) and **AIR-16** (grammar rulings) carry `Gaia spec` in their own Rung column, **AIR-17** carries
`Gaia G0`, and **AIR-09** carries `R8 / Gaia tooling`; `AE####` and `GA####` are one registry
discipline minted by AIR-02; and two ballots of **this** `GB` series name rungs on the **Aether**
ladder (**GB-9** → Aether R0, **GB-8** → Aether R8). ✅ **Both RULED 2026-08-30**, and one of the
two cross-ladder claims did not survive the ruling: **GB-8 no longer names Aether R8 for the whole
census** — its id half lands at **G0, on this ladder**, and only the *link* half rides R8. GB-9's
"→ Aether R0" was always a decision deadline rather than a build dependency, and it is now closed.

**And each campaign finds the other's defects.** Three defects in *shipped* code — the `Or` dense
arm (Aether **KE1** / rung R0), `any_changed_since` over a dense or bitset bind source (**GK-2**
below), and the load path's dropped `requires` closure (**F4(ii)** below) — are **one mechanism**:
code that resolves a per-archetype pool without screening the storage kind, and so is blind to the
kinds that own no pool. **Two of the three were raised here, in kernel and UI code the logic
campaign owns.** The joint account, the couplings table, and the still-owed enumeration of the class
are in [`../AETHER-GAIA-REVISION-2026-08-29.md`](../AETHER-GAIA-REVISION-2026-08-29.md) §One
mechanism, three shipped defects and §Aether and Gaia are one body of work. A change touching any
coupling above updates **both** sides in the same commit — the diverged-pair cost is already
measured in this repo.

## The industry map, in one paragraph

Identity converged on a **pair** (durable file id + file-local object id) — Unity and Godot arrived
independently, Unreal's path-as-identity is the negative control (an entire redirector subsystem).
Composition converged on **patch-not-replace** with per-field granularity; no production system has
multiple inheritance. The most important negative result: **AOT compilation removes the parser, not
the runtime** — Slint still ships a full property graph; Svelte retreated from compile-time-only
reactivity; qmlsc silently degrades to an interpreter. Zero-cost reactivity is achievable only by
CONSTRUCTING the language so bindings are statically enumerable and the arrow is inverted
(sink asks source via ticks) — which is exactly what `boyko_ui`'s shipped bind path already does.
Logic creep has a documented trajectory (Paradox: literals → … → "calculated on every frame,
massive lag"); what stops it is refusal plus a pressure valve (curves), not discipline.

## Rung ladder

| Rung | What | Gate |
|---|---|---|
| **G0** | This directory; ratified decision lines (AIR-17); the AI-ORIENTATION correction (its "Gaia type-level substrate CLOSED" line was refuted by inventory). ✅ **The two ballots this row used to hold — F1 and F4 — were both answered 2026-08-30** (F1 by the owner, F4 delegated and ruled), so **G0 carries no open ballot.** ⚠ It is still NOT done: GB-8's ruling puts the AIR-census widening ON this row, and that edit has landed on no branch | docs exist before any grammar commit **AND** every AIR cross-note on a ratified line in gaia DECISIONS resolves to a defined AIR item (AIR-17's own second clause, restored here in AIR-17's own scope). A file that RESOLVES but is STALE against the ratified syntax does not satisfy a cross-reference — `LANGUAGE.md` is the standing example (PENDING M6). ✅ **GB-8 RULED 2026-08-30 [delegated], and this row now carries work.** (1) **No per-site waivers**, at any of the four censuses, ever; where a property is not decidable as written, narrow the PREDICATE and print the narrowing in the failure message. (2) **The AIR census widens to all of `docs/` and lands HERE, at G0** — a one-constant change, green at zero remediation. (3) **The LINK census is a separate deliverable at Aether R8** — measured 1636 relative targets under `docs/`, **59 dead across 11 files, 44 of them in `docs/AUDIT-2026-05-23.md`**; landing it with the id half would hold the free half hostage to 59 repairs. ⚠ The "188 of 302" this row used to cite is **not this tree's number and no in-tree gate produces it**: [`internal_docs_anchors.rs`](../../tests/internal_docs_anchors.rs) run live prints **735 anchors, 116 waived** — and the precedent is *stronger* than that aggregate, because the waiver concentrated entirely in the one document admitted under it, which waives **89 of 177 (50.3%)**. ⚠ The census file the ruling names, `tests/gaia_g0_citation_census.rs`, exists on `feat/threadpool-ke16` and **not on this branch**. |
| **G1** | **GK-4**: derive-emitted name-keyed field tables + typed constructors in `boyko_macros` (the macro-time bake route — no reflection needed even at bake). ✅ **F1 RULED BY THE OWNER 2026-08-30: this route, NOW**, not sequenced behind EG2, with the standing constraint that nothing here may foreclose streaming. ✅ **GB-5 RULED BY THE OWNER 2026-08-30 (recorded 2026-08-31): the field-table FREEZE is UNBLOCKED** — a scene document MAY declare an engine-derived field and the authored value is the INITIAL value. The disposition column **must not be a boolean**: all twelve measured members are *conditionally* derived, so the column records the CONDITION and the WRITER, and it is born behind the default-off bake feature so it never reaches the game binary. G1 is unstarted — `field_table` / `FieldTable` / `field_by_name` grep empty across `boyko_macros` and `boyko_ecs` — so the cost is one attribute and one column with zero consumers | red-first and TEXT-FREE (G1 predates the grammar, so no gate of G1's may parse anything): `bake_one(<typed ctor calls built from the derive-emitted field table>) == save_world(hand-built world)` byte-for-byte for one component; plus a probe pinning `BindText`'s actual Serializability class |
| **G2** | Language core: lexer (EOL normalization before the CST) → lossless CST → canonical printer. ✅ **GB-3 and GB-4 both RULED 2026-08-30, so the two ballots that held this rung are closed** — and the rulings hand G2 four deliverables rather than freeing it: (1) close the asset SIGIL **jointly with Gaia's arithmetic operator vocabulary** (the glyph cannot be chosen first — R3 makes whitespace insignificant, so any glyph that can open a binary operator is ambiguous; recommendation `~`); (2) regenerate [`PENDING-SYNTAX-PLAN.md`](PENDING-SYNTAX-PLAN.md) R4/R5/R7 and Tiers 1-2, which GB-3 supersedes; (3) land the undeclared **`from=` → `source=`** rename on `bind` that GB-4's measurement surfaced; (4) rewrite [`LANGUAGE.md`](LANGUAGE.md), which is stale against the ratified syntax. ⚠ G2 also still carries **F9's residual VALUES question** (may a document set a flag on an individual authored object) | `print(parse(canon)) == canon`; **absorbs the text entrance of G1's gate — `bake_one(text) == save_world(hand-built world)` byte-for-byte** — which cannot be stated at G1; round-trip compared on the WORLD side with a comparator GENERATED from the component registry (two in-tree proofs of why a hand list fails: the green-over-divergence equivalence gate and the 10-of-19 printer loss); parser dispatch + printer list + gate comparator = ONE generated table + coverage census |
| **G3** | Identity: asset/object ids, `@`-references, mandatory `link`, offline bake resolution. ✅ **GB-3 RULED 2026-08-30 — G3 carries no open ballot**, and gains the **asset-ref dangle check** from it: every sigil-marked token resolves at bake, and unresolvable is a coded `GA####` refusal naming the pack and the path, blamed on the reference's own span. ⚠ **F8** (name-vs-id) remains the candidate ballot for this rung, and is still open | red fixtures: dangling ref, duplicate id, anonymous-target refusal, stale patch target; the unstable-field-type bake lint (GN1) |
| **G4** | Composition: templates / `abstract` / patches / the priority ladder / `remove` / bake budgets / provenance sidecar. ✅ **GB-1 and GB-4 both RULED 2026-08-30 — G4 carries no open ballot.** From GB-1: a template expansion occupies **no ladder layer** — it is one step on the inheritance-DEPTH axis, ranked where `extends` ranks, resolution depth-then-ladder; an unlabeled write is `base`. From GB-4 it gains one deliverable and one red fixture: the instance form is `instance <name> extends\|copy <base-ref>` with the linkage word **mandatory**, `from` deleted, and **a missing linkage word is a coded refusal at a known span** — which is what stops G4 buying a second silent-default class beside the forgotten `layer=` it already carries | **file-order independence**: `bake(files) == bake(shuffle(files))` byte-identical over ≥2 shuffled permutations plus the reversed order, on a fixture whose ladder layers span several files; byte-identity scoped to the bake OUTPUT (diagnostics may still list files in walk order); the permutation count is reported by the test. Plus the two-writes-one-layer red fixture (the error must name BOTH files). Plus the Godot-#32179 fixture (diff-at-save eating an intentional override). Plus the per-axis bake-budget fixture set (DECISIONS §The logic line: one committed red fixture per axis). ⚠ The former `bake(1) == bake(W)` form is **struck as vacuous**: `W` has no referent anywhere in this corpus and nothing makes the bake parallel, so the gate could not fail — the exact class this pass repairs. If a parallel bake is ever intended it arrives as its own design line with a defined `W`. |
| **G5** | **data** profile (smallest). ✅ **F2 and F3 both ANSWERED 2026-08-30 — G5 carries no open ballot**, and this row's own wording is corrected by F2's ruling: tables bake to **one archetype column (`StorageKind::Table`)**, *not* "dense columns". A dense id is signature-excluded (`is_signature_storage` matches `StorageKind::Table` alone), so the ballot's "dense-column archetype" denotes nothing here — `get_or_create_archetype(&[DenseRow])` returns **the empty archetype**. Rows materialize **eagerly at load**; the `[DefOf]`-style generated row constants resolve to an **`Entity` captured at load**, never a row index; a table is pinned. Both file shapes bake through **one schema** (F3). Plus curves + `scalable`, contracts | per-profile fixtures; named red-first, the **eagerness** fixture: a violated `contract` on a field NOTHING reads must fail the bake, through both entrances — a plain declared field, and a field that a template expanded but no one ever reads — with blame on the file+span of the violating VALUE (Jsonnet's lazy assertions are the negative control) |
| **G6** | **scene** profile. ✅ **F4 and F5 both ANSWERED 2026-08-30, and F5 REWRITES THIS ROW'S SCOPE.** Owner, on F5: *"Все сразу грамотно по списку с самого начала."* Streaming is **no longer deferred to G8**: `load_cell` / `unload_cell`, **GK-1's cross-load map with a declared lifetime** and **cross-cell reference resolution** are all inside G6, together with the cell catalog. F4 supplies the seam — GK-3's suppress-then-fixup pass, four ordered sub-passes, with sub-pass 4 SPLIT into **4a** (relationship relink through the installed fn-pointer table, no user code) and **4b** (user hook dispatch) — and the **stable-asset-id carrier**: a stable asset NAME in the file resolved to the existing `MeshHandle(u32)` at load, no new component type. ⚠ GK-1 and GK-3 must land TOGETHER or the cross-cell half is silently half-present. ✅ **GB-6(a) RULED 2026-09-03: the scene-form fork is decided** — Gaia TEXT is the source the editor saves, the BINARY is what ships — so **G6 is no longer ballot-blocked** | reconstruct-and-compare on the catalog; **plus F4's coverage census enumerating all five insert-path mechanisms × three storage kinds, not hooks alone** |
| **G7** | **ui** profile. ✅ **F6 RULED BY THE OWNER 2026-08-30: `.ui` is MIGRATED to the Gaia `ui` profile and the old format DELETED in the same campaign.** ⚠ The price rides here rather than being discovered later: the migration is done **blind**. ⚠ The prerequisite this row used to state — *"a windowed UI pass and a real `UiPlugin`"* — is HALF STALE: a complete `UiPlugin` exists ([`plugin.rs`](../../crates/boyko_ui/src/plugin.rs)) and so does the production pack/upload/draw path; **the measured gap is that nothing WIRES them** — `crates/boyko_app/src` has zero `ui` references. **G7 carries no open ballot of its own**; its blockers are F6 and the wiring. ✅ **RULED BY THE OWNER 2026-09-03: editor v1 authors UI documents too** (*«И UI тоже в v1»*), so the editor's *assets are Gaia* rule has no exception on day one — and **this rung's POSITION moves UP the ladder** rather than sitting after G6. It does not become unblocked: the wiring prerequisite still binds. What moves with it is the `.ui` deletion trigger — a coverage census over `UiTextComponent::ALL` ∪ `UiBackground` on the WORLD side, which needs no window — now written BEFORE the windowed pass lands rather than after G6 | the STILL-FRAME gate, re-axed onto **counts, not a clock**: still frame over a HUD with 200 bindings — bind-sink writes executed **== 0** (observable because every generated sink is set-if-changed) **and** the tick slots read by `any_changed_since` PINNED to the fixture's expected value; red-first by dirtying exactly one source, after which BOTH counters must move. A wall clock cannot say which work disappeared, and the delta-subtraction form alone was unfalsifiable. ✅ **GB-7 RESOLVED 2026-08-30 (standing rule): NO wall-clock companion — the count gate stands alone.** Measured, not asserted: the timed loop's inputs are archetype count, bound-TYPE count and row count, and **not** the binding count (`dynamic_bound_ids` is a deduplicated type set, [`bind_system.rs:56-60`](../../crates/boyko_ui/src/binding/bind_system.rs)), so the still frame is **332 ns at +0% for 10× the bindings** but **+82% for 2× the rows** and **+101% for 2× the bound types**. `Instant::now()`'s step is 100 ns, so the frame is ~3.3 ticks with a 15-25× single-call tail; the red-first delta (0 → 1 sink write) sits at signal-to-noise **0.05-0.50**, which the count gate resolves exactly and no clock does. A clock over the **scan itself**, world pinned, belongs to **GK-2**'s design pass. ✅ **GB-9 RESOLVED 2026-08-30, and this rung's codegen rules change with it**: the `Or`-over-dense emission ban is **DELETED with a record** — its ground was fixed and gated by Aether R0/KE1, and the fallback ground (D4) reserves the Aether *surface* `or(...)` while the ban governed *generated code*, which ratified GN2 says the baker never emits. What this rung owes INSTEAD: the red fixture asserts the **generator does not emit a change-gate over a non-signature storage kind** (GK-2's ground, still live). |
| **G8** | ⚠ **RESCOPED 2026-08-30 by the owner's F5 ruling: the streaming half and GK-3 both moved UP into G6.** What is left here is **GK-2** alone — the per-column last-changed tick — behind its own design pass. **This row must not be read as a place to defer streaming work to;** that reading is what F5 rejected, and the row is kept rather than deleted so the move is auditable | defined by GK-2's own design pass; G8 cannot close before that pass exists and names its own red-first oracle. Nothing here pre-commits GK-2's oracle. ⚠ If a genuine streaming *remainder* is ever identified after G6 lands, it arrives as its own line with its own gate — not by inheriting this row's former wording |

> **Merge note, 2026-09-10 (`merge/ke16-into-render`).** Both branches rewrote every row of this ladder.
> The nine rows above are `feat/multi-paradigm-render`'s, which carry the later dispositions
> (GB-5, GB-6 and the 2026-09-03 G7 re-position). `feat/threadpool-ke16` wrote its own nine rows
> where the 2026-08-30 rulings were decided, and they carry measured ground the rows above do not
> repeat. They are kept verbatim below rather than fused sentence-by-sentence, because fusing two
> authors' measured claims into one cell invents a third claim neither wrote. Where the two
> disagree the rows ABOVE are the later state.

<details><summary>The same nine rungs as <code>feat/threadpool-ke16</code> wrote them, 2026-08-30 — kept by the merge</summary>

| Rung | What | Gate |
|---|---|---|
| **G0** | This directory; ratified decision lines (AIR-17); the AI-ORIENTATION correction (its "Gaia type-level substrate CLOSED" line was refuted by inventory). ✅ **The two ballots this row used to hold — F1 and F4 — were both answered on 2026-08-30** (F1 by the owner, F4 delegated and ruled), so **G0 carries no open ballot.** ⚠ **It is still not done:** GB-8's ruling put the AIR-census widening ON this row, and that edit has not landed | docs exist before any grammar commit **AND** every AIR cross-note on a ratified line in gaia DECISIONS resolves to a defined AIR item (AIR-17's own second clause, restored here in AIR-17's own scope). A file that RESOLVES but is STALE against the ratified syntax does not satisfy a cross-reference — `LANGUAGE.md` is the standing example (PENDING M6). ✅ **GB-8 RULED 2026-08-30, and this row now carries work.** (1) **No per-site waivers**, at any of the four censuses in [`tests/gaia_g0_citation_census.rs`](../../tests/gaia_g0_citation_census.rs), ever; where a property is not decidable as written, the remedy is the one that file already practises — narrow the predicate and print the narrowing in the failure message. ⚠ The "188 of 302" this row used to cite is **not this tree's number and no in-tree gate produces it**: `internal_docs_anchors.rs` run live prints **735 anchors, 116 waived** — but the precedent is *stronger* than that aggregate, because the waiver concentrated entirely in the one document admitted under it, which waives **89 of 177 (50.3%)**. (2) **The AIR census widens to all of `docs/` and it lands HERE, at G0** — a one-constant change (`G0_DIRS` → `markdown_under("docs")`), **green at zero remediation** — the outside-scope citations sit in 5 files and **every id cited resolves inside `AIR-01..AIR-18`**, so none is a repair (⚠ the *count* is not pinned: it was 21 before the ruling was written and 40 after, because the ruling's own text cites `AIR-06`; the property is the ruling, the number is an observation with a timestamp) — and the sibling KE/KM census in the same file already runs corpus-wide from G0. (3) **The LINK census is a separate deliverable and lands at Aether R8**, not here — measured 1636 relative targets under `docs/`, **59 dead across 11 files, 44 of them in `docs/AUDIT-2026-05-23.md`**; landing it with the id half would hold the free half hostage to 59 repairs. Ruling in this campaign's decision log: [`DECISIONS.md`](DECISIONS.md) §Census discipline; body and rejected alternatives: [`../OPEN-QUESTIONS.md`](../OPEN-QUESTIONS.md) §2026-08-29. |
| **G1** | **GK-4**: derive-emitted name-keyed field tables + typed constructors in `boyko_macros` (the macro-time bake route — no reflection needed even at bake). ✅ **F1 RULED BY THE OWNER 2026-08-30: this route, NOW**, not sequenced behind EG2, with the standing constraint that nothing here may foreclose streaming. ⚠ **"No F1" is not "buildable": G1 still carries an open ballot, and it is a different one — GB-5 blocks the field-table FREEZE.** What GB-5's analysis already establishes is that the per-field-flag mechanism this row would freeze is **refuted**: all 12 measured engine-derived field members are *conditionally* derived, so a per-field boolean pins a predicate that is false for every case it covers. The table may be designed and emitted; the **disposition column for engine-derived fields may not be minted** until GB-5 is answered | red-first and TEXT-FREE (G1 predates the grammar, so no gate of G1's may parse anything): `bake_one(<typed ctor calls built from the derive-emitted field table>) == save_world(hand-built world)` byte-for-byte for one component; plus a probe pinning `BindText`'s actual Serializability class |
| **G2** | Language core: lexer (EOL normalization before the CST) → lossless CST → canonical printer. ✅ **GB-3 and GB-4 both RULED 2026-08-30, so the two ballots that held this rung are closed** — and the rulings hand G2 four concrete deliverables rather than freeing it: (1) **close the asset sigil JOINTLY with Gaia's arithmetic operator vocabulary** (the glyph cannot be chosen first — R3 makes whitespace insignificant, so any glyph that can open a binary operator is ambiguous; recommendation `~`); (2) regenerate [`PENDING-SYNTAX-PLAN.md`](PENDING-SYNTAX-PLAN.md) **R4/R5/R7 and Tiers 1–2**, which GB-3 supersedes; (3) land the undeclared **`from=` → `source=`** rename on `bind` that GB-4's measurement surfaced; (4) rewrite [`LANGUAGE.md`](LANGUAGE.md), which is `ratified-stale` and still spells `extends @iron_sword` and bare-string asset refs. ⚠ **G2 also still carries an open ballot: F9's residual VALUES question** (may a document set a flag on an individual authored object) is a *grammar* question and lands here | `print(parse(canon)) == canon`; **absorbs the text entrance of G1's gate — `bake_one(text) == save_world(hand-built world)` byte-for-byte** — which cannot be stated at G1; round-trip compared on the WORLD side with a comparator GENERATED from the component registry (two in-tree proofs of why a hand list fails: the green-over-divergence equivalence gate and the 10-of-19 printer loss); parser dispatch + printer list + gate comparator = ONE generated table + coverage census |
| **G3** | Identity: asset/object ids, `@`-references, mandatory `link`, offline bake resolution. ✅ **GB-3 RULED 2026-08-30 — G3 carries no open ballot**, and gains two deliverables from it: the **asset-ref dangle check** (every sigil-marked token resolves at bake; a coded `GA####` refusal naming pack and path, blamed on the reference's own span, resolved through a bake-time analogue of the shipped `PathIndex`), and the **build item GB-3 uncovered** — ⚠ *the asset-ref target registry does not exist*: `grep -rnE "get_by_name\|by_path\|load_path\|handle_for_name\|name_to_handle"` over `crates/boyko_ecs/src/ecs/core/asset/` + `crates/boyko_render/src/` returns **0**. ⚠ Not buildable ahead of G2 regardless, and **F8** (name-vs-id) remains the candidate ballot for this rung | red fixtures: dangling ref, duplicate id, anonymous-target refusal, stale patch target; the unstable-field-type bake lint (GN1); **plus the asset-ref dangle fixture** — a sigil-marked reference to an absent asset must refuse with a code, not resolve to a default |
| **G4** | Composition: templates / `abstract` / patches / the priority ladder / `remove` / bake budgets / provenance sidecar. ✅ **GB-1 and GB-4 both RULED — G4 carries no open ballot.** From GB-4 it gains one deliverable and one red fixture: the instance form is `instance <name> extends|copy <base-ref>` with the linkage word **mandatory**, `from` deleted, and **a missing linkage word is a coded refusal at a known span** — that refusal is what stops G4 buying a second silent-default class beside the forgotten `layer=` this row already carries | **file-order independence**: `bake(files) == bake(shuffle(files))` byte-identical over ≥2 shuffled permutations plus the reversed order, on a fixture whose ladder layers span several files; byte-identity scoped to the bake OUTPUT (diagnostics may still list files in walk order); the permutation count is reported by the test. Plus the two-writes-one-layer red fixture (the error must name BOTH files). Plus the Godot-#32179 fixture (diff-at-save eating an intentional override). Plus the per-axis bake-budget fixture set (DECISIONS §The logic line: one committed red fixture per axis). ⚠ The former `bake(1) == bake(W)` form is **struck as vacuous**: `W` has no referent anywhere in this corpus and nothing makes the bake parallel, so the gate could not fail — the exact class this pass repairs. If a parallel bake is ever intended it arrives as its own design line with a defined `W`. |
| **G5** | **data** profile (smallest). ✅ **F2 and F3 both ANSWERED 2026-08-30 — G5 carries no open ballot**, and the row's own wording is corrected by F2's ruling: tables bake to **`StorageKind::Table`**, *not* "dense columns". A dense id is signature-excluded, so the ballot's "dense-column archetype" denotes nothing here (measured: `get_or_create_archetype(&[DenseRow])` returns **the empty archetype**); `Dense` is the answer only when the row component is *also* carried by entities outside the table, which is decidable at bake and is one derive attribute either way. Rows materialize **eagerly at load**; the generated row constants resolve to an **`Entity` captured at load**, never a row index. Both file shapes bake through **one schema** (F3) | per-profile fixtures; named red-first, the **eagerness** fixture: a violated `contract` on a field NOTHING reads must fail the bake, through both entrances — a plain declared field, and a field that a template expanded but no one ever reads — with blame on the file+span of the violating VALUE (Jsonnet's lazy assertions are the negative control) |
| **G6** | **scene** profile. ✅ **F4 and F5 both ANSWERED 2026-08-30, and F5 REWRITES THIS ROW'S SCOPE.** Owner, on F5: *"Все сразу грамотно по списку с самого начала."* Streaming is **no longer deferred to G8**: `load_cell` / `unload_cell`, **GK-1's cross-load map with a declared lifetime**, and **cross-cell reference resolution** are all inside G6, together with the cell catalog. F4 supplies the seam — **GK-3's suppress-then-fixup pass**, four ordered sub-passes ((1) require closure at parse time via the existing `LoadColumn::Construct`, (2) declared flags, (3) the existing remap, (4) hooks then drain to a fixpoint) — and the **stable-asset-id carrier**: a stable asset NAME in the file resolved to the existing `MeshHandle(u32)` at load, no new component type. ⚠ **GK-1 and GK-3 must land TOGETHER or the cross-cell half is silently half-present**, because a `ChildOf` across a cell boundary only enters the reverse index when sub-pass 4's link hook runs. ⚠ **Six GK-1 requirements are now G6's, each from a measured blocker** — global key rather than file-local `EntityId.0`; legal insert-after-`finalize` (today's release build silently misses an unsorted tail); removal **and a reverse edge** for `unload_cell`; a world-owned lifetime; **lifting the fresh-world contract**, whose guard is a `debug_assert!` that *vanishes in the shipping build* so `load_cell` would corrupt the dense store silently in release; and a second `LoadEntityPolicy` variant. ⚠ **G6 may still be ballot-blocked: GB-6(a)** blocks it *if* the scene-form fork is a ballot rather than a delegated decision — that classification is exactly what GB-6 asks | reconstruct-and-compare on the catalog; **plus F4's coverage census enumerating all five insert-path mechanisms, not hooks alone**; plus a red fixture that a fixup run over ONE cell's newly loaded entities is the same result as the whole-world run (the per-entity idempotence the ruling stands on) |
| **G7** | **ui** profile (prerequisites: a windowed UI pass and a real `UiPlugin` — UI does not reach the screen today). ✅ **F6 RULED BY THE OWNER 2026-08-30: `.ui` is MIGRATED to the Gaia `ui` profile and the old format DELETED in the same campaign.** ⚠ The price rides here and is recorded rather than discovered later: **the migration is done blind** — nothing renders a Gaia HUD to judge before the old format is gone, because this rung's own prerequisites do not exist. **G7 carries no open ballot of its own**; ⚠ but see GB-6 below, whose (b) half — the conditional-visibility valve — is the pressure valve that stops this profile's logic creep, and which today has a deadline and no rung | the STILL-FRAME gate, re-axed onto **counts, not a clock**: still frame over a HUD with 200 bindings — bind-sink writes executed **== 0** (observable because every generated sink is set-if-changed) **and** the tick slots read by `any_changed_since` PINNED to the fixture's expected value; red-first by dirtying exactly one source, after which BOTH counters must move. A wall clock cannot say which work disappeared, and the delta-subtraction form alone was unfalsifiable. **GB-7 RESOLVED 2026-08-30 (standing rule): NO wall-clock companion — the count gate stands alone.** Measured, not asserted: the timed loop's inputs are archetype count, bound-TYPE count and row count, and **not** the binding count (`dynamic_bound_ids` is a deduplicated type set, `boyko_ui/src/binding/bind_system.rs:56-60`), so the still frame is **332 ns at +0% for 10× the bindings** but **+82% for 2× the rows** and **+101% for 2× the bound types** — a threshold here tracks everything the fixture does not pin. `Instant::now()`'s step is 100 ns, so the frame is ~3.3 ticks with a 15-25× single-call tail; the red-first delta (0 → 1 sink write) sits at signal-to-noise **0.05-0.50**, which the count gate resolves exactly and no clock does. Body and full table in [`../OPEN-QUESTIONS.md`](../OPEN-QUESTIONS.md) §2026-08-29; the ruling also lands in [`DECISIONS.md`](DECISIONS.md) §UI bindings item 9. A clock over the **scan itself**, with the world pinned, belongs to **GK-2**'s design pass. ✅ **GB-9 RESOLVED 2026-08-30, and this rung's codegen rules change with it** — the ballot this row did not name, whose `Blocks` field read "**G7**'s codegen rules". Ruled **option (b): the `Or`-over-dense emission ban is DELETED with a record** ([`DECISIONS.md`](DECISIONS.md) §UI bindings item 8 carries it). Its ground was fixed and gated by Aether R0; the fallback ground (Aether D4) was measured and rejected — **D4 reserves the Aether *surface* `or(...)` while the ban governed *generated code*, and this campaign's own ratified GN2 says the baker emits no Rust**, so the ban had no subject. ⚠ It could not be re-grounded on KE13 either: `Or` folds `NEEDS_CHANGE_DETECTION` and `EcsMaster::query` const-refuses it, so the banned shape cannot reach a `QueryView` at all. **What this rung now owes instead:** the ban's fixture discipline rides item 8's third row — the red fixture asserts the **generator does not emit a change-gate over a non-signature storage kind** (GK-2's ground, still live) — and the shapes with **no oracle** if a generator ever does emit `Or` over dense are filed here: `Query<Entity, Or<..dense..>>`, `Added<Dense>` in `Or`, arity > 2 with more than one dense arm, and KE13 on the point-lookup path. ⚠ Unreconciled in that same work order: it lists **GB-6** against G7, while GB-6's own body names **G6**; not resolved here. |
| **G8** | ⚠ **RESCOPED 2026-08-30 by the owner's F5 ruling: the streaming half and GK-3 both moved UP into G6.** What is left here is **GK-2** alone — the per-column last-changed tick — behind its own design pass. **This row must not be read as a place to defer streaming work to;** that reading is what F5 rejected, and the row is kept (rather than deleted) so the move is auditable | defined by GK-2's own design pass; G8 cannot close before that pass exists and names its own red-first oracle. Nothing here pre-commits GK-2's oracle. ⚠ If a genuine streaming *remainder* is ever identified after G6 lands, it arrives as its own line with its own gate — not by inheriting this row's former wording |

</details>

## Kernel requests born from the design (each gets its own design pass)

| | What | Why |
|---|---|---|
| GK-1 | a cross-load map (global object id → Entity) with a declared lifetime | ⚠ **"Cheap to generalize" was optimistic, and F5's ruling makes the real requirements G6's** — see the G6 row. The one that must not be lost: `load_dense_store`'s fresh-world guard is a `debug_assert!` (two sites, `load_writer.rs:703` and `:795`), so a merge load corrupts the dense store **silently in release** — and editor undo is structurally a merge load. ⚠ Follow-on, not a ballot: GK-1 as specified would make `LoadEntityMap` a world-owned `Resource` keeping its `Vec`, which Principle 0 forbids; the in-tree cure is `PathIndex`'s `VmColumn` shape. Still true that it unlocks cross-cell refs (F5), and it must land **together with GK-3** |
| GK-2 | a per-column last-changed tick on write | today's still-frame gate `any_changed_since` is an O(live rows) scan documented as "cheap" — a gate one rung below its own documentation; the flecs per-table step is the right middle. **The cost is not the whole rationale: the mechanism is also CORRECTNESS-blind** — `any_changed_since` resolves per-archetype pools, and non-signature storage owns none, so a bind source on a dense (or bitset) component is invisible to it and the gate is never true (DECISIONS §UI bindings, item 8) |
| GK-3 | the post-load fixup seam (F4) | the loader runs no hooks: reverse indexes absent, asset refcounts at 0 — a loaded scene's meshes are retirable mid-game, and its children never compose their parent's pose (transform propagation descends ONLY through `Children`, [`propagation.rs:349`](../../crates/boyko_scene/src/propagation.rs), and `Children` classifies `Serializability::Ignore` with no rebuilder). ✅ **F4 RULED 2026-08-30: this seam is the four-sub-pass suppress-then-fixup pass**, it lands at **G6** together with GK-1, and sub-pass 4 splits into 4a (relink, fires no user code) and 4b (hooks) |
| GK-4 | derive-emitted field tables + typed ctors | the bake route that avoids both the rejected EG2 and the unlanded C11. ✅ **F1 RULED BY THE OWNER 2026-08-30: taken NOW**, not sequenced behind EG2 |

> **Merge note, 2026-09-10 (`merge/ke16-into-render`).** `feat/threadpool-ke16` wrote GK-1 and GK-3 differently (GK-2
> and GK-4 are identical on both sides). Its two rows are kept here; the rows above are this
> branch's, which carry the `LoadEntityMap`/Principle-0 follow-on and the `Children`-propagation
> measurement that side does not have, while its rows carry the build cost it measured.

<details><summary>GK-1 and GK-3 as <code>feat/threadpool-ke16</code> wrote them — kept by the merge</summary>

| | What | Why |
|---|---|---|
| GK-1 | a cross-load map (global object id → Entity) with a declared lifetime | ⚠ **"Cheap to generalize" was optimistic, and F5's ruling makes the six real requirements G6's** — see the G6 row. The one that must not be lost: `load_dense_store`'s fresh-world guard is a `debug_assert!`, so a merge load corrupts the dense store **silently in release**. Still true that it unlocks cross-cell refs (F5), and it must land **together with GK-3** |
| GK-3 | the post-load fixup seam (F4) | the loader runs no hooks: reverse indexes absent, asset refcounts at 0 — a loaded scene's meshes are retirable mid-game. ✅ **F4 RULED 2026-08-30: this seam is the four-sub-pass suppress-then-fixup pass**, and it lands at **G6**, together with GK-1. What it costs to build was measured and is small: **three `pub` promotions and a driver** — `for_each_required_id_excluding`, `DeferredEcsMaster::from_world`, and a reverse `EnableTagId::from_component_id` (the forward conversion is public, the reverse has no path) |

</details>

## Owner ballots — the fork bodies with prices live in [`docs/OPEN-QUESTIONS.md`](../OPEN-QUESTIONS.md), in THREE dated sections

The table below is the INDEX only. `DECISIONS.md` has no `§Forks` and must not gain one: it is a
log of RULINGS, and an open question does not belong in it — a rulings log that carries open forks is
how a fork gets silently settled by proximity.

**Where each body lives.** **F1–F7** were raised 2026-08-28 and carry their full bodies in
§*2026-08-28 — Gaia: the owner ballots, two of them blocking*. **F8, F9 and F10** were born in the
2026-08-29 corpus audit and have no body in that earlier section at all — theirs are in
§*2026-08-29 — Corpus audit of the aether-v2 + gaia plans*, which is also where the `GB` series
lives. **The rulings** are in §*2026-09-03 — the Gaia register catches up with the owner*.

⚠ **This table's third column used to be headed "Recommendation", and every cell in it was the
PLAN'S OWN recommendation.** `git log -G"Recommendation" -- docs/gaia/CAMPAIGN.md` returns exactly
two commits: `a4591c56`, whose message says the ballots are **held**, and `c1a9b8ec` on
`feat/threadpool-ke16`, which renamed the column there. Reading the old column as a decision record
is precisely how a recommendation becomes believed as a decision — F1's cell happened to match what
the owner later ruled, which is the shape nobody re-checks — so the rename is part of the repair and
not cosmetics.

✅ **STATUS 2026-09-03 — fourteen ballots were put to the owner on 2026-08-30 and he answered
twelve.** Six he settled outright (**F1, F3, F5, F6, F7** and **GB-3**'s adoption); four he
**delegated** (**F2, F4, F9, GB-4**), now ruled; four more were settled by standing rule (**GB-1,
GB-7, GB-8, GB-9**); **GB-5** he ruled on 2026-08-30 and the ruling reached only `DECISIONS.md` on
the working branch, which is why every index read it as open; and **GB-6** he disposed on 2026-09-03
in both halves. **Unanswered and still open: F8, F10, and F9's residual VALUES question.** The
rulings landed on `feat/threadpool-ke16` (commits `6f75ee9e`, `c1a9b8ec`, `b7a9931d`, `b6c41237`),
none of which is reachable from this branch; `97c504c8` is the merge-base. ⚠ **2026-09-10: those commits ARE reachable
from this branch now** — the merge `merge/ke16-into-render` landed `feat/threadpool-ke16` here.
The two sentences before this one are the record of why the 2026-09-03 sync was written, not a
live statement of reachability.

| # | Question | Disposition (2026-08-30 unless noted) | Blocks |
|---|---|---|---|
| **F1** | The bake route into the byte format: the audited-and-rejected EG2 reflection seam, or macro-time GK-4 now with EG2 as a later upgrade? | ✅ **RULED BY THE OWNER — macro-time GK-4, NOW.** *"Do it properly right away. But bear in mind the world must support streaming."* Both halves bind. Rejected: sequence EG2 first. ⚠ The ballot's `RequiredCtor`-is-unbakeable inference is **refuted** — a GK-4 baker is a Rust program linked against the derive tables and calls the fn pointer trivially; only the *evaluator over text* cannot | **G1** — no longer ballot-blocked |
| **F4** | What "loaded" means — **WIDENED from hooks to every insert-path mechanism**: (i) hooks; (ii) the `requires` closure; (iii) flag initial state; (iv) relation reverse index; (v) asset refcounts. Options: a specified post-load fixup pass (a), the loader fires attach hooks (b), or forbid hook-dependent components in baked assets (c) | ✅ **RULED [delegated] — (a), as SUPPRESS-THEN-FIXUP: four ordered sub-passes on the clone path's own precedent.** (b)'s *mechanism* is adopted as sub-pass 4; its *timing* is refuted at a **measured 100%** mis-link rate (saved/fresh id overlap 8/8). (c) is worse than "untenable": it forbids every mesh, material, light, camera and particle effect. Carries the stable-asset-carrier addendum. **Amended 2026-09-03**: sub-pass 4 SPLITS into 4a (relink) / 4b (hooks); the census is 5 mechanisms × 3 storage kinds; 4b reports per hook class | **G6, and the engine's load semantics generally** — no longer ballot-blocked |
| F2 | Where DataAsset data lives at runtime: entity-shaped dense columns + generated row constants (a) or a new resource region in the format (b) | ✅ **RULED [delegated] — (a), AMENDED three ways.** Storage is **`StorageKind::Table`**, not dense; rows materialize **eagerly at load**; a table is loaded by its own explicit load and **pinned**. The runtime handle is an `Entity` captured at load, never a row index. ⚠ **CONFIRMED 2026-09-03 on CORRECTED grounds, the original size argument SUPERSEDED**: `COMMIT_GRANULE` is symmetric across both options, the resident floor keys on SCHEMA count rather than FILE count, and `VmColumn` refuses a 40-byte element outright — margin ≤ 4×, not two orders of magnitude | G5 |
| F3 | Table form: single-file-per-asset only, or also a table file baking N rows into one archetype column | ✅ **RULED BY THE OWNER — both, over ONE schema.** The table file is a **spelling**, not a second type system. Rejected: a table dialect. Settled by code rather than taste: `create_archetype` dedups by id set and `load_archetype` appends at the current row head, so N one-row documents and one N-row document land in the same archetype | grammar |
| F5 | Streaming scope: format-ready-loader-later (cell catalog now, `load_cell`/unload/GK-1 later), or the whole streaming half inside G6 | ✅ **RULED BY THE OWNER — option (b), EVERYTHING FROM THE START.** *"Все сразу грамотно по списку с самого начала."* ⚠ **This cell printed "(a)" — the option he REJECTED — until 2026-09-03.** The largest change on this page: **G6 absorbs the hardest half**, because a per-load `LoadEntityMap` cannot express references BETWEEN chunks | **G6 — scope REWRITTEN, see the rung row** |
| F6 | `.ui` fate after absorption: migrate + delete in the same campaign, or freeze until an owner-eval of a Gaia HUD | ✅ **RULED BY THE OWNER — (a), migrate and DELETE in the same campaign.** Price accepted and recorded at G7: the migration is done **blind**, because that rung's prerequisites do not exist yet | G7 |
| F7 | Mods: ratify "out of v1" explicitly, or accept that a text-mod pipeline ships the bake tool to PLAYERS as a separate executable | ✅ **RATIFIED BY THE OWNER — mods are NOT supported for now**, stated explicitly so silence stops being a default decision. The constraint is recorded in its **narrow** form (no reflection in the game binary), so a later mod campaign is not blocked by a sentence that never meant to block it | — |
| **F8** | Name-vs-id for objects: is the object NAME the id (then the `--assign-ids` machinery is deleted and rename tooling replaces it), or does syntax ruling [**PENDING R4**](PENDING-SYNTAX-PLAN.md) grow a separate id slot beside the name? (**not** Aether rung R4 — the two `R#` series are unrelated) | ⚠ **UNANSWERED — still open, and deliberately untouched by every ruling of 2026-08-30.** No recommendation is offered here: this **REOPENS the ratified `gaia fmt --assign-ids` identity ruling**, so AIR-10 requires the trigger and the measurement to be stated before it is reopened. GB-3's Part 4 and F4's carrier addendum are **F8-neutral by construction** and say so at their own sites | TBD — owner scoping |
| **F9** | Flags carrier: a per-entity enable-bit region in the format plus one spelling, or a coded refusal naming Aether's `flags (…)` group | ⚠ **PARTLY RULED [delegated] — three eliminations, one question escalated back.** (1) `FLAGS_DIRECT` must **not** fire on the load path — it would overwrite every saved bit with its attach-time value; (2) the refusal option **may not be chosen**, because it contradicts ratified **AB-6**; (3) if a carrier lands, its spelling is **`flags (X = true)`** shared verbatim with Aether, keyed on GK-1's global object id, riding F4's pass. **STILL THE OWNER'S:** may a document set a flag on an **individual authored object**? | **G2** (the residual question) |
| **F10** | Bundle: expand at bake vs refuse. **Pre-question: is this a ballot at all** — PENDING Tier 3 and Part D contradict each other on that point | ⚠ **UNANSWERED — still open, and deliberately untouched.** No ruling of 2026-08-30 settles it by implication; each ruling that could have brushed it says so at its own site. ⚠ The pre-question is unanswerable by reading: both sides were rewritten into mutual notices of the contradiction and neither states a position | TBD — owner scoping |
| **GB-1** | Which layer of the priority ladder a template EXPANSION and an UNLABELED write occupy | ✅ **RESOLVED by standing rule — and the owner resolved it by sending it back.** Neither gets a ladder layer: expansion is one step on the inheritance-DEPTH axis, ranked where `extends` ranks, resolution depth-then-ladder; an unlabeled write is `base`. K2 is dissolved, not diagnosed | **G4** — no longer ballot-blocked |
| **GB-2** | The colour transfer function: `#RRGGBBAA` sRGB-decoded at bake, vs raw bytes into a LINEAR field | ✅ **RULED BY THE OWNER — `#RRGGBB` is `#RRGGBBAA` with maximal `AA`**: a defaulted field, not a second literal kind, which dissolves the reopen rather than answering it. Plus the arity direction the clause never covered: an 8-digit literal at a 3-component field is a **coded bake refusal naming the field**, never a silent alpha drop | **G2, G5** — no longer ballot-blocked |
| **GB-3** | The reference taxonomy is short one kind — asset references are neither lexical/copy nor `@`-entity. One ballot in four parts so R4 is rewritten once | ✅ **THIRD KIND ADOPTED BY THE OWNER; the four-part rewrite RULED [delegated].** (1) the spelling is a **sigil**, uniform and position-independent, so *a bare word is never a reference* — ⚠ but **the glyph is NOT minted**: it is closed at **G2 jointly with the arithmetic operator vocabulary** (recommendation `~`). (2) style references are the **FIRST** kind. (3) **`$hole` STAYS**, reclassified lexical in every position. (4) *"declared node ⇒ `@`"* is **WITHDRAWN**, replaced by *what does bake do with this reference?* ⚠ The GN1 premise is refuted: GN1 is the law over all three kinds | **G2, G3** — no longer ballot-blocked |
| **GB-4** | Instance re-spelling: (a) linkage-in-slot vs (b) linkage-in-head | ✅ **RULED [delegated] — (a), and the linkage word is MANDATORY**: `instance <name> extends\|copy <base-ref>`, no default. `from` is deleted (head-position occurrences measured: **zero**). Rejected (b) on four grounds, the sharpest being that it has **no refusal for the omitted case** while (a) does. ⚠ Rider owed to G2: an undeclared `from=` → `source=` rename on `bind` | **G4** / Tier 2 — no longer ballot-blocked |
| **GB-5** | May a scene document declare an ENGINE-DERIVED field (`PointLight.position`)? | ✅ **RULED BY THE OWNER (recorded 2026-08-31, `b6c41237`) — PERMIT AS SEED.** *"the third is the most logical — it is simply a starting point in space; obviously this data exists to be manipulated and will not be static."* The disposition column **must not be a boolean** (all twelve measured members are conditionally derived) — it records the CONDITION and the WRITER. His acceptance condition is discharged by construction: the column sits behind the default-off bake feature. ⚠ The ruling commit touched `DECISIONS.md` and nothing else, which is why four registers on the working branch still say OPEN | **the G1 table freeze — UNBLOCKED** |
| **GB-6** | §Relations' two unclassified "ratify" imperatives: the scene-form fork, and the EnableTag-toggling visibility valve with a deadline and no rung | ✅ **DISPOSED 2026-09-03 BY THE OWNER, both halves.** (a) The scene-form fork is **RULED**: the Gaia TEXT is the source the editor saves, the BINARY is the compiled artifact that ships, and the alternative the ballot could not name — shipping a scene as TEXT and parsing it at runtime, Godot's `.tscn` shape — is declined, so the four downstream reassignments STAND. (b) The valve is **not a Gaia ballot at all**: *"gaia это не логика а данные"* — it MOVES to **Aether R3** and is struck from this series | **G6 — no longer ballot-blocked**; (b) is now Aether's |
| **GB-7** | The wall-clock companion beside G7's count gate | ✅ **RESOLVED by standing rule — NO wall-clock companion; the count gate stands alone.** The model case of a ballot a measurement settled: 332 ns at **+0% for 10× the bindings** but **+82% for 2× rows** and **+101% for 2× bound types**, at a 100 ns timer step, with the red-first delta at signal-to-noise 0.05-0.50 | **G7** — no longer ballot-blocked |
| **GB-8** | The corpus-wide link/id census: which rung owns it, and whether it may carry per-site waivers | ✅ **RULED [delegated], in three parts** — no per-site waivers ever; the AIR census widens to all of `docs/` and lands at **G0**; the LINK census is a separate deliverable at **Aether R8** (59 dead targets across 11 files). ⚠ The "188 of 302" figure is not this tree's; the live gate prints 735 anchors / 116 waived, and the real precedent is the concentration — 89 of 177 (50.3%) in one document | **G0** (id half) / **R8** (link half) |
| **GB-9** | Does the `Or`-over-dense generated-code ban survive the kernel fix (KE1)? | ✅ **RESOLVED by standing rule — option (b): the ban is DELETED with a record.** Its ground was fixed and gated by Aether R0/KE1; the fallback ground (D4) reserves the Aether *surface* `or(...)` while the ban governed *generated code*, which ratified GN2 says the baker never emits — so the ban had no subject. ⚠ Its deadline ("decide before R0 lands") **expired unanswered**; R0 landed with GB-9 open | **G7**'s codegen rules — no longer ballot-blocked |

> **Merge note, 2026-09-10 (`merge/ke16-into-render`).** This table exists on both branches and every shared row differs.
> The nineteen rows above are `feat/multi-paradigm-render`'s: they are the later state, they are
> the only side that carries **GB-1, GB-2, GB-7, GB-8 and GB-9** as rows at all, and they record
> GB-5 and GB-6 as ruled. `feat/threadpool-ke16` wrote its fourteen rows on 2026-08-30, where the
> rulings were taken, and its cells carry the measured ground the 2026-09-03 sync compressed. Its
> status paragraph and its table are kept below unedited. ⚠ Two of its rows are superseded rather
> than merely shorter: **GB-5** and **GB-6** are recorded there as OPEN, and both have since been
> ruled — read them as the state on 2026-08-30, not as live. ⚠ **Every sentence in the block below that calls either of them open is struck in place and dated, 2026-09-10:** GB-5 was RULED BY THE OWNER on 2026-08-30 — PERMIT AS SEED, recorded 2026-08-31 by `b6c41237`; ground at [`DECISIONS.md`](DECISIONS.md) §GB-5 (`docs/gaia/DECISIONS.md:1262`) and the who-table row `docs/OPEN-QUESTIONS.md:80`; GB-6 was DISPOSED BY THE OWNER on 2026-09-03, both halves; ground at the who-table row `docs/OPEN-QUESTIONS.md:81` and §*2026-09-03* (`docs/OPEN-QUESTIONS.md:258`).

<details><summary>The ballot index as <code>feat/threadpool-ke16</code> recorded it, 2026-08-30 — kept by the merge</summary>

✅ **STATUS 2026-08-30 — fourteen ballots were put to the owner and he answered twelve.** Six he
settled outright (**F1, F3, F5, F6, F7**, and **GB-3**'s adoption); four he **delegated** (**F2,
F4, F9, GB-4**), and those are now ruled with their ground in [`DECISIONS.md`](DECISIONS.md); two he
asked to be **analysed, not decided** (**GB-5, GB-6**), and those ~~stay OPEN~~ with the analysis
attached to their bodies so he can rule from one reading. **Unanswered and still open: F8 and
F10** — plus **F9's residual VALUES question** (may a document set a flag on an individual authored
object), which the delegated ruling deliberately did not settle. The table below records each
disposition; the ground lives at the site each row names.

⚠ **DATED NOTE, 2026-09-10 (`merge/ke16-into-render`) — this paragraph and the table under it are `feat/threadpool-ke16`'s record of 2026-08-30, not a live index.** The two ballots it sends back for analysis have since been ruled: GB-5 was RULED BY THE OWNER on 2026-08-30 — PERMIT AS SEED, recorded 2026-08-31 by `b6c41237`; ground at [`DECISIONS.md`](DECISIONS.md) §GB-5 (`docs/gaia/DECISIONS.md:1262`) and the who-table row `docs/OPEN-QUESTIONS.md:80`; GB-6 was DISPOSED BY THE OWNER on 2026-09-03, both halves; ground at the who-table row `docs/OPEN-QUESTIONS.md:81` and §*2026-09-03* (`docs/OPEN-QUESTIONS.md:258`). `F8`, `F10` and F9's residual VALUES question are untouched by this note.

| # | Question | Disposition (2026-08-30) | Blocks |
|---|---|---|---|
| **F1** | The bake route into the byte format: the audited-and-rejected EG2 reflection seam, or macro-time GK-4 now with EG2 as a later upgrade? | ✅ **RULED BY THE OWNER — macro-time GK-4, NOW.** *"Do it properly right away. But bear in mind the world must support streaming."* Both halves bind: G1 takes the GK-4 route and no part of the bake design may foreclose streaming. Rejected: sequence EG2 first. ⚠ The ballot's `RequiredCtor`-is-unbakeable inference is **refuted** — a GK-4 baker is a Rust program linked against the derive tables and calls the fn pointer trivially; only the *evaluator over text* cannot. Ground: [`DECISIONS.md`](DECISIONS.md) §Inherited pipeline | **G1** — no longer ballot-blocked |
| **F4** | What "loaded" means — **WIDENED from hooks to every insert-path mechanism**: (i) hooks (the loader runs none: reverse indexes absent, asset refcounts at 0); (ii) the `requires` closure; (iii) flag initial state; (iv) relation reverse index; (v) asset refcounts. Options: specified post-load fixup pass (a), loader fires attach hooks (b), or forbid hook-dependent components in baked assets (c) | ✅ **RULED [delegated] — (a), as SUPPRESS-THEN-FIXUP: four ordered sub-passes on the clone path's own precedent.** (b)'s *mechanism* is adopted as sub-pass 4; its *timing* is refuted — hooks fired during the load mis-link every relation at a **measured 100%** rate (saved/fresh id overlap 8/8), which is worse than today's empty index. (c) verified worse than "untenable": it forbids **every mesh, material, light, camera and particle effect**. Two ballot corrections: (ii) is the **dynamic by-id entrance**'s defect, live in the engine one call from gameplay code; (iii) **splits in two**, and only the authored half is F9's. Carries the stable-asset-carrier addendum (a stable NAME resolved to `MeshHandle(u32)` at load — `MeshRef` does not exist, `grep -rn "\bMeshRef\b" crates/` → 0). Ground: [`DECISIONS.md`](DECISIONS.md) §Load semantics | **G6, and the engine's load semantics generally** — no longer ballot-blocked |
| F2 | Where DataAsset data lives at runtime: entity-shaped dense columns + generated row constants (a) or a new resource region in the format (b) | ✅ **RULED [delegated] — (a), AMENDED three ways.** Storage is **`StorageKind::Table`**, not dense (measured: `get_or_create_archetype(&[DenseRow])` returns **the empty archetype**, so "dense-column archetype" denotes nothing here); rows materialize **eagerly at load**, never lazily; a table is loaded by its own explicit load and **pinned**, so `unload_cell` never touches it. The runtime handle is an `Entity` captured at load, never a row index. Price of the ballot's headline — 4000 rows of a 40 B type — measured at **511 KiB**; (b) saves **0.13 MB** and costs the whole per-`ResourceId` serialize seam from scratch (`grep -rniw "resource" crates/boyko_serialize/src/` → **0**). Ground: [`DECISIONS.md`](DECISIONS.md) §Data tables | G5 |
| F3 | Table form: single-file-per-asset only, or also a table file baking N rows into one dense column | ✅ **RULED BY THE OWNER — both, over ONE schema.** The table file is a **spelling**, not a second type system. Rejected: a table dialect. Ground: [`DECISIONS.md`](DECISIONS.md) §Data tables | grammar |
| F5 | Streaming scope: format-ready-loader-later (cell catalog now, `load_cell`/unload/GK-1 later) | ✅ **RULED BY THE OWNER — option (b), EVERYTHING FROM THE START.** *"Все сразу грамотно по списку с самого начала."* `load_cell`/`unload_cell`, GK-1's cross-load map with a declared lifetime, and cross-cell reference resolution ship **with** the scene profile. ⚠ This is the largest change on this page: **G6 absorbs the hardest half of the design**, because a per-load `LoadEntityMap` cannot express references BETWEEN chunks. Ground: [`DECISIONS.md`](DECISIONS.md) §Streaming scope | **G6 — scope REWRITTEN, see the rung row** |
| F6 | `.ui` fate after absorption: migrate + delete in the same campaign, or freeze until an owner-eval of a Gaia HUD | ✅ **RULED BY THE OWNER — (a), migrate and DELETE in the same campaign.** Price accepted and recorded at G7: the migration is done **blind**, because G7's prerequisites do not exist yet. Ground: [`DECISIONS.md`](DECISIONS.md) §Language shape | G7 |
| F7 | Mods: ratify "out of v1" explicitly, or accept that a text-mod pipeline ships the bake tool to PLAYERS as a separate executable (re-stating the constraint as "reflection never in the game binary") | ✅ **RATIFIED BY THE OWNER — mods are NOT supported for now**, stated explicitly so silence stops being a default decision. The constraint is recorded in its **narrow** form (no reflection in the game binary), so a later mod campaign is not blocked by a sentence that never meant to block it. Ground: [`DECISIONS.md`](DECISIONS.md) §Refusals | — |
| **F8** | Name-vs-id for objects: is the object NAME the id (then the `--assign-ids` machinery is deleted and rename tooling replaces it), or does syntax ruling [**PENDING R4**](PENDING-SYNTAX-PLAN.md) grow a separate id slot beside the name? (**not** Aether rung R4 — the two `R#` series are unrelated) | ⚠ **UNANSWERED — still open, and deliberately untouched by every ruling of 2026-08-30.** No recommendation is offered: this **REOPENS the ratified `gaia fmt --assign-ids` identity ruling**, so AIR-10 requires the trigger and the measurement to be stated in the ballot body before it is reopened. GB-3's Part 4 and F4's carrier addendum are both **F8-neutral by construction** and say so at their own sites — a *carrier form* (name-as-key) is not a *name-vs-id ruling* | TBD — owner scoping |
| **F9** | Flags carrier: a per-entity enable-bit region in the format plus one spelling, or a coded refusal naming Aether's `flags (…)` group | ⚠ **PARTLY RULED [delegated] — three eliminations, one question escalated back.** RULED: (1) **`FLAGS_DIRECT` must NOT be fired on the load path** — `apply_attach_flags_*` has **8 call sites in 4 files**, all spawn/migration, and firing it would overwrite every saved bit with its attach-time value (the door the player unlocked comes back locked); (2) **the refusal option may not be chosen** — it contradicts ratified **AB-6** (*a language may not refuse what the derive accepts*) and has no honest did-you-mean; (3) if a carrier lands, its spelling is **`flags (X = true)`** shared verbatim with Aether (AB-13), keyed on GK-1's global object id, riding F4's GK-3 pass. ⚠ Also corrects the F4 census: the save side is guarded by **poollessness** (`save.rs:190-193`), not by the signature filter — so a carrier never touches `filtered_signature_mask`. **STILL THE OWNER'S:** may a document set a flag on an **individual authored object**? Ground: [`DECISIONS.md`](DECISIONS.md) §Flags in the byte format | **G2** (the residual question) |
| **F10** | Bundle: expand at bake vs refuse. **Pre-question first: is this a ballot at all** — PENDING Tier 3 and Part D contradict each other on that point, and the contradiction must be resolved before the question is put | ⚠ **UNANSWERED — still open, and deliberately untouched.** No ruling of 2026-08-30 settles it by implication; each ruling that could have brushed it says so at its own site | TBD — owner scoping |
| **GB-3** | The reference taxonomy is short one kind — asset references are neither lexical/copy nor `@`-entity. One ballot in four parts so R4 is rewritten once | ✅ **THIRD KIND ADOPTED BY THE OWNER; the four-part rewrite RULED [delegated].** (1) spelling = **a sigil**, uniform and position-independent, so *a bare word is never a reference* — but ⚠ **the glyph cannot be chosen before Gaia's arithmetic operator vocabulary is closed** (R3 makes whitespace insignificant, so a glyph that can open a binary operator is ambiguous); recommendation `~`, closed by **G2 jointly with the operator list**. (2) style references are the **FIRST** kind (§Refusals already ratifies *flattened by bake*), and the ballot's own example list must drop "a style record". (3) **`$hole` STAYS**, reclassified as the lexical kind in *every* position. (4) *"declared node ⇒ `@`"* is **WITHDRAWN**, replaced by *what does bake do with this reference?* ⚠ The ballot's GN1 premise is **refuted** — GN1's first resolution is a load remap, so GN1 is the law over all three kinds, not the asset kind's behaviour. Ground: [`DECISIONS.md`](DECISIONS.md) §Identity and references | **G2, G3** — no longer ballot-blocked |
| **GB-4** | Instance re-spelling: (a) linkage-in-slot vs (b) linkage-in-head | ✅ **RULED [delegated] — (a), and the linkage word is MANDATORY**: `instance <name> extends|copy <base-ref>`, no default. `from` is deleted (measured over **code fences only**: 1 `\bfrom\b` hit in 44 fenced lines, and it is `from=` as a field key on `bind`, so head-`from`'s count is **zero**). Rejected (b) on four grounds, the sharpest being that it has **no refusal for the omitted case** while (a) does — and on the generator axis, (b)'s omission error is the *invisible* one. Price of (a), stated: one mandatory word per instance line. ⚠ Rider: the corpus contains an undeclared `from=` → `source=` rename on `bind`, owed to G2. Ground: [`DECISIONS.md`](DECISIONS.md) §The instance spelling | G4 / Tier 2 — no longer ballot-blocked |
| **GB-5** | May a scene document declare an ENGINE-DERIVED field (`PointLight.position`)? | ⚠ ~~OPEN — the owner asked for the trade-offs, not a ruling, and a ruling here would be a defect.~~ **Struck 2026-09-10 by the merge `merge/ke16-into-render`: GB-5 was RULED BY THE OWNER on 2026-08-30 — PERMIT AS SEED, recorded 2026-08-31 by `b6c41237`; ground at [`DECISIONS.md`](DECISIONS.md) §GB-5 (`docs/gaia/DECISIONS.md:1262`) and the who-table row `docs/OPEN-QUESTIONS.md:80`. This row is the record of 2026-08-30, not a live status.** The **analysis** is attached to the ballot body ([`../OPEN-QUESTIONS.md`](../OPEN-QUESTIONS.md) §2026-08-29). What it settles is only this: **the per-field-flag mechanism the ballot assumes is refuted** — all **12** measured engine-derived field members are *conditionally* derived, including `Transform.translation`, and **both** dispositions are pinned by green committed tests today. Four options (refuse / permit / permit-as-seed / refuse-conditionally) are each stated in their strongest form with prices | **the G1 table freeze** |
| **GB-6** | §Relations' two unclassified "ratify" imperatives: the scene-form fork, and the EnableTag-toggling visibility valve with a deadline and no rung | ⚠ ~~OPEN — analysed, not ruled.~~ **Struck 2026-09-10 by the merge `merge/ke16-into-render`: GB-6 was DISPOSED BY THE OWNER on 2026-09-03, both halves; ground at the who-table row `docs/OPEN-QUESTIONS.md:81` and §*2026-09-03* (`docs/OPEN-QUESTIONS.md:258`). This row is the record of 2026-08-30, not a live status.** Analysis attached to the ballot body. (a) The scene-form narrowing is stated **declaratively in four documents** and has already had **four downstream reassignments** filed against it, so the live question is whether to relabel it *"decided, owner may veto"* — and ⚠ *"the alternative, a shared SceneModel, is priced higher"* is its **own only witness** (`grep -rn "SceneModel" docs/ crates/` → **1 line**, that one). (b) The valve's runtime **already ships three times over** in `boyko_ui` and its Gaia side is ratified; the entire gap is **one Aether construct that toggles a named `flag`**, and R3 is the only honest candidate today | **G6** if (a) is a ballot; **CG-3** / the Aether ladder for (b) |

</details>

A veto point (decided, owner may veto): first-class `remove` in inheritance is IN the v1 grammar —
default-inexpressibility costs more later (the Unity nested-prefab lesson in miniature).

## Relations

- **aether-v2** *(decided — GB-6(a), RULED BY THE OWNER 2026-09-03)*: `scene` in Aether stays
  dev-bootstrap-only; **the Gaia binary is the only shipped scene form**. The owner's model:
  *"когда мы изменяем сцену мы можем ее сохранить, в таком случае меняются исходники gaia — ну типа как
  json файл. После этого да, мы уже компилируем эту сцену в бинарь."* So the Gaia **TEXT** is the source
  the editor edits and saves, and the **BINARY** is the compiled artifact that ships. ⚠ The
  alternative this line never named is recorded here, because a claim whose only witness is itself
  cannot be vetoed: it was **shipping a scene as TEXT and parsing it at runtime** (Godot's `.tscn`
  shape), and the owner declined it. `SceneModel`, the alternative the line DID name, occurs exactly
  once in the whole repository — in the line asserting it is priced higher. The four downstream
  reassignments already filed against this line therefore **stand**.
- **The conditional-visibility valve MOVED OUT of Gaia, 2026-09-03** (GB-6(b)). The owner:
  *"Не ясно вообще в чем вопрос и причем тут gaia. gaia это не логика а данные. Логика у нас либо в
  расте либо в aether."* Traced, the ballot was **misfiled, not wrong**: Gaia's own ratified rule
  already says structure bakes and only VALUES react, and that a condition in a Gaia file is a token
  REFERENCE and never an expression. The data side says only *this node's visibility is controlled
  by token X*; the logic that decides X is Rust or Aether. The one missing piece is entirely on the
  Aether side — **no Aether construct toggles a flag**, while the runtime ships three times over
  (`UiWorldCulled` / `UiWorldHidden` / `UiWorldOccluded`). **It now rides Aether rung R3**, beside
  `tag`, `flag` and the `scene` surface, and is struck from the Gaia `GB` series.
- Recorded separately as a **carrier gap**, and still owed: the two authored-scene emission fixes on
  the first line — `link` as the shared remap spelling, and component references riding mandatory
  explicit `stable_name` — ride no rung at all today. They are asserted here and land nowhere; the
  pass that names their carrier is owed and was never part of GB-6.

> **Merge note, 2026-09-10 (`merge/ke16-into-render`).** The bullets above are this branch's 2026-09-03 record of how
> GB-6 was ruled in both halves. At this same anchor `feat/threadpool-ke16` carried the ANALYSIS
> written while GB-6 was still open — the ground the ruling was taken on — and the `CG-1..CG-3`
> carrier-gap subsection, which exists on that branch alone and which the third bullet above
> refers to without naming ids. Both are kept below, unedited. The analysis is dated 2026-08-30
> and describes GB-6 as OPEN: read it after the ruling, not instead of it. ⚠ **Every sentence in it that calls the ballot open is struck in place and dated, 2026-09-10:** GB-6 was DISPOSED BY THE OWNER on 2026-09-03, both halves; ground at the who-table row `docs/OPEN-QUESTIONS.md:81` and §*2026-09-03* (`docs/OPEN-QUESTIONS.md:258`).

#### GB-6 as `feat/threadpool-ke16` left it on 2026-08-30, before the ruling — kept by the merge

- **aether-v2**: `scene` in Aether stays dev-bootstrap-only; the Gaia binary is the only shipped
  scene form (the alternative, a shared SceneModel, is priced higher). ⚠ ~~Open ballot GB-6 — still
  OPEN as of 2026-08-30, deliberately: the owner asked for the trade-offs, not a ruling.~~ **Struck 2026-09-10 by the merge `merge/ke16-into-render`: GB-6 was DISPOSED BY THE OWNER on 2026-09-03, both halves; ground at the who-table row `docs/OPEN-QUESTIONS.md:81` and §*2026-09-03* (`docs/OPEN-QUESTIONS.md:258`).** The
  **analysis** is attached to the ballot body in
  [`../OPEN-QUESTIONS.md`](../OPEN-QUESTIONS.md) §2026-08-29 and is written so he can rule from one
  reading. Two measurements from it belong here, on the line they are about:
  - **The disposition is already stated declaratively in four other documents** — the revision
    record's *landed-changes* table, [`../AETHER-LANG-PLAN.md`](../AETHER-LANG-PLAN.md) §3.7,
    [`../AETHER-V1-SURFACE-REVIEW.md`](../AETHER-V1-SURFACE-REVIEW.md), and
    [`../aether-v2/CONSTRUCTS.md`](../aether-v2/CONSTRUCTS.md) §`scene` — and **four downstream
    absences (`N-14`…`N-17`) have already been reassigned to Gaia on its basis, each with a named
    carrier.** A question still open cannot have had four consequences filed against it.
  - ⚠ **The one thing missing is the price of the rejected alternative:** `grep -rn "SceneModel"
    docs/ crates/` returns **exactly one line in the whole repository**, and it is the line above
    asserting the alternative is priced higher. **The claim is its own only witness.** Writing that
    price is one paragraph, and it is the only thing that makes a veto meaningful — so it travels
    with the relabel either way.
  GB-6 classifies both imperatives:
  - **(a) the scene-form fork.** If it is a ballot, it **Blocks G6** and gets a body in
    OPEN-QUESTIONS. If it is delegated, the line is relabelled "(decided, owner may veto)" — the
    shape the `remove` veto point above already uses. It cannot stay in between.
  - **(b) the "conditional visibility" pressure valve** (an EnableTag-toggling action idiom).
    It carries a DEADLINE — "before the first designer asks, or the document side caves" — so it
    must get an F-id AND a rung on the **Aether** ladder: a deadline with no rung can never come
    due, and is therefore not a schedule.
    ⚠ **GB-6's analysis measured the gap and it is ONE construct, not a design.** Three of the four
    pieces already ship: `flag` is a ratified Aether construct with `enabled`/`disabled` filter
    terms in the shipped v1 grammar; **the engine already runs this exact idiom in production three
    times over** — `UiWorldCulled` / `UiWorldHidden` / `UiWorldOccluded`
    (`crates/boyko_ui/src/world/components.rs`) are bitset tags owned by three separate
    authorities, deliberately split so *"the three never race a shared bit"*, and the layout pass
    skips a root with the bit set; and Gaia's document side is ratified already (*a condition in a
    Gaia file is a token reference, never an expression*). **The missing piece is exactly one
    thing: Aether has no construct that TOGGLES a flag** — `CONSTRUCTS.md` has 16 construct
    sections and no `action` among them, and a machine action block can write one but
    `machine … on entity` is R5, which is blocked on ballot **AB-7**. Cost on **R3** (the only
    honest candidate: it owns `tag`/`flag`, it owns `system` and `set`, and it is where the
    deadline's *"the surface hardens without it"* failure actually occurs): **one construct or one
    modifier that writes a named `flag` bit, its refusal wording, and a trybuild golden** — no new
    storage kind, no new tag, no new runtime. ⚠ **And the asymmetry is the finding:** the corpus's
    own sentence promises *two* valves — *"what stops it is refusal plus a pressure valve"* — and
    only one was built. The data profile's valve (`curves` + `scalable`) is in the exhaustive
    allowed list of §The logic line; the document profile's valve got a deadline and no rung.
  Recorded separately as a **carrier gap**: the two authored-scene emission fixes on this line —
  `link` as the shared remap spelling, and component references riding mandatory explicit
  `stable_name` — ride no rung at all today. They are asserted here and land nowhere; the pass
  that names their carrier is owed and is not part of GB-6.

### Carrier gap — `CG-1`..`CG-3`: asserted here, landing nowhere

**This subsection is their single home.** They were unlosable only as prose inside a longer bullet,
which is how an item with no rung disappears. Ids are minted here so both ladders can cite them; the
`CG` series is otherwise unused in this corpus (`grep -rn "\bCG-[0-9]" docs/` returned nothing
before this line). **None of the three can be assigned a rung by this pass** — that is the owner's
or the architect's call, and none of them is a ballot question, so `OPEN-QUESTIONS.md` is the wrong
register too. What they need is an owner and a rung, and the pass that names one is **owed**.

| id | The item | Why it is stranded | Who could own it |
|---|---|---|---|
| **CG-1** | **`link` as the shared remap spelling** for authored-scene emission — the same keyword doing the same job on both sides of the boundary, so an entity-bearing field is remapped identically whether it was written in Aether or in Gaia | Asserted in §Relations above and in **no rung's deliverable list**, on either ladder. Related but **not** the same item: [`PENDING-SYNTAX-PLAN.md`](PENDING-SYNTAX-PLAN.md) **M3** already owns the *cross-check* between the two carriers (bake refuses a `link` mismatch in either direction, two red fixtures at **G3**) and **M10** owns the bake refusal for an `Entity` field authored without `link`. What has no carrier is the **shared spelling decision itself** in Aether's emitted authored scenes. ⚠ **GB-6's analysis found the asymmetry decides this, and it produces a checkable consequence:** the Gaia side is already gated (M3 + M10) and only the **Aether emitter's** half is unowned — but M3's fixture is specified as refusing a mismatch *"in either direction"*, **which presupposes the Aether side emits `link` at all**. So **M3's fixture cannot be authored honestly until CG-1 has a carrier**: written today it is either red by construction or vacuous — the "gate that cannot fail" class, arriving through an unassigned rung rather than through bad wording | Aether **R3**'s `scene` surface, or Gaia **G3** beside M3/M10 |
| **CG-2** | **Component references ride mandatory explicit `stable_name`** in authored-scene emission | The `stable_name` *ruling* is ratified and carried ([`DECISIONS.md`](DECISIONS.md) §Identity and references — the bake refuses the default module-path name), and its resolution axis is specified at PENDING **M8**. What rides no rung is applying it to **Aether-authored scene emission**, which is where the assertion was made | same as CG-1 |
| **CG-3** | **GB-6(b)'s pressure valve** — the "conditional visibility" `EnableTag`-toggling action idiom | It carries an explicit **DEADLINE** — *"before the first designer asks, or the document side caves"* — and no rung. The corpus states the consequence itself: **a deadline with no rung can never come due, and is therefore not a schedule.** GB-6(b) classifies the *line*; it does not schedule the valve. ⚠ **Measured 2026-08-30 (GB-6's analysis): the gap is ONE construct**, because three of the four pieces already ship — the ratified `flag` construct, the shipped three-tag idiom in `boyko_ui`, and Gaia's ratified token-reference condition. What is missing is an Aether construct that **toggles** a flag; there is no `action` construct in `CONSTRUCTS.md` at all, and the `machine` route inherits R5's AB-7 blocker | the **Aether** ladder (GB-6(b) says so in as many words: it "must get an F-id AND a rung on the Aether ladder"). **R3 is the only honest candidate today** — it owns `tag`/`flag`, `system` and `set`, and it is the rung where the deadline's own failure mode ("the surface hardens without it") occurs. R5 would work if the valve is spelled as a two-state machine, at the price of inheriting **AB-7**, a blocker the valve does not need; Gaia **G7** can carry the document-side spelling but **not** the action, which is an exported Aether symbol by ratified rule — and splitting it G7/R3 gives the valve two homes, the CG-1/CG-2 disease this section is about |

⚠ **CG-1 and CG-2 are the two "authored-scene emission fixes" of §Relations, not new work**, and
they must not be confused with the *other* pair of authored-scene emission fixes recorded on the
Aether side ([`../aether-v2/CONSTRUCTS.md`](../aether-v2/CONSTRUCTS.md) §`material`, `scene`: the
one-extras-bundle collapse and `spawn_batch` grouping). Those two also ride no rung. Four items,
two homes, one shared property.

- **AI-orientation**: AIR-08/09/16/17 bind Gaia from the spec; diagnostics ride the AIR-01 envelope
  from the baker's first commit; `GA####` codes from the shared registry discipline.
- **Out of scope**: mod pipeline (F7 — ratified 2026-08-30), non-Latin text shaping, graph
  materials. ⚠ **The EDITOR is out of this campaign's deliverables but is no longer independent of
  it**: the owner ruled on 2026-09-03 that the editor edits Gaia DIRECTLY — scenes, data assets
  and ui documents — and saves Gaia TEXT, so Gaia's authored surface IS the editor's document
  format. The editor's own design lives on `feat/threadpool-ke16` under `docs/editor/` and does
  not exist on this branch. ⚠ **2026-09-10: the merge `merge/ke16-into-render` landed that
  branch here, so `docs/editor/` DOES now exist on this branch. The sentence before this one is
  the record of why the pointer was written, not a live statement.**
