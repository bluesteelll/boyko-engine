# Live W/E codes that NO test names, one per line as `CODE  reason`.
#
# A LEDGER, not an allowlist, and the distinction is the whole design. Check 5 asserts it in BOTH
# directions: an unlisted unnamed code reds, and a listed code that has since acquired a test also
# reds. The list can therefore only shrink, and every row names why it is here rather than what it
# excuses.
#
# This is a `.txt` on purpose: the walker's corpus is `.rs` files, so the ledger cannot satisfy its
# own check. v1's check 5 kept the allowlist inside the file being scanned and was self-defeating.
#
# Written at L6, when check 5 first armed and could count. It found eight unnamed rows out of
# twenty; five were L6's own and got observing tests instead of a row here.

E0201  the next statement at the site is `std::process::abort()`, so a test that reached it would
       take the harness process with it. Reachable only from a fire-and-forget task panic, which
       the pool catches precisely in order to abort. Removing this row needs an out-of-process
       fixture (spawn a child, assert its exit code and its stderr), which is a test-harness
       capability this repository does not have yet.




E3001  `boyko_demo`'s web entry, inside `#[cfg(target_arch = "wasm32")] fn wasm_start`. The
       function does not EXIST in a native build, so no test on the platform the gates run on can
       name it, let alone drive it. Removing this row needs a wasm test harness -- and the wasm
       build is itself blocked upstream in `boyko_ecs` (the layout asserts fail 32-bit const-eval),
       with a CI leg that is explicitly non-fatal. Two things have to land before this row can go.

E3004  `boyko_app::diag::report_windowing_unsupported`, inside `#[cfg(not(windows))]`. Same shape
       as E3001 and the mirror image of it: the reporter is compiled out on the platform the gates
       run on, so an observer here would not compile, and one written under
       `#[cfg(not(windows))]` would never execute. Its two siblings E3002/E3003 ARE observed
       (`diag.rs`'s `#[cfg(test)] mod tests`), which is what makes this row a platform statement
       rather than an admission that the shape was not tested.

# THREE ROWS WERE DELETED AT L8c, and the reason is worth more than the rows were.
#
# `W9203` and `W9216` were **stale, and had been for rungs**. Both rows said their emission could
# not be reached -- W9203's "needs all lanes claimed by live threads", W9216's "the injection seam
# does not exist" -- while `boyko_ecs`'s own profiling tests were driving each condition and
# asserting `report_count(..) >= 1` on it. The rows survived because those assertions named a BARE
# NUMBER (`report_count(9203)`), and check 5 scans for the IDENTIFIER. Two true tests and two rows
# saying they did not exist, coexisting because nothing compared them.
#
# `W9210`'s row was true about its EMISSION and is deleted anyway, because check 5's rule is about
# NAMING and L8c's `flag_code` table test now names it. What the row said is not lost -- it moved
# into that test's doc comment, where it is still true: exhausting the user-zone arena inside a
# shared test binary poisons every sibling that later registers a dynamic zone, so provoking it
# needs its own integration binary.
#
# The shape to remember: this ledger records "no test NAMES this code". It cannot record "no test
# PROVOKES this condition", and the two were being conflated. Where the second is what matters, it
# belongs beside the test that would have to change.
