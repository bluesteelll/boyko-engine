//! The successor to `planned_construct_names_its_rung`.
//!
//! `scene` was the LAST construct §9 listed as planned, and rung A6 landed it — so the
//! planned-construct arm self-destructed exactly as its comment promised, and every keyword in the
//! registry now dispatches. What replaces that golden is the proof the removal went cleanly: an
//! unrecognized head is now unambiguously a MISSPELLING, and the canonical §6.1 diagnostic (the
//! full supported list + did-you-mean) is the whole truth about it.
use aether::aether;

aether! {
    scen lab { }
}

fn main() {}
