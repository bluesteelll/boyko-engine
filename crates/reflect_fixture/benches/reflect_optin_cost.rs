// GATES G0: `reflect_optin_cost` — G7b's in-sitting bench target. A stub at G0 so the
// target list is complete from the first rung: a target required by a later rung and
// created by no rung is this campaign's most-repeated defect (twelve benches in a gate
// table, none of which existed — logging L10).
//
// G7b lands the measurement: ON leg only, in ONE process, ABBA-counterbalanced, with the
// A-vs-A twin as the sitting's floor (GATES D11) — it measures the derive's per-component
// residue for a type that opted in, INCLUDING the first-touch `TYPE_INFO` registration.
// It must NEVER be read as a measurement of the OFF configuration: that claim belongs to
// G7a's codegen identity, which is decidable where a timing null is only a statement
// about the instrument's floor (GATES D8).

fn main() {
    // Deliberately empty until G7b.
}
